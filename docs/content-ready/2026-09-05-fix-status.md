# App 审查问题修复与内容准备状态

日期：2026-09-05。最新状态：代码回归通过；用户再次授权后已完成后端与 App 重启及公开接口联通检查；三门课程已正常发布并完成 PC/数据库读回，App 登录后页面验收与隐私正式发布仍待补。以下不等于全 App 验收通过。

## 课程发布补验（用户完成正常 MFA 后）

- 通过真实 PC 内容管理页新增并发布 `nexgrid-account-safety-202609`、`nexgrid-order-ledger-202609`、`nexgrid-task-learning-202609`，均为 v1。现共 9 门课程：4 已发布、5 原有草稿；未删除或覆盖旧课程，未更换推荐位。
- 数据库只读复核：三条课程 status=1、version_no=1、revision=1、奖励 10 NEX、通过分数 100、重试上限 2、每门两题；完成条件 `all_questions_correct`、元数据事件 `course.completed`。六条标题/正文词条均 PUBLISHED v1，中英越逐字段一致；三门测验题干、选项与答案索引全部一致。
- 首次保存因英文正文超过 i18n 版本表 VARCHAR(1024) 失败，已确认无残留；精简三语正文后通过正常 UI 重建发布。本轮只调整发布素材，未修复超长输入的服务端错误处理或字段容量。
- App 学习代理未登录返回 401 AUTH_REQUIRED，浏览器停在正常登录页；PC 登录不等于 App 登录。App 目录、详情与三语切换仍需用户登录后补验。未提交测验、领取奖励或执行真实交易。
- 隐私中文草案继续保持未发布：真实运营主体、地区、隐私联系方式、处理方、保存期限和跨境安排仍缺，不能编造后发布。
- 五项服务仍监听于正式端口：PC 3002、App 5173、后端 8110、MinIO 9000、Gemma 8010。本轮无代码修改或服务重启。
- 发布范围自评：初审 98、复审 99；仅覆盖正常发布流程与 PC/持久化读回，不代表 App 全量或奖励业务验收。以下较早的“课程未发布”内容为历史记录，以本节为准。

## 12:40 重启补验

- 用户明确要求由助手执行重启。核实正式项目后停止旧后端 PID 18980、App PID 5876；既有运行守护使用保存的正常配置重新启动两项服务，未停 PC、MinIO、Gemma 或守护任务。
- 新后端 PID 20536（8110）、新 App PID 30072（5173）均核对为正式仓库进程。守护日志分别记录 recovery_verified。
- 后端 `state?history=listings` 与 App 代理 `state?history=transactions` 均 HTTP 200、code=0，包含 items 和 PRODUCTION 标识，证实新接口已加载。未登录个人订单历史返回 401；App 法律条款代理返回 code=0；PC 会话检查按预期返回 401。
- App/PC 首页、MinIO ready、Gemma health 均 HTTP 200；原 PC/MinIO/Gemma 进程未被重启。未登录，因此未补验个人账户完整业务流程，也未发布课程或隐私政策。
- 启动日志发现 F4 leadership pool 的 `team.ui.F.pool.ratio` 配置触发 INVALID_RATE，相关结算调度已保护性停用。此为业务配置问题，未在重启任务中擅自更改。
- 重启验收自评：初审 98、复审 99（仅针对进程归属、接口与健康检查，不代表业务全量验收）。

## 已完成代码修改

1. Genesis 持仓以完整服务端编号作为操作身份，不再压缩哈希，避免编号碰撞导致买卖或撤单目标错误；四处卡片只缩短展示，完整编号仍用于操作和无障碍标签。
2. App H2 生命周期读取 PC 的 `growth.trial.auto_push_killed` 开关，真值解析与 PC 保持一致；只关闭自动推送，不误关其他试用能力。
3. Genesis 挂单、成交、个人订单和释放记录新增 100+1 游标分页。App 逐页读取，后续页失败不伪装完整列表；个人查询按认证用户、正式环境及未删除状态双重隔离。
4. 钱包列表、行情行、团队分佣说明入口补充键盘语义。修复检查器对带字符串条件的数值 tabindex 误判，并接入常规测试链，不提升门限。
5. 正式 App 平台介绍隐藏无可靠公共来源的“累计支付”指标，不用当前用户的购买金额冒充平台累计发放金额。

## 本轮验证

- App 类型检查通过；334 个测试文件、1619 项 Vitest 全通过；H5 production 构建通过。
- a11y：12 项行为/解析测试通过，21 项故障注入红测通过，恢复后 gate 通过。gate 仍报告 15 处需要人工判断以及既有零语义可点击项，不能将此称为全站无障碍认证。
- 后端定向 79 项通过：AppGenesisHistoryServiceTest 5、GenesisHistoryPaginationContractTest 1、AppGenesisServiceTest 25、AppTrialLifecycleServiceTest 48。未声称本轮后端全量测试通过。
- 两轮独立复审发现的长编号布局、检查器解析、测试泛型编译、课程首次版本问题均已修正。代码复审自评：初审 98、复审 99；分数不代表部署验收。
- 相关 diff whitespace 检查通过。保留原有未提交工作，未提交或推送混合工作区。

## 重启前的阻断记录与内容待办

- 执行环境拒绝服务重启命令，该命令未执行。原 App 5173、PC 3002、后端 8110、MinIO 9000、Gemma 8010 均仍监听；前端首页、MinIO 与 Gemma 健康检查可访问。Gemma IPv6 健康检查需绕开系统代理才能正确读回 200。
- 后端仍为原 PID 18980，新的 history 查询目前返回旧版 state 结构，缺少 items。新 App 分页代码与旧后端未完成运行态联调；在受支持的正常方式重启后端与 App 前，Genesis 相关读取不能宣称可用。
- PC 停在正常双因素身份验证页。隐私政策管理员接口未登录返回 401。未绕过 MFA、未直接写库、未虚构发布成功。
- 三门课程文件是本地待发布材料，既未在 PC 保存，也未发布到 App。每门 10 NEX、三语、两题、100 分通过、2 次重试。需正常账号完成 MFA 后创建/发布，并做 App 读回；课程奖励仍受平台资金覆盖等检查约束。
- 隐私政策是待核实的中文稿。运营主体法定名称、经营地区、隐私联系方式、服务商、保存期限及跨境安排必须核实，才能制作最终三语版本并正式发布。
- 团队网络佣金“文档停用”与 F2 运行规则冲突尚待业务选择：暂停 F2 或保留 F2 并修订说明。没有擅改资金规则。FAQ 缺少运营内容同样尚未执行发布。
- 内容审查进一步核实 `AdminActorResolver` 优先使用认证主体，已登录管理员不能通过请求体 operator 伪造审计归因；未将这一初步疑点列为缺陷。实际发布仍须使用正常认证身份。

## 后续验收条件

1. 重启、项目归属、公开 history 响应和未登录拒绝已补验；正式用户个人历史与 App 可见页面仍需登录后验收。
2. 正常 MFA、课程创建及发布已完成；仍需 App 用户登录后核验课程目录、详情、三语切换。不为验收触发真实交易或领取奖励。
3. 补齐隐私主体等事实后发布最终版本，并检查 App 注册前与设置中的隐私入口、正文和版本。
4. 明确团队佣金业务规则后再执行对应配置或文档改动。
