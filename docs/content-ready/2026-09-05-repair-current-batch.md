# 2026-09-05 App / PC / Java 本轮修复与剩余事项

范围：继续处理最新检查问题；正式 UniApp、PC、Java。保留既有未提交修改，未提交或推送 Git。真实支付、短信、链上、硬件交付等外部对接不在本轮验收范围。以下是本批代码修复，不等于全 App 已验收通过。

## 已实施的代码修复

| 问题 | 修复 |
| --- | --- |
| 提现资格请求缺少策略版本，加载/失败误报零额度 | 资格请求必带 policyVersion；策略加载前不预检；加载、错误与真实零额度分开；版本冲突刷新策略。提交前冲突清理冻结并停止，不提交提现。 |
| 最新订单依赖数组位置 | 按有效 placedAt 选最新订单，不依赖排序与翻页顺序。 |
| 商城固定把手机当作用户当前设备 | 从服务端已激活设备推导当前硬件和下一升级商品；未取得权威设备不显示推导；补 Pro v2 / Rack P2 阶梯、实际已有设备金额及统一比例尺。 |
| 配额页面把有效直推当累计邀请 | 新增 directInvites 独立事实；旧后端未提供时显示未知，不借用晋升指标。 |
| 领导池保护停用误报网络失败 | HOLD 独立显示暂缓结算；不诱导网络重试。另修账本失败按钮错误刷新领导池，改为刷新账本来源。 |
| 首页公布规模被当作实时接入 | 明示平台公布估算，与真实网格在线数分开；个人排名陈旧不再改变整卡公布口径标签。 |
| 消息抽屉缺少后续页 | 增加忙态、键盘可用的加载更多入口。 |
| 收益榜不显示名次 | 显示服务端 rank，缺失 bestFor 时不渲染破折号行。 |
| 单会话退出读屏标签错误 | 标签说明单个设备，不再宣称退出所有设备。 |
| 任务相对时间随页面停留失真 | 使用页面时钟定期刷新并在卸载时清理。 |
| 隐私发布缺少默认语言仍成功 | PC 与 Java 均阻止没有英文回退版本的正式发布；单语言草稿仍允许保存。未发布虚构主体协议。 |
| 无跳转目标的通知仍跳转默认页面 | 服务端未指定 CTA 时只标记已读，不猜测跳转。 |
| 可保存 App 没有文案的语言 | 正式支持 en / zh / vi；旧不支持值读取规范化，非法写入拒绝。 |
| 安全会话列表一次全量加载 | 后端每页最多 20 个其他会话，首屏另置顶当前会话；稳定 ID 游标，App 加载更多、去重、防旧响应跨账户合并；TTL 空白规范化与鉴权一致。 |
| F4 月库存未接支付占用 | 钱包支付在扣款前锁定并校验月配额，成功支付同事务记录 SOLD 占用；暂停/耗尽先拒绝；已支付回放不重复消费。App 配额售罄禁购，支付入口显示明确不可用原因。 |
| PC 月库存明细混入历史月份 | App/PC 汇总、PC 明细、配额 CAS 均统一 UTC 当月半开区间。 |
| 开发者总览把未开放 API 写成现成功能 | 保留能力列表但明确为规划、未开放；批准申请不代表上线。语言切换时卡片文案改为响应式。没有将未实现的 OpenAPI 当作已经实现。 |

## 验证证据

- App：类型检查通过；全量 Vitest **352 文件 / 1675 用例通过**；最终 H5 构建通过。
- Java：9 个定向套件 **167 用例，0 失败、0 错误**（UserProfile 9、UserSecurity 23、SecurityController 2、OrderCommand 30、OrderMapper 8、Privacy 6、TeamQuota 3、OpsTeam 85、QuotaMonth 1）。TTL 补修后 UserSecurity 23 项再次通过。不是整个后端全量测试，未提供覆盖率结论。
- PC：隐私及内容编辑器相关 **5 用例通过**，生产构建和构建内类型检查通过。BUILD_ID `L0p8TNWErXwkzlTrAybxr`；首页引用的 15 个静态资源全部 HTTP 200。
- 独立只读复审覆盖提现版本冲突、商城设备事实、领导池/账本错误态、月配额锁/幂等和会话分页。复审发现的月份边界与 TTL 空白问题已修复并回归。
- SQL 只读验证：新配额关联查询、会话分页查询可被当前 MySQL 解析；会话分页使用用户索引。当前库当月已支付、命中配额的单品和套装明细均无记录，不存在本环境需要补算的当月历史占用。迁移至其他环境仍需先核对历史订单，不可据此推断其他库不需要补算。
- PC 真实页面：Super Admin 已登录并打开 F4，显示 Pro 0/200、Rack 0/80、领导池比例未配置；SU Admin 已登录并打开 A2。未审批、拒绝或修改既有待审事项。
- App 真实页面：重启前现有会话看到“平台公布口径 / 公开规模估算 · 非实时接入统计”。指定尾号 3771 账号正常登录流程提示“手机号或密码不正确”，已停止重试并请求用户手动登录；**该账号修复后完整真实页面回归未完成**。

## 运行状态与回退

已重启正式 PC、Java、UniApp。2026-09-05 19:54 本机检查：PC 3002 / PID 38688、App 5173 / PID 37936、Java 8110 / PID 24396、MinIO 9000 / PID 5636、Gemma 8010 / PID 16976；均核实进程属于正式项目/服务。

PC / App 首页、Java 服务条款接口、App Genesis 代理、MinIO ready、Gemma IPv6 health 均成功；无认证 PC session 代理返回预期 401。端口/PID 为检查时快照。服务守护继续运行，未停止共享基础设施。

PC 旧构建保留在 `.next-backup-20260905-1950`，更早备份也保留。独立构建临时类型路径已还原到正常 `.next` 路径。本轮无业务数据删除；未执行真实购买、提现、转账、领取、改密、退出他人会话或配置发布。

## 尚未闭环，不能标记全部完成

1. **独立单笔最高金额 / 每日累计最高金额**：现有“余额比例”和“每日次数”不能替代金额限制。本批没有完成这两个新金额规则的 PC → Java → App 能力，也没有随意填入资金上限。待用户明确金额规则/数值后继续实现与 A2 验证。
2. **领导池实际比例**：PC 实际读回仍未配置。需要用户指定比例后由 superadmin 提交、suadmin 审核；不能自行假定比例或直接改库绕过 A2。
3. **OpenAPI 实际能力及文档发布**：已修正未上线能力的展示承诺，但实例启停、账单、行情及事件交付尚未完整对接，开发文档也未发布。真实世界部分不在本次范围；内部可实现部分仍需后续闭环，不能把提示文案修改当成 API 实现。
4. **隐私正式发布**：校验修复已做；真实运营主体等信息仍缺，不编造身份信息。已有课程及正式内容未覆盖。
5. **真实验收**：待指定 App 账号登录后检查提现只读资格预检、会话加载更多、商城当前设备/升级、配额、领导池、任务及通知。没有进行资金写入或 PC 配置写入→A2→App 读回验收。
6. **月配额压力边界**：单测及静态锁顺序复核已做；跨月并发、数据库故障事务回滚、大批套装并发仍未进行完整集成压力验收。

本批交付自评：初审 98 / 100、复审 99 / 100，仅评价上述已实施代码、回归和运行恢复，不代表尚未闭环项目通过。
