# 本次内容发布材料与状态

2026-09-05：三门课程已通过 PC 正常认证会话逐一创建并发布为 v1，PC 与数据库读回已通过；App 未登录，用户侧页面验收待补。隐私草案仍未发布。

发布核验：新增三门后共 9 门课程（4 已发布、5 原有草稿）；保留原有课程及推荐位。每门 10 NEX、两题、100 分通过、2 次重试；PC 实际选项为全部答对（`all_questions_correct`）及课程完成（`course.completed`）。输入文件保留 `publishState: DRAFT` 表示创建阶段，不是当前线上状态，禁止据此重复创建。三门课程的三语标题、正文、全部题干/选项/答案索引均与库中已发布数据逐字段一致。

首次保存曾因英文正文超过 `nx_i18n_message_version.en_value` 的 VARCHAR(1024) 限制失败；确认事务回滚且没有残留后，将三语正文精简到 1024 字符内，经正常 UI 重建发布成功。正文长度分别为 ZH/EN/VI：260/875/810、288/945/878、289/966/869。本轮未修改该字段结构或错误处理代码，超长内容报错问题仍存在。

- `2026-09-05-learning-courses.json`：三门原创中/英/越课程，各 10 NEX（后台允许的最低课程奖励）、两道题、100 分通过、2 次重试。仅阅读和测验，不要求真实交易。保留已有课程，不覆盖旧草稿。`courseId` 为导入清单元数据：从请求体删除并放入 `POST /api/admin/content/i18n-learning/courses/{courseId}` 路径，其他字段对应 `LearningCourseUpsertRequest`；首次版本必须为 `v1`。创建后使用返回的 revision，按正常发布接口提交。必须遵守权限、MFA、幂等键、奖励资金覆盖检查，不允许写库绕过。若同名课程已存在，先读取比较，不覆盖操作员内容。
- `2026-09-05-privacy-draft.md`：依据当前功能编写的中文发布稿；主体、联系信息、实际服务商、地区、保存期限及权利处理流程须核实。未发布、未生效，不能以缺失事实的草稿替换正式协议。核实后再制作含义一致的英文和越南文，并从 PC 正常发布。

验收：创建响应不是发布成功；发布后应从 App 实际使用的公开/用户接口读回，检查课程数量、三语标题正文、题目、版本、奖励和隐私政策版本，并在 App 页面查看。不得以模拟响应、伪造回调或直接修改存储作为发布凭据。

尚未完成：App 代理 `/api/content/learning/courses?language=zh` 未登录返回 401 AUTH_REQUIRED，真实浏览器显示 App 登录页；未声称 App 三语页面、课程完成或奖励入账已验收。未提交测验、领取奖励或执行真实交易。隐私草案需补齐真实主体、联系渠道及数据处理事实后再发布。
