# 2026-09-05 App / PC / Java 修复记录

## 已修复范围

| 原问题 | 修复 |
| --- | --- |
| 试用状态 GET 在只读事务中写每日额度，导致 500 | 查询不再初始化额度；领取命令初始化并原子扣减。同一命令固定业务日期，覆盖跨午夜边界。 |
| 兑换记录含 FAILED 时整份响应解析失败 | 支持失败状态及提示，仍拒绝未知状态。 |
| 首页、收益页设备容量写死为 6 | 读取服务端 slotCap；未取得有效设备数据时显示未知容量，不提供添加入口。 |
| 日产排行按商品展示顺序、名次被压成 5、首项误标最优 | 后端按日产降序后分页；保留真实名次，移除无依据的最优标签。 |
| PC 新手单项奖励可改但 App 不采用 | DAY_ONE 统一使用新手总奖励；PC 移除单项修改入口，后端拒绝绕过入口的旧命令。周/月奖励不受影响。 |
| 团队入口固定承诺 50K 奖池 | 三语改为查看当前排名与奖池，不伪造活动状态或金额。 |
| 课程三语标题/正文超长导致数据库错误 | PC 与 Java 同步限制每字段 1024，稳定业务提示；覆盖超限与边界长度。 |
| PC MFA 失败提示笼统、过期无恢复入口 | 保留具体错误码，提示过期/无效/重复使用，清理过期挑战并提供重新登录。未开启绕过。 |
| App refresh 403 后残留登录态与并发恢复竞态 | refresh 终止类错误清会话；revision 围栏防止旧请求踢掉新会话；恢复单飞行，登录等待恢复，Cookie 轮换串行化。普通资源权限 403 不误登出。 |

## 运行态追加修复

试用转正两条路径遗漏设备规格，并将实体设备标为 CLOUD，造成整份设备接口返回 `E3_DEVICE_SPEC_INCOMPLETE`，以及错误套用云份额容量规则。

- 转正创建设备改为从对应商品/SKU 保存 GPU、显存、机房和严格解析的功率，实体显存与功率必须为正；源数据不完整时创建失败，由现有事务整体回滚。
- 实体设备保留商品的 DEVICE/SERVER 类型；SHARE 才映射为 CLOUD_SHARE。
- 定点修复设备 1142：RTX 4090 / 96GB / 65W / DC-Singapore-01 / SERVER。仅修规格、类型及更新时间，未修改日收益、订单金额、余额、账本。
- 修复脚本约束 id、实例号、来源订单、产品、TRIAL 来源和原字段状态，可重复执行。原字段为 GPU/显存/机房 NULL、功率 0、类型 CLOUD；修复脚本保留在后端 `scripts/sql/20260905_repair_trial_device_1142_specs.sql`。
- 实际经 5173 代理读取设备接口已返回 code=0，slotCap=3，设备 1142 使用 `capacityApplyToS1`。

另修复全量测试暴露的 Genesis 历史 mapper 缺少项目要求的 BaseMapper 继承；未修改原有未跟踪的 Genesis 测试文件。

## 验证与边界

- App：337 个测试文件、1637 项通过；type-check、H5 build 通过。
- PC：定向回归 6 项通过，类型检查及 Next 构建通过；真实页面确认新手统一奖励入口、三语课程 1024 字提示。
- Java：最终全量 5129 项，0 失败、0 错误，65 项依测试条件跳过；日志为后端 `logs/repair-verified-tests.log`。
- MySQL：新设备 INSERT 的 EXPLAIN 解析通过；规格约束只读校验中，合法 96GB/65W 被接受，零显存、零功率、模糊功率及缺失功率均被拒绝。
- 实际试用 GET 成功；实际日产排名从 75、45、18、13、1、0.19 到 0，名次 1–7。
- 浏览器确认首页容量 3/3、故障态显示未知容量并隐藏添加入口、团队入口不再固定显示 50K。
- 认证单元回归覆盖正常权限 403 不退出、终止刷新、空 Cookie 启动、并发恢复、旧请求与新登录隔离。真实单标签刷新恢复已验证；双标签刷新被服务条款确认页挡住，未代用户接受，不能视为完整双标签登录验收通过。无 Web Locks 的旧浏览器仅保证同一上下文串行化。
- 未主动购买、转账、兑换或领取奖励，未改业务奖励比例或绕过 MFA；正常启动后既有后台调度仍会运行。

## 尚需真实信息或本人操作

1. 隐私协议仍缺运营主体、联系渠道、保存期限及第三方/地区等确认资料，不能凭空发布。
2. 领导池比例仍为未配置状态，未猜测数值或启用。
3. App 当前服务条款确认需账号本人完成。
4. 最后一次 Java 重启命令在执行前被工具策略拒绝；未尝试绕过。当前 8110 仍是此前 PID 24476，已运行原九项后端修复，但最新试用转正规格/类型/正值约束尚未加载。必须完成受允许的服务重启后再验收新创建设备路径；不能宣称全部修复已在运行态生效。

保留所有原有工作区修改。未将混合工作区打包提交或推送。PC 旧构建保留于 `.next-before-repair-20260905`，未删除。

初审与独立复审针对上述范围进行，不代表支付、发奖和真实世界第三方全流程验收。

评分范围：代码初审 98/100、独立复审 99/100；运行态整体验收未完成，因最后 Java 重启受阻及本人条款确认尚未完成，不计为整体通过。
