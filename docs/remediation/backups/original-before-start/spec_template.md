# SPEC-<层><序号>-<slug>

> 复制本模板创建工单 spec。命名示例：SPEC-2a03-i18n-fill-modal.md（L2a 第 3 单）
> 硬规则：status 必须为 approved 才许开工实现；完成时必须回填「完成回执」。

| 字段 | 值 |
|---|---|
| 状态 | draft / approved / done |
| 层/工作流 | 如 L2a 弹窗重构 |
| 端 | admin / frontend(参考源) / uniapp / cross |
| 模型层级 | spec=T0；实现=T1/T2（按 MASTER-PLAN §9） |
| 关联缺陷 id | 台账 id 列表，如 AD-C-014, AD-C-022 |
| 关联规格卡 | modal-specs/ 路径（弹窗类必填） |

## 1. 背景与问题
（缺陷簇描述 + repro 摘要，引用台账即可不复述全文）

## 2. 目标与非目标
（做什么；明确不做什么防止范围蔓延）

## 3. 改动文件面声明
（精确列出将改动的文件/目录——并行编排靠它做交集检查，漏报视为流程违规）

## 4. 方案
（实现方案要点；横向组件类必须走「摸调用面→重写→多点自检→二审」四步）

## 5. 同形全站扫描范围
（本缺陷的同根因模式，用什么 grep/检查在全站扫，扫描结果列在完成回执）

## 6. 验收断言（机器可测）
（逐条列出：verify 检查项 / E2E 步骤断言 / 五元组项 / 任务式验证步骤——人工"看起来对"不算）

## 7. 拟新增哨兵
（本单关闭时绑定的哨兵：落点文件 + 触发条件 + 跑红→绿验证方式）

---

## 完成回执（实现完成时回填）
- 验证证据：（tsc/verify 输出摘要、截图路径、E2E 结果）
- 同形扫描结果：（命中数与处理）
- 哨兵：（已落地哨兵标识 + 跑红→绿记录）
- PR：（链接/commit）
- 台账更新：（涉及缺陷 id → verified/closed）

