# App 已发现问题修复结果（2026-09-05）

范围：正式 UniApp、真实 PC 管理端和 Java 后端；不涉及真实支付、外部设备交付、外部身份核验等对接验收。保留各仓库原有未提交修改，未推送代码。

## 已处理

| 问题 | 修复与验证 |
| --- | --- |
| 免费试用旧路径可能自动扣款 | 禁止旧 charge/redeemEarly 入口，返回明确拒绝；到期只结束试用，购买仍须用户主动确认。历史 autoChargeAtEnd 配置不再开启扣款。 |
| 试用转购设备复制模拟收益参数 | 改从实际商品读取每日参数；已将一条历史设备的 38.52 USDT / 65 NEX 修正为商品的 1 / 1。未补发、扣回或更改已结算收益。 |
| 设备交付功率为零 | 普通订单、组合订单、两条换购交付分支均读取 SKU 功率并校验；两条历史实物设备功率由 0 修正为 250 W。四条 SQL 均通过 MySQL EXPLAIN，无真实下单。 |
| 质押/兑换时间偏移、质押时钟停滞 | 统一解释无时区的服务端时间为 UTC+8；保留显式时区，拒绝非法日期。质押按服务端时钟推进并在账户切换时清理状态。 |
| PC 推荐课程未被 App 消费 | 移除首门课程伪推荐，首页与教程中心消费真实 featured。通过 PC 正常确认将已发布的账户安全课程设为推荐，App 两个入口均回读一致。 |
| F2 存在无结算消费者的可编辑参数 | 版税支付阈值和平级奖励比例停止开放调整，PC 明示未接入；后端普通更新与 A2 重放入口均拒绝，未擅自启用奖励或改变提现规则。 |
| Nova 缺少人工交接 | 新增用户确认入口；仅以本账号服务端会话/轮次生成最多 5 条脱敏问题，复用人工客服会话与幂等能力。失败问题未持久化时转普通客服并提示重新描述，不错误复制旧问题。 |
| 转人工离页/重试边界 | 隐藏页面使旧请求失效但保留重试键；旧请求不能清除新请求状态。UUID 异常有安全回退，账户切换隔离。 |
| PC 内容概览硬编码 | 命名空间、词条、推荐课程标题改用真实数据；页面回读为 3 个命名空间、24 条词条。 |
| 验收测试课程公开展示 | 已通过 PC 正常下架 H3 事件验收课程，保留学习与奖励历史。当前 App 显示 3 门正式课程。 |

历史设备修正脚本及精确条件回退脚本位于后端 `scripts/sql/20260905_audit_device_projection_repair.sql` 与同名日期的 `audit_device_projection_rollback.sql`。回退未执行。

## 验证证据

- App 全量 Vitest：340 个文件、1649 条测试通过；类型检查和生产 H5 构建通过。
- 后端本次相关 16 个测试类合计 364 条通过；不是后端全量测试结果。
- PC 新增定向回归 5 条通过，生产构建通过。旧 F2 E2E 已更新拒绝语义并做语法检查，未在真实环境执行其写操作。
- 多 Agent 独立复审覆盖试用、设备 SQL、PC/A2 参数入口、课程回读和 Nova 交接边界；发现的追加问题已修复并复跑。
- 真实浏览器验证了 PC 下架/推荐配置写后回读，以及 App 首页、课程列表、质押空持仓页面、Nova 转人工确认与取消。未向真实人工坐席发送测试会话，未购买、转账、质押或领取奖励。
- Java、PC、App 已重启到对应真实项目；App 最后一次重启后再次验证页面和代理。Java 与 App 配置接口为 200；PC 未登录会话代理为预期 401；PC 页面引用的 21 个静态资源均为 200。
- MinIO 与 Gemma 健康检查均为 200，未停止共享基础设施。各目标端口均核对了正确项目进程。
- 三个仓库 `git diff --check` 通过。未声明覆盖率达到 80%，也未将未执行的资金/真实交付流程认定为通过。

## 仍需真实信息或业务决策

1. 隐私协议：缺少可核实的运营主体、隐私联系渠道及数据保存等信息，不能编造后发布。
2. 领导池：关键比例仍未配置，保持暂停状态；未擅自选择资金分配比例。
3. 开发者文档：尚缺已审核的正式发布内容/配置，不能仅开启开关假称已对接。
4. Nova 完整人工接单及资金类修复的真实交易验收未执行；本次依据单测、SQL 检查及安全页面路径验证，不等同真实业务闭环通过。

自评针对上述限定范围：初审 98/100，复审 99/100；未覆盖项目已单列，不作“全 App 无 Bug”保证。
