# 交接文档 · Nexion 包 D′「USDT 链上通道接优盾云」接入规格

- **产出时间**:2026-08-06
- **移交原因**:原账号用量将尽,换号继续
- **本档位置**:工作区内为长期留存版(本文件);系统临时目录另有一份镜像 `%TEMP%\claude\handoff-nexion-udun-20260806.md`,临时目录可能被清理,**以本文件为准**
- **工作目录**:`D:\WORKS\PLAN`
- **工作线**:🅿 产品规格(纯文档线)· **定级 L**
- **当前阶段**:`nexion-workflow` P1.5 规格先行 —— **大纲已产出,卡在硬停等门,等主人签字**

---

## 一、任务是什么

为 Nexion 平台写「USDT 链上通道接优盾云(uduncloud / 现名 Cregis)」的接入规格,走 `nexion-spec` 三位一体标准,L 级。

**主人的原始约束(逐条必须守)**:
1. 产物放 `D:/WORKS/PLAN/PRD/specs/`。
2. 平台接口**按自建系统的契约设计**,优盾只是该接口后面的**过渡实现**;自建完成后替换优盾、平台接口不变。
3. 规格必须回答三条:**MD5 弱验签怎么补** · **单机热钥的爆炸半径** · **月套餐到期禁用 + 200U 赎金的退出路径**。
4. **只写文档,不碰任何代码仓。**
5. L 级:**先出大纲停等主人签字,再展开**。

---

## 二、已完成(全部已落盘,不必重做)

| 产物 | 路径 | 说明 |
|---|---|---|
| **大纲提案(主交付)** | `PRD/specs/CHAIN-UDUN_USDT链上通道接入规格_v1.0.md` | 状态 `Draft · 大纲待主人签字`。含一手事实取证、两条被推翻的前提、架构主张、七个功能单元清单、三条硬问题的拟定答案、三个待拍板项、展开计划 |
| **上游契约索引** | `PRD/specs/CHAIN-UDUN_上游契约索引_供D′展开用.md` | 三个 agent 精读上游 1064 行后抽出的**行号索引**(边界三分 / 异常路径 / 参数面 / 采购分类 / 结构性失效的承诺)。**这份最贵,约 45 万 token 换来的,展开时直接用,别再全文重读上游** |

**机器门状态**:包 D 的 10 道门 2026-08-06 实测**全绿**,新增文件未弄红任何一道。命令:
```bash
node "C:/Users/jason/.claude/skills/nexion-spec/chain-gates.mjs"
```

---

## 三、卡在哪 —— 三个待主人拍板项

细节与选项对比在大纲 §五,**不在此复述**。一句话版:

1. **接入深度(总开关)**:A 厚接入 / **B 薄接入(已推荐)** / C 不接直接自建。不定这项,七个功能单元没法写——归集、对账、冷存、退出四条线的写法全由它决定。
2. **归集怎么做**(优盾无归集接口):人工客户端 / 不归集 / **自建归集脚本(已推荐)**。
3. **过渡期对外口径**:禁止宣称多签、冷存托管、机构级密钥保管(当前形态下不成立)。

🔴 **主人回复拍板后**,就地把大纲展开成七个功能单元全文,按大纲 §六 的五步走。**拍板前不许往下写。**

---

## 四、这次回源核到的关键事实(新会话别再当传闻处理)

全部带出处写在大纲 §〇,**已实读、可复核**(2026-08-06):

- 优盾对外**只有 5 个接口**,**没有归集接口、没有任何链上查询接口**。
- 充值回调**不给区块哈希、不给确认数、不给事件序号** —— 而这三样是上游入账判据的必需输入。
- 验签 `sign = md5(body + key + nonce + timestamp).toLowerCase()`,单一共享密钥双向使用。
- 错误码含 `4166 商户没配置套餐` / `4169 商户已禁用` —— **套餐状态是接口级开关**。
- 服务协议原文:「優盾錢包的私鑰信息是存儲在您本地的(客戶端電腦或優盾硬件錢包中)」;含单方停服 / 单方调价 / 30 日单方改约条款;管辖为中华人民共和国法律。
- ⚠️ 该协议**面向自然人用户**,企业商户版条款可能不同 → **须索取企业版合同原文**再定稿相关章节。
- ⚠️ 「停用超 15 天取回资产收 200 USDT」原出处 URL 现 **404,本次未复核** → 已标注,**不作设计依据**(其风险已由错误码与协议条款独立坐实)。

🔴 **两条被推翻的既有前提**(大纲 §一,这是本次最重要的产出):
1. 改向决议写「优盾覆盖链上四项」,实测**只覆盖地址派生与出金签名两项**;扫链与归集都不覆盖 → 平台仍须自接链上数据源。
2. 「自建后替换优盾、接口不变」——接口能不变,**地址不能**(私钥恒属当初派生它的密钥树)→ 真实形态是双通道长期并存,**退出成本随使用时间单调增长**。

---

## 五、建议调用的 skill

| skill | 何时 | 为什么 |
|---|---|---|
| `nexion-workflow` | **开场第一件事** | 总编排,判工作线 + 定级 + 守停等门 |
| `nexion-spec` | 展开规格全程 | 三位一体七块标准 + 机器门 `spec-lint --strict` + §5c 大型规格骨架化 + §5d「修法先证伪」 |
| `ponytail` | 已在 SessionStart 自动激活 | 规格同样适用:不复述上游、不建第二个源 |
| `nexion-design` | 仅当写到前端交付项 / 用户可见文案时 | UI 与文案强制前置 |
| `done-review` | 宣布完成前 | 6 维矩阵 + 防自欺 6 问 |
| `nexion-audit` | 全文展开后 | 五层对抗式审计。**派单禁暗示期望结论** |

---

## 六、本任务特有的坑(踩过 / 已知)

1. **上游规格行极长**:`CHAIN-NexGrid_USDT自建托管架构规格_v1.0.md` 单次 Read 会超 token 上限。分段读(每次 25-40 行),或直接用上面那份契约索引。
2. **别把上游内容抄进 D′**。可枚举内容的唯一源是 `chain-skeleton.json`,规则理由的唯一源是上游散文。抄一遍 = 建了个会漂的副本(包 D 实测手工传播漏改率约 40%)。
3. **上游仍在审计中(R41 前后),行号会漂**。引用优先用小节标题,行号只作定位辅助;跳转后先确认内容对得上。
4. `PRD/` 在 `D:\WORKS\PLAN` 根下,**该根目录不是 git 仓库**;删改文件先 Move 到 `.trash\<时间戳>\`,禁硬删。
5. 优盾官网 `home.udun.io/agreement` 是 JS 渲染,WebFetch 取不到正文,**须用浏览器工具**;`www.uduncloud.com/geteway-interface` 用 WebFetch 可取。
6. 别改 `BATCH-20260731-reqs12-triage.md` 的状态板 —— D′ 规格未签字,现在登记属于抢跑。

---

## 七、明确不做

- 不碰任何代码仓(uniapp / admin-ops / janus 一律不动)。
- 不重写、不作废包 D 自建规格,不搬运其正文(只引用)。
- 不作法律 / 合规断言(管辖、牌照一律标「须法务判断」)。
- 不定价、不排期。

---

## 八、环境备注

- 平台 Windows,shell 为 PowerShell(Bash 工具也在,各用各的语法)。
- 有 bash / node,无 jq。
- 本次会话开了一个浏览器面板指向 `home.udun.io`,可直接关掉,无副作用。
- 无后台任务、无定时循环在跑,直接接手即可。
