# 高保真合并 B4 / App 跨仓验收报告（2026-07-28）

## 验收结论

B4/App 充值、提现和地址换绑边界已按当前 App `D:\workspace\NX1.0` 的真实远程模式完成语义合并，验收通过。App remote 模式不再以 mock 登录态、mock Card 表单或本地地址重绑状态作为业务真值；由于真实服务尚无新地址所有权证明链，换绑能力在 remote 明确下线，避免把一个永远失败或可直接篡改提现地址的按钮交给用户。

## 锁定范围

- 跨仓哨兵必须解析当前 `NX1.0`，不再固定查找旧 `Nexion-uniapp`。
- App 现役存储键以 `nexgrid-*` 为权威真值。
- Top-up 可选择 TRC20；Card 在没有真实支付命令时失败关闭。
- Withdraw 地址来自服务端 KYC 配对事实，不提供自由文本地址输入。
- 刷新和重新登录后仍不能手输提现地址。
- 地址换绑在 remote 入口明确禁用；直达页只显示暂未开放与联系客服，不渲染地址输入或 TRC20/ERC20 选择器；显式 mock 原型仍保留设计走查能力。
- M9/M10 已闭环映射由真实文件覆盖门棘轮锁定。

## 首次用户真实走查

在全新 remote 会话中，浏览器先到登录页，使用一次性真实用户登录后，从 Home → Me → Top-up 进入充值。选择 Crypto 和 TRC20 后可见 TRC20 充值信息；切到 Card 时页面失败关闭，未挂载本地 Visa/Mastercard 表单，账单及账户业务存储前后完全一致。

随后从 Me → Withdraw 进入提现，页面不存在地址输入框，换绑入口为禁用状态；派发点击事件仍留在 Withdraw。刷新、重新登录并再次进入后，地址继续只读、换绑继续禁用。直接访问换绑路由只能看到“暂未开放，请联系客服”，地址输入和两个网络选择器均不渲染。全程页面脚本错误为 0。

证据：

- `D:\workspace\bug-pic\hifi-merge-20260728\B4-App\01-remote-card-fail-closed.png`
- `D:\workspace\bug-pic\hifi-merge-20260728\B4-App\02-withdraw-rebind-disabled.png`
- `D:\workspace\bug-pic\hifi-merge-20260728\B4-App\result.json`
- `D:\workspace\bug-pic\hifi-merge-20260728\B4\01-visible-entry-refresh-relogin.png`
- `D:\workspace\bug-pic\hifi-merge-20260728\M9-M10\result.json`

## 技术与墨菲复审

- App 全量单测：54 文件、242 项全部通过。
- App `vue-tsc --noEmit` 通过；H5 生产构建通过。
- PC 验证门：路径解析 3/3、存储键 25 处引用/58 个现役键、B4 合同 3/3。
- M9/M10 覆盖门检查 M1–M11，读取 PC、后端、App 共 24 个真实证据文件，M9/M10 保持 locked closed。
- PC B4 从可见侧栏进入、刷新、退出重登后均返回权威 overview 200，8 个 dial，`pageErrors=[]`。
- M9 实跑 App Janus 上报/命令与 PC Janus；M10 实跑 App Security/KYC 与 PC Security，全部真实 API 200，`mockSuccessResponses=0`。
- 最终全锁定浏览器 4/4 通过；App 结果为 `remoteCardFailedClosed=true`、`localBusinessStateUnchanged=true`、`withdrawAddressInputCount=0`、`refreshPreservedReadonlyContract=true`、`rebindEntryDisabled=true`、`directOpenRebindUnavailable=true`、`pageErrors=[]`。

## 失败关闭与清洁夹具

- remote 冷启动忽略旧持久认证态并默认未认证；凭证只存在内存，刷新后由真实登录重新建立会话；只有显式 mock 模式允许原型直开。
- Card 未接真实支付命令时不可操作，不写入本地账单/账户状态。
- remote 地址换绑明确下线；入口与直达页都不可进入本地 mock 状态机。要重新开放，必须先完成新地址所有权证明、在途提现阻断、幂等/CAS、审计和换绑后冻结的后端闭环。
- 一次性用户 `60723152641` 仅用于真实登录与 KYC 地址走查；验收后其会话、KYC 和用户记录已精确删除。M9/M10 的动态用户、会话、KYC、Janus evaluation/device 也已清理，最终复查均为 0。

## 独立评分

B4/App 最终独立初审 `99/100`，墨菲复审 `99/100`；全范围非 owner 初审 `99.2/100`、墨菲复审 `99.5/100`，均超过通过门槛；P0/P1/P2/P3 遗留均为 0。非阻断发布边界：本轮真实用户证据为 H5 Chromium，Android/iOS 原生设备仍需进入正式发布矩阵。
