# PC 管理后台遗留功能完成与验收报告（2026-08-10）

## 结论

本轮从 75 个活跃 `flagship` 导航叶反向核对可见操作、PC 客户端、后端状态机与业务消费者，以及 UniApp/Janus 的实际消费链。原审计确认的 **51 项遗留功能已全部完成**；机读能力台账已去除 13 行重复能力声明，当前为 **240 行：`built=217`、`readonly=23`、`pending=0`、`missing=0`**。

这里的“完成”指可见入口、服务端权威写入或明确只读真源、权限、幂等/CAS、审计、失败关闭、业务消费者及结果回读达到该能力的验收口径。需要真实银行、PSP、邮件或实体设备的能力采用显式 Test/Acceptance Sandbox 完成；生产环境没有真实供应商时一律失败关闭，不把 Mock 回执冒充生产结果。

## 51 项完成清单

### A–E：平台、驾驶舱、用户、资金、设备（19 项）

| ID | 完成结果 |
|---|---|
| OPS-A-07a | 全球限流参数接服务端 CAS/审计、Redis 总闸及 IP/路由/认证主体分桶；登录、回调、健康探针预算隔离。 |
| OPS-A-07b | 提现强审阈值由 A3 权威配置进入提现快照和状态机；缺失或非法值失败关闭。 |
| OPS-A-09 | A4 生命周期完成 `new→pending_publish→gray→full→disabled`，事件写入和投递前均消费状态。 |
| OPS-A-12 | A1 权限字典登记接真实权限表、超管权限、幂等与必达审计。 |
| OPS-A-27 | A2 发起人可撤回本人待审提案，锁行/CAS/释放锁/审计闭环。 |
| OPS-B-13 | 站内收件箱和受控 Webhook 真实投递、确认、重试/死信及回执闭环；邮件为隔离 Sandbox。 |
| OPS-B-14 | B5 风险信号完成 `open→handled→resolved` 状态机、版本 CAS 与审计。 |
| OPS-C-02a | 单用户换机/回收已接 PC 可见命令、服务端事务状态机和权威回读。 |
| OPS-C-21 | C3 发起人撤回资产调整完成 CAS、幂等、并发冲突与审计；回读不变量失败整事务回滚。 |
| OPS-C-23 | 本地解绑、PSP 撤销命令、lease/重试/终态审计已闭环；真实 revoke adapter 缺失时失败关闭。 |
| OPS-C-34 | Tokenization 增加显式 DISABLED/Sandbox/Provider 边界和环境隔离；生产缺 verifier 时拒绝绑定。 |
| OPS-D-15 | D3 敏感对账导出改为唯一 POST 路径，要求理由、幂等、二次确认和高敏审计；B2 普通导出独立。 |
| OPS-D-19 | 免审阈值与 SLA 进入提现状态机；Cregis 回调 inbox、非终态、乱序、查询补偿和 HOLD 边界闭环。 |
| OPS-D-21 | PC finance BFF 放行 VietQR 凭证登记，页面可到达真实幂等/审计端点。 |
| OPS-D-22 | 独立 VND Sandbox 订单、账本、签名回调与重放闭环；不触碰真实钱包。 |
| OPS-E-16 | E5 telemetry/activity 接真实服务端观测接口、处置入口和结果回读。 |
| OPS-E-18 | E2 完成权威 assignment/claim/complete、Kill/VRAM、可信设备证明、lease 过期和奖励账本。 |
| OPS-E-19 | E3 产能、floor、豁免与 taskLock 由服务端下发并进入真实设备/任务行为；环境隔离。 |
| OPS-E-20 | Trade-in 报价、提交与订单参数改走服务端权威链，远程下单不再消费 Mock 折抵。 |

### F–I：佣金、金融产品、增长、内容（20 项）

| ID | 完成结果 |
|---|---|
| OPS-F-04 | 冷却期改为单权威引擎键；写失败时配置、审计、outbox 与幂等态一起回滚。 |
| OPS-F-07 | 硬件配额写入真实 tier/usage，校验不得低于已用量，并以旧额度做 CAS。 |
| OPS-F-07a | 配额使用记录可按 ID 原子回收，重复/不存在失败关闭。 |
| OPS-F-08 | 区域大使按明确 applicationId 审批，并原子落四类预算权益证明。 |
| OPS-F-09 | 排行榜按真实 memberUserId 取消当期资格，候选查询真实排除该用户。 |
| OPS-F-11 | SKU 奖励经持久履约队列、库存预占、用户 entitlement、失败重试与审计/outbox 授予。 |
| OPS-F-12 | 多层暂停合并为单张 A2 批命令，任一子项失败整批回滚。 |
| OPS-F-13 | 合并退出保护接真实配置与 Unilevel 结算消费者。 |
| OPS-F-14 | F3 周期+残差改为单批事务/A2 命令，失败不部分生效。 |
| OPS-F-15 | `settleCron` 由同一 Cron parser 写时校验，存量脏值可观测且调度失败关闭。 |
| OPS-F-16 | 日/周/月/累计四周期奖金均有消费者；持久 checkpoint 补齐停机期间漏结周期。 |
| OPS-F-17 | poolUsd/minUsd/paused、top1/top5 进入真实候选、结算、账本和审计。 |
| OPS-F-18 | PC 展示活动暂停项并可按用户+奖种恢复。 |
| OPS-F-19 | F5 按当前筛选生成脱敏 BOM CSV，并返回真实文件名、行数与下载。 |
| OPS-F-26 | 批量补发先锁定/预校验全部源事件，再统一写入；后项失败整批回滚，源事件唯一约束防重复。 |
| OPS-G-14 | G1 单档恢复改走专用 restore 端点并通过 J1 恢复权限、B1 红线和审计。 |
| OPS-H-12 | H3 编辑、状态迁移、归档、删除完成 CAS、持久幂等重放和真实认证审计主体。 |
| OPS-I-14 | 三个 v3 业务频道接入服务端 cadence 和运行时 adapter。 |
| OPS-I-15 | 动态 Nova 通道采用事实源 allowlist、持久 fanout cursor 和每批 250 用户的可恢复投递。 |
| OPS-I-16 | UniApp 增加真实左滑手势并上报 `swipe_conversion`；remote 模式不回退 Mock timer。 |

### K–M：风控、分析、客服（12 项）

| ID | 完成结果 |
|---|---|
| OPS-K-16 | Janus 可信证明可驱动收益释放；生产与 Sandbox 用户、钱包、entry 三重隔离。 |
| OPS-K-17 | 完成命令领取、ACK、applied proof、签名/nonce 防重放与目标版本校验；生产无证明时失败关闭。 |
| OPS-L-08 | L1 KPI 跨域入口改为真实路由和权威筛选条件，不再 toast 占位。 |
| OPS-L-09 | L2 B4/H1/I/F 归因入口均可导航并生成相应下钻上下文。 |
| OPS-L-10 | UniApp 启动真实行为采集；fixture 仅 Test/Acceptance，MOCK 事实从生产聚合/监管导出排除。 |
| OPS-M-17 | 多用户专属客服绑定改为单批命令，最多 100 人、批量预检/写入、事务原子和稳定幂等。 |
| OPS-M-19 | 客服档案与席位携带 expectedVersion，MyBatis CAS 冲突后权威刷新，不自动覆盖。 |
| OPS-M-20 | M3 使用专属转交目标和 runtime 模板端点，M3-only 角色不再依赖 M1 权限。 |
| OPS-M-21 | 工单升级等待服务端完成，失败时保留弹窗、输入和命令上下文。 |
| OPS-M-22 | 会话创建采用后端返回的真实 conversationNo，不再保留临时 ID。 |
| OPS-M-23 | FAQ 正文+状态复合编辑走一次完整更新；仅状态变化才走状态端点。 |
| OPS-M-24 | M1–M5 读取按叶权限隔离，SSE 未授权终止不重试，KPI 的 0 与不可用分开显示。 |

## Mock / Sandbox 与生产边界

| 能力 | 开发验收范围 | 生产边界 |
|---|---|---|
| B5 邮件 | Test/Acceptance profile 返回 `SANDBOX_DELIVERED`，不计入真实送达率 | 生产无邮件 provider 时禁止保存/投递 |
| C1 Tokenization / PSP revoke | Sandbox token、撤销命令、重试和环境隔离可完整验收 | 生产 verifier/revoke adapter 缺失时失败关闭 |
| D7 越南法币出款 | 独立 Sandbox 订单、账本、回调与重放 | 不扫描真实提现、不改真实钱包 |
| D5 Cregis | 管理 Sandbox 与真实回调状态机可验收 | status=6 保持链确认待处理；无 BSC 终局证明不结算 |
| E2 任务证明 | Acceptance Sandbox 使用隔离奖励账本 | 生产仅接受设备绑定的可信证明 |
| K6 Janus | 显式 Sandbox 完成 executor/ACK/proof 闭环 | 生产仅接受设备绑定 HMAC；Sandbox 证明不能释放生产收益 |
| L6 行为夹具 | fixture 仅 Test/Acceptance 且标记 MOCK | 生产聚合、热力图和监管导出只读 PRODUCTION |

**开发完成（Mock/Sandbox）不等于生产供应商已接入。** 本报告只签开发闭环和失败关闭，不签真实资金、真实邮件、真实 PSP、实体设备或外部银行的生产可用性。

## 验收结果

- 结构与台账：75/75 活跃叶有唯一能力声明；反向写点覆盖无遗漏；能力 ID 唯一；`pending=0`、`missing=0`。
- 后端：最终候选完整 `mvn test` 通过，**3165 tests，0 failure，0 error，0 skipped**；真实 MySQL 事务、资金、幂等、CAS、架构和环境隔离专项均进入完整测试编译/执行链。
- PC：`npx tsc --noEmit` 与 production `npm run build` 通过；正式 `verify`、状态合同、正/反向动作门在最终候选重新执行并记录于相邻回执。
- UniApp：完整 `npm run verify` 通过，包含 type-check、KYC 移除、probe safety、earnings、liveness 和 H5 build/runtime。
- Janus：依赖按锁文件安装后 `npm run verify` 通过，包含 type-check 与 KYC 移除合同。
- 真实用户走查：按《页面业务逻辑通顺性验收方法》从可见侧栏完成 75/75 活跃页面首轮、逐页刷新、浏览器返回、退出重登四轮走查；34 个本轮受影响页面各保留入口、稳定态、安全导航恢复后、刷新后、重登后 5 张全页截图（合计 170 张），同时保留 6297 条网络事件、170 条步骤日志、控制台日志与 Playwright trace。四轮均 75/75；`pageErrors`、非预期 `consoleErrors`、管理接口 4xx/5xx 和真实请求失败均为 0。登录前一次匿名 session 401 属预期安全探测，单独记账而未洗成错误为 0。候选 Build ID 为 `y-mI9UTdxKTpEmuKRPGmr`，证据位于 `D:\workspace\bug-pic\acceptance\PC-remaining-51\runs\20260811-final-v8`。
- 真实动作与恢复：在 `acceptance + LOCAL_SANDBOX` 下由可见控件完成 D7 隔离 Mock 出款单创建与签名回调；证据包含两次真实 POST 200、完整 trace 和同一订单的 `source=mock`/`sandbox=true` 回读。刷新、退出登录、重新登录后该订单仍为 `COMPLETED`；真实银行通道保持关闭。D5、K6、L6、M1、M3、M5 另完成可见入口、刷新、浏览器返回和重登复核：D5 展示真实免审阈值/SLA 控件，K6 显式显示执行环境，L6 将无生产事实标为生产空态，M1/M5 不再把加载或不可用误报为成功，M3 自动重连预算耗尽后显示人工“重新连接”入口。
- 51 项能力证据矩阵：上表 51 个 ID 均在 `docs/ops-actions.manifest.json` 以唯一 `restAction/restActions` 或 typed `runtimeEvidence` 绑定到现役 PC、后端及必要的 UniApp/Janus 消费链；状态合同同时验证正向锚、活跃写点反向覆盖、服务委派、失败关闭及 Mock 隔离。75 页主轨只签页面发现、渲染和恢复连续性，不冒充 51 个高敏动作均在浏览器里执行；高敏/资金/批处理业务效果由后端完整测试、真实 MySQL 事务测试及对抗故障注入证明，D7 作为外部数据型代表另保留完整可见动作链。
- 运行态补漏：走查发现并修复 E5 `/observability` PC BFF 漏路由，以及旧库缺 F4 配额字段、L6 `source_environment` 字段导致的运行错误；迁移和路由契约均已进入自动测试。

## 快照与发布边界

- 报告绑定当前本地混合工作树；相邻 SHA-256 清单只证明选定证据文件内容，不可重建三仓完整 dirty tree，也不等于远端 `main` 或发布候选。
- 本轮没有执行真实资金/供应商动作，没有提交、推送或部署。
