# 高保真合并 D1 模块验收报告（2026-07-28）

## 验收结论

D1 充值通道按真实后端权威合同完成语义合并，验收通过。高保真中的 mock 回包、免登录和本地成功态没有进入真实 PC 管理端。

## 锁定范围

- 运营端可见入口：D 域 → D1 充值通道。
- 五条现役充值轨：TRC20、BEP20、ERC20、VietQR、Card。
- Card：单笔 30–5000 USD、费率 3.5%；VietQR 单笔上限 5000 USD。
- Card 上限写入：权限、参数边界、CAS、幂等、未知结果同号重试。
- 数据迁移：新增/退役通道、存量配置值、启停态、删除态与幂等重跑。

## 首次用户真实走查

从 PC 可见入口进入 D1，页面展示五条权威充值轨和金额规则。触发一次未知结果后刷新页面，待核对命令仍可见；退出并重新登录后使用原 `commandKey` 重试。第二次请求返回服务端“相同请求仍处理中”409，完整请求胶囊和待核对面板继续保留；再次刷新后第三次重试仍使用同一请求号及完整请求体。页面脚本错误为 0。

证据：

- `D:\workspace\bug-pic\hifi-merge-20260728\D1\01-five-rails-and-same-key-retry.png`
- `D:\workspace\bug-pic\hifi-merge-20260728\D1\result.json`

## 技术与墨菲复审

- PC `npm run verify`：18/18 阶段通过，包含 TypeScript、runtime mock import guard、路径解析、跨仓通道哨兵、D1 合同、B4 合同、M9/M10 覆盖门和生产构建。
- D1 合同：4/4；跨仓通道哨兵：19 项；路径解析：3/3。
- 后端全量回归：350 个测试套件、2817 项测试，0 失败、0 错误、2 跳过；D1 定向测试再次通过。
- migration 在本地数据库执行两次；探针证明存量 `5000/0/1` 不被覆盖，回滚探针后现役 Card 行为 `5000/1/0`。
- 浏览器锁定用例：1/1 通过，`pageErrors=[]`，`reloginPreservedPendingCommand=true`、`inProgressPreservedPendingCommand=true`，三次同号、同请求体。

## 失败关闭与回归边界

- 后端或 BFF 返回缺字段、非有限数、越界金额时不补默认值，整条配置失败关闭。
- 未知结果只保留同一完整命令胶囊，不生成新命令冒充重试；24 小时后过期。
- 迁移不恢复运营员已停用或已删除的通道。
- 旧 BTC/ETH 通道不再出现在现役列表。

## 独立评分

独立模块初审 `99.1/100`，墨菲复审 `99.4/100`，均超过通过门槛；P0/P1/P2/P3 遗留均为 0。
