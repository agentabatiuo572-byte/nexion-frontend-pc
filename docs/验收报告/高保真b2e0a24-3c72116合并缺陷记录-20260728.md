# 高保真 b2e0a24 / 3c72116 合并缺陷记录（2026-07-28）

## 范围

- 高保真来源：`rhythm-configurable` 分支的 `b2e0a24`、`3c72116`。
- 真实交付仓：PC 管理端、后端、当前 App `D:\workspace\NX1.0`。
- 合并原则：按真实业务语义合并，不跨仓摘取 mock 数据、免登录、静态成功回包或本地业务真值。
- 变动模块：D1 充值通道；B4/App 充值、提现与钱包地址重绑；M9/M10 既有闭环项只增加棘轮式覆盖门。

## 缺陷闭环

| ID | 发现方式 | 缺陷 | 根因 | 修复 | 终验 |
| --- | --- | --- | --- | --- | --- |
| HF-01 | 跨仓哨兵 | 固定查找旧 `Nexion-uniapp`，无法找到当前 App | 路径被写死 | 增加统一路径解析器，显式支持 `D:\workspace\NX1.0`，找不到时失败关闭 | `workspace path resolver` 3/3 |
| HF-02 | D1 首次用户走查 | PC 与后端未完整表达五条现役充值轨，Card 上限不是可写权威参数 | 高保真展示与真实接口合同未同步 | PC、后端统一为 TRC20、BEP20、ERC20、VietQR、Card；Card 为 30–5000 USD、3.5%，上限使用真实 CAS/幂等命令 | D1 Playwright 1/1；跨仓通道哨兵 19 项通过 |
| HF-03 | 数据迁移墨菲复审 | 通道迁移可能覆盖已存在的配置值、启停态或删除态 | `upsert` 默认值与存量运营状态混用 | 仅补缺失记录；存量 `config_value/status/is_deleted` 原样保留；退役 BTC/ETH | migration 合同测试通过；数据库幂等重跑与探针通过 |
| HF-04 | D1 技术复审 | 读取端可能接受越界 Card/VietQR 上限 | 只在写入端校验 | 读取、写入与 PC BFF 均执行严格数值/边界校验，歧义响应失败关闭 | D1 合同 4/4；后端定向测试通过 |
| HF-05 | 未知结果复审 | 旧未知结果指纹存在碰撞风险，且运营员没有明确的同号重试入口 | 短哈希和不可见的临时状态不足以承担资金配置命令 | 使用唯一 `commandKey` 保存完整请求胶囊；同标签页跨刷新/重登保留 24 小时；页面明确显示“核对后使用原请求号重试” | Playwright 证明 `retrySameKey=true`、`retrySameBody=true` |
| HF-06 | B4 跨仓合同 | 旧存储键检查仍以 `nexion-*` 为现役真值 | 品牌键迁移未同步到哨兵 | 哨兵读取当前 App 真实源码，将 58 个 `nexgrid-*` 键作为权威集合并检查 25 处引用 | App storage-key parity 通过 |
| HF-07 | App 真实浏览器走查 | remote 新会话可能带着本地默认登录态打开业务页，首屏登录跳转又可能早于路由就绪 | mock 默认认证态和旧持久认证态可能泄漏到真实模式；启动时序不稳 | remote 冷启动完全忽略持久 auth，默认未认证；登录重定向带就绪重试；凭证仅在内存会话保留，刷新后必须重新登录；mock 直开只保留在显式 mock 模式 | auth 默认测试 3/3；真实浏览器冷开和刷新均先登录 |
| HF-08 | App 退出/重登走查 | 清理账户域时，部分 Pinia store 壳不完整会触发运行时错误 | 清理逻辑直接假定完整 store | Voucher、Free Trial 与其他账户 store 统一经过安全绑定层 | App 全量单测、类型检查与浏览器 `pageErrors=[]` |
| HF-09 | B4 真实远程走查与安全复审 | 地址重绑页在 remote 模式仍读取本地 mock 配对状态；若直接做 CAS 换绑又会缺少新地址所有权证明 | 原型状态机不能作为真实账户真值；只做鉴权/CAS/审计仍不足以证明用户控制新提现地址 | remote 提现地址继续只读服务端 KYC；换绑入口明确置灰，点击留在提现页；直达换绑页只显示“暂未开放，请联系客服”，不渲染表单或网络选择器；mock 原型仅留在显式 mock 模式 | B4/App Playwright 1/1；remote safety gate 2/2 |
| HF-10 | B4 真实远程走查 | Card 在无真实支付命令时仍可能展示本地表单 | mock 表单没有按运行模式隔离 | remote 不挂载本地 Card 表单，显示不可用；点击不改变账单或账户存储 | `remoteCardFailedClosed=true`、`localBusinessStateUnchanged=true` |
| HF-11 | M9/M10 技术复审 | “已闭环”可能因后续删改真实映射而回退 | 旧检查只看声明文本 | 覆盖门直接读取 PC、后端、App 的 24 个证据文件并锁定 M9/M10 | FE/BE mapping closure ratchet 通过 |
| HF-12 | 最终构建 | 新 Playwright 中 `window.uni` 缺少 TypeScript 声明 | 浏览器注入对象不在 DOM 标准类型中 | 测试内增加最小局部类型收窄，不扩大生产全局类型 | PC `verify` 18/18 通过 |
| HF-13 | D1 独立墨菲复审 | 未知结果用原号重试若返回 `409 IDEMPOTENCY_REQUEST_IN_PROGRESS`，旧实现会删掉完整请求胶囊 | 所有无 unknown header 的失败都被误判为原请求终态 | 请求前区分新命令与既有 unknown；既有命令在 409/401/403 等未闭环失败下继续保留，只在成功后删除 | `unknown → 刷新 → 退出重登 → 409 → 刷新 → 第三次同号重试` 通过 |
| HF-14 | 非 owner 终审 | 最新候选缺 PC B4 可见入口和 M9/M10 live 调用链证据 | 早期只完成静态映射门与 App B4 走查 | 新增 PC B4 刷新/重登走查；M9 实跑 App Janus 上报/命令与 PC Janus，M10 实跑 App Security/KYC 与 PC Security | Playwright 2/2，所有真实 API 200，mock 成功响应 0 |

## 清洁夹具

真实 App 走查使用一次性用户 `60723152641`。每轮验收后均按精确 `user_id` 删除会话、KYC 和带指定 referral code 的用户记录；最终复查三张表以及 M9/M10 的动态用户、Janus 设备均为 0。未删除或修改其他用户数据。

## 当前结论

以上缺陷均已修复并完成锁定范围重跑；无未关闭阻断项。终局非 owner 初审 `99.2/100`、墨菲复审 `99.5/100`，P0/P1/P2/P3 均为 0。模块评分与独立审查意见分别写入 D1、B4/App 模块验收报告。
