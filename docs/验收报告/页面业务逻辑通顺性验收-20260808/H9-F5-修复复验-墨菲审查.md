# H9 / F5 修复复验——跨域墨菲终审

- 终审日期：2026-08-08
- 终审角色：非实现者、只读跨域墨菲审查
- 唯一有效批次：`H9-F5-UA-FIX9-20260808-0424`
- 验收方法：`D:\nexion\页面业务逻辑通顺性验收方法.md` v1.3
- 旧批次：FIX5～FIX8 全部作废，不作为本报告的最终结论依据
- 审查边界：未由本审查者发出 POST / PATCH / DELETE，未修改产品代码、配置或业务数据；仅复核源码、测试、截图、trace、候选产物，并执行只读数据库查询

## 1. 最终裁决

| 模块 | 墨菲复审分 | 裁决 | 当前硬缺陷 | 发布门 |
|---|---:|---|---|---|
| H9 对外公布数据 | **100 / 100** | **PASS** | 0 | 无激活中的 HOLD |
| F5 佣金事件审计 | **99 / 100** | **PASS** | 0 | 无激活中的 HOLD |
| 总体 | **99.5 / 100** | **PASS** | 0 | FIX9 候选可进入后续发布流程 |

结论：FIX9 已同时闭合真实可见入口、服务端真值、权限边界、结果未知、CAS/并发、幂等、刷新重登、H9 用户侧投影、F5 A2/D4、资金副作用、审计/outbox 与验收数据清理。未发现阻断级、高、中、低产品缺陷。F5 扣 1 分只表示本轮只读终审没有在真实数据库中主动制造“CAS 后、outbox 前后”的服务器进程级故障；现有事务源码、异常单测和运行态副作用证据已经足以放行，不构成 HOLD。

## 2. 候选身份与证据完整性

### 2.1 候选身份

- PC `.next/BUILD_ID`：`Cuyz8v6zWkxu-26tR-Jd_`
- 后端候选 JAR：`D:\workspace\nexion-backend\target\nexion-backend-0.0.1-SNAPSHOT.jar`
- 后端 JAR SHA256：`83B4B03EE12710E7F2A12948DDBB459B55FE155B19EBC67C9F7F02977902A7A2`
- 现场只读复算结果与 FIX9 evidence 中的候选身份一致。

### 2.2 锁定证据

| 模块 | runtime-evidence SHA256 | trace SHA256 | 结果 |
|---|---|---|---|
| H9 | `55CF1857A0FBDFA75CB2F8C995874237FEFF430409FB4EB86765791F3A1B717A` | `CCA00EED51D7BCCBFD706004151B2F6C2042FE912466F4D43130D3B202E76CB4` | 与委托值一致 |
| F5 | `BA2DA08DC6F56D6B26BFACCDED4A997AE9D05ADA82EBEDFAFB92B95F9D960FD5` | `7397B69E2CC487F03296EAC319170ACED3DAB18B024730B09E3834F89CC0472F` | 与委托值一致 |

证据根：`D:\workspace\bug-pic\.restricted\hard-block-method-acceptance-20260808\H9-F5-UA-FIX9-20260808-0424`。Playwright `.last-run.json` 为 `passed` 且 `failedTests=[]`。两份 trace 均可读；其中可见预期的 H9 `409/422/403/401` 与受控断网、F5 `503/409/403/401`，没有与运行证据矛盾的请求结果。

## 3. H9 独立复审

### 3.1 v1.3 评分

| 维度 | 得分 | 复审依据 |
|---|---:|---|
| 定位与对象 | 10/10 | 从后台真实 H9 菜单进入；对象、影响去向、配置含义清楚 |
| 数据一致性 | 12/12 | DB、管理 API、公共 `/api/config/platform`、H5 首页与全球网络精确一致 |
| 操作逻辑 | 13/13 | 可见编辑、影响预览、理由确认、保存、恢复均为完整闭环 |
| 产品完整性 | 10/10 | 权限、校验、版本、审计、outbox、恢复路径均存在 |
| 真实用户路径 | 10/10 | PC 管理端与 UniApp H5 均从真实入口执行，无主路径 mock |
| 状态逻辑 | 10/10 | v27 → v28 → v29；保存值与精确恢复值均正确 |
| 权限与安全 | 10/10 | 只读/无写角色可见但写 403；无菜单角色菜单隐藏且读 403 |
| 控件有效性 | 5/5 | 合法值按钮可用，越界值按钮禁用，放弃改动恢复真值 |
| 文案与反馈 | 5/5 | 成功版本、CAS 冲突、断网未生效/原样重试提示均明确 |
| 异常处理 | 5/5 | 409、422、断网均 fail-closed，配置不被覆盖 |
| 审计/幂等/回滚 | 10/10 | 保存和恢复各 exact-one；拒绝路径零 mutation audit/outbox |
| **合计** | **100/100** | **PASS** |

### 3.2 墨菲攻击结论

1. **权限与越权：PASS。** `hReadonly`、`hNoWrite` 的 PATCH 均为 403；`hNoMenu` 菜单不可见且 GET 为 403。页面隐藏与服务端鉴权不是二选一，而是双层生效。
2. **CAS/并发：PASS。** 双操作者基于旧 v27 并发，胜者写成 v28，败者返回 409；界面明确提示刷新核对，DB 保持 28,433，不发生覆盖。
3. **幂等：PASS。** 保存请求携带稳定 key，同 key replay 仍为 v28，没有第二次版本推进。保存证据中 audit、idempotency、outbox 均恰好 1 条。
4. **非法输入与事务副作用：PASS。** 客户端越界值阻止提交；绕过 UI 的非法 `fleetDevices=-1` 返回 422。409 与 422 各形成一条确定性幂等结果，但配置版本仍为 v28，mutation audit=0、outbox=0。
5. **结果未知：PASS。** 受控连接中断后界面不宣称成功，提示“没有生效/原样重试”；DB 保持 28,433，刷新重新读取服务端值。该路径与成功幂等 replay 共同排除“未知结果后铸新命令并重复写”的风险。
6. **H5 真实投影：PASS。** 保存后公共配置 `fleetDevices=28433`，UniApp 首页“网络脉搏/在线设备”和全球网络均精确显示 28,433，consoleErrors 为空；恢复后两处均精确显示 28,432。此前“真实 fleetDevices 非零但首页持续为 0”的缺陷未复现。
7. **配置缺失 fail-closed：PASS。** UniApp 真后端模式只接受合法 `fleetDevices × onlineRatePct`；配置同步失败或越域时 `activeDevices=0`，不会继续展示本地 seed/mock 为平台真值。
8. **恢复与审计：PASS。** 验收前值 28,432/v27，临时保存 28,433/v28，最终精确恢复 28,432/v29。恢复动作的 audit、idempotency、outbox 也各恰好 1 条；保留不可变审计轨迹是正确行为，不属于测试污染。

H9 当前没有剩余 HOLD 条件。若发布产物的 BUILD_ID/JAR hash 改变、用户侧不再精确投影服务端值、409/422 出现配置或 outbox 副作用，须立即重新 HOLD 并按新候选全量复验。

## 4. F5 独立复审

### 4.1 v1.3 评分

| 维度 | 得分 | 复审依据 |
|---|---:|---|
| 定位与对象 | 10/10 | 从真实 F5 菜单进入；五条隔离佣金事件对象明确 |
| 数据一致性 | 12/12 | 页面状态、DB 状态/版本、operation、ticket、outbox、D4 对齐 |
| 操作逻辑 | 13/13 | 冻结、解冻、提前解锁、驳回、并发冲突均完整执行 |
| 产品完整性 | 10/10 | A2 双人决策、D4、RBAC、CAS、审计、清理均闭合 |
| 真实用户路径 | 10/10 | 真实页面点击、真实 A2 审批、真实 D4 链接，无主路径 mock |
| 状态逻辑 | 10/10 | 合法转换、非法理由、驳回和 stale writer 均符合状态机 |
| 权限与安全 | 10/10 | 只读/无写/无菜单/匿名四类边界均 fail-closed |
| 控件有效性 | 5/5 | 短理由禁用；动作按钮随状态和权限正确变化 |
| 文案与反馈 | 5/5 | 503 未知、同一命令号、409 冲突、终态文案均可理解 |
| 异常处理 | 5/5 | 503 后保留输入，刷新同 key 重试；驳回后业务不变 |
| 审计/幂等/回滚 | 9/10 | 运行态 exact-one + 源码事务 + 异常单测充分；未主动做真实 DB 进程级故障注入 |
| **合计** | **99/100** | **PASS** |

### 4.2 墨菲攻击结论

1. **权限与越权：PASS。** 只读/无写角色的真实 PATCH 均为 403；无菜单角色菜单隐藏且 GET 为 403；匿名 GET、PATCH 均为 401。A2 提案者与独立审批者分离。
2. **HTTP 200/code 0 误报未知：PASS。** 正常提案、批准、驳回均正常收敛，`productFindings=[]`；不再把有 `data` 的成功包提示为“结果未知”。
3. **真正结果未知：PASS。** 受控注入 A2 503 后，弹窗明确显示“结果暂不确定/同一命令号”，输入不丢；刷新后相同业务输入复用原 Idempotency-Key，产生的恢复票据被独立审批员驳回，事件 229 仍为 COOLING/v0。
4. **幂等：PASS。** 冻结批准使用同 key replay 后版本仍为 1，`COOLING_TO_FROZEN` operation 恰好 1 条；没有重复动作或重复资金记录。
5. **CAS/并发：PASS。** 事件 228 的胜者先写 FROZEN/v1，旧快照提案在执行时返回 409；最终仍为 FROZEN/v1，stale ticket 保持 pending，没有假批准或第二次写入。
6. **状态机：PASS。** 事件 225 冻结后解冻为 COOLING/v2，不绕过冷却；事件 226 提前解锁为 UNLOCKED/v1；事件 227 的提前解锁被驳回后仍为 COOLING/v0；非法短理由没有提交。
7. **历史资金不回写：PASS。** 冻结、解冻、提前解锁都没有新增或篡改历史入账：五条 fixture 初始各 1 条 ledger，关键事件操作后仍各 1 条。只有 COOLING→UNLOCKED 产生一次 canonical outbox，不产生重复钱包流水。
8. **D4 契约：PASS。** F5 使用服务端返回的 canonical `ledgerBizNo`，事件 226 跳转 `/finance/ledger?bizNo=F2-NETWORK-226`；D4 精确返回 1 条、金额 +2.22 USDT、状态处理中。旧的 `CM-{eventId}` 空查问题未复现。
9. **outbox：PASS。** closure 恰好只有 `[CM-226, COMMISSION_UNLOCKED, PUBLISHED]`；aggregate 维度是 `COMMISSION/CM-226`，不是 ticket 号；其它状态变化不误发。
10. **事务与回滚：PASS。** F5 CAS 更新、operation、canonical outbox、账务策略和审计位于 `@Transactional` 更新边界内，A2 决策通过 `AdminIdempotencyTransactionExecutor.runClaimed(..., rollbackFor=Exception.class)` 执行；outbox 抛错单测证明异常向事务边界传播。真实运行又证明 409/驳回/冻结/解冻无额外 ledger/outbox 副作用。
11. **刷新重登：PASS。** 独立新 context 重新登录后，事件 226 仍显示“已解锁可提”，事件 225 仍显示“冷却计提中”，不是 localStorage/mock 伪真值。

### 4.3 清理复核

FIX9 runtime-evidence 已断言：`events=0`、`ledgers=0`、`operations=0`、`outboxes=0`、`reserveRows=0`、`idempotencyRecords=0`。

本审查者随后从 FIX9 trace 提取全部 14 个真实 Idempotency-Key，并对实库做只读复核：

| 项目 | 实库剩余数 |
|---|---:|
| FIX9 trace 的 14 个精确 Idempotency-Key | 0 |
| FIX8 曾遗留的 14 个精确 Idempotency-Key | 0 |
| 事件 225～229 / marker 事件 | 0 |
| 对应 wallet ledger | 0 |
| commission operation | 0 |
| COMMISSION/CM-225～229 outbox | 0 |
| A2 tickets | 0 |
| F5 marker audit | 0 |
| B1 隔离 reserve row | 0 |
| FIX9 临时管理员 | 0 |
| FIX9 临时角色 | 0 |

FIX8 的“只按 RUN_ID 清 idempotency、漏掉 `f-config-*` 与 `a2-approve/reject-*`”问题已在 FIX9 改为收集实际命令号、精确删除并计数断言，未复现。

F5 当前没有剩余 HOLD 条件。若后续批次出现 `idempotencyRecords != 0`、outbox 不为 exact-one、D4 不再按 `F2-NETWORK-{eventId}` 精确定位、CAS 败者改变状态或任何冻结/解冻/提前解锁新增历史 ledger，应立即 HOLD。

## 5. 跨域不变量结论

| 不变量 | H9 | F5 |
|---|---|---|
| UI 不以 localStorage/mock 充当服务端真值 | PASS | PASS |
| 无权限菜单隐藏且接口拒绝 | PASS | PASS |
| 结果未知不宣称成功 | PASS | PASS |
| 同意图重试复用命令号 | PASS | PASS |
| CAS 败者不覆盖胜者 | PASS | PASS |
| 确定性失败无业务副作用 | PASS | PASS |
| 刷新/重登仍读服务端状态 | PASS | PASS |
| 下游契约使用 canonical 标识 | PASS | PASS |
| 审计/outbox 数量与业务动作一致 | PASS | PASS |
| 验收临时数据完成清理或精确恢复 | PASS | PASS |

## 6. 最终发布门

本报告对锁定的 FIX9 候选给出 **PASS**。不存在需要产品修复后再验的当前缺陷，也不存在激活中的环境 HOLD。

以下任一事实改变时，本 PASS 自动失效并转 HOLD：

1. PC BUILD_ID 或后端 JAR SHA256 与本报告不一致；
2. 使用非 FIX9 evidence/trace 替换本报告证据；
3. H9 发布后公共配置、首页网络脉搏、全球网络不再精确一致；
4. F5 的 A2、CAS、canonical D4、exact-one outbox 或零历史资金回写任一回退；
5. 验收重跑留下事件、账本、operation、outbox、ticket、idempotency、临时账号/角色残留。

## 7. 审查者自评

- 初审：99/100（发现并复现 FIX8 遗留 14 条 idempotency 记录，阻止带污染证据放行）
- 复审：99/100（基于全新 FIX9 重验 hash、trace、源码契约、实库状态与清理，不沿用历史 FAIL/HOLD）
- 当前血量：100/100
