# H9 修复复验：独立审查（FIX9 最终批次）

- 审查日期：2026-08-08
- 审查人：独立真实用户验收 Agent，未参与实现。本报告只审阅锁定批次的原始交互、运行时与审计证据；未读取或修改业务源码、测试数据或其他报告。
- 方法：`D:\nexion\页面业务逻辑通顺性验收方法.md` v1.3；本轮重新完整阅读并从头评分。
- 唯一依据：`H9-F5-UA-FIX9-20260808-0424`。此前任何 runId、结论和分数均已废弃。
- 原始证据：`D:\workspace\bug-pic\.restricted\hard-block-method-acceptance-20260808\H9-F5-UA-FIX9-20260808-0424\H9`
- 校验：`runtime-evidence.json` SHA-256 `55CF1857A0FBDFA75CB2F8C995874237FEFF430409FB4EB86765791F3A1B717A`；`trace.zip` SHA-256 `CCA00EED51D7BCCBFD706004151B2F6C2042FE912466F4D43130D3B202E76CB4`，均与锁定值一致。
- 候选：PC build `Cuyz8v6zWkxu-26tR-Jd_`；后端 JAR SHA-256 `83B4B03EE12710E7F2A12948DDBB459B55FE155B19EBC67C9F7F02977902A7A2`。

## 一、结论与评分

| 轮次 | 得分 | 结论 |
|---|---:|---|
| 初审 | 98/100 | PASS |
| 墨菲复审 | 99/100 | PASS |

**最终结论：PASS；最终得分取较低值，为 98/100。H9 锁定范围允许上线。**

初审已超过 96，墨菲复审已超过 98；两轮均无 v1.3 硬性不通过项。

## 二、页面、入口与锁定范围

- 页面/业务对象：增长与运营节奏 → H9「对外公布数据」，管理向用户端公布的设备总数、在线比例、抖动、用户基数与算力分位配置。
- 真实可见入口：PC 在真实登录和一次性验证码后，展开左侧「增长与运营节奏」，点击可见 H9 菜单。`trace.zip` 的 `4-trace.trace` 记录了登录、MFA、可见导航、输入、确认和等待；`01-visible-entry.png` 显示页面、结构化控件、单位、版本和中文说明。
- 关联用户端：H5 从可见首页 `#/pages/index/index` 到网络脉搏/全球网络；不是以后台成功推断 H5 成功。
- 删除或收缩项：本锁定批次为保存/投影/证据闭环修复，提交的页面、截图和 trace 未显示 H9 的路由、卡片、字段、按钮或下钻被移除；本批次记录为“无”。本结论限 H9 锁定范围，不替代其他模块的产品能力基线审查。

## 三、真实用户与数据闭环

1. **真实保存**：可见表单将设备总数 28,432 改为 28,433，填写理由并确认；`PATCH /api/admin/growth/public-stats` 为 200，`03-visible-save-success.png` 显示“版本 v28·已记审计”。运行时版本为 v27 → v28。
2. **刷新与重登**：trace 中重新登录、MFA 和再次从可见菜单进入页面；`05-refresh-relogin.png` 仍显示 v28/28,433，排除仅前端临时状态。
3. **H5 saved 精确投影**：`/api/config/platform` HTTP 200；`user-saved/entry-probe.json` 的 expected/projected/active 均为 28,433、无控制台错误和渲染失败。`user-saved/02-network-pulse.png` 显示“在线设备 28,433”，`03-visible-globe.png` 显示“在线节点 28,433”。
4. **CAS 与幂等**：第二运营员用旧版本得到 `PATCH 409`；`04-cas-conflict.png` 明确说明“本次未覆盖，刷新后核对重试”。同一 `Idempotency-Key` 的重复提交均为 200，但保存路径精确只有一条审计、一条幂等记录、一条 outbox，版本仅推进到 v28。
5. **422 与断网恢复**：非法输入请求为 `422`，页面显示“超出合法范围”；故障注入的 `Route.abort` 与主流程分离，页面给出保存失败/原样重试语义，解除拦截并刷新后重读服务端值。没有将 -1 故障注入误记为成功。
6. **RBAC**：只读和“有菜单无写”均读取成功而 `PATCH 403`；无菜单角色菜单不可见且深链 `GET 403`。菜单、路由/读取、按钮/写接口三层一致。
7. **精确恢复及 H5 restored**：恢复写为 `PATCH 200`，版本 v28 → v29、设备数回到 28,432。H5 再次从首页进入，`/api/config/platform` 为 200，expected/projected/active 均为 28,432；`user-restored/02-network-pulse.png` 与 `03-visible-globe.png` 分别可见 28,432。

## 四、审计、幂等、outbox 与拒绝契约

| 场景 | 审计 | 幂等 | Outbox | 数据结论 |
|---|---|---|---|---|
| 可见保存 v27 → v28 | 精确 1 条 `GROWTH_H9_PUBLIC_STATS_UPDATED`，`SUCCESS/HIGH`；含操作者、理由、28,432→28,433、newVersion=28、键和时间 | 精确 1 条，结果 0、版本 28 | 精确 1 条 `H9:PUBLIC_STATS` / `admin.growth_config_changed` | 真写入且单次生效 |
| 409 并发、422 非法值 | mutation audit 0 | 两个确定性结果精确存在（409、422） | mutation outbox 0 | configVersion 保持 v28，无副作用 |
| 精确恢复 v28 → v29 | 精确 1 条 `GROWTH_H9_PUBLIC_STATS_UPDATED`，`SUCCESS/HIGH`；含操作者、理由、28,433→28,432、newVersion=29、键和时间 | 精确 1 条，结果 0、版本 29 | 精确 1 条同主题事件 | 恢复值与开始快照一致 |

本服务的明确风险契约是：**required audit 仅覆盖成功的高风险变更**；409/422 是确定性拒绝，不发生 mutation audit/outbox，但会持久化幂等结果并证明配置版本不变。该契约满足 v1.3 对“高风险写操作记录操作者、理由和前后值”的硬性要求：本批次两次真实高风险写均有完整审计。v1.3 将“成功有审计、拒绝/失败没有审计”列为需验证的常见风险，但没有把“零副作用的确定性拒绝必须另建 mutation audit”列为硬性不通过条件；本轮已以持久化结果、零 audit/outbox 副作用和版本不变完成该风险验证。

## 五、评分明细

| 维度 | 初审 | 墨菲复审 | 依据 |
|---|---:|---:|---|
| 页面定位与业务对象 | 10 | 10 | 说明、字段、影响预览与公布对象一致。 |
| 数据来源与一致性 | 12 | 12 | PC、公共配置、H5 首页和全球网络精确同值。 |
| 操作闭环 | 13 | 13 | 可见保存、真实 PATCH、刷新重登、精确恢复均成立。 |
| 产品能力完整性与删除合规 | 9 | 9 | 本锁定范围无可见能力收缩证据；结论限 H9。 |
| 真实用户走查与路径可达性 | 10 | 10 | Chromium trace、可见登录/MFA/导航及 H5 截图齐备。 |
| 状态机 | 10 | 10 | v27→v28，冲突不覆盖，恢复至 v29。 |
| 权限与安全 | 10 | 10 | 三类受限角色与三层权限均 fail-closed。 |
| 表单及控件合理性 | 5 | 5 | 数字范围、单位、理由与影响确认均可见。 |
| 文案、状态反馈与多语言 | 5 | 5 | 成功、冲突、越界和重试均为可操作中文。 |
| 异常处理 | 4 | 5 | 初审保留一分给故障注入的天然局限；复审确认 409/422/断网均安全且可恢复。 |
| 审计、幂等和回滚 | 10 | 10 | 两次成功变更均精确审计、幂等、outbox；拒绝零副作用有持久化证明。 |
| **合计** | **98** | **99** | **最终 98/100，PASS** |

## 六、缺陷、受阻项与回归判断

- P0/P1/P2/P3：本锁定范围未发现未闭环缺陷。
- 受阻项：无；真实 Chromium/Playwright 证据完整，非环境受阻。
- 原问题回归：H5 不再将已公布设备数显示为 0；保存态 28,433 和恢复态 28,432 均从用户可见首页、网络脉搏和全球网络复核。
- 上线边界：允许 H9 锁定范围上线；后续若把成功以外的拒绝审计提升为合规要求，应作为新的风险契约/产品要求另行验收，不追溯改变本批次结论。

