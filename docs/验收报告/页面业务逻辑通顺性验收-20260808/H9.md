# H9 对外公布数据——页面业务逻辑通顺性修复复验

## 最终结论

**PASS，最终 98/100。H9 本轮锁定范围已通过生产发布门。**

| 审查 | 得分 | 结论 |
|---|---:|---|
| 独立初审 | 98/100 | PASS |
| 独立墨菲复审 | 99/100 | PASS |
| 跨域墨菲终审 | 100/100 | PASS |

验收依据：`D:\nexion\页面业务逻辑通顺性验收方法.md` v1.3。唯一有效批次为 `H9-F5-UA-FIX9-20260808-0424`；FIX5～FIX8 和修复前的 FAIL 报告均不作为最终结论。

## 已修复问题

修复前，H5 已收到 `/api/config/platform.publicStats.fleetDevices` 的真实非零值，但 App store 在远程配置异步到达前把 `activeDevices` 初始化为 0，配置到达后没有重新基线化，导致首页和全球网络长期显示 0。

本轮已将 H9 远程公开统计纳入响应式同步：合法服务端投影到达后重新计算用户侧设备数；配置缺失、非法或同步失败仍 fail-closed，不使用本地 seed/mock 冒充平台真值。

## 真实用户闭环

- 从真实登录、MFA 和可见「增长与运营节奏 → H9 对外公布数据」入口进入，没有用深链或本地状态绕过入口。
- 管理端将设备总数从 28,432 保存为 28,433，版本 v27→v28；刷新、退出并重新登录后仍读取到 28,433。
- H5 从真实首页进入网络脉搏和全球网络，两处均精确显示 28,433，与 `/api/config/platform` 同值。
- 同一幂等键重放只产生一次版本推进、一次审计和一次 outbox；旧版本并发写返回 409，不覆盖胜者；非法值返回 422；受控断网不显示假成功。
- 只读和有菜单无写角色写接口为 403；无菜单角色不见入口且深链读取为 403。
- 最后通过真实页面恢复为 28,432，版本 v28→v29；H5 两处同步恢复为 28,432。

## 审计、拒绝与恢复

两次成功高风险写各有且只有一条 `HIGH/SUCCESS` 审计、一条幂等记录和一条 outbox，包含操作者、理由、前后值和新版本。409/422 持久化确定性幂等结果，配置版本不变，mutation audit/outbox 均为 0，符合该服务的 success-only mutation audit 契约并证明零副作用。

## 证据与门禁

- 独立报告：`H9-修复复验-独立审查.md`
- 跨域报告：`H9-F5-修复复验-墨菲审查.md`
- 原始证据：`D:\workspace\bug-pic\.restricted\hard-block-method-acceptance-20260808\H9-F5-UA-FIX9-20260808-0424\H9`
- runtime SHA-256：`55CF1857A0FBDFA75CB2F8C995874237FEFF430409FB4EB86765791F3A1B717A`
- trace SHA-256：`CCA00EED51D7BCCBFD706004151B2F6C2042FE912466F4D43130D3B202E76CB4`
- 后端全量：2966 tests，0 failure，0 error，5 skipped。
- PC：`npm run verify` 58/58，生产构建 PASS。
- UniApp：type-check、H9 runtime 4/4、H5 build 均 PASS。
- Playwright：H9/F5 最终候选 2/2 PASS；H9 用例 1.3 分钟。

## 发布边界

本结论只放行 H9 本轮锁定范围，不替代 D5、G4、K1、K6、D7 等模块各自的 HOLD 条件，也不等同于 Nexion 整体生产发布许可。
