# F5 修复复验：独立页面业务逻辑审查

- 审查日期：2026-08-08
- 唯一证据批次：`H9-F5-UA-FIX9-20260808-0424`。
- 审查角色：未参与本轮实现的独立审查 Agent；以第一次使用佣金运营后台的运营人员视角，只读审阅机械执行器产出的 Playwright trace、逐步截图与运行态原始摘要。
- 方法：`D:\nexion\页面业务逻辑通顺性验收方法.md` v1.3。结论仅覆盖 F5 的冻结、解冻、提前解锁、A2 审批、未知结果恢复、CAS、幂等、RBAC、D4、资金/审计/outbox、刷新重登和夹具清理；不外推其他模块。

## 证据身份与完整性

| 项目 | 值 |
|---|---|
| PC 构建 | `Cuyz8v6zWkxu-26tR-Jd_` |
| 后端 Jar SHA-256 | `83B4B03EE12710E7F2A12948DDBB459B55FE155B19EBC67C9F7F02977902A7A2` |
| 原始证据目录 | `D:\workspace\bug-pic\.restricted\hard-block-method-acceptance-20260808\H9-F5-UA-FIX9-20260808-0424\F5` |
| runtime 摘要 SHA-256 | `BA2DA08DC6F56D6B26BFACCDED4A997AE9D05ADA82EBEDFAFB92B95F9D960FD5` |
| Playwright trace SHA-256 | `7397B69E2CC487F03296EAC319170ACED3DAB18B024730B09E3834F89CC0472F` |
| 交互执行方式 | Playwright headless Chromium；真实登录 + MFA + 可见 F5/A2/D4 页面控件。主流程未以 mock 网络、接口直调或隐藏深链替代。 |

## 初审

结论：**通过，98/100**。

| 维度 | 分数 | 独立证据判断 |
|---|---:|---|
| 页面定位与业务对象 | 10/10 | 截图 `01-visible-entry.png` 显示从可见 F5 菜单进入佣金事件审计，佣金状态、动作及 D4/B1/L4 入口可理解。 |
| 数据来源与一致性 | 12/12 | F5 状态、D4 账本及运行态夹具一致；CM-226 的账本键为 `F2-NETWORK-226`。 |
| 操作闭环 | 13/13 | 提案、独立 A2 决定、列表回读、D4 下钻、刷新重登均形成真实链路。 |
| 产品能力完整性与删除合规 | 9/10 | trace 返回六类佣金和完整状态筛选；本包未附独立 Git 新增/修改/删除清单，扣 1 分。 |
| 真实用户走查与路径可达性 | 10/10 | 真登录/MFA、可见导航、可见行操作、D4 与新会话重登均有 trace 和截图。 |
| 状态机 | 10/10 | 冻结→解冻回冷却、提前解锁批准、提前解锁驳回、CAS 败者均符合锁定状态机。 |
| 权限与安全 | 10/10 | 只读与有菜单无写均为 403；无菜单读为 403；各被测身份经 MFA。 |
| 表单及控件合理性 | 5/5 | 操作理由通过可见表单提交；503 后理由保留，用户能继续恢复。 |
| 文案、状态反馈与多语言 | 5/5 | HTTP 200/code 0 的提案与 A2 决定正确表现为 pending/approved/rejected；503 只进入未知结果恢复，不伪报成功。 |
| 异常处理 | 4/5 | 503、409、403 有原始证据；本包未单列 404/422 的用户侧截图，扣 1 分。 |
| 审计、幂等和回滚 | 10/10 | 17 条审计；提前解锁精确一条 outbox；同键审批安全回放；六类夹具残留均归零。 |

### 已形成闭环的真实流程

1. **503/未知结果恢复**：CM-229 发起冻结时注入 503。trace 显示真实可见弹窗两次保留同一理由；运行态摘要确认 commandKey 跨刷新保持不变，恢复提案由独立 A2 驳回，最终仍为 `COOLING`。因此未知结果没有被当作成功，也没有造成状态或资金误写。
2. **冻结→解冻**：CM-225 经独立 A2 批准冻结后再批准解冻，最终 `COOLING`、版本 2；解冻仅恢复冷却期，不产生提前解锁资金副作用。
3. **提前解锁批准与资金一次性**：CM-226 经独立 A2 批准后为 `UNLOCKED`、版本 1。D4 下钻实际打开 `bizNo=F2-NETWORK-226`，截图 `03-d4-linked-view.png` 显示精确一条 `+$2.22 USDT` 佣金账本；运行态 outbox 精确为 `[CM-226, COMMISSION_UNLOCKED, PUBLISHED]` 一条。
4. **提前解锁驳回**：CM-227 的提前解锁被 A2 驳回，最终保持 `COOLING`、版本 0，无资金状态迁移。
5. **CAS 与幂等**：CM-228 的获胜方冻结后，旧版本提前解锁得到 409，最终仍 `FROZEN`、版本 1。相同键的 A2 批准回放返回同一批准结果，未增加状态或资金副作用。
6. **权限与连续性**：`fReadonly`、`fNoWrite` 写均为 403；`fNoMenu` 无菜单且读为 403。新浏览器上下文重新登录后，截图 `04-refresh-relogin.png` 显示 CM-226 已解锁、CM-228 已冻结，与服务器闭包一致。
7. **审计与精确清理**：运行态摘要记录 17 条审计；最终 `events`、`ledgers`、`operations`、`outboxes`、`reserveRows`、`idempotencyRecords` 六项均为 0。先前批次留下的 14 条幂等记录已在本轮执行前按本批采集的 commandKey 精确删除；本报告的通过判断以本轮最终 `idempotencyRecords: 0` 为准。

## 墨菲复审

结论：**通过，99/100**。

复审从最坏后果反向检查：

- 503 不会诱导二次提交：原理由和稳定 commandKey 经刷新仍可恢复，随后独立 A2 驳回，CM-229 保持冷却。
- 并发旧版本不能覆盖获胜状态：CM-228 的 409 后仍只有获胜冻结结果。
- 提前解锁资金影响严格一次：CM-226 只有一条 `COMMISSION_UNLOCKED/PUBLISHED` outbox，且 D4 以同一 `ledgerBizNo` 返回唯一账本。
- 审批拒绝和未知恢复均不改变 CM-227/CM-229 的 `COOLING` 状态。
- 六类清理对象归零，包含此前遗漏的幂等记录；因此不会将测试 commandKey 污染后续验收或掩盖未来重复提交问题。

本轮未发现 P0/P1/P2/P3 产品缺陷。与初审相同，唯一扣分项是 404/422 的单独用户侧截图未随原始包归档；它不影响本轮已锁定的主流程、503、403、409 与资金一次性结论。

## 最终判断

- 初审：**98/100，通过**
- 墨菲复审：**99/100，通过**
- 最终得分：**98/100**（取较低值）
- 最终结论：**PASS**（仅限 F5 本报告锁定范围）

F5 已具备与本次范围匹配的真实浏览器、状态、权限、审计、账务、幂等/CAS、未知结果恢复、刷新重登及清理证据，可结束此前针对该范围的 HOLD。
