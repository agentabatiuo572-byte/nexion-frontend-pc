# F5 单笔佣金处置——页面业务逻辑通顺性修复复验

## 最终结论

**PASS，最终 98/100。F5 本轮锁定范围已通过生产发布门。**

| 审查 | 得分 | 结论 |
|---|---:|---|
| 独立初审 | 98/100 | PASS |
| 独立墨菲复审 | 99/100 | PASS |
| 跨域墨菲终审 | 99/100 | PASS |

验收依据：`D:\nexion\页面业务逻辑通顺性验收方法.md` v1.3。唯一有效批次为 `H9-F5-UA-FIX9-20260808-0424`；FIX5～FIX8 和修复前的 FAIL 报告均不作为最终结论。

## 已修复问题

1. HTTP 200 / 业务码 0 的 A2 提案或审批不再被误报为「结果未知」；只有传输失败、5xx 或确实无法判定的响应进入未知结果恢复，并保留原理由和稳定 commandKey。
2. F5→D4 不再把佣金显示号当账本业务号；后端返回权威 `ledgerBizNo`，页面按该字段下钻，并以 `finance_d4_read` 做服务端与页面权限约束。
3. 刷新后的事件读模型返回真实 `version`、`frozenFromStatus`；自动解锁按 expectedVersion 做 CAS 并推进版本，同事务只写一次 operation、审计和 outbox，不生成第二笔账。

## 真实用户闭环

- 从真实登录、MFA 和可见 F5 菜单进入；全部高敏动作通过真实 F5 与 A2 页面完成。
- 冻结→解冻：`COOLING→FROZEN→COOLING`，解冻恢复冻结前冷却态，不绕过冷却期。
- 提前解锁批准：`COOLING→UNLOCKED`，D4 精确进入 `F2-NETWORK-226` 并只显示一条对应账本；精确一条 `COMMISSION_UNLOCKED/PUBLISHED` outbox。
- 提前解锁驳回：保持 `COOLING`，没有资金状态迁移。
- 受控 503：页面仅显示结果未知，刷新后理由和 commandKey 不变；恢复后由独立 A2 驳回，最终仍为 `COOLING`。
- 并发旧版本得到 409，不能覆盖获胜状态；同键审批重放不重复迁移、记账或审批。
- 只读、有菜单无写和无菜单角色在菜单、路由、按钮和真实接口层均 fail-closed；新浏览器上下文重登后状态仍与服务端一致。

## 审计、账务与清理

本批次记录 17 条匹配操作的审计。佣金事件、账本、operation、outbox、B1 临时分录和幂等记录均按本轮实际业务键精确清理，最终六类计数全部为 0。跨域终审又以实库只读查询确认 FIX9 的 14 个幂等键、FIX8 遗留的 14 个幂等键及 F5 验收业务数据均为 0。

## 证据与门禁

- 独立报告：`F5-修复复验-独立审查.md`
- 跨域报告：`H9-F5-修复复验-墨菲审查.md`
- 原始证据：`D:\workspace\bug-pic\.restricted\hard-block-method-acceptance-20260808\H9-F5-UA-FIX9-20260808-0424\F5`
- runtime SHA-256：`BA2DA08DC6F56D6B26BFACCDED4A997AE9D05ADA82EBEDFAFB92B95F9D960FD5`
- trace SHA-256：`7397B69E2CC487F03296EAC319170ACED3DAB18B024730B09E3834F89CC0472F`
- 后端全量：2966 tests，0 failure，0 error，5 skipped；事务 outbox 异常传播定向测试 PASS。
- PC：F5 目标门和 `npm run verify` 58/58，生产构建 PASS。
- Playwright：H9/F5 最终候选 2/2 PASS；F5 用例 57.0 秒。

## 发布边界

本结论只放行 F5 本轮锁定范围，不替代其他模块的 HOLD 条件，也不等同于 Nexion 整体生产发布许可。
