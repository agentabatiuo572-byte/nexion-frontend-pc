# D5 固定网络确认费最终独立复验报告

> 复验日期：2026-08-07  
> 复验对象：当前共享工作树  
> 后端：`D:\workspace\nexion-backend`  
> PC：`D:\workspace\nexion-ops-console`  
> UniApp：`D:\workspace\NX1.0-UniApp`  
> 权限边界：只读审查、测试与 GET；未创建提现、未改配置、未改数据库或运行态业务状态；唯一人工写入为本报告。

## 1. 最终判定

### 代码与自动化门：PASS

当前共享工作树已经消除上一轮两类红门：

1. PC `networkConfirmFeeUsd` 缺失/null 的旧默认兜底测试已改为 **fail-closed**，D5/WD01/WD02 定向合同测试本轮 **23/23 PASS**，不再沿用旧的 16/18。
2. `smallAmountThresholdUsd`、`payoutSlaHours` 不再作为 PC 可写项；后端 DTO 只为识别恶意/旧客户端字段保留 setter，服务层收到任一字段即返回 `409 D5_EXECUTOR_HOLD` 且零写入。后端 `testCompile` 与 D5 定向测试均已恢复绿色。

### 发布级业务逻辑验收：HOLD

本轮没有发现可判定为 FAIL 的确定性资金错账；固定网络确认费主链的代码、合同、自检与构建均通过。但依照《页面业务逻辑通顺性验收方法》，仍不能把静态代码和 mock 单测替代为真实用户资金链证据：

- 浏览器载体本轮不可用，未完成 PC/H5 的已登录可见入口、刷新和重登走查。
- 只读授权禁止创建提现、修改 D5 版本或注入账本失败，因此未动态证明真实数据库上的 stale `policyVersion` 409、开启抵扣的 `min + ceil`、服务端桶不足拦截、钱包/NEX/订单/账本故障回滚及历史单不改。
- 后端现有 D5 单测仍缺少上述几条提交链聚焦用例；这不是已证实的产品故障，但不足以签发发布级 PASS。

**最终状态：HOLD（代码门 PASS，真实资金链待补验）**。

## 2. 当前基线

| 仓库 | 分支 / HEAD | D5 相关状态 |
|---|---|---|
| `nexion-backend` | `main` / `3c08956` | D5 服务、DTO、授权、mapper、迁移及测试存在未提交修改/新增 |
| `nexion-ops-console` | `main` / `e6d5e5b` | `d-client.ts`、WD02 测试及本报告未提交 |
| `NX1.0-UniApp` | `UniApp` / `e04f5d9` | withdrawal API、页面、store、自检脚本未提交修改 |

本报告只对应当前工作区快照。HEAD 号本身不包含全部被验收改动，发布前应锁定提交后再复跑。

## 3. 修复红门复核

### 3.1 旧 fallback 测试已真正转为 fail-closed

- PC `tests/wd02-network-confirm-fee-contract.test.mjs:147-170` 当前明确断言三网组缺失/null、缺键、null 值、越界和非 0.5 步进均拒绝。
- `lib/admin/d-client.ts:1569-1627` 要求 `sourceByField.networkConfirmFeeUsd` 和完整三网组，合法域 `[0,25]`；读取失败不使用本地 1/1/5 种子。
- 1/1/5 常量仅供运营点击“恢复默认”形成一次真实后端写入，不参与读取兜底或用户下单定价。

**判定：PASS。**

### 3.2 WD01 假执行器已关闭

- PC `app/components/domain-views/d-tabs/d5-params.tsx:42-62,187-241` 的 drafts/控件只保留每日次数、余额比例、三网络费和 NEX 抵扣率；页面不再提供小额免审线、到账时效输入或提交按钮。
- 页面 `:211-213` 明示“小额免审与到账时效执行器尚未形成真实业务闭环，本页不提供修改，也不会显示已生效”。
- PC `D5OwnedChanges` 可写集合不含 `smallAmountThresholdUsd/payoutSlaHours`。
- 后端 `WithdrawalLimitsUpdateRequest.java:48-56` 保留字段存在性识别；`OpsFinanceService.java:503-509` 在幂等 claim、配置写入和审计之前返回 `409 D5_EXECUTOR_HOLD`。
- 后端 `OpsFinanceServiceTest.java:402-417` 验证这两个字段返回 HOLD，配置值与 `withdrawal.d5.version` 均不变化。
- policy 继续返回两字段，只作当前服务端配置的诊断/版本投影，不代表可写执行器已接通。

**判定：PASS。**

## 4. 固定网络确认费逐项证据

### 4.1 三网值域、步进、默认值与整组 CAS

- 迁移 `scripts/migrations/20260807_nexion_hard_blockers.sql:76-83` 种入 TRC20/BEP20/ERC20 = `1/1/5`；重复执行只更新元数据，不覆盖现有运营值。
- `OpsFinanceService.java:2163-2169` 要求三网键集合精确一致，逐值校验 `[0,25]` 与 0.5 步进。
- `OpsFinanceService.java:542-554` 先对 `withdrawal.d5.version` 做加锁读取和版本 CAS；过期返回 `409 CONFIG_VERSION_CONFLICT`。
- `OpsFinanceService.java:590-640` 在同一 `@Transactional(rollbackFor=Exception.class)` 事务内写三网、审计、outbox，最后递增聚合版本。
- PC `d5-params.tsx:220-235` 只允许三值作为一个对象一次提交，输入步进 0.5，任一字段非法时整组提交按钮禁用。

**判定：PASS（代码/合同）；真实双运营员竞争与数据库回滚为 HOLD。**

### 4.2 `/api/withdrawals/policy` 完整费用口径与版本

- `AppWithdrawalService.java:67-92` 返回最小额、每日次数、余额比例、三网固定费、NEX 抵扣率、诊断投影、`policyVersion`、H1 冷却/合规、启用网络、J1 门及来源。
- `AppWithdrawalService.java:400-436` 从服务端配置读取三网费与抵扣率，校验范围/步进/正值，并把版本、费用、抵扣率、诊断投影和启用网络纳入服务端哈希。
- `AppWithdrawalServiceTest.java:167-188` 验证 1/1/5、抵扣率、启用网络、版本和 D5+H1 口径，并确认旧比例费/惩罚费字段不再出现在 policy。
- UniApp `withdrawal-api.ts:131-179` 对 policy 做严格解析，不接受字段缺失或费用越界。

运行态未登录 `GET http://127.0.0.1:8110/api/withdrawals/policy` 返回 401，证明受保护；本轮没有已登录 App 用户 GET payload。

**判定：PASS（代码/测试/鉴权）；已认证运行态 payload 为 HOLD。**

### 4.3 submit 的 `policyVersion/useNexFeeOffset`

- `AppWithdrawalController.java:34-42` 始终把请求中的 `policyVersion` 和显式抵扣意图传给服务，不使用兼容重载代客户端补版本。
- `AppWithdrawalService.java:107-142` 把金额、网络、地址、版本和抵扣意图共同纳入请求哈希；版本空或过期返回 `409 WITHDRAWAL_POLICY_VERSION_CONFLICT` 并回当前版本。
- UniApp `withdrawal-api.ts:268-282` 的真实 POST 只提交用户意图、版本和幂等键，不提交客户端自造费用快照。
- `wallet-withdraw.vue:850-985` 在确认前冻结账号、网络、地址、金额、抵扣意图、版本和幂等键，失败后重新拉 policy。

**判定：PASS（契约）；真实 stale 409 为 HOLD。**

### 4.4 无意图不烧 NEX；开启时 `min + ceil`

- 兼容重载 `AppWithdrawalService.java:95-103` 明确用 `false`，不会把旧调用者默认为抵扣。
- `AppWithdrawalService.java:169-187`：关闭时 required/burned/waived 均为 0；开启时 requiredNex=`ceil(networkFee/rate)`，burn=`min(walletNex,requiredNex)`，抵扣额封顶网络费，实付不为负。
- `AppWithdrawalServiceTest.java:92-138` 验证默认关闭时 NEX 扣减为 0、无 NEX 账本、网络费 $1、到手 $99、新单 H1 惩罚为 0，并核对落单快照。
- UniApp `selfcheck-withdrawfee` 本轮 45/45，覆盖足额/不足额/零余额、ceil、费用封顶、0 费网络、无意图烧 NEX 毒样本及快照等式。

**判定：PASS（无意图链）；后端开启抵扣分支没有聚焦单测/真实账本，保持 HOLD。**

### 4.5 服务端快照、新单 H1=0、历史安全

- `AppWithdrawalMapper.java:117-138` 插单持久化 D2 费用组成、`d5_policy_version` 与 `d5_use_nex_fee_offset`。
- `AppWithdrawalService.java:169-187,236-257` 对新单固定 H1 penalty=0，按服务端网络费计算后先保留钱包、再插单、再发 USDT/NEX 账本。
- 迁移只给 `nx_withdrawal_order` 增加 nullable 版本/抵扣意图列，没有 UPDATE 历史提现单；旧费用快照列保留。
- PC WD02 合同测试覆盖旧单/新单判型、旧单等式、新单等式、null 旧字段和“double DTO 把旧字段零填”的毒样本。
- UniApp 自检覆盖失败单 NEX 独立退款键及历史单只有 `nexBurned>0` 才退款。

**判定：PASS（迁移与解析安全）；真实历史行抽查为 HOLD。**

### 4.6 钱包/NEX/订单/账本事务与桶校验

- `AppWithdrawalService.java:105` 的明确版本 submit 使用 `@Transactional(rollbackFor=Exception.class)`。
- `AppWithdrawalMapper.java:19-46` 对用户、钱包、地址加 `FOR UPDATE`；钱包更新还带 version CAS。
- 业务顺序为钱包 USDT/NEX/pending CAS → 订单 → D4 USDT/NEX 账本；任一步异常抛出，使外层事务回滚。
- `AppWithdrawalServiceTest.java:140-165` 验证同一幂等请求只扣一次钱包、只插一单、账本不重复。
- `AppWithdrawalService.java:161-163` 在资金移动前调用服务端 `EarningsReleaseService.assertWithdrawable`；`EarningsReleaseServiceTest` 4/4 通过。

现有 `AppWithdrawalServiceTest` 仍把 `EarningsReleaseService` 以 null 注入，未验证提交服务与桶拦截的集成；也没有 MySQL 故障注入来证明订单/账本失败时数据库零半态。

**判定：HOLD。**

## 5. 权限、并发、幂等、回滚、刷新/重登

| 维度 | 本轮证据 | 状态 |
|---|---|---|
| 权限 | 未登录 App policy 和 D5 管理 GET 均为 401；D5 GET/字段写权限在控制器和授权器分离；WD01 假写字段走服务端 HOLD | **PASS（静态/未登录）**；低权限角色 403 **HOLD** |
| 并发 | D5 聚合版本加锁+CAS；三网整组事务；钱包行锁+版本 CAS | **PASS（实现）**；双操作者实竞态 **HOLD** |
| 幂等 | 版本/抵扣意图进入请求哈希；重放测试确认钱包/订单/账本不重复 | **PASS（单测）**；数据库并发/UNKNOWN 恢复 **HOLD** |
| 回滚 | submit 与三网配置均为回滚事务，异常上抛 | **PASS（事务设计）**；真实故障注入 **HOLD** |
| 刷新 | PC/UniApp 都在失败时清空旧快照并重新拉服务端权威数据 | **PASS（代码/合同）**；真实浏览器刷新/重登 **HOLD** |

## 6. 本轮实跑结果

### 后端

```text
D5 定向：104 tests，0 failure，0 error，0 skipped，BUILD SUCCESS
全量：2906 tests，0 failure，0 error，5 skipped，BUILD SUCCESS
```

定向范围：`AppWithdrawalServiceTest`、`OpsFinanceServiceTest`、`D5WithdrawalAuthorizationTest`、`D5WithdrawalEventSchemaMigrationContractTest`、`D2WithdrawalClosureContractTest`、`EarningsReleaseServiceTest`。

### PC

```text
D5/WD01/WD02 定向合同：23/23 PASS
npm run verify：58/58 gears PASS
Next.js production build：PASS
```

### UniApp

```text
selfcheck-withdrawfee：45/45 PASS
selfcheck-withdraw-freeze：10/10 PASS
npm run verify：PASS（type-check + KYC removal contract）
npm run build:h5：PASS
```

### 运行态只读 GET

```text
GET :8110/api/withdrawals/policy      -> 401
GET :8110/api/admin/withdraw/limits   -> 401
GET :3002/finance/params              -> 200
```

浏览器控制载体本轮返回不可用，未取得已登录页面 DOM 或真实 App policy payload；此项按“验收环境受阻，待补验”处理，不降格为产品 FAIL。

## 7. HOLD 转 PASS 的最小补验

1. 在隔离测试账号和可回滚夹具中，以已登录 App GET 取得真实 policy，确认三网 1/1/5（或当前运营值）、完整费用口径和 `policyVersion`。
2. 补后端提交聚焦测试：空/旧版本 409；offset=true 的足额/不足额 `min+ceil`；offset=false 永远零烧；服务端桶不足 409。
3. 做 MySQL 集成故障注入：订单、USDT 账本、NEX 账本任一步失败时钱包/NEX/pending/订单/账本均回滚。
4. 用两名运营员验证三网整组 CAS：旧版本 409，三值、版本、审计和 outbox 零半态。
5. 从 PC/H5 登录页完成可见入口、刷新、退出重登；核对新单 H1=0、历史单仍显示原快照。
6. 把当前改动锁定到明确 revision 后，按同一 revision 重跑上述证据。

## 8. 初审、复审与生命值

- 初审：**97/100，通过**。旧测试结论已作废，本轮独立实跑三端门并核对 WD01 HOLD、固定网络费、版本、抵扣意图、事务与历史安全。
- 复审：**99/100，通过**。没有把自动化全绿冒充真实资金链 PASS，也没有把浏览器环境受阻误判成产品 FAIL。
- 任务完成恢复 10 点，生命值按上限计：**100/100**。
