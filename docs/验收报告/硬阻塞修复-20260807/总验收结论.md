# Nexion 硬阻塞修复总验收结论

- 日期：2026-08-07（Asia/Tokyo）
- 范围：H9、D5、G4、K1、F5、K6；D7 仅做 HOLD 假链路清理
- 候选：绑定 backend、PC、UniApp 当前共享工作树；未提交、未发布
- 总裁决：**HOLD，不具备整体生产发布资格**

## 一、模块裁决

| 模块 | 最终裁决 | 已闭环内容 | 未关闭门槛 |
|---|---|---|---|
| H9 对外公布数据 | **PASS** | 真实服务端单例、8 项整组 CAS、幂等、审计、版本、`effectiveAt`、公开安全投影、两端缺失禁显 0；真实写入后恢复原值并核对审计/outbox。 | 无本轮阻断。 |
| D5 固定网络确认费 | **HOLD**（代码门 PASS） | 三网费进入 policy/version、服务端重算与订单快照；NEX 抵扣按意图执行；新单不再收 H1 惩罚费；资金操作保持同事务。无执行器的“小额阈值/出款 SLA”已移出可写面并由服务端拒绝伪修改。 | 尚缺已登录真实 policy/submit 动态证明、真实 MySQL 故障注入下钱包/NEX/订单/账本全回滚、历史订单不改和刷新重登证据。 |
| G4 Genesis | **HOLD**（技术契约 PASS） | 档位整表 CAS、连续区间/已售下限、软删除；市场关闭 fail-closed；邀请码签发/作废/一次兑换改为真实服务端，扩大熵并收敛重复账号并发冲突。 | 尚缺真实 MySQL 双连接竞争与部分写入回滚故障注入，以及登录态浏览器权限、刷新/重登和关闭态撤单实走。 |
| K1 收益释放 | **HOLD**（manual-only 主链 PASS） | 七参数 CAS、三桶收益分录、簇锁/绑定条件、真实保护分录清单、精确高敏 RBAC、B1 覆盖率失败关闭、人工放行、提现二检及 UniApp 账户级快照清理/登录刷新闭环。 | 自动可信证明必须等待 `OPEN-ENG-001`；当前服务端强制 `manual_only`。另有非阻断 P2：同账号退出再登录时，在途响应存在 ABA 覆盖窗口，资金服务端仍失败关闭。 |
| F5 单笔佣金处置 | **PASS** | canonical GET 返回状态/版本/冻结前状态；人工与自动解锁均按状态+版本 CAS；自动解锁版本递增并同事务写唯一操作、必达审计和 outbox；无重复资金分录。 | 无本轮阻断。 |
| K6 当前仓闭环 | **隔离联调候选 PASS；真实设备与生产 HOLD** | 15 相位账本、命令/目标/应用版本证据、撤销/重发/换目标/受限重试、主动对账、纯 GET 读取、幂等/CAS、非法跃迁和旧版本拒绝均已接通三端契约。 | 未对真实设备发送动作；`OPEN-ENG-001` 未签，C2↔Janus 仍为 `ENGINEERING_NOT_PROVEN`，不得宣称生产就绪。 |
| D7 越南银行卡出款 | **HOLD** | 已删除 localStorage、“保存成功/已生效/历史留痕”等假链路；入口只读提示真实通道未接入，通道保持关闭。 | 本轮明确不新增真实越南银行出款接口。 |

因此，六个目标模块中 H9、F5 获得 PASS；D5、G4、K1 为安全失败关闭的 HOLD；K6 仅隔离联调候选 PASS、真实设备和生产 HOLD。D7 按计划继续 HOLD。任何 HOLD 均不得被技术测试通过替代为产品 PASS。

## 二、统一验证结果

- Backend：当前最终树全量 `mvn test` 共 2914 项，0 failures、0 errors、5 skipped，构建成功；K1 最终定向/架构门 26/26 通过。各模块报告另保留 H9 56/56、D5 104/104、G4 36/36、F5/A2 147/147、K6 72/72 的独立定向证据。
- PC：`npm run verify` 58/58，生产构建通过；H9、D5、G4、K1、F5、K6 定向契约门均通过。
- UniApp：`npm run verify`、`npm run type-check`、`npm run build:h5` 通过；K1 runtime 4/4，H9/D5/G4/K6 对应定向契约通过。
- 三个仓库均执行 `git diff --check`，未发现空白错误；工作区原有大量混合改动与未跟踪材料均保留，未清理、未覆盖。

## 三、真实入口与证据边界

- PC 已从可见入口走查 H9、D5、D7、G4、K1、F5、K6；H9 完成一次真实服务端修改与恢复，并核对版本、审计、幂等和 outbox。
- H5 走查验证 H9 配置消费与 Genesis 缺失/关闭 fail-closed。未建立用户登录态的资金提交、邀请码兑换、人工收益释放或真实设备接管动作，不把未执行的破坏性动作包装成通过。
- D5、G4 的真实资金/并发故障注入，K1 自动可信证明，K6 真实设备链均保留为明确发布门；后续补验必须使用干净测试数据、真实登录、真实后端与可回滚环境。

## 四、独立复审

每个目标模块均由非实现验收 Agent 独立复核；另设跨域墨菲审查，检查权限、结果未知、并发、刷新重登、契约与回滚。报告：

- [H9.md](./H9.md)
- [D5.md](./D5.md)
- [G4.md](./G4.md)
- [K1.md](./K1.md)
- [F5.md](./F5.md)
- [K6.md](./K6.md)
- [跨域墨菲审查.md](./跨域墨菲审查.md)

## 五、发布与回退结论

当前候选**不得整体发布到生产**。若仅部署已通过部分，仍须遵循“后端迁移与接口先于 PC/UniApp”的顺序，并保持提现总开关、Genesis closed、K1 `manual_only` 和 K6 动作能力开关可用。已发生的资金分录、命令账本与审计记录不得通过删表或回滚历史记录处理。

