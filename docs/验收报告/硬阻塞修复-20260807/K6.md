# K6 最终独立复验：15 相位接管账本与三端对账

- 验收日期：2026-08-07（Asia/Tokyo）
- 验收身份：K6 独立验收 Agent
- 范围：`nexion-backend`、`nexion-ops-console`、`NX1.0-UniApp`、`C2--Janus`
- 执行边界：只读审查、编译/测试、数据库只读查询和 HTTP GET；未调用登录或任何设备动作接口，未修改产品代码、配置、数据库或设备命令状态；仅覆盖本报告。

## 最终裁决

| 验收层级 | 结论 | 裁决 |
|---|---|---|
| 本地三端契约 / 隔离联调候选 | **PASS** | 先前阻断项已闭环：15 相位、CAS、读写权限、持久幂等、主动对账 `fresh=false`、成功/撤销/对账证据、旧命令拒绝、完整 handoff 相位和 PC 超时提示均成立。 |
| 真实设备实景 | **HOLD（未执行，不是实现失败）** | 本次明确禁止发送设备动作；数据库中接管账本当前为 0 行，故没有真实 C2 投递、WebView/原生载体 handoff 或撤销清理回执可供实景复现。 |
| 生产 / 投产 | **HOLD** | `OPEN-ENG-001` 未发现签署件；C2 文档仍为 `DESIGN_FROZEN / ENGINEERING_NOT_PROVEN`。联调 PASS 不得外推为生产链路就绪。 |

本报告的 **PASS** 仅表示当前代码、迁移和隔离行为契约已满足 K6 联调候选门；生产结论单列且保持 **HOLD**。

## 逐项证据

### 1. 15 相位、账本字段与迁移：PASS

- MySQL 实库只读核验：`nx_janus_takeover_execution` 已存在 `actual_target_version`、`actual_target_catalog_version`、`handoff_receipt`、`row_version`；`chk_janus_takeover_phase` 实际包含 15 个相位，失败分类约束包含 7 类。当前表行数为 0。
- 创建迁移定义了 15 相位、命令 ID/版本、批准与实际目标、设备应用/App 版本、交接凭证、失败面、因果来源、主动对账字段和行版本：`nexion-backend/scripts/migrations/20260807_nexion_hard_blockers.sql:250-286`。
- 已部署旧表的补丁迁移补齐实际目标版本/目录版本和 handoff receipt，并被启动迁移脚本纳入：`scripts/migrations/20260807_k6_evidence_hardening.sql:1-18`、`scripts/apply_startup_schema_migrations.ps1:18-19`。
- 服务端迁移图禁止 `LOADING → SUCCEEDED`，成功必须经过 `HANDOFF_FETCHING → HANDOFF_MERGING → HANDOFF_ACKED`：`JanusTakeoverService.java:35-43,175-183`。

说明：当前表是每个 SID 的权威执行投影，并非逐相位追加事件表；本期规格要求的当前账本字段与 CAS 均已具备，审计动作另写审计中心。

### 2. 批准目标、实际目标、版本、失败和因果来源：PASS

- ACTIVATE/CHANGE_TARGET 消费前重新读取精确 `targetId + targetVersion + catalogVersion` 且要求 ACTIVE；批准目录不是由 App 自报：`JanusTakeoverService.java:144-160,196`。
- `SUCCEEDED`、`REVOKED` 与 reconciliation 均要求实际目标三元组、`deviceAppliedVersion`、`deviceAppVersion`、`handoffReceipt`；非撤销结果还必须与批准目标三元组完全相等，设备应用版本必须等于当前命令版本：`JanusTakeoverService.java:168-193`。
- 失败只能属于 7 类；FAILED/REVOKE_FAILED 必须有分类和消息，`failure_phase` 以失败前相位写入，不再依赖可选 failure code：`JanusTakeoverService.java:176-182`、`JanusTakeoverMapper.java:86-108`。
- 人工链路写 `cause_request_id`，策略链路写 `cause_decision_id`；人工审计 detail 同时带 idempotency key，可由请求号反查。`cause_audit_id` 当前仍是可空扩展位，未作为本期因果来源唯一门槛：`OpsJanusService.java:202-204,385-396`、`JanusTakeoverMapper.java:39-60`。
- PC 归一器保留实际目标版本/目录版本、设备/App 版本、凭证和三类因果 ID；详情页展示命令/设备应用版本、批准/实际目标、失败分类和人工请求/审计或自动判定来源：`k6-contract.ts:255-274`、`device-detail.tsx:216-238`。

非阻断改进项：详情目前把实际目标版本/目录版本和 `deviceAppVersion` 保留在模型中但未全部展开显示；这不影响服务端成功判定，但后续可提升人工取证可读性。

### 3. 管理端五动作与读写边界：PASS

- revoke、revoke:resend、target、retry、applied:refresh 均由 `risk_k6_write` 保护；纯读 GET applied 由 `risk_k6_read` 保护：`OpsJanusController.java:71-93`。
- 实际未认证 GET `/api/admin/janus/devices/K6-READ-ONLY-PROBE/takeover/applied` 返回 `401 ADMIN_AUTH_REQUIRED`；未发送写请求或设备命令。
- `GET applied` 只调用普通 `find`，不加锁、不创建 reconciliation、不更新行；只有 `POST applied:refresh` 才经理由、幂等键和 `expectedVersion` 创建请求：`JanusTakeoverService.java:121-142`。
- PC 采用 `POST applied:refresh → 携 reconciliationId 轮询 GET`；15 秒超时返回 `fresh=false` 并明确提示未拿旧值冒充实时值：`k6-client.ts:298-305`、`device-detail.tsx:103-112`。
- viewer 不渲染接管写动作；后端 RBAC 仍为最终裁决：`device-detail.tsx:240-248`。

### 4. 撤销、重发、换目标、重试与回滚：PASS（联调契约）

- 撤销只进入 `REVOKE_PENDING_ACK`；设备上报带 reset 三元组与证据后才进入 `REVOKED`：`JanusTakeoverService.java:71-80,180-193`。
- resend 只允许 `REVOKE_PENDING_ACK + REVOKE`，仅增加 attempts 与行版本，不改 command ID/command version：`JanusTakeoverService.java:82-92`、`JanusTakeoverMapper.java:79-84`。
- 换目标先校验批准目录精确版本；retry 只接受 FAILED 且 failure class 为 `delivery` 或 `webview`，`handoff/lease/cleanup/target/contract` 均不能原地重试：`JanusTakeoverService.java:94-118`。
- UniApp 的 REVOKE 先应用 RESET，再上报 `REVOKED + none/0/0 + command/app version + receipt`；异常上报 `REVOKE_FAILED/cleanup`：`janus-c2.ts:81-101`。

真实载体是否确实关闭目标页、清理账户态并恢复服务端权威，仍需设备/载体回执和 C2 生产信任契约证明，因此真实设备与生产层保持 HOLD。

### 5. App 协议、越级/倒序与旧命令：PASS

- 默认导航器即使 launcher/openURL 接受，也会抛 `JANUS_HANDOFF_PROOF_UNAVAILABLE`，不会写成功态；只有真实载体返回非空 handoff receipt 才存储应用态：`janus-runtime.ts:41-67,102-124`。
- App 成功上报顺序为 `RECEIVED → LOADING → HANDOFF_FETCHING → HANDOFF_MERGING → HANDOFF_ACKED → SUCCEEDED`，最终带批准目标三元组、命令/App 版本与 receipt：`janus-c2.ts:104-140`。
- 服务端用封闭相位图及 `row_version + command_id + command_version + fromPhase` CAS 拒绝越级、倒序和旧命令：`JanusTakeoverService.java:163-183`、`JanusTakeoverMapper.java:86-108`。
- 设备运行态在任何导航/写存储之前拒绝低版本命令与同版本不同 commandId；同命令同版本且已有 receipt 只幂等返回：`janus-runtime.ts:92-101`。
- 本次额外以 esbuild 内存加载真实模块并注入模拟存储/导航验证：低版本与版本碰撞均在 navigator 调用数 0、存储不变时拒绝；精确重放返回原 receipt。另验证空运行态 QUERY_APPLIED 不发 progress，完整成功路径按上述 6 个相位有序上报且证据三元组精确一致。该测试没有写产品文件、没有导航真实 URL、没有发送网络设备动作。

## 权限、未知结果、并发、刷新/重登、回滚矩阵

| 维度 | 结论 | 证据与边界 |
|---|---|---|
| 权限 / RBAC | **PASS** | read/write 已拆分；viewer UI 收口；未认证 GET 实测 401。未用登录 POST 获取真实会话，故未做多角色在线点击。 |
| CAS / 并发 | **PASS（实现级）** | 管理写进入持久幂等事务，业务动作在 READ_COMMITTED 事务内；账本用 `FOR UPDATE`，更新再以 rowVersion/command/version/fromPhase CAS。因禁止写数据库，未执行真实双写竞争。证据：`AdminIdempotencyTransactionExecutor.java:34-40,105-111`、`JanusTakeoverMapper.java:30-34,62-84,86-132`。 |
| 结果未知 | **PASS** | PC 对写入 fingerprint 持久保存稳定 key，网络中断/5xx/回包不完整保持 unknown；重试复用原 key。后端 revoke/resend/target/retry/applied:refresh 全部进入 `AdminIdempotencyService`，成功响应持久回放、payload 不同冲突。证据：`k6-client.ts:87-208`、`JanusTakeoverService.java:65-132,208-214`。 |
| 主动对账 | **PASS** | 写权限 POST 只创建 request；GET 纯读；无本地权威运行态时 App 静默不造证，PC 超时保持 `fresh=false`。 |
| 刷新 | **PASS（代码/契约）** | 写请求 key 存 sessionStorage；刷新后同 fingerprint 复用。详情重新读取服务端账本；reconciliationId 和 fresh 状态在服务端，不依赖组件内成功态。 |
| 重登 | **HOLD（未执行）** | 本次不允许登录 POST，未做真实退出/重登；代码没有用浏览器本地态代替服务端账本。未知写入 key 留在当前 tab 用于同请求恢复，fingerprint 含 method/path/body/operator。 |
| 回滚/撤销 | **PASS（联调契约）/ HOLD（真实设备）** | pending ACK、同 commandId resend、RESET evidence 与 REVOKE_FAILED 均成立；真实目标页关闭和账户态清理未执行。 |

## 测试与运行证据

- 后端定向测试：`JanusTakeoverServiceTest + OpsJanusServiceTest + OpsJanusRemoteTargetServiceTest + AdminIdempotencyServiceTest`：**72 tests，0 failure，0 error**；同时完成主/测试源码编译。
- PC `npm run test:k6-contract`：**18/18 PASS**。
- PC `tests/k6-takeover-observability-contract.test.mjs`：**11/11 PASS**。
- UniApp `npm run type-check`：**PASS**。
- UniApp `scripts/hard-block-k6-runtime-contract.test.mjs`：**2/2 PASS**。
- UniApp 两组内存行为复验：**PASS**（旧命令/碰撞 fail-closed、精确重放幂等；空运行态不造 fresh、成功相位与证据顺序正确）。
- MySQL information_schema 与只读 COUNT：**PASS**（迁移字段和 15 相位约束已落地；账本 0 行）。
- 未认证 GET：**401 ADMIN_AUTH_REQUIRED**。

测试/证据上限：未发送真实设备动作，因此没有真实 C2 投递 ACK、真实 WebView/native handoff receipt、撤销清理回执、在线多角色点击、两写者实库竞争或刷新/重登录像；这些不能被单元/契约绿灯替代。

## OPEN-ENG-001 生产门

`C2--Janus/00-COLLEAGUE-GUIDE.md:5-13,148-188` 与 `01-CODEX-START-HERE.md:137-177,247-272` 仍把 `OPEN-ENG-001` 签署、contract digest、producer/consumer conformance、生产拓扑/权威/信任/NFR 裁决列为生产实现前置门。本次检索未发现签署人、签署日期或 APPROVED/ACCEPTED 决议，当前状态仍是 `ENGINEERING_NOT_PROVEN`。

因此：**联调候选 PASS；真实设备 HOLD；生产 HOLD。** 在签署与真实载体验证完成前，禁止把“命令受理”“openURL 返回”或“模拟 receipt”表述为生产接管成功。

## 独立自审

- 初审：**98/100，通过**。逐条反查 15 相位、实际证据、RBAC、读写边界、持久幂等、CAS、空运行态、设备旧命令、刷新/重登、撤销及 OPEN-ENG-001；对禁止执行的实景项单独标 HOLD。
- 复审：**99/100，通过**。重新核对实库约束、三端测试输出和报告结论，确认 PASS 未越过“隔离联调候选”边界，生产仍为 HOLD；确认未发送设备动作且仅覆盖本报告。
