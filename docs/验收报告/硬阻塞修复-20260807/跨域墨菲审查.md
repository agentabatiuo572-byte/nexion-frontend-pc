# 硬阻塞修复最终跨域墨菲复审

- 复审时间：2026-08-07 23:40（Asia/Tokyo）
- 复审角色：非实现、跨域、只读墨菲审查 Agent
- 范围：H9、D5、G4、K1、F5、K6；D7 强制 HOLD
- 仓库：backend `main@3c089566202700515f677d43ce61407d146c4eb5`、PC `main@e6d5e5b129961669df463b0d29f9b48f2d314561`、UniApp `UniApp@e04f5d962fd648a90c5e193973df7e7e504e660d`
- 工作树状态：backend / PC / UniApp 分别有 265 / 222 / 90 条未提交记录；结论绑定上述时点的稳定共享工作树，三个 HEAD 本身不能重放本轮候选
- 执行边界：只读源码、文档、测试和既有报告；本 Agent 只额外运行无真实业务写入的 K1 单元/合同测试。未调用登录、资金、审批、邀请码、配置或设备动作接口，未发送 POST/PATCH/DELETE，未修改产品代码、配置、数据库或业务/设备状态；本文件是唯一编辑文件

## 一、最终候选发布裁决

**总判定：HOLD。**

本轮旧报告中的 H9/D5/G4/K1/F5/K6 确定性代码阻断，经当前源码、专项报告和三端门禁复核，均已关闭；本复审没有发现仍可复现的阻断级、高级或中级确定性产品缺陷。因此，当前共享树不再是上一版的 `FAIL`，可以进入“锁定 revision、隔离联调、补真实载体证据”的候选阶段。

但是，它仍不能签发整包生产 PASS：

1. **D7 按任务要求固定 HOLD。** 当前页面、registry 和产品更新日志已统一为“真实通道未接入、只读、关闭、不假保存”，这是正确的失败关闭，不是交付完成。
2. **K6 生产固定 HOLD。** `C2--Janus/02-PACKAGE-MANIFEST.md:6` 仍为 `DESIGN_FROZEN / ROADMAP_FROZEN / ENGINEERING_NOT_PROVEN`；`00-COLLEAGUE-GUIDE.md:155-170`、`01-CODEX-START-HERE.md:137-177` 要求 `OPEN-ENG-001` 签署后才能进入生产工程。本轮没有发现签署人、签署日期或 APPROVED/ACCEPTED 决议。
3. **D5、G4、K1 仍欠只读授权无法制造的真实资金/双事务/刷新重登证据。** 这些是发布证据 HOLD，不是当前已证实的代码 FAIL。
4. **三仓尚未形成可重放 revision。** 发布前必须冻结兼容提交并在同一 revision 重跑门禁；不能用“当前脏树全绿”替代可追溯制品。

分层裁决如下：

| 层级 | 结论 | 允许范围 |
|---|---|---|
| 当前源码与自动化门 | **PASS** | 可以锁候选、提交、部署隔离环境并补验 |
| 隔离联调候选 | **HOLD** | D5/G4/K1 仍需真实竞争、故障注入、刷新/重登；K6 可做隔离联调但不得触发生产设备 |
| 整包生产发布 | **HOLD** | D7 未实现；K6 `OPEN-ENG-001` 未签；revision 尚未锁定 |
| 当前确定性产品故障 | **0 项** | 本轮不再维持旧版 FAIL |

## 二、当前门禁与证据上限

同一稳定工作树的主门记录：

- Backend 全量：**2914 tests / 0 failures / 0 errors / 5 skipped**。
- PC：`npm run verify` **58/58 gears PASS**，单独 production build PASS。
- UniApp：`npm run verify` PASS，K1 runtime **4/4 PASS**，H5 build PASS。
- 本 Agent 在最终 K1 修复后额外运行 backend `RiskReleaseParamsServiceTest,EarningsReleaseServiceTest,AppWithdrawalServiceTest`：**22/22 PASS**；UniApp `hard-block-k1-runtime-contract`：**4/4 PASS**。

端口 8110 当前是早前验收 jar，不是本轮稳定源码构建。本复审没有用该进程的响应为当前候选背书，也没有把历史 H9 真实写链、旧数据库行或旧模块报告中的运行态数字外推到当前源码。自动化绿灯同样不能替代真实资金、双连接竞争、真实设备 handoff 和退出重登录像；这些在下文明确记为 HOLD。

## 三、跨域墨菲矩阵

| 不变量 | 当前结论 | 复审结果 |
|---|---|---|
| RBAC / 越权 | **代码 PASS / 在线角色矩阵 HOLD** | H9、D5、G4、K1、F5、K6 都由服务端 authority 最终裁决；K1 人工放行使用独立 `risk_k1_cluster_release`，K6 read/write 已拆分且 GET applied 纯读。仍需用 read-only、writer、无权限三类真实会话补 200/403/控件矩阵。 |
| 结果未知 / 幂等 | **PASS（实现）/ 实断链 HOLD** | 管理写进入持久幂等边界；PC 对 H9、D5、G4、K1、F5、K6 的未知结果保留稳定 command key，确定结果后才清除。尚未在当前 revision 注入“服务端已提交、响应丢失”并核对单审计/单账本。 |
| 配置缺失 fail-closed | **PASS** | H9 缺聚合失败关闭；D5 三网费用/版本缺失拒绝；G4 catalog 缺失仍可查看但交易关闭、sale policy 缺失关闭交易；K1 七参数缺失 503，未签可信证明时服务端强制 `manual_only`；D7 通道关闭；K6 未批准目标或缺 handoff 证据不能成功。 |
| CAS / 并发 | **PASS（结构）/ 实竞争 HOLD** | H9/D5/G4/K1/F5/K6 均具 expectedVersion、行锁或状态/版本 CAS。G4 邀请账号唯一键、K1 簇锁、F5 自动解锁单胜者和 K6 command/version/fromPhase 已补齐；仍欠 D5/G4/K1 的真实 MySQL 双连接竞争证据。 |
| 单命令事务 / 半提交 | **PASS（设计与单测）/ 故障注入 HOLD** | D5 钱包/订单/双账本、G4 多档写、K1 桶迁移/审计、F5 状态/operation/audit/outbox 均在回滚事务；K6 管理命令与幂等成功结果同事务。真实 MySQL 中途失败后的零半态尚未全部注入验证。 |
| 刷新 / 重登真值 | **代码 PASS / 实走 HOLD** | H9/D5/G4/F5/K6 都重新读取服务端；K1 已在账号 rebind 时清空快照、拒绝跨账号迟到响应，并在登录成功后立即刷新。真实退出重登未在当前制品执行。 |
| 跨端契约 | **PASS** | H9 八键/分位等价；D5 policyVersion/三网费/NEX 意图等价；G4 catalog/sale/account 单源；K1 `clusterRestricted` 为必填布尔且提现缺快照为 0；F5 PC 读取后端 `status/version/frozenFromStatus`；K6 精确目标三元组、commandVersion、receipt 与 15 相位一致。 |
| 历史资金不回写 | **PASS** | D5 迁移只加 nullable 快照列；K1 只对新奖励建立三桶，不重分类旧余额；F5 只加 version/provenance，不补第二笔 ledger。未发现 UPDATE 历史提现、收益或佣金金额的迁移。 |
| 回退 / 止血 | **PASS（安全态）/ 生产恢复 HOLD** | D5 假执行器返回 `D5_EXECUTOR_HOLD`；G4 closed 禁新交易但保留查看/撤单；K1 未签证明强制 manual-only；F5 解冻不等于提前解锁；K6 无 receipt 不成功；D7 通道关闭。K6 真实撤销清理和生产恢复需 OPEN-ENG-001 后验证。 |
| 真实接口 / localStorage / mock 真值 | **PASS（目标范围）** | H9/D5/G4/K1/F5/K6 PC 均调用真实 BFF/后端；D7 已删除本地假保存；K1 客户端放款函数为 no-op；K6 本地空 runtime 不上报 fresh。sessionStorage 仅保存未知命令号，不作为业务成功真值。 |

## 四、逐模块最终结论与剩余 HOLD 条件

### H9：PASS

当前结论：**模块 PASS；没有剩余产品硬 HOLD。**

- `OpsGrowthController` 对 GET/PATCH 分别使用 `growth_h9_read/write`；`GrowthPublicStatsService` 对完整八键、分位表、版本、注册锚和 effectiveAt 做服务端最终校验。
- 聚合缺失/非法返回不可用，不补本地营销假数；公开投影不泄露默认值、理由或审计。
- 版本 CAS、持久幂等、必达审计与 outbox 均有自动化证据，PC/UniApp parser 与后端允许 `tops=0`、累计百分比持平、末档小于 100 的规则一致。

剩余非阻断项：锁定 revision 后做一次 writer/read-only/anonymous 权限烟测、同 key 断链回放和双操作者旧版本 409。`h9-client.ts` 的历史矛盾注释与 PC parser 纵深校验不足属于低风险维护项，不改变 PASS。

### D5：代码 PASS，发布 HOLD

当前结论：**代码/合同 PASS；真实资金链发布 HOLD。**

- 三网固定确认费、NEX 抵扣意图和 policyVersion 由服务端权威定价并写订单快照；无抵扣意图不烧 NEX，历史订单不按新配置回算。
- `smallAmountThresholdUsd`、`payoutSlaHours` 已从 PC 可写集合移除；旧/恶意客户端提交这两个字段时，后端在写配置、审计和版本前返回 `409 D5_EXECUTOR_HOLD`，不再出现“保存成功但业务不变”。
- PC 已删除缺失三网组时的 1/1/5 读取兜底，缺失/null/缺键/越界/非 0.5 步进全部失败关闭。

HOLD 转 PASS 条件：

1. 隔离账号真实 GET policy，并提交 offset=true/false、空/旧 policyVersion、桶不足四类用例。
2. 对钱包 CAS、订单、USDT ledger、NEX ledger 逐点故障注入，证明任何失败均无半扣、半单或半账。
3. 两名运营员用同版本竞争三网整组，败者 409 且配置/版本/审计/outbox 无半态。
4. 刷新、退出重登后核对新单快照和历史单原值；不清理或回写历史资金证据。

### G4：增量修复 PASS，端到端 HOLD

当前结论：**代码/合同 PASS；最终端到端签字 HOLD。**

- 档位更新在写前拒绝 `to >= next.rangeTo`，构造候选整表再次验证连续、正区间和已售上限；多行更新、版本推进、审计同事务。
- catalog/state/sale policy 缺失时公开查看仍返回明确不可用投影，但交易关闭；closed 不阻断合法查看和撤单。
- 邀请码已扩展为 16 hex 生成；同码同用户重放返回持久结果，同账号不同码由唯一键终裁，`DuplicateKeyException` 归一为 409，不泄露原码或其他用户。
- 资格、邀请码和市场状态均从服务端恢复，PC/UniApp 不维护本地邀请码真值。

HOLD 转 PASS 条件：

1. 真实 MySQL 双连接验证同账号不同码、同码不同账号和同版本双档位写竞争。
2. 注入“第一条档位 SQL 成功、第二条/版本/审计失败”，确认整表与版本原样回滚。
3. 已登录浏览器从可见入口走角色权限、刷新、退出重登、closed 查看/撤单。
4. 非阻断收口：输入正则从 `[A-Z0-9]{16}` 收窄为生成器实际的 `[A-F0-9]{16}`，减少合同漂移面。

### K1：手工安全候选 PASS，自动可信证明与真实资金链 HOLD

当前结论：**最终两项修复已关闭；manual-only 候选 PASS；自动可信证明/生产资金签字 HOLD。**

- 七参数 PATCH 现在要求 expectedVersion；陈旧页面 409，关系约束和配置缺失均失败关闭。放宽免费位、阈值、窗口、releaseMode 或绑定要求前执行 B1 覆盖率门；B1 不可靠返回 503、低于红线返回 422。
- 普通用户 Janus report 不再调用资金证明入口；`nexion.k1.trusted-attestation-enabled` 默认 false 时，对外强制 `manual_only` 且禁止切换 `attest_or_manual`。自动入口只有未来受信 carrier 可调用，并可按 `freeSlotRequiresBinding` 校验设备绑定。
- 人工放行使用独立高敏权限 `risk_k1_cluster_release`、稳定幂等键、分录行锁、B1 覆盖率、条件桶迁移和必达审计；上述动作与幂等成功结果处于同一事务。
- 簇免费位在计数前锁簇/用户 scope，锁缺失 409；新奖励统一走三桶并同步钱包 `lifetime_earned`，提现在锁钱包后再次核对簇状态和受保护 USDT。
- UniApp 已修复账号快照隔离：rebind 清空快照，迟到响应不能写入新账号；登录成功立即拉服务端 release-status；`clusterRestricted` 缺失/非法拒绝，缺快照时可提上限为 0。最终增量测试 backend 22/22、UniApp 4/4 全绿。

HOLD 转 PASS 条件：

1. 自动证明必须在可信设备身份、carrier 认证、nonce/防重放和调用 owner 经 `OPEN-ENG-001` 或等价签署冻结后再启用；在此之前保持 property=false/manual_only。
2. 用两个真实 MySQL 连接竞争同簇最后一个免费位，并核对只释放一个账户、审计一次。
3. 对 B1 unavailable/below-redline、release SQL、audit 写失败做集成故障注入，证明桶不变且同 key 可安全恢复。
4. 使用两个 App 账号执行登录 A→退出→登录 B→迟到 A 响应、刷新与重登；核对 B 从未看到 A 的桶或可提额度。

### F5：PASS

当前结论：**模块 PASS；没有剩余产品硬 HOLD。**

- 后端 canonical GET 明确投影 `status/version/frozenFromStatus`，PC 已读取 `status`，刷新后不再把 frozen/unlocked/rejected 统一回退为 cooling。
- SQL 先判断显式 FROZEN/REJECTED/REVERSED，再做到期推导；冻结跨过旧 unlockAt 不会自动变为可提。
- 手工与自动解锁共享状态/version CAS；自动胜者写 operation、required audit、outbox，同事务失败回滚。解锁只推进可提状态，不再补第二笔 ledger；解冻固定恢复 cooling，不等于提前解锁。
- 结果未知使用 sessionStorage 稳定命令号，但成功真值始终由服务端回读；服务端 RBAC、maker-checker 和 A2 回放仍为最终裁决。

剩余非阻断项：在锁定 revision 后补两操作员实库竞争、刷新重登录像和 audit/outbox 故障注入。PC parser 对缺失 `status` 仍有 `cooling` 兼容回退，建议后续改为协议错误失败关闭；当前后端响应和服务端状态机使其不构成已证实越权或错账。

### K6：隔离联调候选 PASS，真实设备 HOLD，生产 HOLD

当前结论：**三端合同/隔离联调 PASS；真实设备 HOLD；生产因 OPEN-ENG-001 强制 HOLD。**

- 服务端封闭 15 相位，禁止 `LOADING -> SUCCEEDED`；成功、撤销和主动对账必须携实际目标 id/version/catalogVersion、deviceAppliedVersion、deviceAppVersion 和 handoffReceipt，并与批准目标和当前命令版本一致。
- 管理五动作均为 write 权限 POST；GET applied 只读，不再创建 reconciliation。PC 主动对账先 POST 请求，再轮询 GET，超时保持 `fresh=false`。
- PC 未知结果保留稳定 key；服务端 revoke/resend/change-target/retry/applied-refresh 全部进入持久幂等与 rowVersion CAS。
- UniApp 在导航/存储副作用前拒绝低版本与同版本不同 commandId；空 runtime 对 QUERY_APPLIED 静默不造证。默认 launcher/openURL 接受后仍抛 `JANUS_HANDOFF_PROOF_UNAVAILABLE`，只有真实 carrier 返回 receipt 才能落成功态。
- viewer 不显示写按钮，服务端权限仍为最终裁决；刷新后详情从服务端账本重建，不以组件或 localStorage 成功态为真。

HOLD 转 PASS 条件：

1. **生产前必须签署 OPEN-ENG-001**，冻结唯一设备执行者、设备身份、目标制品摘要、信任/NFR、版本兼容和回退责任；当前不得声明生产实现已开始或已可用。
2. 使用真实签名制品完成投递、load/ready、handoff、账户交接、主动对账和撤销清理回执；openURL 返回不算成功。
3. 补真实 read-only/writer 角色会话、双写竞争、响应丢失重放、刷新和退出重登。
4. 保持默认无 receipt 即失败的安全态；不得为了演示把 mock receipt、空 target 或本地 runtime 作为 fresh。

### D7：强制 HOLD

当前结论：**HOLD，且当前不存在假成功。**

- 页面明确“真实出款通道未接入”、通道关闭、只读、不保存、不提示生效。
- `lib/admin/registry/d.ts:41` 与 `docs/后台产品更新日志.md:82-91` 已同步相同边界；上一版的产品声明冲突已关闭。
- `payout-vnd-local` 已移除，D1-D6 不依赖它；当前可见入口只用于说明边界。

HOLD 转 PASS 条件：真实越南银行出款通道、报价、订单、账本、审计、RBAC、CAS/幂等、失败恢复和刷新重登全部接入并完成独立真实用户验收。在此之前不得恢复本地保存、伪历史或“已生效”提示。

## 五、最小放行顺序与回退要求

1. 先冻结 backend + PC + UniApp + migrations 的兼容 revision；记录迁移清单和制品摘要，在该 revision 重跑 backend 2914、PC 58/58+build、Uni verify/K1 4/4+H5 build及各模块专项门。
2. 在隔离数据库和隔离账号补 D5/G4/K1 的双事务、故障注入、断链重放、刷新/重登；资金动作只在可回滚夹具执行，禁止清理审计/幂等/账本证据。
3. H9、F5 可随锁定候选进入 staging；D5/G4/K1 未补证前只允许受控验收，不签生产 PASS。
4. K6 只允许隔离联调；OPEN-ENG-001 与真实设备证据未完成前保持生产开关/载体失败关闭。D7 始终关闭。
5. 回退以跨仓兼容集合执行，不单退某一端。数据库迁移只向前兼容；不得 UPDATE 历史资金、删除审计/幂等记录或用旧客户端默认值恢复业务真值。

## 六、独立初审、复审与生命值

- 初审：**98.1/100，通过**。重新以当前稳定树核对旧阻断逐项关闭、三端门、RBAC、未知结果、fail-closed、CAS、事务、刷新重登、历史资金、回退和 K6 文档门；未使用旧 8110 jar 冒充当前动态证据。
- 复审：**99.3/100，通过**。在 K1 最终账号隔离、B1 放宽门和人工释放门落地后重读源码，独立重跑 backend 22/22 与 UniApp 4/4；把已关闭缺陷从 FAIL 移除，同时保留 D5/G4/K1 实证 HOLD、D7 强制 HOLD 和 K6 OPEN-ENG-001 生产 HOLD。
- 血量：初审、复审均超过门槛，无扣血；任务完成恢复 10 点，最终 **100/100**。

> 评分评价本次复审质量，不代表整包已生产放行。最终候选总判定仍为 **HOLD**；当前没有确定性 FAIL，但剩余 HOLD 条件完成前不得对外宣称全量生产可用。
