# L 域 Owner Final13 验收报告

运行：`pc-full-acceptance-20260729-114336`  
候选：`Final13`  
执行时间：2026-08-02 03:03–03:45（Asia/Tokyo）  
范围：L1 KPI、L2 漏斗/Cohort、L3 财务、L4 设备/任务/网络、L5 导出与监管、L6 用户行为热力图。  
载体：PC/H5 使用 Chromium Playwright，`workers=1`、`trace=on`；App 使用锁定 App 根的 Vitest 与 Node 合约；数据库、MinIO、A2、A4、Outbox 使用真实客户端复核。

## Owner 结论

**Owner 初审通过，98.2/100（>96）。** Final13 的 L1–L6 已从真实登录和可见侧栏完整执行，覆盖刷新、退出和重登、权限五层、完整写入生命周期、未知结果恢复、幂等、CAS、并发审批、L3/L4 数据库链、L6 App 生命周期与隐私边界、A2/A4/Outbox、故障矩阵和精确恢复清理。

本轮发现 **0 个 P0、0 个 P1、0 个 P2、0 个 P3 产品缺陷**。出现的四类失败均已用后续证据归因为载具或共享工作目录问题，没有把它们计作产品通过证据：首登 DOM 已进入控制台后旧标题短暂存留；同一 TOTP 步重放；共享 `target` JAR 被并行构建覆盖；清理载具用大小写敏感比较核对已被服务端规范化为小写的 actor。所有载具问题均在受限证据目录闭环，未修改 PC、后端或 App 产品源码。

## Final13 候选锁

| 项目 | 锁定值与终态复核 |
|---|---|
| 运行锁 | `FINAL13-RUNTIME-LOCK.json` SHA-256 `E01C6DEB2E93203067670156BFD22F0C42DF0672CE4CA58B0A5F9A2D142AC15E` |
| PC | HEAD `1d9dc8dffa4014cf85e935f287819fed44793206`；build `lqlGobSf9dgLarrRkjFGn`；PID `23716` 存活；根页 HTTP 200 |
| 后端 | HEAD `f4a943ec1fe57b6f8148565194296ac05ce71131`；PID `18752` 存活；锁定运行时 JAR SHA-256 `34706D1FC0AD02923AEB6217B405D90FAA47730387266E56D9336BA0564557A7` |
| 数据 | MySQL `nexion_acceptance_20260729_114336`；Redis DB 13；MinIO `nexion-acceptance-20260729-114336` |
| App | `D:\workspace\.acceptance\pc-full-acceptance-20260729-114336-app-master`；HEAD `0e2178b59af96b198188fc5992e9e7fa48425a00` |
| 未认证边界 | PC 根页 200；`/api/admin/auth/session` 401 |

共享工作区 `target` JAR 在 L3 前置门禁时被另一并行构建覆盖。锁定 PID 没有重启；后续门禁使用候选锁目录中的不可变运行时 JAR，而不是可变工作区构建产物。该处理只校验运行中的 Final13 候选，没有继承 Final12 结论。

## L1–L6 完整闭环

| 模块 | Final13 实测 | 结论 |
|---|---|---|
| L1 | 8 个权威 KPI；缺失 spark 不补零；真实 CSV；已提交但响应丢失后复用原幂等键恢复同一权威结果。 | 通过 |
| L2 | 五段产品漏斗与六项生命周期事实同源；Cohort/留存与 CSV；空切片 422；同键同载荷重放、异载荷 409。 | 通过 |
| L3 | Owner 创建、同键重放、异载荷冲突；maker 自批 403；两名独立 exact-checker 竞争，只有一名 200，另一名 409；下载成功。 | 通过 |
| L4 | 聚合与网络树真实导出；隔离链 A→B→C 的深度 1/2 分别为 2/3 行；循环、多路径、时间窗、EXPLAIN 和深度 10 容量失败关闭。 | 通过 |
| L5 | 聚合导出和监管报告真实创建；状态、MinIO、下载、A2/A4 一致；空范围和不支持请求不伪造成功。 | 通过 |
| L6 | 权威行为聚合和 CSV；仅聚合字段、无 PII；App 后台/失联暂停，重新登录刷新；服务端身份和会话是权威。 | 通过 |

写链运行清单 SHA-256：`834823B11FFD98D24F8BF2AB84DE56F7BA2542E1D532CC6F24E7F3AFE1545759`。L3 双人运行证据 SHA-256：`863BD6D102CDC52D3426122777A954E0262F2B04D1D1B1C719AEFFBA974DBC1A`。

## 真实入口与权限五层

- Owner 从真实登录页完成 MFA，经可见侧栏逐一进入 L1–L6；逐页刷新后 URL、内容和权限不漂移；退出后会话失效，重新登录后 L1/L6 仍可达：`1/1 passed`。
- readonly：L1–L6 菜单、路由和权威数据可读，按钮禁用，直接写接口 403。
- no-write：保留菜单和读取权限，写按钮及写接口失败关闭；刷新、重登不恢复写权限。
- no-menu：无 L 菜单；直接 URL、读接口、写接口均 403；刷新和重登后仍不越权。
- 五层权限载具合计 `3/3 passed`；匿名 session 为 401。

## 墨菲故障矩阵

| 边界 | Final13 结果 |
|---|---|
| 401 | 退出后会话/API 未认证；重新登录恢复。 |
| 403 | readonly/no-write/no-menu 写入及 maker 自批均由服务端拒绝。 |
| 404 / 500 | L1 清空旧权威数据并禁用导出；解除后恢复 8 个 KPI。 |
| timeout / offline | 页面明确失败关闭，重连后恢复，不保留假成功。 |
| malformed 200 | L1/L4/L5/L6 均拒绝畸形成功包并禁用写出。 |
| 409 | L2 同键异载荷、L3 双 checker CAS 竞争均只有一个权威结果。 |
| 422 | L2 空切片不生成空成功报告。 |
| 503 / 未知结果 | 只在提交后丢失响应，复用原幂等键恢复同一报告，不重复创建。 |

故障矩阵：L1 `1/1 passed`；L4/L5/L6 malformed-200 `3/3 passed`。首次失败为同一 TOTP 时间步重放，原始 trace 已保留，进入新时间步后按原范围重跑通过。

## 数据库、A2/A4、Outbox 与 App

- L4 DB 递归证明 `proofPassed=true`：深度 1 为 2 行、深度 2 为 3 行；循环和多路径原始候选 7、去重后 6；深度 10 五次最慢 `33.306ms`，5000 行上限内通过。
- 七个治理报告在清理前均为 `READY`，每个具备业务表、审计和 `admin.report_exported` Outbox 证据；清理后保留 17 条相关审计和 8 条相关 Outbox。
- L6 直接聚合导出：Outbox `1`、A2 审计 `1`、schema revision `302`、registered `1`、rowCount `4`，且可变报告/MinIO 伪绑定为 `0`。
- L6 清理载具首次显示 `l6ImmutableExport=null`，根因是夹具用户名含大写后缀，而服务端按规范保存小写 actor。按身份大小写不敏感核对后严格得到上述 `1/1`，属于载具比较错误，不是产品缺证。
- App 行为 API/服务测试 `12/12 passed`；App 生命周期/隐私合约 `3/3 passed`。
- PC 全部 `tests/l*.test.mjs` 为 `81/81 passed`。

## 精确清理与终态

- 七个业务报告、下载授权、MinIO 对象和对应可变幂等记录全部清除；不可变审计与 Outbox 保留。清理结果 SHA-256：`9D4E8EB574FB0F04684C52A3B23D6345176FE4ED11E27120D3CE6C9611E0EE0E`。
- L4 隔离夹具：删除 user `3`、KYC `3`、sponsorship `2`、team `5`；四类终态残留均为 `0`；非夹具指纹恢复；A2/A4 保留。
- 含载具适配失败轮次在内，共 22 个临时账号均已清除 MFA、解绑、停用并撤销会话；10 个临时 L 权限角色均删除，活跃角色关系为 `0`。
- 精确删除本轮 162 条可变幂等记录，终态残留 `0`；没有删除审计或 Outbox。
- 终态哨兵：`D:\workspace\bug-pic\.restricted\pc-full-acceptance-20260729-114336\L\final13-owner\terminal-sentinel.json`。

## 载具问题记录

1. 标准首登载具依据旧标题判断状态；页面已进入控制台后继续等待已卸载输入框。受限派生载具改为以真实输入控件和 `aside` 终态判定，最终 bootstrap `3/3 passed`。派生载具 SHA-256 `E725651F596A8A7B5B4281DC13FC02BBAE6DA3DFF72D265C554EA76C5768C884`。
2. 共享工作区 JAR 被并行构建覆盖，未改变锁定 PID。改用候选锁目录的不可变运行时 JAR 后，L3 完整链 `1/1 passed`。
3. 两次首次故障矩阵登录落在已消费的 TOTP 步；新时间步原范围重跑通过。
4. L6 清理核对对 actor 大小写敏感；按服务端规范化身份核对后 A2/A4 严格各一条。

这些问题均有失败 trace 和通过后的新证据；没有修改产品源码，没有提交、推送或修改共享缺陷台账。

## 缺陷与评分

| 等级 | 数量 |
|---|---:|
| P0 | 0 |
| P1 | 0 |
| P2 | 0 |
| P3 | 0 |

Owner 初审评分：**98.2/100**。扣分来自首登与身份规范化载具适配、共享工作区构建产物可观察漂移风险；不属于产品缺陷。

Owner 结论：**Final13 L1–L6 通过，允许交由独立非 Owner 执行同范围对抗性复审。** 本报告不是非 Owner 复审签字，复审必须独立达到 >98。

初审超过 96，不扣血；任务完成恢复 10 点，当前血量按上限计为 **100/100**。
