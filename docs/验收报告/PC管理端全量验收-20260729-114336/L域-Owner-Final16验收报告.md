# L 域 Owner Final16 验收报告

运行：`pc-full-acceptance-20260729-114336`；候选 Final16；PC Build `WHtnx71HibGEUaMlXb3rk`（3002/PID 7444）；后端 PID 22996、JAR SHA-256 `13A103A65FEA04FD8829305160C9496151BC0EB426DEBBACABB533D0DB4752E5`。

## 结论

**未签发通过，候选已由 Final17 替代。** 本轮在替代通知前完成了真实 MFA、可见侧栏的 L1–L6 首次走查与刷新/退出重登；但 L3 的 -100 专项和 L1/L2/L4/L5/L6 写链没有在 Final16 上形成完整绿色闭环，故不以已有历史结论代替本轮结论。

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 独立 MFA 权限夹具 | 通过，3/3 | `L/final16-owner/pw-permission-setup-r4` |
| L1–L6 侧栏、逐页刷新、退出重登 | 通过，1/1 | `L/final16-owner/pw-owner-visible-r2` |
| L3 `-100%` 正常显示、`<-100` 失败关闭、恢复 | 未闭环 | `pw-l3-mom-delta*`：首次 MFA 时间窗 401；重试进入 L3 后页面以“无权限访问”失败，未将其当作通过 |
| L1/L2/L4/L5/L6 写链、幂等/CAS、A2/A4/outbox/DB/MinIO/App | 未闭环 | `pw-owner-write-r3` 在 L1 可见写按钮定位失败前停止；无成功业务写入 |
| Final16 夹具恢复 | 通过 | L4 清理脚本和精确数据库核验均为 0 残留 |

失败路径保留 trace、视频、截图和错误上下文，未覆盖为产品通过证据。Final17 应重新从真实 MFA 登录入口完成 L1–L6 全域 Owner 复验，特别是 L3 的权限到接口五层一致性及 `-100` 边界。

## 精确恢复

- L4 三节点隔离网络夹具已由 `l4-isolated-network-fixture-cleanup.ps1` 恢复。
- Final16 临时账号 `99982–99985`、账号状态、角色关系、角色 `4572/4573`、菜单和权限关联、会话均按精确 ID 软删除/撤销。
- 清理后活动残留均为 `0`：账号、账号状态、角色关系、角色、角色菜单、角色权限、会话。
- 审计和 outbox 未删除。

## 评分

初审：**不通过（未评分）**；复审：**未触发**。原因是 Final17 在本轮闭环前已替代 Final16；不以不完整候选签发分数。
