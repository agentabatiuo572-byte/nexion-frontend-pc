# PC 管理端 A–M 全量验收缺陷台账

- Run ID：`pc-full-acceptance-20260729-114336`
- 范围：A–M 共 13 域、75 模块
- 状态口径：`待确认 / 已确认 / 修复中 / 待复验 / 已关闭 / 环境受阻`

## Final19 权威终态

本节是本 Run 的最终状态口径。下方长表保留各候选发现、修复和复验的时间线，历史行中的“待修复/待复验”描述不得覆盖本节的 Final19 结论。

- 13/13 域均有通过的 Owner 报告和轮换非 Owner 报告；Owner 初审均严格大于 96，非 Owner 复审均严格大于 98。
- Final19 统一终验为首轮 75/75、刷新 75/75、退出重登 75/75；所有错误数组为 0，109 条导航取消未配对为 0。
- 未关闭产品缺陷：`P0/P1/P2/P3 = 0/0/0/0`。
- 验收环境事件和载体失败均保留原始证据，不冒充产品缺陷，也不复用失败候选签发。

| 最终闭环编号 | 级别 | 问题 | Final19 状态与证据 |
|---|---:|---|---|
| E-FINAL16-PRESIGN-001 | P1 | E1 直接挂载已过期的持久化 MinIO 预签名 URL | **已关闭。** 改为认证 preview-url 动态刷新并失败关闭；Final18 E Owner 98/99、非 Owner 99.2/99.4，E1–E6 整域通过。 |
| L-FINAL16-BOUNDARY-001 | P1 | L3 把合法 `momDelta=-100` 判为畸形，并依赖 D3/B2 通用 Treasury 权限 | **已关闭。** `-100` 合法、低于 `-100` 拒绝；L3 使用 `bi_l3_read` 专用聚合端点和单一 `REPEATABLE_READ` 事务，通用接口对 L-only 仍 403。Final18 L Owner 97.4、非 Owner 98.7/99.2。 |
| M-FINAL18-SSE-001 | P1 | M3 SSE 首帧约 25 秒，快速导航前没有替代 200 | **已关闭。** 认证后立即发送 `: ready`，发送失败注销并关闭。Final19 Owner 首帧 296.2 ms、非 Owner 5.9 ms；M Owner 98.2、非 Owner 99.3。 |
| FINAL75-CARRIER-001 | P3（测试） | 请求完成/失败使用可变当前模块归因；M1 头像在自动化快速切页时被取消 | **已关闭。** 请求在发起时冻结阶段标签；M1 等待可见头像完成加载后再离开。严格同阶段/方法/路径配对规则未放宽，最终未配对取消 0。 |

### 历史行关闭映射

历史长表的逐项根因和过程证据继续保留；下表按责任域把旧的“待修复/待复验”状态映射到最后一次权威域签发。共享认证、导航、BFF、幂等和跨域项还必须同时满足 Final19 的后端 3015 项全量门禁、PC 1005 项合同及 75 模块统一终验。

| 域 | Owner 关闭证据 | 非 Owner 关闭证据 | 最终域结论 |
|---|---|---|---|
| A | `A域-Owner-Final13验收报告.md`，97.4 | `A域-非Owner-Final15复审-M.md`，99.2 | P0–P3=0 |
| B | `B域-Owner-Final13验收报告.md`，98.5 | `B域-非Owner-Final14复审-A.md`，99.3 | P0–P3=0 |
| C | `C域-Owner-Final13验收报告.md`，97.0 | `C域-非Owner-Final14复审-B.md`，99.3 | P0–P3=0 |
| D | `D域-Owner-Final13验收报告.md`，97.0 | `D域-非Owner-Final14复审-C.md`，99.0 | P0–P3=0 |
| E | `E域-Owner-Final18验收报告.md`，98.0 | `E域-非Owner-Final18复审-D.md`，99.4 | P0–P3=0 |
| F | `F域-Owner-Final14验收报告.md`，98.0 | `F域-非Owner-Final14复审-E.md`，99.0 | P0–P3=0 |
| G | `G域-Owner-Final15验收报告.md`，99.0 | `G域-非Owner-Final15复审-F.md`，99.1 | P0–P3=0 |
| H | `H域-Owner-Final13验收报告.md`，97.6 | `H域-非Owner-Final15复审-G.md`，99.0 | P0–P3=0 |
| I | `I域-Owner-Final13验收报告.md`，99.6 | `I域-非Owner-Final14复审-H.md`，99.3 | P0–P3=0 |
| J | `J域-Owner-Final13验收报告.md`，97.2 | `J域-非Owner-Final14复审-I.md`，99.3 | P0–P3=0 |
| K | `K域-Owner-Final14验收报告.md`，97.4 | `K域-非Owner-Final14复审-J.md`，98.4 | P0–P3=0 |
| L | `L域-Owner-Final18验收报告.md`，97.4 | `L域-非Owner-Final18复审-K.md`，99.2 | P0–P3=0 |
| M | `M域-Owner-Final19验收报告.md`，98.2 | `M域-非Owner-Final19复审-L.md`，99.3 | P0–P3=0 |

版本口径：Final16 是失败候选；Final17 只用于 E1 预检；Final18 完整重跑 E/L；Final19 完整重跑 M 并承担最终 75 模块统一终验。上表不表示 13 个域都在 Final19 重新执行了完整双人域验收。

## 历史发现与闭环时间线

| 编号 | 域/模块 | 级别 | 复现步骤与当前行为 | 根本原因 | 影响范围 | 修复责任 | 状态与复验证据 |
|---|---|---|---|---|---|---|---|
| G-FINAL10-001 | G1 Staking 最小额 / B1 红线 | P1 | Final10 权威响应 `redlineBreached=true`；真实页面把 `usdt30d.minStake 21→20` 判断为放大操作并禁用确认按钮，但同一身份直接调用 `PATCH /api/admin/market/staking/pools/usdt30d/params/min` 返回 HTTP 200 / `code=0` 并落库。隔离环境两次形成 20→21→20；主库未写 | PC `g1-staking.tsx` 为 min 编辑声明 `amplifiesWhen: decrease`，后端 `OpsNexMarketService.stakingParamLoosens()` 仅处理 APY 上升和 penalty 下降，漏掉 min 下降，导致页面门禁可被 API 绕过 | G1 五层失败关闭、B1 红线、脚本/被篡改客户端；阻断 G 域 Final10 Owner 签发 | H（轮换修复）/G Owner 完整重跑/F 非 Owner 对抗复审 | **已确认，待修复。** penalty 对照路径在同一红线下正确返回 `422 COVERAGE_BELOW_REDLINE`，排除 B1 环境失效。缺陷 trace SHA-256 `99914B37…4F75`，截图 `1DF7E949…4442`；DB before/after、A2 26 条、server-authoritative outbox 26 条与精确 cleanup 均在 `G/final10-owner/disposable/`。隔离 DB/Redis/MinIO 和 G 专属 PID 已销毁，主服务未停。 |
| G-FINAL11-001 | G1 App 质押真源 | P1（候选） | Final11 范围 App `D:\workspace\NX1.0\src\store\staking.ts` 仍标记 `MOCK-ONLY`，硬编码 `STAKING_APY` / `STAKING_PENALTY` / `STAKING_MIN` 并使用 account-scoped storage；G1 跨端合同 `5/6`，读取 `src/api/staking-api.ts` 直接 ENOENT | App 尚未接入 `/api/config/staking/pools`、`/api/stakes` 的服务端权威真实质押生命周期 | 用户端 G1 参数、下单、领取与提前赎回可能不服从后台 G1 真值；PC/public 投影不能代替 App 闭环 | 待指定轮换修复方 / G Owner 完整重验 | **待修复。** Final11 不修改候选源码；精确源码、合同与运行证据 `G/final11-owner/final11-g-owner-evidence.md`。G1 min API 绕过修复本身已复验为 UI 禁用 + direct API `422 COVERAGE_BELOW_REDLINE`，不得以该项关闭代替 App 缺口。 |
| G-FINAL11-002 | G1 App 跨账号在途响应隔离 | P1 | 独立复审在实际 App 与锁定 worktree 均稳定复现 4/4：A 账号的质押读取、质押命令、回购读取、回购命令延迟 200，会在切换到 B 或退出登录并清空 store 后再次写回 A 的持仓/订单与 wallet | `bindAccount/logout` 只清空数组与 pending key，没有请求 generation/AbortController；成功响应 applySnapshot 前未校验账号/session epoch | 跨账号资产数据泄露、错误余额/持仓展示及后续命令 | 原 G1 修复者返修 / G Owner整域重跑 / 独立非修复者复审 | **已确认，返修中。** actual/locked 四类动态时序证据 SHA-256 分别 `D795C9C3…24A80`、`09DD08F2…BC6DA`；必须覆盖切号与logout、read与write，旧成功响应不得污染新会话。 |
| G-FINAL11-003 | 实际 App 测试依赖闭环 | P2（构建） | G1修复后实际 `NX1.0 npm run type-check` 因 `src/api/staking-api.test.ts` 无法解析 `vitest/types` 失败 | 生产源码目录新增Vitest测试，但实际仓库未声明对应devDependency/类型边界 | 实际App类型门禁和可复现测试 | App修复协同 / 主控串行门禁 | **修复完成，待复验。** 已把E登录contract移至仓库测试目录，并正规补齐Vitest devDependency与lock；实际App type-check已恢复全绿。G1返修后仍须重跑全测/type-check/H5 build。 |
| G-FINAL11-004 | G1/G7 同账号并发响应时序 | P1 | 跨账号 generation fence 修复后，同一 generation 仍可由较早请求的迟到200覆盖较新读取或已提交命令结果；actual与locked在G1/G7并发read及command前read四类均稳定复现 | store只有账号代际隔离，没有资源级latest-request序列或命令commit barrier | 同一用户快速刷新/网络乱序下的持仓、回购订单与wallet权威展示 | 原G1修复者返修 / 独立复审 / G Owner整域重跑 | **已确认，返修中。** 快速A→B→A、跨generation和late failure已通过，但同generation旧成功仍能回写。需保证每资源只有最新read可提交，命令开始/成功使更早read失效，同时不误杀独立资源请求。 |
| G-FINAL11-005 | G1 参数池并发读取 | P2 | 两个并发pool请求后发先回，先发旧响应迟到仍可覆盖新APY、最小额、罚金或Kill状态 | pool配置读取没有独立latest sequence | G1参数展示与下单前置 | F 轮换修复 / D 非 Owner 复审 / G Owner整域重跑 | **返修及独立复审通过，待新候选整域重跑。** pools latest sequence 已阻止旧成功/旧失败覆盖；actual+locked 对抗12/12、双方内置竞态16/16通过。 |
| G-FINAL11-006 | G7 配置与订单部分成功 | P2 | config读取成功但orders读取失败时，Promise.all整体失败导致合法config被丢弃并置null，错误判断回购不可用 | 独立权威资源被绑定为全成或全败运输/发布单元 | G7开放状态、订单与回购操作 | F 轮换修复 / D 非 Owner 复审 / G Owner整域重跑 | **返修及独立复审通过，待新候选整域重跑。** config/orders 独立 settle、校验和发布；任一失败不再抹掉另一方合法真值。 |
| G-FINAL11-007 | G1/G7 并发加载状态 | P3 | 两个并发read时旧请求先结束会提前把loading置false，而较新请求仍在途 | loading是单一布尔且由任意请求finally清理 | 首次用户进度反馈、重复点击和写门禁 | F 轮换修复 / D 非 Owner 复审 / G Owner整域重跑 | **返修及独立复审通过，待新候选整域重跑。** generation-scoped active request 计数覆盖旧generation finally、bind清pool loading及command/read barrier；actual+locked 对抗12/12、双方内置竞态16/16通过。 |
| A-AUTO-001 | A2 权限验收载具 | P3（测试） | 首轮宽泛“写型控件”统计误报 readonly/nowrite 可见“导出（脱敏）” | 载具选择器/归类错误；产品源码已使用精确 `platform_a2_export` 门禁 | A2 权限证据可信度；不属于产品缺陷 | A / B 复核 | 已关闭；fresh MFA 会话均为 78 纯读 authority 且不含 export，经可见侧栏进入 A2 后 exact 按钮 count=0，真实 export POST 仍为 403 |
| A-AUTO-002 | A-002 A1 状态 CAS 验收载具 | P3（测试/夹具） | 首轮 maker 缺少状态写权限，无法形成 CAS 对照 | 载具错误复用低权限 maker | A-002 运行时证据可信度 | 主控协调 B（轮换修复）/A Owner 复验 | **已关闭。** 新 Run 专属、独立正常 MFA super-policy actors `99622/99623` 从登录页完成预检；status-only probe `99535` 获得一方 `200/code=0`、另一方 `409 ACCOUNT_VERSION_STALE`，同键 replay 无版本二增、异载荷 `409 IDEMPOTENCY_KEY_PAYLOAD_MISMATCH`、结果未知后同键回放成功。A2 audit `3`、A4 outbox `0`、idempotency `4` 符合合同；fixture-admin 仅在 finally 恢复初始 status（version `11→12`），保留 audit/outbox/idempotency。证据 `A/a-auto-002/runtime-final-J_2lEoPlAOsQpy_5AFCCk/A002-pc-full-acceptance-20260729-114336-1785317320722-84d07436.json`，SHA-256 `E5D32AD958EE7A882EAAF466E3CDB5AF1789D514A65EDCAFF0C2E252B3C0D445`。 |
| A-002 | A1 运营账号状态 | P1 | F 遗失 maker 的两个不同 `Idempotency-Key`、相同 `expectedVersion` 并发禁用均返回 200；第二个请求也可重复执行禁用副作用 | `nx_admin` 只有秒级 `updated_at` 展示版本，`updateStatus` 使用非条件 `updateById`，未以独立单调版本做原子条件更新 | A1 状态/Profile/角色/改密及会话、权限夹具恢复；阻断 F–M 专属账号重建 | B（轮换修复）/A Owner 复验 | 待最终运行时复验；RED trace 位于 `bug-pic/.restricted/pc-full-acceptance-20260729-114336/A/domain-permission-fixtures/recovery-playwright/`。F maker 已禁用但未继续 reset-2FA/新建账号。修复代码已覆盖 `nx_admin.version` 迁移、状态/Profile/角色/改密及 MFA/会话 revoke 的 `WHERE id AND version` 原子 CAS，零行 409 且在审计/会话/权限等副作用前失败；受控启动 runner 按 A1→C2 迁移且不在命令行传密码。待主控停服统一重建后，用一次性探针账号验证不同 key 的 200+409、同键回放/异载荷冲突及失败零副作用；随后从已禁用 F maker 状态完成显式 revoke/reset-2FA 和新夹具创建。当前不签发恢复载具 GREEN。 |
| C-001 | C2 用户列表 | P1 | 首次用户从登录页和可见侧栏进入 C2，对用户 `990000151023` 执行“加入禁入名单”；`POST /api/admin/users/account-lists` 返回 422 `A4_SCHEMA_NOT_REGISTERED`，页面关闭确认框且未呈现可行动错误 | 账户名单写入发布 A4 事件，但对应 schema 未在守卫中注册；前端错误态同时存在静默关闭问题 | C2 账户禁入/恢复生命周期、A4 事件闭环 | D | 待复验；后端定向 15/15、PC 合同 7/7、TypeScript 通过；隔离库已实际注册两个 rev 301 schema/8 个有效属性，新候选已重建，待 C Owner 整域重跑 |
| D-AUTO-F10-001 | D Final10 child 路由载具 | P3（测试） | 首轮以 `PLAYWRIGHT_BASE_URL=3304` 标记为 child，但 Playwright 实际只读取 `ADMIN_BASE_URL`，因此请求仍落到主 PC `3002` / 主库 | 验收命令使用了未被配置消费的环境变量；不是产品路由或数据隔离缺陷 | 首轮 D3/D5/D6 “child”证据可信度；主库产生一组可识别 D 储备写与参数版本变化 | D Owner / 主控复核 | **已关闭。** `routing-probe.json` 证明 `3304→18120→child DB` 与 `3002→8110→main DB` 隔离正确；错误载具结果全部排除出 child 结论。主库储备已用等额 OUT 补偿为净额 0，D5/D6 恢复，Final10 D 幂等记录清零，immutable audit/outbox 保留。改用 `ADMIN_BASE_URL=3304` 后 child 核心 `4/4`、D3/D5/D6 成功写及补偿后重跑均通过；终态见 `D/final10-owner/terminal-integrity.json`。 |
| E-001 | E2 设备任务 | P3 | 页面直接展示“当前饱和度来自 /api/admin/devices/tasks 的 sat 字段” | 面向运营用户的页面泄露接口路径和字段实现细节 | E2 文案与可理解性 | F | 待复验；TDD 红 19/21、绿 21/21，全 E 域合同 57/57、TypeScript 与 diff-check 通过；待重建后由 E 从登录入口重跑 E1–E6 |
| E-002 | E3 设备生命周期 | P3 | 页面直接展示 `nx_user_device`、`server-canonical` | 面向运营用户的页面泄露业务表和内部数据来源标识 | E3 文案与可理解性 | F | 待复验；已改为运营业务语言且未改接口合同；Owner 截图位于受限证据目录 `E/e3-owner-live`，待重建后由 E 整域重跑 |
| E-003 | E2 收益与任务引擎 | P1 | 畸形成功响应 `{code:0,data:{}}` 被渲染为零任务/零分类；未出现读取错误和重试，且“调整全局饱和因子”仍可操作 | 权威任务配置无效时高风险入口没有失败关闭，运营员会在伪空态下继续发起调整 | E2 失败关闭与写入安全 | F | 待修复；最终候选 Owner RED 截图 SHA-256 `DDD46715…513483`，修复后必须从登录入口完整重跑 E1–E6 与权限矩阵 |
| SHARED-001 | 公共 Shell / E maker 登录与 E1 | P2 | 仅持 E 权限的 maker 从登录页进入、以及随后由可见侧栏打开 E1 时，客户端主动发起未获授权的 M 内容、B 资金和 L BI GET；共收到 15 个完成态 HTTP `403`（登录 8、E1 7），并产生 15 条对应 console error，`pageerror=0` | 公共 Shell 未按本次会话 authority/menu 对后台预取做门禁：`CommandCenter` 无条件加载 L BI 与 B 域数据，侧栏/顶栏徽标无条件加载 M 内容快照 | 无对应权限的所有专属角色；阻断最终“非预期管理接口 4xx/5xx=0、console error=0”门禁 | 主控协调共享修复；不得由 E Owner 改共享代码 | 已确认，待修复/复验；受限原始证据 `E/final-no-write-rerun/owner/maker-cross-domain-403.json`。均为浏览器脚本发起的 completed Fetch 403，非导航取消；精确清单、调用链和时点见 E Owner 报告。 |
| SHARED-002 | B 会话隔离验收载具 | P3（测试） | 旧探针只挂起第 1 个 `/api/admin/treasury/b-domain` GET，却允许同一 readonly 会话发出的第 2/3 个合法读取返回；页面的 fresh readonly `0.0%` 被误判为 maker 快照泄漏 | 多个 Shell/页面组件并发读取同一 B-domain 权威接口，载具未封锁全部匹配请求；随后还把页面真实 loading 文案误限定为 shell 文案 | B 会话隔离证据可信度；不是产品缺陷 | B Owner / 主控复核 | **已关闭：载具误报。** 最终候选 `J_2lEoPlAOsQpy_5AFCCk` 中全部匹配 GET 均挂起（2 个）时，maker→readonly 的 adminId、权限与 key 哈希均切换；页面 `loading=true`、`hasData=false`、无旧双账本/0.0%，放行后才显示 readonly 数据。证据 `B/final-J_2lEoPlAOsQpy_5AFCCk/shared-002-all-requests-held-final.json`、PNG、trace、log。 |
| SHARED-003 | A2 自定义 checker 行级可见与决策授权 | P1 | F maker 从登录页、可见 F1 提交 `WO-260729180933288-600` 后，自批 403、同对象并发 409 均正确；独立 checker `ACC_CHECKER_114336` 具 `platform_a2_read`、`platform_a2_operation_approve` 和 A2 菜单，但可见 A2 显示待确认 0、看不到该 F 提案。该 checker 又能以已知 operationId 直接调用 reject 成功。夹具锁阶段又对 A6 提案 `WO-260729183503301-200` 跨域审批成功，使 `network_f1_write` 生效；这是同一缺陷的第二个旧 JAR RED，不是授权 GREEN | `A2AccessPolicy.current()` 按硬编码角色码分域，未识别自定义 RBAC 角色时固定 `allowedDomains=["__NONE__"]`；`approve/reject` 仅校验 `platform_a2_operation_approve`，未复用同一行级 scope，造成“页面不可见但可按 ID 决策” | 所有非内置 role code 的 A2 maker/checker；阻断 F1–F5 独立双运营员成功写闭环，并存在跨层权限合同不一致 | 主控协调共享 A2 修复；F Owner 不改共享代码 | **已确认，待新 JAR 重启/整域复验。** 发现后立即停 F 写，未继续 F2–F5。原 F1 operation 终态 `rejected`，F.prize.name 保持 `Nexion V-Rank`。目标 checker 夹具现为精确 84 权限/88 菜单，`network_f1_write` 恰好 1 次；可见 A1/A2 已撤销旧会话并真实 MFA 重登，session 与角色一致、刷新稳定。旧 A6 RED 有 7 条脱敏 A2 日志；A4 无匹配，按边界记 N/A。证据 `F/final-ODaNTve/fixture-lock-checker-f1-write/`，`post-red-cleanup-evidence.json` SHA-256 `4FDFAFB7…E1FA2`。新 JAR 后必须由另一合规 A6 checker 完成可见审批，并证明 F checker 的 A6、未知 ID、跨域决策均 403，再从登录入口完整重跑 F1–F5。 |
| F-001 | F5 佣金补发 | P2 | 注入畸形 200 `{code:0,data:{}}` 后，页面虽显示 `F5_OVERVIEW_RESPONSE_INVALID`，但“批量补发 (0)”仍显示为可用；点击无提示、无对话框、无请求 | 权威快照校验失败后只设置错误文案，未让高风险操作同步失败关闭 | F5 批量补发及未知结果安全性 | G | **已关闭。** Final6-r3 锁定候选 `njacAG-OYV84-mfOdWA9v / 93A9B39E…AF92C` 上，F Owner 从真实登录和可见侧栏整域重跑；F1–F5 畸形包失败关闭 `5/5`，F5 显示 `F5_OVERVIEW_RESPONSE_INVALID` 与不可操作原因，补发按钮为 `0`，撤销注入并从可见入口恢复真实数据。最终无写/负向 `16/16`、pageerror/非预期 console error/非预期管理接口 5xx 均为 `0`。证据 `F/final6-r3-owner-readnegative-r2/`，服务端快照 SHA-256 `6323FCD7914BAC05612DDE8573D34538A06F6A30C747FA19DE8CFE6B337D3DA2`。 |
| F-002 | F5 异常阈值 / A4 事件 | P1 | Final4 从真实登录和可见 F5 点击“调整阈值”，`PUT /api/admin/teams/commissions/anomaly-config` 返回 `422 A4_SCHEMA_NOT_REGISTERED`；事务回滚且未形成 operation/outbox | F5 发布 canonical `admin.commission_anomaly_config_changed`，但启动迁移未注册对应 A4 schema | F5 阈值调整、审计与 A4/outbox 闭环 | G（轮换修复）/F Owner 复验 | **已关闭。** revision `303` ACTIVE schema 与 6 个 required 属性已应用；Final6-r3 从登录页及可见 F5 入口执行临时写入和精确恢复，各生成 `SUCCESS/ANOMALY_CONFIG` operation、各有 A2 审计 `1` 和 canonical A4 outbox `1`。独立 finally 精确删除仅本轮产生的两行默认配置，恢复物理不存在与 API 默认 `3/20`，非目标 fingerprint 不变，immutable operation/audit/outbox 保留，F pending=`0`。证据 `F/final6-r3-f25/f25-success-cleanup.json`，SHA-256 `C12488E7D9EDAAC9349FA646BBE74795CDCF4D8C8EC995B5A46B158CF6548B91`。 |
| H-CAND-001 | H1 增长阶段沙盒 | 待定 | 沙盒提交链中第二次 `GET /api/admin/growth/phases` 被浏览器取消后，页面永久停留“数据加载中/提交中” | 隔离 repeat-each=3 均通过，现有证据支持并行载荷下请求取消；未能稳定归因到产品 | H1 沙盒提交后的恢复能力 | H Owner 定位 | 环境归档，不计产品缺陷；证据位于 `H/h1-sandbox-repeat3` |
| H-001 | H8 邀请与结算（真实邀请链） | P1 | 首次登录的普通 App 用户无法通过正式产品接口读取本人注册时服务端生成的邀请码；H8 邀请卡/分享/海报只能读取本地 `user.referralCode`，远端登录响应未提供该字段，因而不能可靠生成真实邀请链接 | 注册服务已生成并写入 `nx_user.referralCode`，但缺少“当前认证用户读取本人邀请码”的最小 App 合同；`UserSession` 未携带该字段，App 也没有对应 client | H8 邀请、分享、海报及下游归因；阻断真实邀请链 | I（轮换修复）/H Owner 复验 | 候选边界已复验：未认证 `401 AUTH_REQUIRED`、ADMIN `401 USER_AUTH_REQUIRED`；TDD 后端 5/5、App API+canonical+分享门禁 7/7、type-check、H5 build 通过。仍待可销毁子环境执行普通 App 用户真实邀请链、maker/checker 结算及非 Owner 复审，未关闭。 |
| H-002 | H8 A2 真实结算提案 | P1 | 可销毁子环境中，独立 maker 从登录页、可见 H8 入口点击“执行真实结算”并提交 A2 proposal；`POST /api/admin/platform/audit/operations` 返回 `403 A2_BUSINESS_CONTEXT_UNMAPPED`，未生成 operation，checker 无法批准 | 前端 `sourceDomain=H8`、`command.domain=H`、`command.op=h8_referral_settlement`、`target=H/referral_settlement_batch/pending` 均已正确提供；后端 `AuditReplayBusinessPermissionGuard.delegatedDescriptor` 只处理 C/K/J/E/F，漏掉 H，虽然权限映射已有此 H8 op | H8 A2 提案、双运营员批准、钱包/D4/A2/A4/outbox 全链；阻断最终结算验收 | M（轮换修复）/H Owner 复验/I 非 Owner 复审 | **待修复，不签分**；GSg 单次真实浏览器 trace、截图和 `blocked.json` 位于受限 `H/child-final-GSg/h8/`。本次无 operation、结算、钱包或账本写入；App inviter→invitee 归因链已独立通过且测试用户/会话/钱包/OTP/绑定已精确清理，audit/outbox 保留。 |
| H-003 | H8 正式 App inviter→invitee 归因注册 | P1 | 修复候选中，child 独立 inviter 已注册并取得服务端 canonical referral code；invitee 使用该码通过 OTP 后调用 `POST /auth/users/register` 返回 `500` | child 后端日志记录 `MySQLTransactionRollbackException: Deadlock found when trying to get lock`；该异常未转换为业务可恢复错误，正常归因注册对普通用户暴露 500 | 阻断正式邀请归因、H8 待结算生成、PC A2 双人结算及 F/App 下游消费 | M（轮换修复）/H Owner 完整重跑/I 非 Owner 复审 | **待修复，不签分**；仅一次正式链证据，未重试。受限证据 `H/child-final-GSg/app-referral-chain.json`、`backend.stdout.log`；本次无 invitee、A2 operation、结算、D4 台账。待精确清理 inviter、会话、钱包和 OTP；audit/outbox 保留。 |
| H-P1-001 | H 域五层权限 | P1（误报） | 旧即时断言在 `page.goto('/growth/phase')` 后首帧看到原 H1 URL | 路由 guard 异步收敛：首帧 `.hdom=0`、H1 GET 已 `403`，随后 1 秒起 URL 重定向 `/`；刷新一致，三个独立 context 的 0/1/3/10 秒采样均相同 | H1–H5/H7/H8 路由层与首次用户可见性 | H Owner / I 非 Owner 复核 | **已关闭：时序误报，无产品改动**；权限用例已改为 `expect.poll`，最多等待 10 秒，随后完整重跑 readonly、nowrite、nomenu `3/3` 通过。证据：`H/route-guard-timing-post-fixture-20260729` 与 `H/final-candidate-owner/route-guard-rerun`。 |
| I-CAND-001 | 公共登录/导航、I6 | 环境事件 | 并行运行中出现登录后 20 秒无 aside，以及 I 顶层展开后 I6 入口未出现 | 低载荷独立 Chromium 重验 3/3：每轮真实登录、侧栏 I1–I6、逐页 200、刷新和重登均通过；因此无法归因产品 | 全域登录后导航发现性 | I Owner | 已关闭为环境事件；证据 `I/candidate-001-rerun/`，18 条 I 读取均为 200，pageerror/console/非预期 API 4xx/5xx 均为 0；282 条 `ERR_ABORTED` 为跨域后台请求在 SPA 导航时取消 |
| I-001 | I6 i18n 草稿版本 CAS | P1 | 修复前两个独立 maker/checker 会话使用不同幂等键、相同 `expectedVersion=v1` 并发写时均返回 200，并生成 v2/v3 | 版本检查与新版本生成未形成 MySQL 原子 CAS | I6 双运营员草稿编辑、版本链和所有依赖 `expectedVersion` 的并发写 | J（轮换修复）/I Owner 完整重跑/H 非 Owner 复审 | **已关闭。** 锁定候选 `nf0qGeqytfzJ1e_lX9TMR` / `D1D33E` 上，I Owner 用两个独立正常 MFA 浏览器会话和真实 MySQL 确定性重跑：严格一个 200、一个 `409 I18N_MESSAGE_VERSION_CONFLICT`；数据库仅 v1（软删除）与唯一 v2；赢家审计 1，输家审计/A4/outbox/ticket/lock 均 0；同键回放数据库不变；后端已提交但客户端响应中断后，同键重试与再次回放仍为同一 v2，审计仅 1。finally 后 message/version/idempotency/ticket/lock 均 0。I1–I6 Owner 全域重跑通过且无新缺陷。脱敏证据 `I/final-nf0q/i001-runtime-gate/i001-runtime-safe.json`，SHA-256 `13910106E7A6F6CCA543409C9FF416C2CC26B0469A24954F6F180D9C3852E679`。H 非 Owner 对抗复审仍是 I 域最终签发门禁，但不再阻止关闭该已修复缺陷。 |
| I-FINAL10-CARRIER-001 | I6 I-001 双运营员 MFA 验收载具 | P2（测试） | 修复前 checker 的 20 秒 `/mfa/verify` listener 在 `freshTotp()` 等待之前启动，maker 已推进全局步长时 listener 会在 OTP 输入和点击前耗尽 | TOTP 生成/边界等待与有限网络监听顺序错误 | I6 双运营员 CAS、结果未知和当前候选签发；不属于产品缺陷 | J 修复 / I Owner 整域复验 | **已关闭。** 先取得 fresh TOTP，再紧贴 click 建立 listener；fake-clock 合同通过。Final5 当前候选中 maker/checker 均完成 MFA，CAS 严格 `200 + 409 I18N_MESSAGE_VERSION_CONFLICT`，结果未知同键恢复稳定，I-001 cleanup 五类全 0。证据 `I/final10-owner-final5/i001-runtime-gate/i001-runtime-safe.json`（SHA-256 `D4546EA4…3FBC`）和 trace（`4C137A00…4323`）。 |
| I-FINAL10-CARRIER-002 | I3 A2 生命周期跨 spec MFA | P2（测试） | Final5 串行整域中，I1–I5 于 `12:01:32.180Z` 刚用 maker 完成 fresh-MFA 重登；I3 于 `12:01:33Z` 再用同账号提交同一 TOTP 步长，`/api/admin/auth/mfa/verify` 返回 `401 ADMIN_MFA_CODE_REPLAYED`，20 秒无 shell，未发生 I3 业务写 | I3 载具的 `lastTotpStep=-1` 是文件级局部状态，只记忆本文件消费的步长，无法感知上一 spec 已用同一账号消费当前服务端 TOTP 步长 | 阻断 I3 maker 提案、独立 A2 审批、CAP 恢复及 I 域 Final10 完整签发；服务端正确 fail-closed，不属于产品缺陷 | J 已转 Final11 修复 / I Owner 整域重跑 / H 非 Owner 复审 | **修复完成，待 Final11 复验；Final10 不再修改或重跑。** J 已实现跨进程原子 TOTP step lease：账户只存 HMAC 哈希，受限证据不存 secret/code；临界/占用步长有界等待，200/401-replay 原子记结果且 replay 只重试一次。合同 `3/3`、TypeScript、Playwright `--list` 通过，尚未替代真实 Final11 整域证据。Final5 `10/11`；I3 trace `38FD8EE4…480B`，归因 `E522F670…186B`；I 可变夹具/pending/lock 全 0，CAP 恢复，全局基线 `2/1` 未动。 |
| K-CAND-001 | K4 评分覆盖 | 候选（未确认） | 历史一次模型发布回填后覆盖返回 500 / MySQL deadlock；本轮专用真实 UI 三轮均先返回 409 `K4_SCORE_CONCURRENT_UPDATE`，刷新后重试 200，未再出现 500 | 竞争写已由 CAS 失败关闭；历史单次 deadlock 未能在独立双运营员、隔离用户、三轮真实浏览器中稳定复现 | K4 模型发布、人工覆盖、重算及 A2/A4 留痕 | K Owner | 已关闭为未确认候选；round-1-final/2/3 每轮 publish=200、stale override=409、刷新重试=200、无 pageerror/非预期 console/K4 request failure，DB active override=0、无 draft；A2/A4 与幂等记录均已核对 |
| K-001 | K 域菜单权限 | P1 | 原 no-menu 账号仍带 K read authority 且 `effectiveMenus=[]`，真实侧栏泄露 4 个 `/risk/*` 链接 | 权限夹具语义与“无菜单/无权限”最终合同不一致 | K1–K6 菜单层与路由/API 防绕过 | L（轮换修复）/K Owner 复验 | 已关闭；新精确 `K.json` 将 nomenu 固定为 `unassigned`（authority/menu 双空）。最终 Chromium 4/4：侧栏0、直链拒绝、K1–K6 读403/写403、刷新重登不漂移；证据 `bug-pic/.restricted/pc-full-acceptance-20260729-114336/K/final-owner/permission-matrix-rerun/`。K3–K6 成功写链仍待令牌，不以此条关闭替代整域签发 |
| K-AUTO-011 | K1 首访验收载具 | P3（测试） | 旧脚本在点击簇之前等待仅属簇详情空礼品分支的旧长文案，并硬编码历史簇 `K1-00990730`；当前隔离库权威空簇、总览正确显示“数据尚未接入，不能判定为 0” | 载具未按当前权威 overview 的已知/未知状态分支，也把历史簇存在误当产品前提 | 阻断 K 写链首轮；K2/K5/K6 未执行、无业务写；不属于产品缺陷 | L 非 Owner 修载具 / K Owner 整域复验 | **载具阻断已关闭，整域待重跑。** 当前候选首访 Chromium `1/1`、TypeScript 通过；有簇继续详情验证，无簇验证合法空状态，但后续 `currentCluster` 仍强制隔离测试簇存在。K Owner 必须补齐 K1 专用簇后从登录入口重跑 K1–K6。证据 `K/carrier-fix-k1-current-semantics/`。 |
| K-002 / K1-CONTRACT-012 | K1 反多账户聚类节点合同 | P1 | 本 Run 三名隔离用户的共享设备事实由正常 scheduler 生成 canonical K1 簇；真实 PC 从登录/可见 K1 入口读取时失败关闭，报 `multiAccount.clusters.records[0].nodesJson[0].joinedAt` | 后端把 `MultiAccountNode.joinedAt` 序列化为 JSON 数组（如 `[2026,7,26,18,22,54]`），PC K1 权威合同要求非空字符串 | K1 总览和全部写入口被隐藏，阻断 maker/checker、CAS、幂等、结果未知与 C2 联动；K2/K5/K6 按停写规则未执行 | L（轮换修复）/K Owner 整域复验 | **已关闭。** 后端外发节点改为稳定 ISO local 字符串；读取边界仅规范化合法旧数组/空格字符串。PC 对对象/tuple 均严格校验真实 ISO local 日期时间，拒绝数组、offset、非法日期和异常精度；null/浮点/科学计数/负数/越界/非法长度仍 fail-closed。静态 GREEN：Maven `167/167`、PC K1 合同 `22/22`、TypeScript 通过；第二轮对抗复审 PASS。锁定候选 `nf0q / D1D33E` 上，新 RunID 簇三个节点均为严格 ISO 字符串，真实 UI 完成 maker/checker、CAS、幂等、结果未知、C2 冻结/恢复、刷新重登和 503 恢复，K1 `4/4`。夹具 users/decision/cluster、pending/locks 和可变幂等均已清零；证据 `K/final-nf0q/K1-r5/`、`K/k1-contract-012-static-fix/summary.md`。K5/K6 未执行仍阻止 K 域签发，但不再阻止关闭本缺陷。 |
| J-AUTO-001 | J4 验收载具 | P3（测试） | helper 错误假设登录响应 JSON 必有顶层 `code`，并要求客户端已缓存的 J4 页面每次打开都重新发 GET | 测试 helper 与真实登录合同、合法客户端缓存不一致 | J4 自动化可信度；不属于产品缺陷 | J | 已关闭；保留旧 helper RED，修后 Chromium 1/1、J4 合同 24/24、TypeScript 通过 |
| J-ENV-001 | J1 / B1 基线清理 | 环境受阻 | 历史 withdraw 自动关停待补录已由可见 UI 合法补录为“维持关停”，但 B1 权威覆盖率为 0%（reserve=0、liability=887989.56、redline=100%）；withdraw 与历史 exchange 均无法在 J1 恢复 | 隔离库的 B1 双账本真实前置不达标；J1 正确 fail-closed，不是 J1 产品缺陷 | 阻断 J1 五闸 enabled 基线及 J1–J4 写链重跑 | B Owner 恢复可信覆盖率，J Owner 复验 | 待补验；`J/env-007/after-confirmation-matrix.json` 已证明 pending=0，`b1-coverage-source.json` 和 Chromium trace 证明恢复入口被正确阻断；审计 `J1_AUTO_KILLSWITCH_CONFIRMED#59426` 保留，未伪造 A4/outbox |
| L-AUTO-001 | L1/L2 验收载具 | P3（测试） | 旧 Playwright 脚本断言已过时文案，并把截图写入历史目录 | 测试载具未随当前 UI 更新且证据路径硬编码 | L1/L2 自动化可信度；不属于产品缺陷 | L | **已关闭。** Final4-r5 从真实登录入口完成 L1 结果未知同键恢复、L2 六阶段同源、回放/冲突/空切片和证据路径隔离，Owner `1/1 PASS`；证据 `L/final4-r5/owner/`。 |
| L-006 | L2 漏斗/cohort/留存导出 | P1 | 真实登录并从可见侧栏进入 L2；页面合法展示六条生命周期事实且开放“导出生命周期计数 CSV”。提交后接口返回 `200/code=0`、任务 `READY`，但 `EXP-9252F53D` 为 `rowCount=0`，MinIO 快照仅 45 字节且只有表头 | UI fallback 读取 `dashboard("L2")`，含六条独立事实 `stages`；导出 `estimateRowCount/funnelReportCsv` 改走 `l2Dashboard(..., allowIndependentFallback=false)`，详细事件不可用时既无 `funnel` 也无 fallback `stages`，却仍创建 READY 空快照并发布成功审计/outbox | L2 导出数据完整性；L5 任务、A2/A4 追踪和下游运营判断会把空文件误认为成功事实；阻断 L 域签发 | M（轮换修复）/L Owner 整域复验 | **已关闭。** Final4-r5 从真实登录和可见 L2 入口执行，UI 六阶段与 `EXP-3CA8A467` 六行 CSV 逐项同源；artifact rowCount=6，下载带 UTF-8 BOM，同键回放 `200` 且 reportId 不变、异载荷 `409`、空切片 `422/L2_EXPORT_EMPTY`。Owner 随后完整通过 L1–L6 与 L3 双 checker；报告、MinIO、grant 和幂等已精确清理，A2/A4 保留。证据 `L/final4-r5/owner/runtime-manifest.json`、L2 artifact/download CSV、`business-cleanup/exact-cleanup-result.json`。 |
| L-007 | L4 网络/团队结构明细导出 | P1 | 可见 UI 将 `depth` 呈现为“团队树层级”，并明确“层级越深，导出范围越大”。隔离 A→B→C 三节点夹具只有 A→B、B→C 两条 level=1 邻接事实且没有伪造 level=2 闭包；从登录页和可见侧栏分别选择 1 层、2 层导出后，两个 CSV 都只含相同两条邻接边，SHA-256 也完全相同 | L4 导出查询把物化 `nx_team_member.level` 当作树深度，只做 `BETWEEN 1 AND depth`；当前 F 域生产/消费合同却以 level=1 邻接边递归、level=0 自循环承载 VRank，因此选择 depth=2 不会递归生成 A→C 二层范围 | “团队树层级”选择实际无效，导出范围少于运营员明确选择；影响 PII 导出完整性、审计 rowCount 与下游判断 | M（轮换修复）/L Owner 整域复验 | **已关闭。** Final4-r5 锁定候选 `1znYVcf5Jn3HxNvPctH1v / 76D17B09…DBB` 上，从真实登录和可见 L4 入口执行：depth=1 为两条直接边，depth=2 为三条并新增派生 A→C（`treeDepth=2 / V0 / 0.00`）；CSV SHA-256 `67605A7E…A8B6` / `A2E4B906…7447`。独立只读 DB 证明通过递归深度、反循环、多路径去重、窗口、`EXPLAIN` 与五轮容量，最大 26.031 ms。Owner L1–L6 `1/1 PASS`，L3 两 checker `1/1 PASS`；浏览器非预期错误与失败请求为 0。七个报告、十一条幂等和 MinIO 已清零，A2/A4 保留；5 team、2 sponsorship、3 KYC、3 users 精确删除，非夹具 fingerprint 完全恢复。证据 `L/final4-r5/`。 |
| M-002 | M1 客服岗位调整 | P1 | M1 的“调整岗位”在只应改变 `position` 时还改写服务类型、最大并发和可转交状态，造成非目标客服资料漂移 | 前端 seat-assignment 写入按目标岗位重建了附属字段，而非透传当前权威 profile | 客服排班、专属客服资格及后续 M3 接派单 | A（轮换修复）/M Owner 复验 | **已关闭。** Final6 以 Run 专属 SUPPORT 执行“通用客服→客服主管→通用客服”可见链路；最终 `serviceTypes=[support]`、`tags=[KYC,提现,账户]`、`maxConcurrent=12`、`enabled/transferable=true`、`busy=false`、`status=enabled` 与前快照精确一致，恢复请求 200。证据 `M/final6-owner/support-profile/m-final2-support-restore-safe.json`。 |
| M-003 | M3 即时会话转交 | P1 | 两个启用且可接派单 SUPPORT 坐席同名时，用户在可见弹窗选中目标后，写入层按名称重解析第一条坐席，可能把会话转给错误身份 | `TransferTarget` 只保存 display name，`agentIdForName` 在重名集上取首条；接入判断亦按名称 | 客服会话归属、客户信息最小可见和工单衔接 | A（轮换修复）/M Owner 复验 | **已关闭。** `TransferTarget` 全链路持有唯一 `agentId`，弹窗选择、当前坐席排除、请求 body 及目标接入都按 ID 处理。Final6 两 SUPPORT 实测自转交 `422`、非目标接入 `403`、指定目标成功接入并转工单；刷新/重登稳定，工单/会话 cleanup errors 均为空。证据 `M/final6-owner/m3/m3-final3-transfer-result.restricted.json`。 |
| M-004 | M2 工单权威读取失败关闭 | P2 | Final9 从真实 MFA 登录和可见侧栏进入 M2，注入工单根请求 `401`；页面因等待 M1/M3/M4/M5 的无关请求而长期停留整域加载态，未及时显示“工单数据暂时无法同步”。旧快照还可能在刷新期间继续承担权威展示 | `fetchMContentData` 将 M1–M5 根请求和逐条明细串成一个整域完成点；读取无有界超时；重新加载未先清除旧 `mData` | M2 的 401/403/500/超时/畸形 200 失败关闭、旧数据隔离和写入口安全 | A（轮换修复）/M Owner Final10 整域复验/L 非 Owner 对抗复审 | **修复完成，待 Final10 运行时复验，不关闭。** M1–M5 改为独立任务渐进发布，M2 根/明细失败可在无关请求仍挂起时单独失败关闭；GET 有界 8 秒，重新加载先清空旧快照并以代际 ID 防止旧请求串写。HTTP 200 成功包络仅接受安全整数 `code=0`；tickets 分页强制 `records.length===total` 且 `ticketNo` 非空唯一，缺页/重叠/超额均失败关闭。稳定 RED 保留在 `M/repair-p2/red-final9-r2-output`；静态修复门禁为新增合同 `5/5`、M 定向合同 `65/65`、TypeScript、Playwright 清单及独立 production build 全通过。修复者未复审 M；须由 M Owner 在新 Final10 从登录入口完整重跑 M1–M5，再由 L 非 Owner 对抗复审。 |

| L-008 | L6 用户行为热力图 / App 事件生产 | P1 | Final6 非 Owner 复审在范围真源 App `D:\workspace\NX1.0` 检查 L6 跨端链路：预期的 `src/api/behavior-analytics-api.ts` 与 `src/services/behavior-analytics.ts` 均不存在，`src` 对 `app.page_viewed`、`app.element_clicked`、`clientEventId`、`behaviorTracker`、`behavior-analytics` 的匹配为 0；L6 跨端合同因此在读取首个缺失文件时失败 | 当前受验 App 未实现向后端提交经最小化处理的 L6 行为事件的 client/producer；PC 可读取既有行为聚合并不证明当前范围 App 会持续产生权威输入 | L6 实时热力、筛选、导出与 A2/A4 的跨端真实性；阻断 L 域非 Owner 签发和最终全量通过 | M（轮换修复）/L Owner 完整重跑/K 非 Owner 复审 | **已确认，待修复。** 不得以 `D:\workspace\nexion-frontend-uniapp` 替代范围真源；该工作区的 5/5 测试只作为发现同名实现的旁证，不能闭合本轮 App 范围。Final6 PC 端 L1–L6、L3 双 checker、权限、失败关闭、A2/A4/MinIO 与清理均照实通过，但不能掩盖 L6 App producer 缺失。证据 `L/final6-nonowner-k/l008-cross-app-proof.json`。 |
| L-F10-001 | L1 KPI 协议失败后的导出门禁 | P1 | Final10 将真实 `200/code=0` KPI 列表受控变异为 7 项，页面已显示“L1 权威响应协议校验失败”且 KPI 卡片隐藏，但“导出 KPI 序列 CSV”仍 enabled；在拦截 POST 的 route mock 中点击后实际发出一次 `/api/admin/bi/reports`，带幂等键和完整请求体 | L1 页头动作的 `exportable`/disabled 条件没有绑定严格 KPI 协议校验结果，错误态仍保留写能力 | L1 失败关闭、未知结果/重复提交安全和错误数据导出；阻断 L 域 Final10 Owner 签发 | M（轮换修复）/L Owner 完整重跑/K 非 Owner 复审 | **待修复。** route mock 返回 422，POST 未触达真实后端、无业务写。证据 `L/final10-owner/fault-matrix/l1-malformed-click-summary.json`（SHA-256 `56119C3F…A94`）及 trace。 |
| L-F10-002 | L2 漏斗/cohort/留存 200 响应合同 | P1 | Final10 真实 `/api/admin/bi/funnel/overview` 为 `200/code=0`，含完整 detailed dashboard 和 6 个唯一 lifecycle stage，但页面显示协议错误、0 行并关闭导出 | 后端 `stageEvents[3]` 为 `wallet.reinvest / 二次 checkout.completed`，PC `isStrictL2Dashboard` 只接受精确 `wallet.reinvest`；完整逐谓词诊断只有这一项失败 | L2 首次打开、漏斗/cohort/留存读取及导出主生命周期；阻断 L 域 Final10 Owner 签发 | M（轮换修复）/L Owner 完整重跑/K 非 Owner 复审 | **待修复。** 六键为 `registered/profileCompleted/kycSubmitted/kycApproved/ordered/walletActivity`；UI 正确 fail-closed，但合法 200 无法消费。脱敏 schema 含 124 条全数组字段路径/类型，定位证据 `L/final10-owner/l2-contract/l2-response-contract-diagnostic.json`（SHA-256 `CD828D91…DDC5`）。 |
| SHARED-FINAL11-IDEMPOTENCY-001 | 公共幂等过期调度 / A1、A2、J1、J4 | P1 | Final11 并行 Owner 期间，A1 密码重置最终化与批量过期扫描互相死锁，A1 两次在约 98/113 秒后返回 `503 PLATFORM_BACKEND_TIMEOUT`；J 的 A2 overview 同期 10 秒超时/503 | `markExpiredProcessingUnknownBatch` 在批量扫描中锁住仍处于活跃事务的幂等记录，与请求最终化锁顺序冲突，调度器会阻塞在线管理写链 | 所有使用管理端幂等记录的域；阻断 Final11 候选签发 | 主控协调独立修复 / A、J、F 整域重跑 / 非修复者复审 | **修复及独立复审通过，待Final12运行时关闭。** 新索引配合 `ORDER BY expires_at,id … FOR UPDATE SKIP LOCKED`，在独立 `READ_COMMITTED / REQUIRES_NEW` 事务精确声明可回收行；锁冲突有限重试且耗尽显式失败。定向 `25/25`、真实MySQL并发 `3/3`；startup迁移对双索引名称+列序、GET_LOCK超时/NULL、同名错定义、重复迁移均fail-closed。非修复者初审`99.4`、复审`99.2`。Final12仍须全Maven、真实迁移/启动和A/J/F整域重跑。 |
| SHARED-FINAL11-LOGOUT-001 | 公共认证退出请求生命周期 | P2 | 原始 logout 200 headers 后body被认证代际切换中止；修复该点后，退出导航及 stale guard 又分别令真实在途 `/api/**` 出现 `net::ERR_ABORTED` | 浏览器 fetch 在响应头到达即resolve；认证代际变化后若立即导航或抛错而未完整消费旧响应body，Chromium会取消传输 | 所有域退出重登与最终requestfailure=0门禁 | 主控协调共享认证修复 / 各域重跑 / 独立严格复审 | **r5修复及独立复审通过，待新候选各域重跑。** 移除退出成功强制导航；stale分支完整消费原始body后返回应用内合成409代际失效，正常epoch不预消费。真实浏览器严格链`1/1`、合同/行为`27/27`、bootstrap对抗`3/3`；真实上游200/旧session401均loadingFinished，`requestfailure=0`、pageerror=0、非预期console=0，无线网409、旧数据或权限回灌。非修复者初审/复审`99/99`。 |
| SHARED-FINAL11-AUTH-002 | 退出后浏览器历史恢复 | P2 | 登录进入K2→退出→立即浏览器返回，两次稳定停在“登录状态校验失败/重新校验”，没有显示登录页；logout 200并清cookie，匿名session GET明确401 `ADMIN_SESSION_MISSING` | history/bfcache恢复的旧bootstrap错误态覆盖新认证代际的anonymous真值 | 所有域退出、返回、刷新和历史导航 | 主控协调共享认证修复 / K Owner整域重跑 | **修复及独立复审通过，待新候选整域重跑。** 匿名session 401权威收敛为LoginGate；旧epoch响应不再覆盖新代际。真实后退/前进/刷新/popstate均保持匿名且aside为0，失败恢复与退出后新读前置阻断通过；共享复审初审/复审`99/99`。 |
| C-FINAL11-001 | C3 资产调整反冲权限 | P1 | maker 从可见 C3 创建真实 USDT DEBIT `0.01` 成功；具 `user_c3_adjust_reverse` 的独立 checker 调用反冲接口却返回 `403 C3_ADJUSTMENT_FORBIDDEN` | Controller 已以 `user_c3_adjust_reverse` 授权，但 Service 又把反冲角色硬编码为 `SUPER_ADMIN/FINANCE_LEAD`，形成 RBAC 与业务层双重合同冲突 | C3 主生命周期、最小权限角色和五层权限一致性 | D（轮换修复）/C Owner 整域重跑/B 非 Owner 复审 | **修复及独立复审通过，待新候选整域关闭。** Controller与Service统一精确 authority `user_c3_adjust_reverse`，移除角色硬编码；最小权限反冲、越权403与数据恢复均通过，非修复者初审`99.1`、复审`99.0`。 |
| E-FINAL11-APP-001 | App 登录首屏与 E4/E5/E6 跨端链 | P1 | frozen H5 `https://127.0.0.1:5176/?nx_device=off` 中“立即注册”和“改用验证码登录”均可见但点击/键盘后无路由或表单变化，且无 pageerror/console error | frozen载具未包含本轮真实注册/密码/MFA/OTP与canonical E4/E5/E6消费者 | 阻断普通 App 用户注册/登录及 E4 订单、E5 收益、E6 消费的动态验收 | F/G轮换修复 / D、E非Owner复审 / E Owner新候选整域重跑 | **修复及独立复审通过，待新候选整域关闭。** actual App已具备真实注册、密码、MFA、OTP入口与E4订单、E5设备/收益、E6配置canonical链；认证复审`99/99`、consumer复审`99/99`，App typecheck/H5均通过。旧frozen 5176只保留历史证据，不代表新候选。 |
| E-AUTH-RACE-001 | App认证跨请求会话CAS | P1 | 迟到的A账号认证成功/失败/401可覆盖或清除更新的B账号access/refresh会话 | actual App全局token无revision/CAS，且响应在页面flowVersion校验前写入全局态 | 跨账号会话完整性、退出撤销和后续E域消费 | G轮换修复 / E非Owner复审 | **已关闭。** 单一access+refresh vault/revision；页面按flowVersion+phone+mode确认后提交，迟到A成功/失败/401均不能覆盖或清除B。独立CAS race`4/4`、认证复审`99/99`。 |
| E-AUTH-PROTOCOL-002 | App MFA/OTP严格协议与认证防爆破 | P1/P2 | allowlist绕过密码/OTP锁，OTP可轮换IP重复申请；actual接受HTTP/envelope/message/token不一致的428 | allowlist被错误用于认证失败豁免，缺账号/手机号发送窗口；MFA解析未验证完整协议元组 | 密码撞库、短信滥用、挑战重放与错误提权 | G轮换修复 / E非Owner复审 | **已关闭。** allowlist不再绕过认证锁；账号+手机号冷却/窗口/日限额与IP解耦，旧LOGIN challenge事务内失效，第五次错码原子锁定；仅接受精确HTTP428/envelope428/message/challenge且token为空。后端定向`28/28`、认证复审`99/99`。 |
| E-CONSUMER-UNKNOWN-001 | E4订单结果未知幂等 | P1 | network、5xx、畸形/不完整200或parser protocol错误后清除幂等key，重试可能重复下单 | 仅把部分RemoteApiError视为结果未知，漏掉真实CreatedOrder parser的`ApiError(protocol)` | App真实订单重复创建与金额/设备副作用 | F轮换修复 / D非Owner复审 | **已关闭。** 所有无法证明无副作用的network/5xx/malformed/incomplete200/protocol失败保留同key+冻结payload；仅明确4xx/业务拒绝清理。E定向`16/16`、独立复审`99/99`。 |
| E-CONSUMER-CAS-002 | E5设备激活CAS与审计/outbox | P1/P2 | 迟到旧激活可覆盖较新停用，激活缺required audit/outbox | 激活链缺expectedVersion与SQL行版本谓词，事件副作用未与同态幂等绑定 | 设备状态权威、双运营员/多客户端并发和跨域消费 | F轮换修复 / D非Owner复审 | **已关闭。** App→Controller→Service→Mapper贯通expectedVersion，SQL以row_version CAS，stale激活409；首次激活audit/outbox各一次，同状态重放零副作用。后端定向`24/24`、全Maven`3000/0/0/skip5`。 |
| E-CONSUMER-PROVENANCE-003 | E6配置来源严格校验 | P2 | parser接受空sources或重复source | 来源集合只做类型检查，未验证非空、唯一与合同一致性 | App配置真源、下载入口和切号失败关闭 | F轮换修复 / D非Owner复审 | **已关闭。** 空来源、重复来源及不一致配置均失败关闭；切号迟到响应不得回灌。E定向`16/16`、App typecheck/H5与独立复审通过。 |
| M-FINAL11-001 | M5 只读角色按钮可见性 | P2 | 只读账号进入 M5 仍能看到两个“新增”按钮，虽然 disabled 且写 API 403 | M5 页面的按钮级权限门禁仅禁用，没有按 write authority 隐藏 | M5 按钮层权限、首次用户可理解性和最小暴露 | A（轮换修复）/M Owner 整域重跑/L 非 Owner 复审 | **修复及独立复审通过，待新候选整域关闭。** 无写权限的M5 mutation controls均不渲染，直接写仍403且状态不变；M5合同`11/11`。非修复者二次复审初审`98.8`、复审`99.2`。 |
| M-005 | M1 坐席名册浏览器读取 | P2 | 精确 M reviewer 从真实侧栏进入 M1 时，`/support-agents` 连续三次 `net::ERR_ABORTED`，页面仅显示“坐席数据暂不可用”；同凭证新 context 直接 GET 为 200 | M1 页面或共享数据加载器的请求生命周期/abort 竞态尚未闭环，不是后端权限或数据缺失 | M1 首次进入、刷新与坐席/负载入口可用性 | A（轮换修复）/M Owner 整域重跑/L 非 Owner 复审 | **修复及独立复审通过，待新候选整域关闭。** M1按session/authEpoch共享单次请求，AbortError最多重试一次，侧栏徽标不再触发整套M1；独立浏览器验证无ERR_ABORTED。 |
| M-006 | M2 指派候选合法同名 | P2 | M2 专属 maker 单次读取 `/tickets/assignee-candidates` 返回 9 条严格 `{adminId,name}`，其中两个不同 `adminId` 合法同名；前端要求 name 全局唯一，导致整批协议失败关闭、创建按钮为 0 | 客户端把展示名误作稳定唯一键，超出后端以 `adminId` 标识候选的合同 | M2 工单创建/指派及同名运营账号 | A（轮换修复）/M Owner 整域重跑/L 非 Owner 复审 | **修复及独立复审通过，待新候选整域关闭。** ownerAdminId贯穿新建/转交/升级，同名以`name · #adminId`区分；动态选择第二同名99756，请求/API/MySQL均精确命中99756。M2合同`15/15`，非修复者初审`98.8`、复审`99.2`。 |
| L-F11-001 | L1 KPI 趋势可用性合同 | P1 | Final11 真实 `GET /api/admin/bi/kpi?window=7d` 返回 200、8 条其余字段均合法的 KPI，但每条 `spark=[]`；PC 强制 `spark` 必须恰好 6 个有限数，整页报 `L1_KPI_ROW_INVALID`、KPI 卡为 0 | 后端在六个趋势切片任一不可用时合法返回空列表，PC 未表达“当前KPI可用但趋势样本不足”的合法状态，形成跨仓合同漂移 | L1 首次打开、KPI卡、导出前置与L域写闭环 | M（轮换修复）/L Owner整域重跑/K非Owner复审 | **修复及独立复审通过，待新候选整域关闭。** spark只接受`[]`或恰6个有限数；空趋势明确“趋势数据不足”且不补0，有可用KPI导当前快照、全不可用禁导出、完整6点导序列。L合同`51/51`、TypeScript、production build均通过；非修复者初审/复审`99/99`。 |
| C-FINAL12-001 | C3 资产调整 maker/checker 生命周期 | P1 | Final12 中具 `user_c3_adjust_approve/reverse` 的独立 checker 对真实调整审批仍返回 403；创建却直接成为 `APPROVED` 并由 maker 充当 checker、立即记账 | 创建链把提交与批准合并，服务层状态机和精确 authority 未形成独立复核边界 | C3 资金调整、最小权限、审计/outbox、幂等与 CAS | D（轮换修复）/C Owner整域重跑/B非Owner复审 | **源代码已修复，待新候选运行时复验。** 创建仅落 `PENDING_REVIEW` 与审计；独立精确权限 checker 才能批准并原子落余额、账本、审计和 outbox，maker 复核拒绝。TDD RED 已留存；后端 `108/108 + 30/30`、PC C3 合同 `4/4`、TypeScript 通过。 |
| D-FINAL12-PRODUCT-001 | D3 资金池最小只读角色 | P1 | 修正 Final12 child 载具后，专属账号已具 `finance_d3_read`、可见 D3 菜单和路由，但 `/finance/pool` 显示“D3 数据异常”，不渲染“应付负债 · 9 类科目” | 待 E 轮换修复者沿页面模型、BFF、API 与服务端最小权限响应定位 | D3 首次用户读取、五层权限和 D 域签发 | E（轮换修复）/D Owner整域重跑/C非Owner复审 | **已确认，修复中。** D3/D5/D6 写链 `3/3`、动态幂等/CAS/unknown-result 与精确恢复已通过；IN/OUT 净额 0，PROCESSING 与对象锁残留均为 0。不得用 superadmin 读取替代最小角色复验。 |
| E-FINAL12-001 | E6→App H5 真实消费 | P1 | Final12 指定 App H5 预览白屏，未发出 `/api/config/platform`，因此无法证明 E6 canonical 配置被普通 App 实际消费 | 待 F 轮换修复者从锁定 App 构建/运行入口、启动异常和消费链定位 | E6 跨端真源、下载入口及普通用户首屏 | F（轮换修复）/E Owner整域重跑/D非Owner复审 | **已确认，修复中。** PC E1–E6、五层权限及 E3/E6 的 A2/CAS/幂等/恢复已通过；App 消费动态用例 `0/1`，不得用静态合同代替。 |
| G-FINAL12-001 | G1/G7 同代际乱序与切号回灌 | P1 | Final12 对抗矩阵仅 `3/12`：旧读取可覆盖当前投影或命令回执，账户切换后旧结果可回灌 | 现有 generation/latest-read/command barrier 仍未覆盖所有资源和切号组合 | G1 质押、G7 回购、钱包与跨账号隐私 | H（轮换修复）/G Owner整域重跑/F非Owner复审 | **已确认，待修复。** App 全测 `259/259` 与 G 静态闭环 `23/23` 未覆盖本动态缺陷；快照已恢复、对象锁为 0。 |
| G-FINAL12-002 | G7 配置/订单部分成功发布 | P2 | config 与 orders 一侧成功、一侧失败时，合法结果仍可能被整体失败清空 | 独立资源的 settle/publish 边界仍在部分动态路径被整体错误态覆盖 | G7 开放状态、订单列表与重试恢复 | H（轮换修复）/G Owner整域重跑/F非Owner复审 | **已确认，待修复。** 与 `G-FINAL12-001` 同轮闭环，必须以真实乱序/部分成功矩阵复验，不以旧合同绿灯关闭。 |
| K-FINAL12-001 | K4 风险评分模型版本分配 | P1 | Owner 精确恢复 active v112 并保留不可变 archived v113/v114 后，再创建草稿返回 500 `DuplicateKeyException` | 新草稿使用 `active.version + 1`，没有从全历史 `MAX(model_version) + 1` 分配，撞上已归档版本的唯一键 | K4 恢复后继续迭代、模型生命周期、失败关闭与最终签发 | L（轮换修复）/K Owner整域重跑/J非Owner复审 | **已确认，TDD修复中。** JSON 对象键序的原 `sameModel` P0 是载具误报且已改语义深比较；当前 500 是由正确保留不可变历史后暴露的独立产品缺陷。修复不得删除 archived 历史或复用版本号。 |
| M-FINAL12-001 | M5 发布后 M3 坐席身份消费 | P2 | 完整 M5 发布、刷新/重登并切换到有效第二 SUPPORT 坐席后，M3 发起会话弹窗 30 秒仍显示“暂无可发起身份”；同凭据直查 support-agents 为 `200/10` | 待 A 轮换修复者从 auth/session epoch、导航代际、渐进加载、缓存与取消竞态定位 | M5→M3 跨模块消费、坐席身份与长流程刷新重登 | A（轮换修复）/M Owner整域重跑/L非Owner复审 | **已确认，修复中。** M1、M2/M4、M3基础链、权限与失败关闭均通过；M合同 `67/67`。M5配置已恢复，未创建会话，`DOMAIN_M_WRITE` 已释放。 |

## 环境事件

| 编号 | 事件 | 归因 | 处理 |
|---|---|---|---|
| ENV-001 | 新建空库启动时缺少历史业务表，后端在 RBAC baseline 初始化阶段退出 | 验收环境初始化，不是产品缺陷 | 从上一轮已清理隔离库复制到本 Run 专库，当前 JAR 启动与真实登录通过 |
| ENV-002 | 基线切换前多个域共享旧会话，部分 Playwright 登录停留或被停服中断 | 主控切换窗口，不进入产品结论 | 全部切换前结果作废，正式 Owner 在新专库和锁定进程上从头重跑 |
| ENV-003 | 多域同时创建权限账号时 A1 BFF 出现 `PLATFORM_BACKEND_TIMEOUT` | 共享权限夹具的并发初始化超出当前服务承载，不直接归为单域产品缺陷 | 启用 `GLOBAL_ACCOUNT_LOCK`，由 A 域串行创建公共 maker/checker/只读/无菜单夹具后供其他域复用 |
| ENV-004 | 多个 Playwright 进程共用默认 `test-results`，出现结果目录被其他进程清理导致的 `ENOENT` | 验收载具隔离错误，不是产品缺陷 | 启用 `ARTIFACT_ISOLATION`，所有域使用独立 `--output` 与 `--reporter=line` |
| ENV-005 | H2 首轮脚本退出时未把 `trialOffsetCapUSD` 从 51 恢复到 50 | 测试夹具清理失败；产品归因待排除 | 已发放唯一 H 写令牌，要求先恢复权威快照并核对审计/事件，再继续 H1 隔离复现 |
| ENV-006 | H1 首轮沙盒提交后的读取请求被浏览器取消，页面停留在 loading/submitting | 并行载荷事件；唯一 H 写令牌下同链 repeat-each=3 全部通过 | 已保存隔离 trace；H2 遗留值也已从 UI 恢复为原值 50，并核对 A2/A4/DB |
| ENV-007 | I-CAND-001 在并行窗口出现 I6 入口/登录壳层缺失 | 低载荷隔离重验 3/3 完整通过，支持并行环境归因 | 原始证据位于 `I/candidate-001-rerun/`；记录所有取消请求，I1–I6 每轮均有真实 200，继续在全量终验监控 |
| ENV-008 | G1/G2/G3/G4/G7 App 公共投影在本机无边缘地域头时均返回 `503 GEO_COUNTRY_UNRESOLVED` | 地域过滤器按可信边缘国家 fail-closed；直连 8110 的验收载具缺少生产边缘注入，不是产品缺陷 | 已关闭为环境载具事件：保留无头 503 证据；仅在本机可信 peer 的受控 carrier 注入 `X-Nexion-Edge-Country: JP` 后五项投影均 `200/code=0`，G Owner 7/7 通过。不得启用 loopback 默认放行、修改 App 或伪造客户端信任头。 |
| ENV-009 | I6 写链发布 v2 后，直连 App i18n 投影返回 `503 GEO_COUNTRY_UNRESOLVED` | I6 生命周期脚本未复用 ENV-008 的本机可信 carrier 载具；产品按缺少可信地域的公共 App 路由正确 fail-closed | **已关闭。** I6 载具只在后端地址为 loopback 时注入受控 `X-Nexion-Edge-Country: JP`，非 loopback 立即拒绝；App 产品代码未生成该头。Owner 已从真实登录/可见 I6 入口完整重跑 v1→v2→发布 v2/v3→App 权威回读→归档→回滚 v4→再归档，`1/1` 通过并精确清理。无头、合法 JP、非法 ZZ 边界仍分别为预期的 fail-closed/成功/fail-closed，地域产品策略未放宽。证据 `I/write-owner/i6-lifecycle-rerun/` 与 `I/write-owner/i6-carrier-boundary-20260729-1808.json`。 |
| ENV-010 | I6 maker/checker 同版本并发草稿更新返回 `200 + 403`，未形成预期 `200 + 409` CAS 对抗 | 旧独立 checker 账号不具 I6 编辑权限，权限夹具不能覆盖本域双运营员并发写前置；不是 I6 服务端 CAS 产品缺陷 | **已关闭。** `GLOBAL_ACCOUNT_LOCK` 下建立 RunID 专属最小 checker，authority 严格为 `content_i6_read + content_i6_write`、菜单严格为 `I + I6`；其他 I 写、B 跨域读取和 A2 审批均为 403，刷新/重登不漂移。夹具证据 `I/i6-checker-fixture/fixture-safe.json`。在此前置被纠正后，Owner 的真实双浏览器并发命中 `200 + 200`，已单独登记为产品缺陷 `I-001`，不得继续归为环境事件。 |
| ENV-011 | G1 历史候选响应中断后的精确回退 | 历史候选中，G maker 将 `usdt30d.minStake 20→21` 成功提交后，UI 在 B1 红线下禁用 `21→20`，当时使用一次性恢复锁做单行 CAS SQL 清理 | **仅保留为历史环境恢复记录，不再声称 Final10 API 正确拒绝。** Final10 已在专属隔离库重复证明 UI 禁用但直接 API 返回 200，正式登记为产品缺陷 `G-FINAL10-001`。历史恢复证据不得跨候选用于关闭该 P1。 |
| ENV-FINAL12-001 | A6–A8 cleanup 把 MFA challenge 当作 session；A1 故障矩阵漏绑 Final12 凭据 | 验收载具错误，不是产品缺陷 | B 轮换修复载具：统一真实 MFA 登录并等待新 TOTP 窗口，cleanup 保留原始错误，故障矩阵真实覆盖 404/500/超时/断网/畸形 200；修复后 A Owner 必须整域重跑。 |
| ENV-FINAL12-002 | D child 权限夹具 401、并发 MFA 竞争、固定 voucher 重复及 D6 unknown 探针未命中 | 子环境验收载具漂移，不直接归为 D 产品缺陷 | E 轮换修复载具：使用 Final12 child 受限凭据、动态 Run 前缀/幂等键/voucher、独立 MFA 窗口和同路径替代 200 规则；资金写前后快照、补偿恢复和残留清理。 |
