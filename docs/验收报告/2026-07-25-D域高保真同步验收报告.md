# D 域高保真同步验收报告（2026-07-25）

## 结论

**PASS，可上线。** D1–D6 已按高保真页面同步到 PC 管理后台，主流程全部接入真实后端、MySQL、Redis、审计与事件链路，不使用 mock 或浏览器本地状态作为业务事实源。

- 初审：**98.8 / 100**，通过初审门槛（>96）。
- 非 Owner 对抗复审：**99.2 / 100**，**310 / 310** 项断言通过，超过复审门槛（>98）。
- 最终得分：**98.8 / 100**（取两轮较低分）。
- 最终缺陷：**P0=0、P1=0、P2=0、P3=0**。

## 验收范围

- D1 充值对账：VietQR 五视图、收款账户、人工匹配、按实收核销、登记退回。
- D2 提现审核：查询、详情、延迟复查、状态历史及 D5/K3/K4/C2/C4 联动。
- D3 资金池：九类负债、真实储备注入、D1 待核实入金联动。
- D4 账本/账单：全局账单、单用户账本、Running Balance、钱包权威余额及 D1 入账联动。
- D5 提现参数：日限、余额比例、网络费和 NEX 抵扣率的读取、写入、CAS 与恢复。
- D6 汇率牌价：基准价、点差、锁价窗口、服务端报价、版本历史、幂等与 CAS。
- 跨域：A2 审计、A4 事件、B1 双账本、H1 Phase、L3 财务分析。

## 真实接口与无 mock

- 全部用户路径从真实登录页及可见侧栏进入，经 PC BFF 调用真实后端，并由数据库事实回读验证。
- D1 入账同步更新钱包、累计入金、D4 账本与 D3 负债；D2 延迟状态在刷新和重登后保持；D3 重复凭证不重复注资；D6 报价只由服务端派生。
- 主流程未使用 mock、localStorage、隐藏 URL 或 DOM 注入。故障注入仅用于 401/403/409/422/500 等墨菲路径，不替代业务数据。
- 银行账号密文落库，页面只显示尾四位；写操作携稳定幂等键、版本和业务理由。

## RBAC、异常与跨域

- D1–D6 均从角色授权后的真实侧栏可发现；临时 FINANCE/AUDITOR 角色的菜单、按钮和接口权限一致。
- AUDITOR 可读但无高风险写入口；绕过页面直接写 D1/D2/D3/D5/D6 均返回 403，并可在 A2 查到对应拒绝审计。
- 匿名访问六个 D 域接口均返回 401。
- D1–D6 各完成受控 500 验证：页面清除旧事实、关闭危险写操作，解除故障后可重试恢复。
- 陈旧版本返回 409，非法参数返回 422，不存在资源返回 404；幂等重放不产生重复业务结果。
- D1→D3/D4、D5→D2/B1/H1、D6→付款报价，以及 A2/A4/L3 的跨域事实均经页面和接口双向核对。

## 发现、修复与完整重验

### 已关闭 P1：D4 全局空结果清空独立用户账本

- 现象：单用户账本和 Running Balance 已成功返回后，全局账单筛选为空会清空独立用户账本状态。
- 根因：全局账单状态与单用户账本状态被不必要地耦合。
- 修复：全局请求只维护全局账单，不再读写单用户选择、用户账本和 Running Balance；补充契约测试禁止该副作用回归。
- 验证：D4 与 D1–D6 契约 **10 / 10** 通过，Next.js production build 生成 20 个页面；随后从登录页、可见菜单重新执行 D1–D6 及跨域完整复审，**310 / 310** 通过。

本轮最终没有用局部复测代替全域重验。

## 验证结果

- 初审主流程：11 / 11；D2/D5 扩展：9 / 9；页面错误 0。
- 复审：310 / 310；Playwright `pageerror=0`，非预期网络错误 0。
- D1/D5/D6 CAS、D3 幂等、D2 非法状态迁移以及 401/403/404/409/422/500 均按预期失败关闭。
- 三角色真实登录预检：3 / 3。
- 敏感信息扫描未发现密码、Cookie、JWT、Bearer、完整银行卡号或原始幂等键；未生成 HAR、trace 或录像。

## 清理结果

预清理与最终清理的 **11 项后置条件全部为 0**：临时管理员、临时用户、D1 回单、D2 提现单、D3 注资流水、D4 钱包/账本、D6 验收历史、D1/D5/D6 配置漂移及幂等记录均无残留。

- D1、D5、D6 恢复验收前权威配置。
- D6 恢复为基准价 26000、点差 1.50%、锁价 30 分钟；原有非测试历史完整保留。
- Redis 临时管理员状态键已精确删除，复核无残留。

## 证据链接

- [D 域初审报告](D:/workspace/bug-pic/20260725-domain-reacceptance/d-initial/D域初审报告-20260725.md)
- [D 域独立对抗复审报告](D:/workspace/bug-pic/20260725-domain-reacceptance/d-rereview/D域独立对抗复审报告-20260725.md)
- [D 域复审断言结果](D:/workspace/bug-pic/20260725-domain-reacceptance/d-rereview/evidence/acceptance-result.json)
- [D 域最终清理结果](D:/workspace/bug-pic/20260725-domain-reacceptance/d-rereview/evidence/cleanup-result.json)
