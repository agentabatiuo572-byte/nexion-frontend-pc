# M 域高保真同步验收报告（2026-07-25）

## 结论

**PASS，可上线。** M1–M5 已完成高保真同步后的真实接口闭环；客服总览、工单、在线会话、知识库/SLA、话术与回复模板均以真实后端和数据库事实为准，不使用 mock 或浏览器本地状态。

- 初审：**98.6 / 100**，通过初审门槛（>96）。
- 非 Owner 墨菲复审：**99.3 / 100**，通过复审门槛（>98）。
- 最终得分：**98.6 / 100**（取两轮较低分）。
- 最终缺陷：**P0=0、P1=0、P2=0、P3=0**。

## 验收范围

- M1 客服总览：真实汇总、坐席负载、结果未知同键重试、局部依赖失败隔离及刷新/重登。
- M2 工单：创建、回复、解决、归档、非法迁移失败关闭、审计员只读。
- M3 在线会话：真实用户发起、模板回复、转接/接管、回执、转工单、空闲提醒与自动结束策略。
- M4 知识库与 SLA：FAQ 生命周期、稳定临时 ID、未知结果重试、SLA 配置与 M1 消费。
- M5 话术与回复模板：草稿、发布、归档终态、CAS、幂等、M3 消费及 M1 坐席来源。
- 跨域：M3↔M2↔M1、M4→M1、M5→M3/M1、A2 审计、A4 事件。

## 真实接口、权限与墨菲路径

- 所有浏览器流程从真实登录页和可见侧栏进入，经 3002 PC BFF 调用 8110 后端并从 MySQL 回读。
- M3 超时策略使用真实 GET/PUT、稳定幂等键和 CAS；调度器 WARN/CLOSE 各执行一次，最终状态、系统消息、SSE 和 A2/A4 均完成核对。
- 匿名读写返回 401；审计员页面只读，绕过页面写入返回 403。
- 同键同载荷只执行一次，同键异载荷和陈旧版本返回 409；非法状态、整数/范围/顺序和理由边界返回 422。
- 依赖 500、断网、畸形响应和结果未知均失败关闭，不清空未确认表单，不把错误伪装为空数据或成功。
- M5 已发布模板由 M3 真实消费，归档后从可用列表移除；M4 SLA 与 M5 坐席来源由 M1 真实读取。

## 发现、修复与完整重验

### 已关闭 P1：M3 小数超时被静默截断

- 修复前 `warnMinutes=1.5` 被接受并截断为 1，同时推进版本。
- 修复后前后端均要求整数；非法值返回 422，策略和值不推进，并产生拒绝审计。
- 修复部署后，从 M1 可见入口开始完整重跑 M1–M5 和跨域链路。

### 验收执行器竞态修正

- 非 Owner 复审发现 M3/M5 首次改密登录辅助函数会在首次提交未完成时重复点击；修为单次提交并等待真实改密响应。
- 该项属于验收执行器缺陷，不是产品缺陷；修正后未跳过或放宽任何业务断言，并从 M1 重新执行全量范围。

## 最终验证

- M1 Playwright：通过，16 项业务检查。
- M2 真实浏览器/API：**16 / 16**。
- M3 主流程：**27 / 27**。
- M3 超时专项：**16 / 16**，`defects=[]`。
- M4 Playwright：通过。
- M5 主流程：**36 / 36**。
- M1–M5 + RBAC 契约：**68 / 68**。
- PC 全域锁定合同：**208 / 208**；production build：**20 / 20 页面**。
- 后端 Maven 全量：**2569 / 2569**。

## 恢复、清理与证据

- MySQL 临时管理员、状态、角色、坐席、知识库、会话、消息、回执、超时、转接、工单、附件、幂等、审计和 outbox 残留均为 0。
- Redis 临时会话、session tracker、权限缓存、MFA challenge/counter 和登录保护残留均为 0。
- 配置恢复为：M3 超时 1/5 分钟（版本保持单调递增至 37）、SLA 10 分钟/6 小时、M5 启用且延迟 1500ms、受众为全量用户。
- 最终运行：`D:\workspace\bug-pic\20260725-domain-reacceptance\m-rereview\20260725-m-rereview-final-4`。
- 初审报告：`D:\workspace\bug-pic\20260725-domain-reacceptance\m-initial\M域页面业务逻辑通顺性初审验收报告-20260725.md`。
- 非 Owner 复审证据：`D:\workspace\bug-pic\20260725-domain-reacceptance\m-rereview`。
