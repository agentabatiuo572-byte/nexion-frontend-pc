# 不合理删除恢复总验收结论

## 最终结论

**PASS，允许作为本轮恢复候选；未授权发布。**

2026-07-23 16:42:21 +09:00 的 `f8e8e8f9be53f7cf3fb6865ef4e89591426f0d5a` 确实在“与后端契约对齐”的大重构中停止渲染了本次追查的图表。提交说明没有产品退役决定，因此相关删除被判为不合理。B-1 至 B-7、F-2、F-3 已恢复；同时恢复 C1 导出确认/理由、C3 真实分页等同类回归，并对未接后端的能力保留失败关闭结构。

经 A–M、首页/侧栏、BFF 和相邻后端契约审计，明确有产品禁令、安全理由、真实等价迁移或产品退役决定的删除没有回滚，避免把 mock、明文导出、越权操作或已退役模块重新带回。

## 验收方法执行结果

| 轨道 | 结果 | 关键证据 |
|---|---|---|
| 历史基线/删除授权审计 | PASS | 删除提交、时间、提交正文、前后页面和现行后端字段逐项对照 |
| 首次真实用户主链 | PASS（2/2） | B2/B3/B4/B5/F5/C1/C3/A3/A4 从可见菜单进入并逐页刷新；B2/B3/B4 发出真实筛选参数，F5 类型/状态/游标过滤，C1 导出，C3 页长过滤；B5/F5 退出重登复核 |
| 权限/边界真实浏览器 | PASS（4/4） | B2–B5 与 F5 五个最小角色遍历全部其他恢复接口并得到 403；B5 目标域权限负/正向、撤权即时失效与恢复重登、C3 页数收缩均通过 |
| 契约失败注入 | PASS | F5 畸形 200 失败关闭，权威重试后恢复；B4 预算缺失不造图 |
| 前端契约与正向守卫 | PASS（71/71） | 图表、时间序列、零分母、B5 既有回归门、F5 全量聚合/游标、C3 分页和失败关闭能力位防二次误删 |
| TypeScript | PASS | `tsc --noEmit` 0 错误 |
| Next 生产构建 | PASS | 构建 ID `kDJCuQ43zsiIGGchAaM6L` |
| 后端定向测试 | PASS（171/171） | B3、B5、F5、C1/C3 服务、SQL 与契约测试 0 失败 |
| 夹具清理 | PASS | 一次性账号、角色、权限、C3 业务记录总计 0 |

## 真实运行证据

- 主链 JSON：`D:/workspace/bug-pic/unreasonable-deletion-restoration-20260808/live-20260808-final7-r5/acceptance.json`
- 主链截图：同目录 `01a` 至 `10`。
- 主链 trace：`test-results-restoration-live-final7-r5/**/trace.zip`（2 份）。
- 权限/页数边界 JSON：`D:/workspace/bug-pic/unreasonable-deletion-restoration-20260808/live-20260808-final7-boundaries-r4/boundaries.json`
- 权限/页数边界 trace：`test-results-restoration-boundaries-final7-r4/**/trace.zip`（4 份）；JSON 记录与主链逐值相同的前端 Build ID、后端运行类摘要，并绑定关键源文件哈希。

`final7-r1` 至 `r4` 以及边界 `r1` 至 `r3` 是修复过程中保留的失败/预检证据，不作为 PASS 证据；最终签发只引用上面两组不可覆盖目录。主链一次性 B3 事实夹具和边界账号/权限/业务夹具均在运行后清理为 0。

## 当前阈值口径

- `18%` 每日首购目标线与 `1.2` 新增/流出健康线来自本次 2026-08-08 产品指令的明确数值，不是从历史 mock 反推；接口继续返回对应字段，前端按返回值绘制。
- 两者当前尚不是运营动态配置项。后续如调整，必须先形成版本化产品/配置裁决，并由后端下发；不得只改前端常量。
- B3 当前口径是四级同用户漏斗（注册→首购→复投→提现），每日线是“当日注册→当日首购（Day0）”；2026-08-07 全项目取消 KYC 的现行决定覆盖更早的五级/KYC 历史描述，未恢复已退役绑卡阶段。

## 保留 HOLD

- C3 撤回待放行申请：后端状态转换、权限、CAS/幂等和审计未定义；只恢复结构，不启用假按钮。
- B4 四类运营预算：真实配置未完整时显示不可用，不从资金科目推断。
- E3 若干历史推广参数：证据不足，等待产品裁决/执行器，不把缺失静默解释为退役。

## 发布与工作树边界

- 本轮没有执行 commit、push 或发布。
- 前后端工作树存在大量用户原有并行修改，本轮未清理、重置或覆盖无关改动。
- 原运行实例 3002/8110 保持不动；验收使用隔离实例 3106/8112，证据固化后已停止隔离实例，未留下 3106/8112 监听进程。

## 评分

- 最终独立初审：**99/100，PASS**，严格大于 96。初次初审发现既有 B5 回归门仍指向迁移前文件并给出 95 分阻断；同步门禁并完整重跑 71/71 后重新初审通过。
- 最终墨菲复审：**100/100，PASS**，严格大于 98；P0/P1/P2/P3 均为 0。
- 评分只对 `final7-r5` 主链、`final7-boundaries-r4` 边界及其绑定的当前候选有效；任一源文件、运行构建或后端契约变化后必须重新验收，不得沿用本次分数。
