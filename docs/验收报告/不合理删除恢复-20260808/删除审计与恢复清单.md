# 不合理删除审计与恢复清单

> 审计日：2026-08-08  
> 范围：`nexion-ops-console` 当前 `main` 可达历史中的 A–M 页面、首页/侧栏、BFF、前端契约与相邻后端真实接口；重点复核产品可见能力的删除、隐藏、合并和停止渲染。  
> 判定规则：历史存在或设计已确认的能力默认保留；只有删除前的产品裁决、明确禁令、可验证等价迁移或安全紧急处置才构成删除授权。

## 1. 结论

2026-07-23 的提交 `f8e8e8f9be53f7cf3fb6865ef4e89591426f0d5a` 确实删除或停止渲染了本次追查的主要图表。提交时间是 **2026-07-23 16:42:21 +09:00**，标题是 `feat: align A-M domains with backend contracts and complete integration`。

提交正文只说明同步 A–M 页面、接口白名单、客户端契约、测试和文档，**没有图表退役、产品收缩或等价迁移的书面决定**。四个 B 页面在接入新客户端时被整页重写，历史图表的消费点没有随新契约重新接回；这是大重构中的功能回归，不是产品决定。

该提交横跨 296 个文件、38,015 行新增和 7,270 行删除。把“接后端契约”当作删除理由不成立：契约数据继续返回，而前端停止消费，正是本次九项“后端有、运营看不到”的共同原因。

## 2. 已判定为不合理并完成恢复

| 能力 | 删除时间 / 提交 | 当时实际工程动作 | 为什么不合理 | 当前处置 |
|---|---|---|---|---|
| B-1 出金压力近 8 窗口、70% 红线、恶化/好转 | 2026-07-23 16:42:21；`f8e8e8f` | B5 风险雷达重写，只保留当前值 | 字段继续由后端返回，无退役授权 | 已恢复；零分母显示断点和“不可计算”，不造 0% |
| B-2 近 8 窗口进出、近 8 个月新增入金、到期柱+累计线、负债构成 | 同上 | B2 页面重写为当前快照 | 趋势消费点丢失，无迁移说明 | 已恢复真实历史；无数据时保留不可用态 |
| B-3 新增/流出比、趋势、1.2 健康线 | 同上 | B4 页面只保留节奏快照 | 后端仍有历史字段，无禁令 | 已恢复，并显示扩张/收紧建议；不自动执行 |
| B-4 P0–P3 告警环图、近 7 天告警量 | 同上 | B5 页面重写遗漏分布图 | 告警数据仍返回，无退役依据 | 已恢复；日期标签以服务端为准，不按本地时区补零 |
| B-5 每日首购转化率和 18% 目标线 | 同上 | B3 仅保留周 cohort | 日粒度继续可由真实事实计算 | 已恢复 8 日曲线；注册分母为 0 时为不可计算 |
| B-6 本月运营预算分配 | 同上 | B4 页面遗漏预算环图 | 历史设计仍在，无退役授权 | 恢复结构和真实契约位；四类预算未完整配置时明确不可用，禁止拿财务科目冒充预算 |
| B-7 首购渠道占比、节奏引擎判定位 | 同上 | B3/B4 页面重写遗漏 | 可由真实首购归因和增长/流出事实回答 | 已恢复；无同用户首购事实时不画伪占比 |
| F-2 六类佣金总额、笔数、点击过滤 | 2026-07-23；`f8e8e8f` 所在整合链 | F 页接契约后只剩流水/异常摘要 | 真实六类聚合仍可返回，运营失去支出去向 | 已恢复六类全量聚合和点击过滤 |
| F-3 佣金全量状态分布 | 同上 | 只显示异常冻结样本 | 样本不能代替全量状态 | 已恢复冷却/已解锁/已提现/已撤销/已冻结五态；冻结数来自全量聚合 |
| C1 导出确认和导出理由 | 2026-07-23；`f8e8e8f` | C1 重写时简化为直接导出 | 高敏导出缺少明确确认和审计语义 | 已恢复确认、理由、筛选哈希、脱敏 CSV 和审计 |
| C3 待放行队列分页 | 2026-07-23；`f8e8e8f` | 固定取前 5 条 | 队列超过 5 条时运营看不到后续记录 | 已恢复服务端分页、刷新和页数收缩纠正 |
| C3 撤回待放行申请 | 历史设计存在，现行后端无转换契约 | 页面重写后入口消失 | 未完成不能解释为不需要，但前端也不能伪造成功 | 保留显式失败关闭能力位，等待后端状态机/产品裁决后接通 |
| D2 提现状态机可视化 | 2026-07-02；`4fdb080a` 重构链 | 删除 mock/local 状态时连可视状态一起收缩 | 删除假数据不等于删除状态理解能力 | 当前页面已用真实后端状态恢复，未回灌 mock |
| A3/A4 历史待实现槽位 | 多次契约重构链 | 因未接后端而从页面消失 | 未完成不等于产品退役 | 保留失败关闭结构和明确缺失条件，不展示假数据/假成功 |
| C1 用户 360、L4 佣金结构、K6 控制能力 | 多次整合链 | 旧卡片或旧入口被移除/合并 | 必须验证等价迁移，不能只凭代码存在 | 当前真实入口和后端链已核实；加入正向守卫防止再次消失 |

## 3. 经审计属于合理删除或等价迁移，未回滚

| 删除/收缩项 | 时间或变更链 | 删除理由 / 授权依据 | 处置 |
|---|---|---|---|
| B1 在总览页直接执行紧急动作 | B 域收口链 | 现行契约明确要求 B1 只读，紧急处置归 J1；属于安全分权 | 不恢复操作按钮，只保留风险信息和跳转 |
| L5 明文敏感数据导出 | L 域安全收口链 | 现行安全契约明确禁止明文导出 | 不恢复；仅允许脱敏/受控导出 |
| C4/K5 KYC 全链路 | 2026-08-07 产品变更 | 产品更新日志和移除规格明确决定全项目取消 KYC | 不恢复 |
| E 域假曲线、旧 mock 模型和本地成功态 | 2026-07-02 起的 mock 清理/契约接入 | 防止运营把演示值当真实业务值 | 不恢复 mock；真实能力仍按后端接入 |
| C 域旧账户操作入口 | C 域整合链 | 已迁移至 C2/C3/C5 或用户 360，并保留真实权限和审计 | 保持现行等价入口 |
| D3/D4 旧本地资金模型、D5 旧费率模型 | D 域整合链 | 已由服务端资金池、账本和现行提现 policy 替代 | 不恢复旧模型 |
| F6–F8 独立旧页 | F 域结构调整 | 能力折叠到 F1/F4；实物奖励另有明确退役范围 | 保留等价入口，不恢复重复页 |
| G5/G6 旧金融产品页 | G 域产品结构调整 | 现行产品目录已退役/重编排 | 不恢复 |
| M1–M5 mock/local state | M 域后端接入链 | 五个 `/service/*` 路由和真实页面均在，删除的是本地假状态 | 不恢复 mock |
| 首页 PhaseCard/PhaseStrip、旧个人中心卡片 | 2026-06-16、06-30、07-02 的首页/Hub 重构 | H1/B4/C1/用户 360 已提供真实专页或聚合入口 | 保持等价真实入口；用正向守卫验证可发现性 |

## 4. 尚不能擅自裁决的历史项

- E3 部分旧推广参数缺少足够的现行产品裁决和后端执行证据。当前不把它判成“永久退役”，也不恢复本地假参数；继续按待确认能力记录，产品裁决或真实执行器明确后再处理。
- C3“撤回待放行申请”同理：本轮已恢复能力位，但没有后端状态转换时不得生成可点击的假按钮。

## 5. 防复发门禁

- 《页面业务逻辑通顺性验收方法》新增第八章，强制执行默认保留、删除前授权、删除台账、未完成能力失败关闭和历史误删立即修复。
- 新增正向契约测试，验证恢复图表、F5 聚合、C3 分页及待实现能力位仍存在。
- 删除/隐藏/合并任何可见能力，必须在更新日志中同时记录删除对象、时间、业务理由、授权、影响和替代入口；“接契约”“删 mock”“重构”不能单独作为理由。
