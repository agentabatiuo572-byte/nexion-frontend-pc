# PC 管理端全面测试缺陷记录（2026-07-26）

## 验收范围

- 权威模块源：`lib/nav/console-nav.ts`
- 当前现行域：A–M，共 13 个域
- 当前现行模块：75 个；每个模块由一个独立子智能体负责
- 验收基线：`D:\nexion\页面业务逻辑通顺性验收方法.md`
- 运行链路：PC `3002` → 后端 `8110` → MySQL / Redis / MinIO / RocketMQ 相关业务适配
- 执行原则：首次用户真实浏览器走查 + 域内技术检查 + 域外上下游调用链 + 权限/幂等/CAS/失败关闭/刷新重登/异常恢复

## 缺陷汇总

| 编号 | 模块 | 严重度 | 状态 | 首次用户复现 | 根因 | 影响链路 | 修复与复验 |
|---|---|---|---|---|---|---|---|
| PCFULL-001 | D1 | P1 | 已修复，全量复验通过 | 当前工作区执行后端 `mvn clean package`，2601 项中 1 项失败，无法生成可部署候选包 | D1 VietQR 新增 `RECEIPT_REVIEW`、`LATE_REVIEW` 等状态后，迁移合同测试仍断言旧的连续状态片段 | D1 App intent → PC 回单登记/复核 → D4 入账候选包构建 | 已把合同断言更新为现行完整状态域；后端最终 `mvn package` 2609 项 0 失败、0 错误、1 跳过 |
| PCFULL-002 | D1 | P1 | 已修复，全量复验通过 | 清理旧 `target` 后重新编译，`OpsVietnamPaymentService` 两处调用与 Mapper 新签名不一致，候选包无法构建 | Mapper 已新增银行营业日与 intent 状态推进标志，调用方及测试合同同步不完整 | D1 回单确认日累计、回单登记、App intent 状态推进 | 已同步主代码与测试合同；聚焦测试 16/16、后端全量 2609 项通过 |
| PCFULL-003 | D1→D3 | P1 | 已修复，全量复验通过 | 后端全量测试在 `TreasuryLedgerMapperSqlContractTest` 失败，旧合同要求待核实入金排除 `MATCHED` 视图 | 新真实到账链路中 `MATCHED + OPEN` 仍未入账核销；旧测试按视图筛选会漏记负债 | D1 到账回单 → D3 待核实入金负债 → B1 覆盖率口径 | 合同已改为全部有金额的 `OPEN` 回单进入待核实负债，并禁止再按 `view_type` 漏算；聚焦 7/7、后端全量通过 |
| PCFULL-004 | D1 | P1 | 已修复，全量复验通过 | 新增“终态 intent 第二笔回单”测试把 00:20 到账提交给固定在 00:00 的服务，正确触发“未来到账时间”拦截 | 测试时钟与业务事件时间自相矛盾，无法验证补充回单的真实生命周期 | D1 重复/补充回单登记 → 人工核销 → 防止重开终态 intent | 测试改用 01:00 的延迟操作时钟，保留 00:20 的真实到账快照；聚焦 16/16、后端全量通过 |
| PCFULL-005 | 全量构建基线 | P1 | 已隔离并复验通过 | 一次 `mvn clean package` 在 0 项测试处报 `Truncated class file`，随后同一 `target` 又出现另一进程生成的 D1 定向测试报告 | 共享工作区存在并发 Maven 编译/清理，两个构建同时写 `target`，候选包证据被污染 | 全后端测试与部署候选包 | 并发构建退出后由单一 Maven 进程重新执行完整 package：2609 项通过并成功生成 133,443,575 字节候选 JAR |
| PCFULL-006 | 全模块浏览器验收框架 | P1 | 已修复，A1 预检通过 | A1 预检时，登录表单恢复服务器会话并切换到控制台，旧脚本仍继续填写已卸载的密码框，最终误报超时 | 登录页与已恢复会话存在合法竞态；脚本只按单一页面状态串行操作，退出入口也错误依赖用户名文案 | 75 模块的登录、刷新、退出、重新登录统一走查 | 登录步骤改为同时等待登录框/控制台，并在每次填写后检测会话恢复；退出改为使用 `aria-haspopup=menu` 的可见账号控件；A1 完整走查 1/1 通过 |
| PCFULL-007 | 全模块浏览器验收框架 | P2 | 已修复，A1 预检通过 | A1 完整走查成功后，浏览器仍把退出/重登期间预期的 401 资源撤销记录成通用 console error，脚本误判失败 | 脚本混淆“浏览器网络提示”与未处理 JavaScript 异常；接口 5xx 已由独立 response 监听器检查 | 75 模块退出/重登复验 | console 断言只保留真实脚本错误；网络 5xx 继续由 URL/方法/状态独立采集；A1 完整退出/重登走查 1/1 通过 |
| PCFULL-008 | 全模块运行态 | P0 | 已修复，等待 D4–D6 完整复验 | 登录成功后整站显示 `This page couldn't load`，静态 chunk `0dq5w3f774k.6.js` 返回 500 | 3002 运行的是已被后续构建覆盖的不完整 `.next` 产物，运行进程与静态文件不属于同一构建 | 所有 PC 可见入口、刷新、退出重登和跨域点击 | 停止旧 3002 进程，隔离旧产物，执行 Next.js 生产构建并从同一 BUILD_ID 重启；根路径和登录接口均返回 200 |
| PCFULL-009 | D4 | P0 | 已修复，等待完整复验 | 相同 `(biz_no, asset, direction)` 的账本写入可覆盖既有用户、金额、余额、状态与备注 | `insertLedgerEntry` 使用 `ON DUPLICATE KEY UPDATE`，把幂等重放实现成了历史财务事实更新 | 全部 D4 入账生产者、账实核对、A2 审计证据 | 改为只追加普通 `INSERT`，重复键读取原记录并比较不可变指纹；完全一致才幂等成功，冲突返回 `D4_LEDGER_IDEMPOTENCY_CONFLICT`，退款写入同样只追加 |
| PCFULL-010 | D4 | P1 | 已修复，等待完整复验 | 退款被归为提现；未知用户返回虚构零余额账户；待结算佣金被计入可用余额造成假断点；十万行超限后没有可用缩小条件 | 模糊字符串分类、缺少真实用户校验、状态口径混用、查询合同缺少时间范围 | D4 筛选/用户下钻/运行余额 → D1 钱包、D2/D3、C3 | 采用明确退款优先映射；未知用户 404；只用已入账状态核对可用余额；用户账本与运行余额增加 `from/to` 范围并使用只读一致性事务，页面提供可操作的范围缩小 |
| PCFULL-011 | D4→A2/A4 | P2 | 已修复，等待统一部署后完整复验 | 标准账本写入未保证同事务 outbox，D4 页面也缺少 A2 证据入口；D5 App 提现一度仍直接调用 `AppWithdrawalMapper.insertLedger` 绕过 D4 | 账本事件由部分调用方事后补发，且提现生产者保留私有写账入口，无法保证所有生产者完整通知下游 | D5 App 提现 → D2 订单 → D4 → A4 事件追踪、跨域账本消费者、A2 | 移除提现私有 `insertLedger`；所有非零提现组件统一经 `TreasuryLedgerPostingFacade` 写入，USDT 采用净本金/实际网络费/实际惩罚费且总 OUT=提现额，NEX 抵扣固定先冲惩罚费再冲网络费；每个首次组件写入同事务发布 `wallet.ledger_posted`，精确重放不重复；页面保留 A2/A4 可发现入口 |
| PCFULL-012 | D5 | P1 | 已修复，等待完整复验 | 网络费率/最小值/最大值可配置并显示生效，但真实提现金额、订单和账本均未消费 | D5 仅实现配置读写，`AppWithdrawalService` 没有计算并快照网络费 | D5 → App 提现 → D2 订单 → D4 账本 | 真实提现服务读取三元组并计算网络费，纳入资金预留、订单快照、返回值与账本；保持三元组原子配置和服务端范围校验 |
| PCFULL-013 | D5→App | P1 | 已修复，等待完整复验 | App 提现仍调用本地 mock，且允许后端不支持的 USDT-BEP20 | 运行时默认 mock，提现页未接真实 `POST /api/withdrawals`，前后端网络枚举分叉 | App 钱包 → D5/D2/K3/K4/K5/D3/D4/A4 | App 运行时默认 remote，提现改用真实 API、稳定幂等键和失败关闭；移除 BEP20，仅保留与后端一致的 TRC20/ERC20 |
| PCFULL-014 | D5 BFF | P1 | 已修复，等待完整复验 | D5 PUT 上游断连返回普通 503，前端无法区分“未提交”与“提交结果未知” | BFF 未透传写请求未知结果语义 | D5 配置 CAS/幂等重放 | 写请求上游异常增加 `X-Nexion-Upstream-Outcome: unknown`；前端保留幂等键并要求查询权威状态后再重试 |
| PCFULL-015 | D5→A2/H1 | P2 | 已修复，等待完整复验 | 版本冲突、红线、配置校验等被拒绝操作没有 A2 审计；H1 参数存在双源漂移风险 | 控制器只记录成功，阶段参数部分绕过权威快照 | D5 → A2 审计、H1 节奏 → App 提现 | 控制器对业务拒绝强制写 `REJECTED` 审计；D5/H1 使用统一节奏快照并补定向测试 |
| PCFULL-016 | D6→App | P1 | 已修复，等待完整复验 | App H5 默认 `mode=mock`，页面牌价来自本地 seed，不请求 D6 权威报价 | 未显式配置时运行时回退 mock | D6 → App VietQR 新单锁价 | 默认切换 remote，只有显式 mock 才使用本地种子；App 全量测试 64/64、H5 构建通过 |
| PCFULL-017 | D6→A4 | P1 | 已修复，等待完整复验 | D6 更新没有 outbox；相同值可制造空变更版本；PC/App 只校验正数而不校验派生公式 | 配置、历史、审计与跨域事件没有统一事务闭环，协议校验过松 | D6 → App/D1/D3/D4/B1/B2/A2/A4 | 配置 CAS 成功后同事务发布 `admin.fx_quote_updated`，新增幂等消费者；服务端拒绝 `FX_QUOTE_NO_CHANGES`；PC/App 按统一精度校验 `quote=base×(1+spread)`，页面补下游/A2/A4入口 |
| PCFULL-018 | 全量后端基线 | P1 | 已修复，全量测试通过 | 全量测试暴露 L2 跨午夜留存误成熟，以及 D5 新网络费参数未同步到 K3 路由测试；测试结束后运行中的旧 JAR 又锁住 repackage 目标 | L2 用自然日判断 24 小时成熟度；D5 测试夹具缺少新增配置；Windows 运行 JAR 不能被 Maven 原地重命名 | L2 留存口径、D5/K3 构建、候选包部署 | L2 改为按注册时间加实际天数判断成熟；补齐 D5 夹具；全量测试阶段 0 失败后停止精确旧 PID，再用 `-DskipTests package` 完成同一源码候选包 |
| PCFULL-019 | PC 构建基线 | P1 | 已修复，生产构建通过 | Turbopack/PostCSS 生成损坏的圆角变量选择器，生产构建失败 | 项目根目录残留 `.next.stale-*` 和 `.next-m2-*`，Tailwind 自动内容扫描读入损坏的历史生成文件 | PC 候选包构建和 3002 运行态 | 解析并核对路径后，把三个 `.next*` 目录可恢复地移到仓库外，重新构建成功：20 个页面生成完成、TypeScript 通过 |
| PCFULL-020 | E1 媒体预览 | P2 | 已修复，真实浏览器复验通过 | E1 设备目录可进入，但每张有媒体资源的 SKU 均触发预览 URL 500，首次表面验收被网络 5xx 拦截 | 本地 MinIO 9000 未运行，后端预签名请求连接被拒绝 | E1 SKU 媒体 → MinIO 对象存储 → PC 预览/重试 | 按约定账号启动 MinIO，9000/9001 均监听；重新执行 E1 首次用户浏览器路径，登录、侧栏进入、媒体加载、刷新、退出重登 1/1 通过且无 5xx |
| PCFULL-021 | E1→App 商城目录 | P0 | 已修复，待部署后完整复验 | App 商城、详情、结算、锁额、置换等页面直接读取 `src/mock/products.ts` 的 6 条静态商品，后端不存在 `/api/store/catalog` | PC E1 与 App 商品目录没有真实读取合同，“server-canonical”仅停留在注释 | E1 SKU 上下架/改价/库存/阶段/购买门 → App 商城、详情、订单、设备 | 后端新增只读取 `nx_admin_device_sku` 在售行的用户目录 API；App 新增严格协议解析、响应式目录替换、启动/前台刷新、失败清空缓存并显示重试；Vitest 7/7、App `vue-tsc --noEmit`、后端服务测试 2/2 通过 |
| PCFULL-022 | E1 SKU 筛选 | P2 | 已修复，待 3002 新构建完整复验 | E1 有 SKU 卡片和编辑抽屉，但没有关键词、状态或档位筛选，目录增长后普通运营无法快速定位目标 | 页面直接对全部 `skus` 渲染，缺少目录检索状态和空结果反馈 | E1 列表 → SKU 详情/改价/上下架/删除入口 | 增加名称/SKU ID/GPU/DC 关键词、状态、档位组合筛选，显示结果计数、清除动作和明确空结果；E1 合同测试 13/13 通过 |
| PCFULL-023 | E2→App 任务定价/手机收益 | P1 | 已修复，待统一部署后运行态复验 | App `/earn` 的 locked teaser 与手机五档日产仍直接读取 `src/mock/tasks.ts`、`src/mock/phone-tiers.ts`，PC 修改 E2 后用户端不会同步 | App 缺少 E2 公开只读合同、严格解析和失败关闭状态，后台“server-canonical”没有跨端读取闭环 | E2 六类任务/QUEUE_SATURATION/五档手机收益 → App `/earn` 任务提示与收益展示 | 后端新增 `/api/config/task-pricing`、`/api/config/phone-tiers` 公开只读端点；App remote 模式启动/前台刷新、严格校验精确六类和五档单调性，失败清空权威快照并显示重试，不再回退 mock；App type-check、22/22 定向测试、后端定向测试通过 |
| PCFULL-024 | E2→B1 资金红线 | P1 | 已修复，待统一部署后运行态复验 | 直接上调 min/maxReward、提高 QUEUE_SATURATION、降低 minVRAM 或从 kill 恢复派发时，即使 B1 覆盖率低于红线也可写入 | `updateE2TaskPricing` 没有按“是否放大资金流出”执行服务端 B1 二次校验 | E2 新派单奖励/资格范围/队列饱和因子 → B1 兑付覆盖率、D 域提现压力、A2/A4 | 服务端新增方向感知红线门禁：上调奖励、提高饱和度、降低显存门槛、恢复派发均在低于红线时返回 `COVERAGE_BELOW_REDLINE`；收紧方向仍可用；定向测试覆盖 99/100 红线场景并通过 |
| PCFULL-025 | D5→App 提现命令 | P0 | 已修复，等待运行态复验 | App 真实提交发送 `targetAddress`，后端 DTO 只接收 `address`，返回 422 | 前后端请求字段合同分叉，且此前只做响应解析测试 | App 提现 → 后端 D5/D2/K3/K4/K5 | App 改发 `address`；新增精确 request-body 契约测试；remote 模式同时从后端 KYC 权威记录读取配对地址/网络并在策略或配对事实不可用时失败关闭 |
| PCFULL-026 | D5→App 跟踪/预览 | P1 | 已修复，等待运行态复验 | remote 提交成功后不写 `latestWithdrawal`，跟踪页仍显示旧单或空态；预览漏算 D5 网络费 | remote 分支只跳转，未把 server canonical response 投影进账号快照；费用函数只有 H1 惩罚费 | App 提现 → 跟踪页、D5 费率 → 用户到账预览 | 新增严格 canonical order 投影和持久化；状态/风控路由/holdUntil 全部取服务端；新增 `/api/withdrawals/policy`，预览使用后端 D5 网络费率/上下限、H1 惩罚率/冷却期及可用网络 |
| PCFULL-027 | D5→A4 | P0 | 已修复，数据库迁移通过 | 正确字段提交仍因 `A4_SCHEMA_PROPERTY_NOT_REGISTERED` 整笔回滚 | 多次迁移各自替换 `withdraw.submitted` 属性，live registry 只剩最后一批 K4 字段 | App 提现 → D2/D4/A2/A4 outbox | 新增幂等迁移并在 D4 费用组件闭环后升级为 revision 276，精确注册当前 28 个属性（含抵扣分配与实际网络费/惩罚费）并删除陈旧属性；live `nexion` 已核对 28/28、required/optional 正确、global revision 不回退 |
| PCFULL-028 | D5→D2 费用证据 | P1 | 已修复，等待运行态复验 | D5 网络费参与真实扣费，但 D2 订单没有保存费率、最小值、最大值和网络费组件，运营无法复盘历史单 | D2 只快照 H1 惩罚费和 NEX 抵扣字段 | D5 配置 → App 提现 → D2 审核详情 | D2 新增四个 NOT NULL snapshot 字段，写入、列表、详情、PC 严格解析和费用不变量校验全部接通；历史单明确回填 0，避免用当前策略伪造历史 |
| PCFULL-029 | D5→A2 拒绝审计 | P2 | 已修复，定向测试通过 | 同一失败请求以相同 Idempotency-Key 重放会重复写 `D5_WITHDRAWAL_LIMITS_REJECTED` | 业务命令幂等，但控制器在命令返回后每次都独立写拒绝审计 | D5 CAS/红线拒绝 → A2 | 为拒绝审计增加独立幂等 scope 和稳定请求摘要；同键同失败只写一次，不同 payload 仍冲突关闭；控制器测试覆盖双重放 |
| PCFULL-030 | D5 PC 确认文案 | P2 | 已修复，PC 构建通过 | 用户尚未点击确认时，确认窗影响文案显示“已生效 · 已记审计” | 把提交完成态文案提前放到确认前 brief | D5 参数确认 → 用户决策 | 改为“提交成功后生效并写入审计”；新增合同测试禁止旧完成态文案回归 |
| PCFULL-031 | H1→D5 展示单源 | P1 | 已修复，112 项 D5 聚焦套件通过，等待统一部署后运行态复验 | D5 展示直接读取 legacy `growth.phase.withdraw_*`，真实提交读取 `GrowthRhythmFacade` 月度快照，页面与执行可能漂移 | H1 仍保留新旧两套读取路径 | H1 节奏 → D5 页面 → App 提现 | D5 和旧 D5 参数读面统一调用 `GrowthRhythmFacade.snapshot()`，可靠性失败即关闭；保持 PC 既有 penalty ratio 合同；“legacy 镜像与 facade 冲突时以 facade 为准”测试已随后端 D5 聚焦套件通过 |
| PCFULL-033 | E3→App 真实置换 | P0 | 已修复，源码与定向验收通过，待统一部署后 App/后端运行态复验 | PC 已能调整 E3 置换规则，但 App 设备页、置换窗和结算仍从 `mock/tradein-config` 与 localStorage 生成设备、折抵和“成功”结果；本地字符串设备 ID 也不能满足真实接口的数值 `sourceDeviceId` | App 没有消费后端已存在的用户设备、E3 config/quote/submit；权威设备投影又缺少实付价和累计已结算产出 | E1 商品 → E3 配置/报价/提交 → E4 订单 → D4 钱包账本 → A2/A4 → App 设备/算力/收益 | App 新增严格失败关闭的 E3 API 与账号隔离 canonical store，设备、激活、quote、submit 改走真实后端，remote 分支删除本地钱包/设备/订单假写；后端设备投影补真实实付价和累计产出。App Vitest 5/5、type-check 通过；后端定向 15/15 在共享 Maven 并发禁令前通过，统一部署后补真实用户端完整回归 |
| PCFULL-034 | E3 阶梯边界 | P1 | 已修复，合同/App 测试通过，待统一后端套件与部署复验 | 管理端和手册明确分档为左闭右开 `[0,cut1)`、`[cut1,cut2)`，后端报价及 B1 风险探针却把 `ratio == cut` 留在上一档 | Java 使用 `<= cut`，与页面、App 规则和签字规格不一致 | E3 调参 → B1 流出判定 → App quote/submit → D4 实付 | 报价、B1 探针和 App 预览统一用 `< cut`；新增精确等于界点的服务测试，25% 正确进入第二档。因 PCFULL-005 禁止并发 Maven，新增 Java 测试交由统一串行套件复验 |
| PCFULL-035 | E3 退役控制项 | P1 | 已修复，PC 合同 5/5 通过，待统一部署后页面复验 | 2026-07-06 权威 FEAT-DEV02 已把抵扣唯一公式收口为“实付价 × 阶梯率”，PC 仍展示“置换活动倍率/弹窗节奏/库存软上限”调整并宣称会影响新报价，实际真实 quote/submit 不消费这些字段 | 旧版 E3 运营控件和手册未随签字规格退役，且被错误纳入页面完整性必需键 | PC E3 调参 → A2/B1 → App 报价，形成“按钮成功但业务无效”的假闭环 | 从现行 E3 可编辑面、手册和必需键删除三组退役项，保留后端历史键兼容读取但不再冒充 FEAT-DEV02 业务杠杆；合同测试同时禁止这些文案/键重新进入当前操作面 |
| PCFULL-036 | E5→A2 数据中心变更 | P1 | 已修复，待统一部署后完整复验 | superadmin 从 PC 会创建 A2 申请，但持有 E5 写权限的调用方可绕过 PC，直接请求数据中心新增、编辑、删除、暂停或恢复接口并立即生效 | 五个服务方法只有对象锁检查，没有像强制激活/解绑一样要求 `A2ReplayContext`，导致 maker/checker 仅由前端约束 | E5 数据中心 → E1 SKU / E4 待履约订单 / A2 审批 / A4 事件 | 五个方法在参数校验后、幂等和业务写入前统一失败关闭为 `409 A2_CONFIRMATION_REQUIRED`，只有 A2 replay 可执行；新增五动作直调回归测试并把既有成功/引用保护测试放入 replay 上下文。修前定向测试已稳定红灯证明绕过，修后源码待统一 Maven 与部署复验 |
| PCFULL-037 | E5 数据中心删除确认 | P2 | 已修复，PC 合同测试通过，待 3002 新构建复验 | 删除确认窗声称“系统不做跨域硬阻断”，并提示会保留 `dc_location` 孤儿引用 | PC 文案停留在旧实现，未同步后端现行设备、待履约订单、SKU 三类引用硬保护和清理语义 | E5 删除决策 → E1 / E4 / 设备库存 | 确认窗改为明确服务端三类引用计数必须全部为 0，删除后同步清理运营状态且不删除设备库存；合同测试禁止旧的“无硬阻断/孤儿引用”文案回归，3/3 通过 |
| PCFULL-038 | E4 订单状态机 | P0 | 已修复源码与 PC 合同，待统一 Maven/部署复验 | 首次用户打开一笔 `PENDING/PENDING_PAYMENT/WAITING_PAYMENT`、无 D1/D4 入账事实的订单，页面仍提供“推进下一态”；接口也可把它直接改成 paid，并可继续在无设备/DC/E5 激活事实时推进 provisioning/activated | E4 人工推进只校验相邻枚举和 CAS，没有把资金、设备分配、E5 激活事实作为前置不变量；前端也无失败关闭 | App/E1 下单 → E4 → D1/D4 结算 → E5 配置激活 → A2/A4 | 后端按三段证据门返回 `ORDER_PAYMENT_CONFIRMATION_REQUIRED`、`ORDER_PROVISIONING_EVIDENCE_REQUIRED`、`ORDER_DEVICE_ACTIVATION_REQUIRED`；PC 同源隐藏非法动作并解释缺失证据。新增三项先红后修测试及 PC 合同；按 PCFULL-005 要求未并发 Maven，交统一串行套件与新部署最终复验 |
| PCFULL-039 | E4→App 订单真值 | P0 | 已修复，App E4 定向测试通过，待统一部署后真实账号复验 | remote 模式普通购买仍在 App 本地扣余额、生成订单/账单/设备并自动跳到 activated；`GET /api/orders` 又把 payment_failed、expired、provisioning_failed、refunded、chargeback 折叠成 cancelled | App 订单 store 仍是 mock 状态机，普通结算未调用现有 `POST /api/orders`，canonical 边界只保留主路径 | App 商城/结算 → E4 订单 → D1/D4 支付 → E5 设备 → App 订单/设备 | App 新增严格订单 API、稳定幂等键和账号隔离 canonical 刷新；remote 只提交服务器生成的 pending 订单，支付入账前不再本地扣款/造账/造设备/自动激活；十种状态逐一保留并禁止客户端推进或取消 server-authoritative 行。Vitest 10/10；E4 改动后 type-check 首次通过，交付前全仓复跑被并发模块 `config-remote.test.ts` 的类型缺口阻断 |
| PCFULL-040 | E4 列表与证据追溯 | P2 | 已修复，PC 合同/type-check 通过，待 3002 新构建复验 | E4 列表没有关键词搜索；订单年龄显示 `6388m`；详情看不到 A2 审计、A4 事件、D4 资金证据入口，首次用户难以定位和复盘订单 | 后端已有关键词参数但 PC 未提供输入；年龄 SQL 直接拼分钟；详情只展示本域字段 | E4 列表/详情 → A2/A4/D4 调查链 | 增加订单号/用户/SKU/设备关键词搜索；年龄改为分钟/小时/天；详情提供审计、事件、资金账本深链。PC 合同 4/4、TypeScript 通过 |
| PCFULL-043 | E6→App 公共配置 | P0 | 已修复源码与 App 定向测试，待统一部署后跨端复验 | 后端真实值为入口开启、H5 系数 `0.6`、满额 `4h`，App remote 模式仍只读本地默认的入口关闭、`0.6`、`2h`，PC 调参不会传播到用户端 | App 没有调用后端已存在的 `/api/config/platform`，配置 store 初始化、前台刷新和异常状态均停留在 mock | E6 PC 调参 → 公共配置 → App 算力入口/显卡映射/在线系数/收益展示 | App 新增严格公共配置协议、启动与前台 60 秒刷新、同请求去重和失败关闭；入口、系数、六档 TOPS/关键词、收益换算与下载配置整份校验，接口失败时隐藏入口并暂停依赖配置的本地收益循环。Vitest 10/10、App 全仓 type-check 通过 |
| PCFULL-044 | E6→B1 资金红线 | P1 | 已修复源码，待统一 Maven/部署复验 | 提高 H5 基础托管系数、缩短连续在线满额时长等放大支出方向，即使 B1 覆盖率低于红线也可进入写入 | E6 只做字段范围校验，没有按 before/after 方向调用服务端覆盖率门禁 | E6 在线系数/收益估算/显卡 TOPS/入口 → App 收益 → B1/D 域兑付压力 | 事务内以有效旧值和候选快照判定方向：开入口、提高 H5/收益/折算/TOPS、降低满额时长或基准 TOPS均在低红线时返回 `COVERAGE_BELOW_REDLINE`；收紧与回滚方向仍可执行；新增 99/100 红线定向测试，交统一 Maven 串行复验 |
| PCFULL-045 | E6 显卡结构校验 | P1 | 已修复源码与 PC 契约，待统一 Maven/部署复验 | 单字段均合法时仍可把 G3 TOPS 改到低于 G2，或让两个档位使用同一识别词，造成用户端无法确定档位；App 严格解析会拒绝整份配置并失败关闭 | 服务端 `validateComputeValue` 只校验当前字段，没有在写入前验证完整 G1–G6 候选快照 | E6 显卡映射 → App/未来 PC 载体型号识别、算力与收益估算 | 单参数和批量写均按固定顺序锁定完整配置快照，强制 G1–G6 TOPS 严格递增、全部非空关键词大小写无关唯一；违反时整笔拒绝且零写入。PC 错误文案和回归契约已补，后端测试交统一 Maven 复验 |
| PCFULL-046 | E6 版本与回滚可发现性 | P2 | 已修复，PC 契约 5/5 通过，待 3002 新构建复验 | 页面只提示写入 A2 审计，没有告诉首次用户如何找到历史值、判定生效版本或执行安全回滚，也未说明 H1 阶段边界 | A2/A4 已保留 before/after 和幂等事实，但 E6 操作面没有把现有能力组织成可执行工作流 | E6 调参 → A2/A4 版本链 → App/H5 刷新、H1 阶段、E1/E2/E3/E5 | 新增“版本记录与回滚”和“A2/A4 不可变版本链”入口，明确复制历史 before 值后重新审批、回滚生成新版本；补充 A2 批准即时生效、App/H5 最迟 60 秒刷新、不随 H1 阶段切换且不追溯历史收益的边界说明 |
| PCFULL-050 | F1 四区操作面/BFF | P0 | 已修复源码与 PC 契约，待统一部署后真实浏览器复验 | 首次用户从侧栏进入 F1 能看到 13 阶和奖励配置，但没有晋升流水、人工等级调整、派发流水/处置；`promotion-log`、`reward-payouts` 经 PC BFF 均返回 404 | 后端端点已存在，PC 严格 BFF 未放行，页面只实现阶梯和奖励模板两区 | F1 阶梯/晋升/奖励/派发 → A2/D4/F5 | BFF 最小放行两类查询、人工覆盖、重发/冲正；页面补齐晋升筛选/快照/审计号、人工调整、派发筛选、D4 标识和状态机动作；PC TypeScript 与 F1 契约 4/4 通过 |
| PCFULL-051 | F1 晋升引擎 | P0 | 已修复源码与单测合同，待统一 Maven/部署复验 | 用户一次事件同时满足多阶门槛时，引擎从当前阶直接跳到最高可达阶，中间等级的权益、流水和奖励被跳过 | 引擎先求最高 `reachableIndex`，再直接把 `newIndex` 设为该值 | 订单/推荐/团队事件 → F1 晋升 → F2/F4 权益 → 奖励派发/D4/A4 | 上升路径改为每个触发事件最多前进一阶；降级仍按永久保护配置执行；V0 多阶达标测试改为只晋升 V1，后续事件逐阶推进 |
| PCFULL-052 | F1 晋升证据 | P1 | 已修复源码与契约，待统一 Maven/部署复验 | 晋升查询只显示原因/操作人，未返回资格快照、触发事件 ID、审计号，人工标识还通过 reason 文本猜测 | Mapper 和规范化层遗漏已有证据列，并以字符串约定代替 `is_manual` 真值 | F1 晋升流水 → A2/A4/F5 调查与用户申诉 | 查询直接返回 `snapshot_json`、`trigger_event_id`、`audit_no`、`is_manual`，PC 提供折叠快照与触发/审计标识；禁止再用 reason 猜人工操作 |
| PCFULL-053 | F1 高风险动作/A2/派发状态机 | P0 | 已修复源码与回归测试，待统一 Maven/部署复验 | 持有 F1 写权限的调用方可绕过 PC 直调人工晋升、重发、冲正；冲正对部分非法状态没有统一失败关闭 | 三个资金/权益高风险动作只有权限和对象锁，没有强制 `A2ReplayContext`；reverse 状态集合未白名单化 | F1 人工等级/奖励派发 → A2 双人复核 → B1/D4/F5 | 直调合法动作统一返回 `409 A2_CONFIRMATION_REQUIRED`，只有 A2 replay 可执行；高操作注册表与 replay 权限映射补齐；重发仅 `REVERSED`，冲正仅 `GRANTED/REISSUED/PENDING_GRANT`，对象锁和幂等保持服务端二次防线 |
| PCFULL-054 | F1→App 等级真值 | P0 | 已修复源码与 App 定向测试，待统一部署后真实账号复验 | App remote 模式仍从 `V_RANKS` 静态表和账号 localStorage 展示当前等级、门槛与进度，PC F1 调整无法传播 | 后端没有 App V-Rank 只读合同，App store 在 remote 模式仍沿用 mock seed 和本地写入 | F1 PC 配置/晋升 → App 等级页/团队页/领导池/购买门 | 后端新增公开 13 阶配置与登录用户等级/业绩快照接口；App 增加严格 13 阶协议、remote 启动/账号重绑刷新、错误失败关闭，禁止 remote 本地改等级/进度；Vitest 2/2、App type-check 通过 |
| PCFULL-055 | F1 阶梯权益可见性 | P1 | 已修复源码与 PC 契约，待统一部署后真实浏览器复验 | PC 阶梯只显示门槛、奖励和人数，看不到服务端已配置的层级深度、同级奖、领导池票权、客户端可见性与头衔 | F1 查询与前端类型丢弃联动权益列，运营无法判断调级对 F2/F4/App 的影响 | F1 等级 → F2 网络深度/同级奖、F4 票权、App 展示 | Mapper、服务规范化、PC client 和阶梯行贯通 `label/unilevelDepth/peerBonusRate/votes/visible`；页面逐阶显示真实权益并增加跨域调用链提示 |
| PCFULL-056 | F2 费率/Partner Status 操作面 | P0 | 已修复源码与 PC 契约，待迁移及 3002 新构建真实浏览器复验 | 首次用户页面把 V0–V12 平级奖数据展示成 “Rate Tier 升档”，Partner 使用 bronze/silver/gold，宣称会派生 Direct 基础费率并受 B1；live L1 已漂移为 9.8% 且仍可编辑 | F2 误复用 `nx_v_rank_config` read model，旧 Partner schema/文案未随现行 PRD 收口；L1 缺少服务端不可变约束和数据纠偏 | F2 L1–L7/Partner → B1/A2/F1/F4/App | 页面改为 Standard/Verified/Premium/Diamond 权益档并明确不改费率；删除假 Rate Tier/深度/peer 等非 F2 当前控件和 Direct+L1 叠加文案；L1 页面固定 10%、服务端拒绝编辑，迁移纠正 live 漂移；初始 trace 已锁定旧页面，定向 TS/PC F 域契约 6/6 通过 |
| PCFULL-057 | F2 结算引擎配置闭环 | P0 | 已修复源码并补回归测试，待统一 Maven/部署后真实结算复验 | PC 调整单层暂停、Influence clamp、depth gate、promo 后提示成功，但真实 Unilevel 结算继续使用旧值；`on` 暂停也不会被识别 | PC 写 `team.ui.F.*`，引擎读取无前缀或另一命名空间；暂停只识别 `true`，promo 倍率完全未参与计算 | F2 PC/A2 配置 → 订单结算 → F5 commission_event → D4 账本 → B1/A4 | 结算统一读取 `team.ui.F.*` canonical keys，暂停兼容 `on/true/1`，非法 depth gate 回退 L4，Influence 与 promo 在实际 USDT/NEX 金额前生效；新增“L1 暂停只停一层”和“clamp×promo=真实金额”单测，按约束未并发跑 Maven |
| PCFULL-058 | F2→App 费率与佣金真值 | P0 | 已修复源码与 App 4/4 定向测试，待统一部署后 App/后端运行态复验 | App remote 模式仍加载本地 `UNILEVEL_USDT/NEX`、Partner 门槛和示例佣金事件；PC 调参/后台结算与用户页面金额、钱包累计可长期分叉 | 后端缺少公共 F2 只读投影；commission store 不区分 mock/remote，三个页面直接 import 静态常量 | F2 PC/结算 → 公共配置 → App unilevel/tree/roster/钱包 → F5/D4 | 新增 `/api/config/commission/rates` 同源只读投影和严格 7 层/总和/L1/Partner/clamp/cooling/promo 协议；remote 初始与失败均清空费率及 demo events，禁止本地造佣金；页面改消费响应式 canonical store，Partner 显示 Premium；Vitest 4/4、三个 SFC script compile 通过 |
| PCFULL-060 | F3 PC 结算操作面/BFF | P0 | 已修复源码，待统一部署后真实浏览器复验 | 首次用户能看到平衡公式、H1 封顶和配置，但没有“执行结算”及补发/冲正入口；未登录 `POST /api/admin/teams/binary/settlements` 在 PC BFF 返回 404 | 后端已有受 `network_f3_write` 保护的 writer，PC 严格路由和客户端没有接通 | F3 PC → 结算服务 → F5 补发/冲正 → D4/A2/A4 | BFF 最小放行 assignments/settlements；页面增加带用户、结算日、原因的确认动作和 F5 深链，客户端携带幂等键并在完成后刷新权威读模型；TypeScript 与 F3 合同 2/2 通过 |
| PCFULL-061 | F3 结算策略执行 | P1 | 已修复源码/迁移并补回归，待统一 Maven/部署复验 | PC 可写 `settlePeriod/residualPolicy/spillover/gvResetCron`，但真实引擎此前只消费阈值、比例、暂停和 H1 封顶；“每次对碰清零”没有可证明的消费游标 | 配置读面与资金执行面分叉，结算表只有月累计已消费量，无法安全表达逐次清零 | F3 调参 → 双轨体量 → F5 佣金 → D4 账本 | 引擎严格锁定全部配置、按日/周/月判定窗口、按周期聚合封顶并确定性自动安置；每月清零按当月体量扣当月已消费量，转结按历史体量扣历史已消费量，每次对碰清零使用不可变 `from/through volume id` 游标并以插入 CAS 互斥消费，同一事实重放、并发竞争和新增体量续结均不会重复发钱 |
| PCFULL-062 | F3→A4 佣金事件 | P1 | 已修复源码和迁移，待统一 Maven/部署复验 | F3 成功结算写 commission event、D4 和 A2，但没有同事务 `commission.paid` outbox | F3 钱链完成后遗漏跨域事件边界，A4/下游无法可靠感知 | F3 → A4 → App/F5/财务消费者 | 成功结算同事务发布 `commission.paid`，载荷含用户、类型、币种、金额、较小轨 GV、比例、封顶标记和 commissionEventId；重复结算不重复发布；revision 277 schema/producer/consumer 迁移已应用本地 live DB |
| PCFULL-063 | F3 首次用户可理解性/定位 | P2 | 已修复源码，待 3002 新构建复验 | 页面暴露“左轨/右轨”、内部字段名 `gvResetCron`，结算列表没有状态或用户群筛选，也没有结算日与 owner ID 投影 | 旧技术字段直接进入 UI，读模型缺少运营定位字段 | F3 列表 → 用户申诉/阻塞调查 → F5/A2 | 统一改为 Track A/B 和“归零时间”，增加状态/用户群筛选；后端返回 ownerUserId、settlementDate、cohort，移除数据库表名展示文案 |
| PCFULL-064 | F3→App 双轨真值 | P0 | 已修复源码，App 114/114 与 type-check 通过，待统一部署真实账号复验 | App `src/lib/binary-settlement.ts` 明确是两端各自 mock；双轨页、团队页和佣金 store 从本地 network/commission seed 计算体量、匹配额、周期和事件 | 后端没有 App F3 权威只读合同，App remote 模式仍未绑定真实双轨结算与佣金事实 | PC F3 配置/结算 → App 双轨页/团队页/佣金页/钱包 | 新增仅返回当前登录用户的 `/api/team/binary` 权威投影；App remote 在启动、前台和账号切换时刷新真实轨道、体量、配置、阻断原因及佣金事件，协议失败或切换账号时清空旧快照并失败关闭，禁止以本地 binary/network/commission seed 回退 |
| PCFULL-065 | F3 缺省配置与前端假值 | P1 | 已修复源码/数据，待统一部署复验 | live DB 只注册 paused；读取返回空值时 PC 静默显示 10%、$1,000、$5,000、每月清零等前端假默认，真实 matchRate 已是 13% | F3 migration 缺键，客户端 normalize 用展示 fallback 掩盖服务端缺失 | F3 PC 配置 → 结算引擎/H1/B1 → App | 新迁移幂等补齐 threshold/matchRate/spillover/period/residual/GV reset（不覆盖现有值）；客户端删除资金参数假默认；H1 日封顶由后端权威 rhythm 快照生成标签；live threshold 已从旧验收污染值 0.01 精确恢复 1000 |
| PCFULL-066 | F4 PC 结算操作面/BFF | P0 | 已修复源码，待统一部署后真实浏览器复验 | 首次用户从侧栏进入 F4 只能看到池/配额/大使/榜/票权配置，没有提前结算动作；未登录 `POST /api/admin/teams/leadership-pool/settle` 在 PC BFF 返回 404 而不是认证失败 | PC 严格 BFF 只放行 leadership-pool 单段 GET，F4 页面没有把高风险结算组织成 A2 动作 | F4 PC → A2 → 领导池结算 → F5/D4/A4 | BFF 最小放行 settle 路由；页面增加“提前结算本周池”，只提交 `f4_pool_settle` A2 提案，不直接写资金；TypeScript 与 F4/F 域合同 9/9 通过 |
| PCFULL-067 | F4 奖池比例单源真值 | P0 | 已修复源码与回归测试，待统一 Maven/部署复验 | live PC 显示 `F.pool.ratio=30`，真实结算引擎却读取不存在的 `F.pool.injectRate` 并静默回退 5% | 配置写面与资金执行面使用不同 key，且百分数/小数解析合同缺失 | F4 PC/A2 配置 → 周 GMV 注入 → F5/D4 → B1 | 引擎统一读取 `team.ui.F.pool.ratio`，兼容 `5`、`5%`、`0.05` 并严格限制 0–100%；新增 30% 实际派发回归，禁止旧 key 回归 |
| PCFULL-068 | F4 资格、封顶与冷却 | P0 | 已修复源码与回归合同，待统一 Maven/部署复验 | 页面可修改 `unlockVRank` 和 monthly cap，但真实引擎始终硬编码 V3+、不消费 cap；领导奖又错误继承 F5 30 天冷却 | F4 展示配置没有进入结算策略，公共 coolingDays 被错误套用到无独立冷却的领导奖 | F1 V-Rank/票权 → F4 资格与池额 → F5 状态/D4 | 引擎动态读取解锁等级和月度 cap，cap 按本月已派领导奖扣减；领导奖直接 `UNLOCKED`、coolingDays=0，并按成功台账落 D4 |
| PCFULL-069 | F4 并发幂等/A2/A4 | P0 | 已修复源码与迁移，待统一 Maven/迁移/部署复验 | 同周采用 count-before-insert，两个请求可同时通过并双发；直连写端点可绕过 A2；成功只写佣金/台账，没有强制审计和 `commission.paid` | 缺少数据库周级 CAS，对 maker/checker、审计和 outbox 只靠调用方约定 | F4 结算 → A2/A4/F5/D4/下游佣金消费者 | 新增 `nx_team_f4_settlement_mutex` 唯一周键并 `FOR UPDATE`；直调失败关闭为 `A2_CONFIRMATION_REQUIRED`，A2 replay 权限精确映射 `network_f4_pool_fund`；结算同事务强制审计并发布 schema 合法的 `commission.paid` |
| PCFULL-070 | F4 分配原子性/舍入 | P1 | 已修复源码与合同，待统一 Maven/部署复验 | 单个 commission_event 插入失败时引擎只记录 warning 后继续，形成部分派发；逐人向下舍入会留下无法追踪的池内尾差 | 错误被吞掉，分配算法没有闭合到池总额 | F4 池总额 → 逐用户 F5 事件 → D4/A4 | 任一事件失败立即抛错回滚整周；末位合格用户承接 6 位精度尾差，并在提交前断言已分配总额严格等于本周池额 |
| PCFULL-071 | F4 读模型/首次用户调用链 | P1 | 已修复源码与 PC 合同，待统一部署后复验 | PC 的“周 GMV”来自团队成员累计 volume，结算却来自当周已支付订单；结算/派发窗口为空，页面又声称池/配额/大使/榜由一条总线一次性派发 | 读模型与结算引擎数据源分裂，内部实现推断被写成用户承诺 | F2/F3/F4/F5/H1/App 团队收益 → 运营判断 | 周 GMV 改为与引擎相同的 ISO 周已支付订单；明确周日 23:59 UTC 快照、周一 00:00 UTC 派发；页面按真实边界说明领导池写 F5/D4/A4、补发/冲正/冻结走 F5，配额/大使/榜独立生效 |
| PCFULL-080 | G1 在锁仓位 KPI | P2 | 已修复源码与合同，待统一部署后真实浏览器复验 | live 数据库只有 1 笔 `EARLY_WITHDRAWN` 终态仓位，页面“在锁 position 数”却显示 1，pending/active/mature 均为 0 | G1 概览把 claimed、early-withdrawn、slashed、refunded 终态计入“在锁”总数 | G1 用户仓位 → PC 概览/KPI → 运营判断 | `positionCount` 收口为 pending+active+mature；PC 合同 6/6 覆盖终态排除。部署后当前夹具应显示在锁 0、提前赎回 1 |
| PCFULL-081 | G1→App 质押真值 | P0 | 已修复源码，App 定向测试/type-check 通过，待统一 H5/后端部署复验 | 当前真实 App `D:\workspace\NX1.0` 仍硬编码 APY/最小额/罚款并从本地 seed/localStorage 造仓位，开仓/到期/领取/提前赎回直接修改本地钱包和账单；旧验收合同还误指向废弃 `nexion-frontend-uniapp` | App 未消费后端已有的公共质押池和登录用户质押合同，验收路径又路由到错误仓库，形成跨端假绿 | G1 PC 调参/停售/kill → App 开仓/仓位/领取/提前赎回 → D3/B1/D4/A2/A4 | 合同改指向 `NX1.0`；新增严格 G1 API 与账号隔离 canonical store；App 只读 `/api/config/staking/pools`、`/api/stakes`，三类写命令携带稳定幂等键，协议失败清空并失败关闭，删除本地状态推进、钱包/账单假写；Vitest 5/5、`vue-tsc --noEmit` 通过 |
| PCFULL-082 | G1→B1 方向感知确认 | P1 | 已修复源码与 PC 合同/type-check，待 3002 新构建复验 | live B1 覆盖率 0、红线 100 时，G1 调整 APY/罚款/最小额的确认窗没有使用真实覆盖率，也不能区分放大与收紧方向；同值仍可提交 | G1 没把 coverage 传入共享确认窗，确认窗只有静态 amplifies，无法按 before/after 镜像后端门禁 | G1 APY/罚款/最小额/恢复开售 → B1/D3/D4 | 共享编辑规格增加 increase/decrease 方向；G1 传真实 coverage，APY 上调、罚款下调、最小额下调与恢复开售在低红线时禁用，收紧方向保持可用，同值禁止提交；后端仍为最终裁决 |
| PCFULL-083 | G7→App 回购入口误接 G1 | P1 | 已完成永久修复，G7 真实 API/DB 22/22 通过 | App `wallet-repurchase.vue` 先本地扣余额/造账，再调用 G1 90 天质押命令冒充回购，失败时可出现跨域错账和产品串线 | G7 页面复用了旧 G1 mock store，而后端实际已有独立 `AppRepurchaseService` | G7 回购 → G1 质押 → App 钱包/账单 → D4/A2/A4 | G7 owner 已接管并完成 PCFULL-100：App 只调用 `/api/repurchase/orders*`，真实原子订单、幂等/CAS、钱包/账本、G4 券、H1 快照、A2/A4 与清理闭环全部通过 |
| PCFULL-084 | G2→App 真实兑换 | P0 | 已修复源码、App 定向测试与类型检查通过，待统一部署后真实账号复验 | App `wallet-exchange.vue` 使用本地抖动报价、localStorage caps/KYC/队列、`setTimeout` 本地扣加余额和客户端生成兑换单/账单，PC 调参、G3 报价、C4/K5 风控与真实钱包均无法传播 | 已存在的 `/api/config/exchange/caps`、`GET/POST /api/exchange`、取消接口没有接入 App；生产与 mock 分支未隔离 | G3/G2/C4/K5/J2 → App 兑换 → 钱包/D4/D3/A4 | 新增严格失败关闭的 Exchange API；remote 模式只读服务端报价、caps、费率、KYC、余额、计数、订单与队列，swap/cancel 携稳定幂等键；本地扣款、造单、计数与队列仅保留在显式 mock 分支。Vitest 4/4、`vue-tsc --noEmit` 通过 |
| PCFULL-085 | G2 手续费资金去向 | P1 | 已修复源码与数据库迁移，待统一 8110 部署后真实成交复验 | 服务端计算并从用户兑换净额扣除手续费，事件也带 `feeUsdt`，但 30% NEX 回购销毁池和 70% D1 `fee_buffer` 均无真实账户、流水或原子入账 | 原实现只有“fee allocated server-side”备注，没有任何分配调用或存储 | App/队列成交 → G2 手续费 → G3 回购销毁池 + D1 fee_buffer → B1/L3/A4 | 新增两账户锁、CAS、双流水和不可变 `nx_exchange_fee_allocation`；即时成交与队列成交同事务按 30%/70% 分配，任一账户/流水冲突整笔回滚；迁移已应用本地 live DB，合同 3/3 通过 |
| PCFULL-086 | G2 排队取消资金文案 | P2 | 已修复，PC TypeScript 与浏览器复验通过 | PC 取消排队单确认窗写“退回用户余额”，地域说明写“钱退回”，但真实队列从未扣余额 | UI 把“未单方面锁定资金”的产品原则误写成退款动作 | G2 排队/取消 → 用户余额/D4/A2 | 改为“排队阶段未扣余额；取消只终止后续成交”，避免运营误以为会产生退款账本；PC G2 owner/surface Playwright 均通过 |
| PCFULL-090 | G3 曲线编辑/红线隔离 | P0 | 已修复源码与 PC 浏览器读面，待统一 8110 部署后 Murphy 写探针复验 | live 首次用户从表格只改非当前 D1 波动 2→2.01，当前应急价却从 0.12 被覆盖为当前 D3 帧 0.124；当时 B1 覆盖率 0/红线 100，仍绕过红线抬价 | `updateWeeklyCurve` 每次保存任意帧都无条件 `applyFrame(activeFrame)`，排程参数保存与当前价格发布耦合；自动/手工推进抬价也缺服务端红线二次门禁 | G3 曲线/推进 → B1 → G2/G7/App/D4 | 曲线保存只改 weekly curve，不再改现价/active frame；推进在抬价方向强制 B1 门禁；G3 高风险审计改为 fail-closed。PC 明确“到排程推进时生效”，首次用户 Playwright 1/1、PC type-check、跨仓合同 3/3 通过；按全局禁令未并发 Maven/重启 |
| PCFULL-091 | G3 枚举/零值合同 | P1 | 已修复源码与定向合同，待统一 8110 部署复验 | live 服务端对非法 `oracle=G3_INVALID_ORACLE`、`pin=D9`、`loop=G3_INVALID_LOOP` 全部返回 200 并持久化；同时 UI 允许波动/偏离阈值为 0，服务端却拒绝 0 | 字符串 override 只校验长度、控制项只校验 schedule；数值 override 与周曲线错误地统一要求 `>0` | G3 控制/override → 调度器/喂价/告警/A2/A4 | pin 收口为未钉住或 D1–D7，loop 收口为循环或停在末值，oracle 收口为内部做市/外部喂价；波动与偏离允许 0，价格/成本仍要求正数；新增 Java 回归源码与 PC 跨仓合同 |
| PCFULL-092 | G3 并发覆盖/CAS | P0 | 已修复源码与 PC 合同，待统一 8110 部署并发复验 | live 两个持不同幂等键的 loop 更新同时返回 200，后写覆盖先写；幂等只防同键重放，不能防陈旧页面覆盖 | 请求无 expected state，配置写前未在同一事务锁定并比较权威值 | G3 PC/多运营员 → nx_config_item → A2/A4/调度器 | value 命令增加 `expectedValue`、曲线增加 `expectedFrames`；服务端事务内 `SELECT ... FOR UPDATE` 比较，陈旧请求 409 `G3_STATE_CONFLICT`，同值拒绝；PC 每次提交携当前基线，最终并发探针锁定“一成功一冲突” |
| PCFULL-093 | G3 确认窗方向/生效语义 | P1 | 已修复，PC TypeScript、合同与首次用户浏览器通过 | live B1 覆盖率 0/100 时，曲线抬价、现价抬价和恢复引擎确认窗未完整传真实 coverage/方向；曲线保存又宣称“立即生效” | G3 未复用共享 `amplifiesWhen`、`coverage`、`disallowCurrent`，且 UI 文案与排程边界冲突 | G3 操作确认 → B1/运营决策 → G2/G7/App | 目标价、pump、现价采用 increase 方向门禁并传真实 B1 快照；恢复引擎传 coverage；全部编辑禁止同值；曲线改为“保存排程参数、对应推进时生效” |
| PCFULL-094 | G3→App 行情/PnL 真值 | P0 | 已修复源码，App Vitest 3/3 与 type-check 通过，待 H5 运行态复验 | `NX1.0` 的市场页从 `TOKENS` 静态 NEX=0.171 展示，钱包每 3 秒 `Math.random()` 客户端抖价，成本基准硬编码 0.085；G3 live 现价 0.12 无法传播 | App 行情 store 仍是原型 mock，市场页绕过 store，未消费后端已存在 `/api/config/market/nex` 权威快照 | G3 → App 市场/钱包/PnL → G2 兑换 | 新增严格 G3 market API 解析；remote 模式只读 server-canonical 现价、成本基准、7 帧及 24h 历史，协议/网络失败清空并失败关闭；市场 hero/NEX 行、钱包估值/PnL 全改读 store，随机抖价仅保留显式 mock |
| PCFULL-095 | G4→App 资格门/预售/认购真值 | P0 | 已修复源码、PC 完整浏览器复验通过，待统一 8110 部署后 App 真实账号复验 | PC “资格门与预售”十项配置保存成功并提示“即时生效”，但 `AppGenesisService` 购买不读取任何 `market.genesis.ops.*`；`NX1.0` 仍用 localStorage、固定 847 已售、静态资格和客户端扣款/铸造/挂单/承接 | 后台配置只有写模型，用户购买边界没有策略读取与校验；App 没有 Genesis API/client canonical store，注释中的 `/api/genesis/eligibility` 也不存在 | G4 PC → KYC/账户龄/预售窗口/单人上限 → App 一级认购/二级承接 → 钱包/D4/A2/A4 | 后端新增公开 sale 与登录用户 eligibility 投影及 `/api/genesis/eligibility`，在扣款前强制 KYC、账户龄、J2 地域、预售窗口和一级/二级合并持仓上限，预售期间使用权威预售价；App remote 模式接通 state/account/purchase/list/cancel/buy，禁用本地销售 tick、钱包假扣和本地铸造。App Vitest 3/3、全量 type-check 通过，PC 闭环合同 4/4、真实浏览器 2/2 通过 |
| PCFULL-096 | G4→D4/B1/BI 排放台账口径 | P1 | 已修复源码与跨仓合同，待统一 8110 部署后真实排放批次复验 | 新批次写 `biz_type=GENESIS_EMISSION`，G4 应计与 BI 汇总仍只统计旧 `GENESIS_DIVIDEND`，导致派发成功后概览/报表少算 | “分红→排放”术语升级只改了写端，读端 SQL 和 BI 聚合没有兼容新旧历史事实 | G4 排放批次 → D4 钱包台账 → G4 概览/B1/BI | G4 应计、最近批次与重复批次查询同时兼容 `GENESIS_DIVIDEND/GENESIS_EMISSION`；BI 汇总累加两类；用户事件统一为 `genesis.emission_paid`；PC 跨仓合同 4/4 覆盖 |
| PCFULL-097 | G4 排放批次自然键幂等 | P1 | 已修复源码与单元/跨仓合同，待统一 8110 部署后并发重放复验 | 相同 batchNo 换一个 Idempotency-Key 重跑时，唯一批次行和钱包派发虽不会重复，但服务仍会再次发布批次 outbox 并再写成功审计 | 仅用客户端幂等键包裹命令，未在事务中读取并短路已完成的业务自然键 | G4 批次重跑 → D4 钱包/台账 → A2/A4/通知消费者 | 新增批次行 `FOR UPDATE`；`COMPLETED` 自然键直接返回 `replayed=true`，不再锁持仓、写钱包/台账、发布事件或重复成功审计；处理中批次仍可从 PENDING/FAILED 安全续跑；新增 Java 回归源码与 PC 跨仓合同 |
| PCFULL-098 | G4 资格/预售配置并发 CAS | P1 | 已修复源码与跨仓合同，待统一 8110 部署后并发探针复验 | 两个运营员从同一旧页面修改同一配置，虽然写入被串行化，后提交仍会静默覆盖先提交，幂等键不能识别陈旧页面 | 请求不携带 expected value；服务端只拿全局互斥锁，没有在锁内比较用户看到的基线 | G4 PC 多运营员 → `market.genesis.ops.*` → App 资格/预售 | PC 写请求携当前值；服务端在事务/互斥锁内用 `activeValueForUpdate` 比较，缺 expected 返回 428、陈旧值返回 `409 G4_CONFIG_STATE_CONFLICT`、同值返回 `409 G4_CONFIG_NO_CHANGES` |
| PCFULL-100 | G7→App 专属回购命令 | P1 | 已修复，App 6/6 + type-check + 真实 API/DB 22/22 通过 | G1 临时处置把 G7 页面永久锁死，普通用户无法发起后端已经支持的回购、领取或提前赎回 | App 缺少 G7 专属严格协议层与账号隔离 store，入口仍依赖 G1 handoff | G7 PC 配置 → App 回购 → 钱包/D4/G4/H1/A2/A4 | 新增严格 `repurchase-api`、账号隔离 Pinia store 和可见订单状态机；所有写命令只走 `/api/repurchase/orders*` 且复用稳定幂等键，协议失败清空并失败关闭；真实 100/200 USDT 开单、重放/冲突、早赎/到期领取及完整清理 22/22 通过 |
| PCFULL-101 | I5→G7 App 风险披露 | P1 | 已修复，App 6/6 + type-check，真实 I5 双门后 G7 主链通过 | G7 后端会返回 `RISK_DISCLOSURE_ACK_REQUIRED`，但 App 披露页只写 localStorage，本地“已同意”不能解除服务端用户门禁 | I5 页面未读取服务端法域/版本/七章/阅读凭证，也未提交服务端确认 | I5 法域/版本/阅读时长 → G7/G1/D5 用户资金动作 | 新增严格 I5 API；页面按服务端法域版本和多语言章节渲染，同时要求滚到底、服务端最短阅读计时和勾选，提交原始 read token 后才记录本地投影；真实用户完成 I5 后 G7 22/22 通过 |
| PCFULL-102 | G7 PC 参数同值/零值确认 | P2 | 已修复源码与合同 9/9、TypeScript 通过，待 3002 下一统一构建加载新 UI | live 生产构建允许 APY 同值提交；`if (!value)` 又会吞掉合法的 0 APY/0 罚款/0 张券 | G7 未使用共享确认窗的 `disallowCurrent` 和方向化 B1 规格，并把值存在性误写为 truthy 判断 | G7 调参 → B1 → A2/A4 → 新回购单/G4 | 全部参数禁止同值；APY/培育/券上调与罚款下调按方向镜像 B1；零值只按空值判断而不再被吞掉。当前 3002 是任务前 `next start` 旧构建且按全局禁令未重启，源码/合同已闭环，下一统一构建需补 live 按钮禁用截图 |
| PCFULL-105 | H1 节奏位置/旋钮确认语义 | P2 | 已修复源码与定向合同，待 3002 下一统一构建加载新 UI | “当前节奏位置”多字段弹窗会把未修改的当前月也重复提交，产生多余 H1 审计/outbox；所有旋钮弹窗又一律显示“会增加资金流出”，即使实际是在提高冷却/罚金或降低奖励 | 表单用 truthy 判断代替 before/after 比较；旋钮确认窗把动态方向硬编码为 `amplifies=true` | H1 当前月/进度/8 dial → B1/A2/A4 → D5/F3/G7/App | 只提交确有变化的当前月/进度，整表同值明确提示“未变化，本次未提交”；数值旋钮按倍率/封顶上调、冷却/罚金下调动态识别流出方向，合规审查按当前开启态识别关闭风险；后端 B1 仍为最终裁决 |
| PCFULL-110 | H2→App 试用真值 | P0 | 已修复源码、App 3/3 与 type-check 通过，待统一 8110/H5 部署后完整复验 | `NX1.0` 的资格、开始、倒计时、影子收益、提前转正、取消、延长和失败冷却全部由 localStorage、客户端时间与 `Math.random()` 推进；remote 模式甚至停止试用 poll，已有 `/api/trial/*` 从未被调用；页面还要求绑定仅存在客户端的假卡，而真实结算使用 Nexion USDT 钱包 | App H2 store/page 没有真实 API 层，后端又没有在 App 关闭时推进到期结算，UI 编排仍直接修改本地钱包、账单和设备 | H2 PC 参数 → App 试用 → E 设备/D4 钱包/A2/A4 | 新增严格失败关闭的 `trial-api`；remote 模式按账号读取后端资格/状态/安全策略，所有命令使用稳定幂等键，只接受服务端状态、结算、冷却和设备结果；开始前明确披露到期从 Nexion USDT 钱包自动购买，不再把本地卡冒充真实担保；新增后端到期扫描，即使 App 关闭仍按 grace/extension 边界自动扣款或终止；本地 RNG/钱包/账单/设备假写仅保留显式 mock；App 定向 Vitest 3/3、`vue-tsc --noEmit` 通过 |
| PCFULL-111 | H2 Model A 抵扣上限 | P1 | 已修复源码、PC 合同 4/4 与 TypeScript 通过，待统一部署后真实浏览器复验 | 页面真实返回 19 个参数且可见“试用收益抵扣上限”，点击调整 `trialOffsetCapUSD` 固定返回 422；顶部“抵扣上限”又错误读取优惠上限 `discountCapUSD` | 后端允许键、数值归一化和类型集合漏了现行参数，PC 摘要绑定了相邻但语义不同的字段 | H2 参数 → App 新试用快照/转正结算 → D4 | 后端补齐 `trialOffsetCapUSD` 的白名单、金额校验和 NUMBER 类型；PC 摘要改读同一权威字段；会话列从误导性的“卡 token”改为真实“试用业务号”；定向合同与 TypeScript 通过，初始 live 422 trace 已留存 |
| PCFULL-112 | H2 状态/影子收益时间投影 | P1 | 已修复源码与回归合同，待统一 Maven/部署后真实数据复验 | live `nx_trial_claim` 存在已过 `expires_at` 仍显示 active 的记录；取消/转正会话又按 `daily × duration` 展示满额影子收益，与已结算列不一致 | H2 读模型直接 LOWER(status)，没有时间派生态；奖励展示使用静态理论满额而非 elapsed/settled 事实；延长截止还从试用截止直接加天数，跳过 grace | App state → PC 会话监控 → H1/E/D4/A4 | active/claimed 超时即时投影 grace；进行中按真实 elapsed 且封顶试用天数，终态读取 settled 列；stats 同步派生；App 状态返回安全策略和三段边界；延长截止改为 grace+extension，试用收益不在 grace/extension 继续累积 |
| PCFULL-113 | H2 失败关闭/终态可操作性 | P1 | 已修复源码、PC 合同通过，待统一部署后故障注入复验 | H2 GET 注入 500 后只有错误文本没有重试；`FAILED` 不在前后端终态集合，页面仍暴露强制取消/扣款；余额不足返回失败但 claim 保持 active | 错误态缺恢复动作，终态枚举遗漏，扣款失败没有 CAS 落失败状态和冷却 | H2 PC/App → D4 余额 → K2 冷却/A2/A4 | 错误卡增加可见重试；前后端统一 failed 终态；余额不足用版本 CAS 原子写 `FAILED`、冻结真实收益并开始 cooldown，App 收到失败后立即回读权威 cooldown/结算快照，禁止后续管理员介入 |
| PCFULL-114 | H2 管理员强制处置跨域闭环 | P0 | 已修复源码与合同，待统一 Maven/部署后 A2 执行态复验 | PC 强制取消只裸改 status，没有 closedAt/cooldown/A4；强制扣款私自扣一笔钱包和一笔账本后裸改 redeemed，没有真实折扣/收益抵扣、余额奖励、设备创建和用户事件 | 管理端在 `OpsGrowthService` 复制了简化结算，绕过 H2 单一生命周期服务 | H2/A2 → D4 钱包/账本 → E 用户设备 → A4 用户事件/B1 覆盖率 | 管理员处置在对象锁/J1/终态检查后统一委托 `AppTrialLifecycleService`，复用同一事务、幂等、CAS、B1、D4、多资产奖励、设备创建、冷却、强审计和 A4 outbox；管理审计仅补操作员上下文，不再另造资金事实 |
| PCFULL-115 | H3→App 任务真值与派奖 | P0 | 已修复源码、App 7/7 与 type-check 通过，待统一 8110/H5 部署后真实账号复验 | App remote 模式仍从本地静态任务和 localStorage 读取完成态；访问页面、分享或绑卡可在客户端直接标完成并向本地钱包加 NEX/USDT，PC 的任务奖励与服务端完成事实无法传播 | H3 缺少登录用户任务状态接口，App 沿用原型 `markComplete + credit` 编排，真实领取接口未进入任务页 | E/F/I/A4 canonical producer → H3 completion fact → App 任务/领取 → H1 倍率 → D4/A2/A4 | 新增登录态 `GET /api/quests/state`，只投影 `nx_mission + nx_user_mission + H1`；App remote 首页/任务页只展示服务端状态并通过既有原子 claim 领取，本地完成与本地加款全部禁用；协议/网络失败清空并失败关闭，账号切换丢弃旧响应 |
| PCFULL-116 | H3 PC 配置键、CAS 与事务闭环 | P1 | 已修复源码、PC 合同 3/3 与 TypeScript 通过，待统一部署后写路径复验 | 首日任务仅用脆弱的“显示序号+1”定位；周任务、月度奖励和本周转化卡按钮发送后端不识别的键而固定 422；配置无 CAS/全局互斥，同值可提交，审计失败可能留下已写业务值 | PC 键名与后端允许键分叉，后端更新按数据库自增 id 假设顺序，写配置与强审计未置于同一事务 | H3 PC 多运营员 → nx_mission/nx_monthly_challenge/nx_growth_promo_banner → B1/A2 → App | 任务与月度挑战改用稳定业务 code；所有 H3 配置携 expected value，在 durable `H3_CONFIG` 行锁和事务内比较、写入、强审计；陈旧页/同值失败关闭；奖励放大和促销上架继续由 B1 二次裁决 |
| PCFULL-117 | H3 读模型、暂停促销与错误恢复 | P1 | 已修复源码与定向合同，待统一部署后运行态复验 | PC 首日窗口/三段奖励/周冠军奖励从无关任务行临时拼接，保存配置后页面语义不变；暂停的转化卡被读接口过滤，无法重新上架；GET 失败只有错误文本没有重试 | 写模型和读模型没有使用同一权威字段，促销读取错误地只查 active，错误态缺恢复动作 | H3 配置 → PC 刷新/重登 → App 首页促销 → H1/B1 | 三个摘要直接读取同一 `growth.quest.*` 配置；PC/用户端均读取暂停态但仅 active 对用户展示；缺省迁移只种一条 paused 安全实例；PC 错误卡增加“重试加载”，App remote 促销目标未知时只进入商城列表，不伪造商品详情 |
| PCFULL-118 | H4→App 活动真值/抽奖派奖 | P0 | 已修复源码、App 6/6 与 type-check 通过，待统一 8110/H5 部署后真实账号复验 | App remote 模式仍读取本地静态活动、localStorage 参与/领取态并用客户端随机数抽奖，页面还会直接改本地钱包和账单；PC 创建/修改的活动无法传播 | H4 没有登录用户活动状态投影；App remote 与 mock 分支未隔离，真实 join/claim/spin 接口未成为唯一入口 | H4 PC → App 活动/转盘 → D4 钱包/账本 → A2/A4 | 新增严格失败关闭的 `GET /api/events` 和 App `events-api`；remote 只接受服务端活动、join/claim/spin 结果，未知结果复用同一幂等键并回读对账，本地 RNG/钱包/账单只保留显式 mock |
| PCFULL-119 | H4 活动奖励/状态并发与资金门禁 | P1 | 已修复源码与 PC 合同 3/3 通过，待统一部署后写路径复验 | PC 活动奖励、状态和主推写入不携当前值；奖励只改展示名、不改真实资产类型；进行中可回退待开始；NEX 切 USDT 或激活活动可绕过 B1 | 活动写契约无 CAS/全局互斥，状态机与资金流出方向不完整，claim 仅支持 NEX/BADGE | H4 多运营员 → B1 → App claim → D4/A2/A4 | 所有活动写携 `expectedValue`，后端在 `H4_EVENT` durable mutex 事务内比较并拒绝同值/陈旧页；奖励同时更新 asset/amount 并支持 USDT 原子入账；状态单向推进，NEX→USDT、金额上调和活动激活均过 B1 |
| PCFULL-120 | H4 活动 CMS 跨端字段 | P1 | 已修复源码与增量迁移，待统一部署后 PC→App 传播复验 | PC 新建活动缺少开始/结束时刻、地域、App 跳转地址和真实目标值，App 无法按同一活动配置展示与执行 | `nx_event_quest` 与 PC 请求/读模型未提供完整 CMS 合同，用户端又无 canonical 投影 | H4 PC 活动 CMS → App 活动列表/跳转/进度/时窗 | 请求、SQL、PC 表单和 App 严格协议补齐 UTC 时窗、geo、受限 `/pages/*` href、target/trackable；新增 `cta_href` 幂等迁移，重复 code/多主推/非法跳转均失败关闭 |
| PCFULL-121 | H4 转盘奖池治理真值/CAS | P1 | 已修复源码与 PC 合同 3/3 通过，待统一部署后并发与派奖探针复验 | 奖池档位 PC 只改显示名/kind，实际派奖仍读旧 `reward_amount/voucher_id/daily_stock`；概率/档位并发可静默覆盖；护栏编辑误走 H3 配置，而运行时读取另一张表；cap 文本可编辑但不参与裁决 | 管理读写合同与 `AppGrowthWheelService` 执行字段分叉，缺少奖池签名 CAS 和专属 H4 护栏端点 | H4 PC → 服务端安全 RNG/库存/预算/B1 → D4/A2/A4 | PC/后端对档位真实 amount/voucher/stock 全量读写；创建/编辑/删除/批量概率使用包含所有执行字段的 pool signature、事务 mutex、同值拒绝；budget/kill 写真实 guard 表并 CAS，历史 cap 改只读且实际上限统一由 dailyStock 执行 |
| PCFULL-122 | H5 签到奖励资产真值 | P0 | 已修复源码、迁移与定向合同，待统一 Maven/8110/H5 部署复验 | 产品更新日志、PC 与 App 均宣称签到发 NEX，但 live 后端 `baseline=1`，签到仍写 `nx_points_ledger`，Day-3/Day-7 仍为 `POINTS`；A4 `daily.checkin` 仍登记 `base_points/reward_points` | “积分下线→NEX 接管”只更新了展示与本地原型，服务端结算、持久化配置和事件契约未同步迁移 | H5 PC 规则 → App 签到/里程碑 → B1/D4 钱包与账本 → A2/A4 | 签到统一按越南业务时区读取服务端规则并原子写 NEX 钱包/D4 账本；基础默认迁至 2 NEX，Day-3/7 迁为 NEX；A4 契约改为 `base_nex/reward_nex/streak_bonus_nex`，旧 POINTS 类型失败关闭 |
| PCFULL-123 | H5→App 服务端权威与未知结果 | P0 | 已修复源码，App 7/7 与全量 type-check 通过，待统一 H5 部署真实用户复验 | `NX1.0` remote 模式仍用 localStorage、客户端日期和 `Math.random()` 推进连签并直接修改本地钱包/账单；里程碑、Saver、Power-Up、榜单均为静态/本地状态，网络超时可能重试发两次 | H5 缺严格 App API 与账号隔离 canonical store，remote/mock 分支未隔离，命令没有稳定幂等键和回读对账 | H5 PC → App 签到/领取/Saver/Power-Up/榜单 → D4/A4/刷新重登 | 新增严格 `/api/points/*` 协议；remote 只接受服务端日期、连签、里程碑、Power-Up 与脱敏榜单，签到/领取/Saver/激活复用稳定幂等键，超时先回读对账；所有本地铸币、RNG、钱包/账单假写只保留显式 mock |
| PCFULL-124 | H5 PC 稳定标识与并发 CAS | P1 | 已修复源码、PC 合同 3/3 与 TypeScript 通过，待统一部署双运营员探针复验 | 连签里程碑和 Power-Up 用 `id-1` 展示、写入时再 `+1`，数据迁移/删除后会错改；规则、奖励和触发天数均无 expected value，两个运营员可陈旧覆盖；Power-Up 两字段连续请求可能出现部分成功 | UI 序号被当作业务标识，幂等键只防同键重放而不防陈旧页面，写 SQL 无 compare-and-set | H5 PC 多运营员 → `nx_growth_checkin_rule/nx_streak_milestone/nx_streak_power_up` → App | 全链改用数据库稳定 id；PC 携可见旧值，后端 SQL 原子 CAS，陈旧请求统一 409 `H5_CONFIG_STALE`；任何失败回读权威值，避免把局部结果继续当作完整成功 |
| PCFULL-125 | H5 断签/Saver/Power-Up 状态机 | P1 | 已修复源码、App 定向测试与类型检查通过，待统一 Maven/部署后边界复验 | 后端使用 JVM/数据库本地日期且只维护签到/里程碑；48 小时断签、Saver 消耗与最长连签恢复没有用户端命令，Power-Up 仅有后台配置无服务端激活/徽章事实 | 用户状态机只在 App 原型实现，服务端缺业务时区、并发锁定、幂等命令、审计和 A4 生命周期事件 | App 跨时区/漏签 → H5 Saver/Power-Up → 徽章/路由 → A2/A4 | 使用 `Asia/Ho_Chi_Minh` 服务端日期与下一重置时刻；Saver 仅在已断签且库存大于 0 时锁行消费并恢复 `min(longest,30)`；Power-Up 按服务端门槛锁定激活并发徽章，写强审计和 `daily.streak_restored/daily.power_up_activated` outbox |
| PCFULL-126 | H7→App 领券/订单抵扣真值 | P0 | 已修复源码，App 定向 7/7、全量 `vue-tsc --noEmit` 通过，待统一 8110/H5 部署真实账号复验 | `NX1.0` remote 模式仍从 `mock/vouchers.ts` 读取券定义，并用账号 localStorage 记录领取/核销；结算把本地 voucherId 发给真实订单接口，服务端没有对应持券事实时必然拒单 | H7 已有 `/api/vouchers`、claim 与订单原子核销边界，但 App 未接严格协议层和 canonical store | H7 PC 配置 → App 领券 → E4 订单折扣/核销 → D4/A2/A4/J1 | 新增严格 voucher API 与账号隔离 store；remote 模式失败关闭，只从服务端读取券定义/持券状态，领取使用稳定幂等键并对未知结果回读；订单仅发送真实 AVAILABLE 券，创建后同时回读订单和券 USED 状态，mock/localStorage 只保留显式 mock |
| PCFULL-127 | H7 已领券暂停/删除/过期语义 | P1 | 已修复源码与跨仓合同，待统一 Maven/部署后状态矩阵复验 | PC 文案声明暂停/删除“已领不回收”，但用户券查询和订单核销 SQL 都要求券定义 active、未删除，导致已领券立即消失且无法使用；过期券也被查询条件直接过滤，用户看不到过期记录 | 定义投放态和用户持券态被错误合并为同一过滤条件，未按领取事实独立投影生命周期 | H7 定义暂停/删除/到期 → App 券包 → E4 订单核销/A4 | 用户查询优先返回本人 grant，暂停/删除定义仍保留已领券；AVAILABLE 到期按服务端时间投影 EXPIRED；核销不再依赖定义 active/未删除，但继续强制有效期、SKU、归属和 AVAILABLE 行锁，暂停/删除不追溯回收 |
| PCFULL-128 | H7 发行库存/并发/CAS | P1 | 已修复源码、迁移、PC 3/3 合同与 TypeScript 通过，待统一 Maven/迁移/双运营员及并发发券复验 | 代金券没有发行上限、库存计数或版本号；H4/F1/用户领券并发可超发，两个运营员可静默覆盖配置，状态/删除没有陈旧页面保护 | 发券只靠 grant 唯一键防单业务重放，没有在券定义行锁内统一裁决总发行量；管理写请求没有 expected version | H4/F1/用户 claim → H7 grant 库存 → PC 多运营员 → A2/A4 | 新增 `issuance_limit/version` 幂等迁移；所有发券来源在券行 `FOR UPDATE` 后统一检查总发行量，精确重放优先返回原 grant；PC 编辑/上下架/删除携版本，SQL 原子 CAS，陈旧请求统一 `409 H7_VOUCHER_STALE`，A2 对象锁继续失败关闭 |
| PCFULL-129 | H7 批次/核销/撤销治理与读失败 | P1 | 已修复源码、PC 3/3 合同与 TypeScript 通过，待统一部署后撤销与审计复验 | PC 只展示券模板 CRUD，看不到来源批次、累计领取、可用、核销、撤销；没有撤销未核销券动作；后端 H7 列表捕获任意数据库异常并伪装为空列表，迁移缺失/数据库故障时仍可能开放新增 | grant 生命周期事实未进入 H7 管理读模型，异常被降级为空业务事实而非失败关闭 | H7 模板/来源批次 → grant 库存/核销 → E4/App → A2/A4/J1 | 管理读模型返回来源批次及 issued/available/redeemed/revoked 计数，PC 展示并提供带版本 CAS 的“撤销未核销”；撤销仅 AVAILABLE→REVOKED、已核销历史不变并写强审计；移除数据库异常吞噬，PC 错误态只显示重试且隐藏全部写入口 |
| PCFULL-130 | H8→App/H5 奖励展示真值 | P0 | 已修复源码，App 23/23 与全量 type-check 通过，待统一部署后真实 H5 复验 | PC 当前基础奖励均为 `0.01`，H1 生效后实际均为 `0.015`，但 App remote 模式继续展示原型默认新人 `5 USDT + 20 NEX`、邀请人 `200 NEX`；PC 改动完全不会传播 | 公共平台配置只有 E6 投影；App config store 在 remote 模式仍保留 `DEFAULT_PLATFORM_CONFIG.rewards`，真实接口存在不等于用户端在消费 | H8 PC 参数/H1 倍率 → App 注册、邀请落地页、分享海报、团队额度、我的入口 | 新增匿名只读 `/api/config/referral-rewards`，只投影 H1 当月倍率生效后的实际发放值及权威 sponsor 来源；App 与 E6 配置并发读取、严格协议解析，远程初始化/刷新失败均把奖励清零而非回退原型值 |
| PCFULL-131 | H8→A2 真实结算绕过 | P0 | 已修复源码与 H8 合同 6/6，待统一 Maven/部署后直调与双人审批复验 | 持有 `growth_h8_settle` 的调用方可直接 POST `/api/admin/growth/referral-rewards/settlements/run`，绕过 PC 的 A2 maker/checker，直接给新人和邀请人真实钱包入账 | 后端 `runSettlements` 只有 RBAC、幂等、串行化互斥、B1 和唯一结算约束，没有验证当前调用是否来自 A2 批准回放上下文 | H8 奖励批次 → A2 双人审批 → 新人/邀请人钱包 → D4/A4 | 在服务层、幂等与任何业务写之前强制 `A2ReplayContext.isReplaying()`，直调统一 `409 A2_CONFIRMATION_REQUIRED`；只有 A2 批准回放可继续，测试同时断言拒绝路径不锁批次、不写钱包/台账 |
| PCFULL-132 | H8 风控暂缓统计口径 | P1 | 已修复源码与合同，待统一部署后 SQL 夹具复验 | 已经成功发奖但仍命中 K1/K2 的关系会继续计入“风控暂缓”；自邀请关系也没有从该计数排除，导致运营数字互相重叠且错误放大 | `totalBlockedByK2` 未关联唯一结算表，也缺少 `s.id IS NULL` 和自邀请排除条件 | H8 风控暂缓 → K1/K2 调查 → 待结算决策/运营统计 | 统计查询关联 `nx_referral_reward_settlement` 并只计未结算关系，补 `s.id IS NULL` 与 `sponsor_user_id <> id`；PC 文案明确该卡只统计尚未发奖的风险关系 |
| PCFULL-133 | H8 PC 协议失败关闭与追溯入口 | P2 | 已修复源码、PC TypeScript 与合同通过，待 3002 新构建复验 | H8 overview 直接 TypeScript 强转，缺字段、负数、未知 lockMode/状态或错误来源仍可能渲染成半可信页面；状态裸显 `SETTLED`，结算后没有 A2/A4/D4 可点击核对入口 | 客户端只有静态类型，没有运行时协议边界；页面停留在“结果以服务端为准”的声明，未把复核链做成可操作入口 | H8 读模型/异常响应 → PC 决策 → A2/A4/D4 追溯 | 新增 H8 严格解析，校验四参数、三项有效奖励、六位小数、非负计数、正倍率、唯一 sponsor 来源和真实结算模式；异常只进入重试态并隐藏写入口；状态本地化，补 A2/A4/D4 深链 |
| PCFULL-134 | H8 有效奖励精度与上限 | P1 | 已修复源码与合同，待统一 Maven/MySQL 边界复验 | 六位小数基础奖励乘 H1 小数倍率后可产生超过六位的小数；PC 严格协议会拒绝，MySQL 钱包/结算/D4 列又可能各自隐式舍入或在 `DECIMAL(18,6)` 溢出，展示值和实际入账存在分叉 | `effectiveRewards()` 直接执行任意精度乘法，且后端参数上限高于 PC/App 可安全表示的范围 | H8 基础参数×H1 → PC/App 展示 → 双钱包/结算表/D4 | 增加唯一 `effectiveAmount`，统一 `HALF_UP` 到六位并在进入展示/资金链前强制不超过 `999999999.000000`；邀请人基础值按 H1 最大 4× 前置限制为 `249999999.750000`，PC 输入同步；PC/App 实际金额解析共用最终上限，超限失败关闭 |
| PCFULL-135 | H8 A2 奖励快照 TOCTOU | P0 | 已修复源码与合同 6/6，待统一 Maven/双人审批并发复验 | A2 提案命令只保存 `limit`；待审批期间修改 H8 参数、切换 H1 月份或倍率后，审批回放会按新金额发钱，复核人批准的不是其看到的金额 | 提案到回放间没有固定 H8 version、H1 月份和有效奖励指纹；执行瞬间的数据库互斥无法保护审批等待期 | H8 PC 提案 → A2 等待/批准 → H1/H8 配置 → 双钱包/D4/A4 | overview 生成包含 H8 version、effectiveAt、lockMode、H1 月份/倍率和三项实际金额的 SHA-256 快照；A2 命令固化 version/month/hash，回放拿 H8 mutex 后重算 CAS，不一致返回 `409 H8_REWARD_SNAPSHOT_CHANGED_REPROPOSE`，结算只使用已验证快照 |
| PCFULL-136 | App 注册未写真实邀请关系 | P0 | 已修复源码、App 23/23 与 type-check 通过，待统一 Maven/OTP provider/H5 真实注册复验 | remote 注册仍运行 `registerVerifiedPhone`、本地 sponsorship、礼包、本地钱包和账单；后端没有注册/邀请码绑定接口，新用户不会产生 `nx_user.sponsor_user_id`，H8 只能消费历史/手工数据 | 公开 H8 奖励投影已接入，但真实身份创建和 sponsor 归因仍停留在 Mock，主链生产者缺失 | App/H5 注册 OTP/邀请码 → `nx_user` → H8 pending → A2 → 双钱包/D4/A4 | 新增服务端注册 OTP 表、手机号/IP 分钟与日限额、TTL、五次尝试和一次性消费；注册事务锁定并解析 canonical 邀请码，只在 `nx_user` 创建时写一次 sponsor id/code，同时初始化 security/wallet、写 outbox 并签发真实 session；remote 页面禁止本地 sponsor/礼包/钱包假写，响应未知时用同一手机号/密码登录恢复已提交身份，重复身份由唯一键失败关闭 |
| PCFULL-137 | I1 PC 发布未进入 App 可见文案与实验归因链 | P0 | 已修复源码，PC 跨仓合同 3/3、App 定向 6/6 与 `vue-tsc --noEmit` 通过，待统一 Maven/部署/H5 真实账号复验 | PC 可发布三语文案并启动实验，但 `NX1.0` 的首页转化横幅仍只读本地 i18n/任务配置；没有 I1 API/store 调用，运营改版、服务端分桶、首曝和已支付订单转化都不会从真实 UI 触发 | 后端只提供按 copyKey 的运行时接口，App 可见组件按位置存在；管理端“文案位置”没有变成用户端可消费契约 | I1 PC 文案/实验 → position → App 首页横幅 → 服务端 assignment/exposure → E4 订单 → A4 | 新增登录用户 `GET /api/content/positions/{positionKey}`，位置必须唯一命中一个 PUBLISHED 文案，零命中 404、多命中 409 失败关闭；App 新增严格协议/store，首页 `home.conversion-banner` 真实渲染服务端三语与实验变体并暴露 QA 标识；订单回读只提交真实 PAID/PROVISIONING/ACTIVATED orderNo，后端再次核对归属和支付/完成事实后幂等记转化/A4 |
| PCFULL-138 | I1 文案换版陈旧页面可静默覆盖 | P1 | 已修复源码与 PC 合同，待统一 Maven/双运营员运行态复验 | 草稿保存、发布新版、历史回滚和下架未强制携带当前 revision；两个运营员同时打开页面时，后提交者可覆盖先提交者刚发布的事实；回滚接口虽已有 expected 字段 DTO，服务层未使用 | 幂等键只防同一命令重放，不防不同命令键来自陈旧页面；写事务虽然锁行，却没有比较页面所见快照 | I1 双运营员 → draft/publish/rollback/archive → App 当前版本/i18n/A4 | PC 四类动作携当前 `expectedRevision`，回滚/下架同时携当前 `expectedVersion`；后端锁定 copy 行后强制 compare-and-set，缺失或不匹配统一 `409 COPY_REVISION_CONFLICT`，在版本/i18n/审计写入前失败关闭 |
| PCFULL-139 | I1 实验框架参数并发覆盖与理由边界不一致 | P1 | 已修复源码与 PC 合同，待统一 Maven/双运营员运行态复验 | 框架参数读取未加锁、请求无 expected value，两个运营员可后写覆盖；理由只要求 6 字且无 200 字上限，与 I1 其余高敏动作 8–200 字规则不一致 | 框架配置被当作普通覆盖更新，缺少陈旧页面识别和统一审计输入边界 | I1 框架默认值 → 新建实验快照 → 分流/统计 | PC 提交当前值 `expectedValue`；后端事务内 `SELECT ... FOR UPDATE` 后比较，陈旧值统一 `409 COPY_FRAMEWORK_VALUE_CONFLICT`；理由统一 8–200 字，同值仍拒绝，成功后再审计 |
| PCFULL-140 | I2 PC 配置未进入 App Nova 真实消费链 | P0 | 已修复源码，App 协议测试 3/3、全量 `vue-tsc --noEmit` 与跨仓合同通过，待统一部署/H5 真实账号复验 | PC 的 10 通道、kill、模板发布/归档对 `NX1.0` 没有效果；remote 模式的 Nova 浮标仍用本地定时器、`Math.random`、mock 文案和 localStorage 通知制造 welcome/team/staking/market 消息 | App 没有接 `/api/notifications`，且 remote 与 mock 共用同一套本地推送入口 | I2 通道/模板 → backend `nx_notification` → App 通知中心/Nova 会话 → CTA | 新增严格 notification API 与 remote store；远程模式禁止任何客户端模拟 push，只轮询服务端通知账本，按 notification id 去重并注入 Nova 会话；启动、前台、下拉刷新和通知页均回读，读/清理/CTA 全部调用真实接口，CTA 使用稳定幂等键 |
| PCFULL-141 | I2 social 已写入但永不对用户可见 | P0 | 已修复源码与跨仓合同，待统一 Maven/部署后真实 social 事件派发复验 | `NovaSocialRuntimeService` 把消息写成 `QUEUED`，但 App feed 只查询 `DELIVERED/READ/SENT/SUCCESS`；I3 有显式 delivered 推进，I2 没有，导致 social 实际永久不可见 | 复用了通知表但漏掉“入队 → 送达”状态推进、数量核对和用户事实提取 | 真实事件同步 → 权重抽样/冷却/slot lease → `nx_notification` → App | I2 派发事务内先原子推进 `QUEUED→DELIVERED`，严格核对 inserted/delivered/facts 三个数量；不一致整体回滚，成功才更新事件 dispatch_count 和 slot 完成态 |
| PCFULL-142 | I2 Nova CTR 的 A4 分子分母均未生产 | P1 | 已修复源码与跨仓合同，待统一 Maven/A4 schema/真实点击复验 | BI L1 用 `nova.push_clicked ÷ nova.push_sent`，但当前数据库没有这两个 schema，I2 代码也没有发布事件；PC “真实 CTR”实际只能是种子/空值 | 通知通用事件只覆盖 `notification.delivered/read/swipe_action_taken`，没有补 Nova KPI 的锁定事件契约 | I2 送达/点击 → A4 schema/outbox → L1 KPI/PC CTR | I2 runtime 幂等 bootstrap 两个 server-authoritative schema，逐用户同时发布 `notification.delivered` 与 `nova.push_sent`；App CTA 在通知归属锁、路由锁和动作幂等写成功后发布 `nova.push_clicked`，重放不重复计数 |
| PCFULL-143 | I2 CTR 可伪造、模板状态机可逆行 | P1 | 已修复源码与定向合同，待统一 Maven/双运营员状态矩阵复验 | 通道更新直接持久化客户端传入 CTR；模板可 `ARCHIVED→PUBLISHED`、`DRAFT→ARCHIVED`，编辑已发布/归档模板会静默重置成 DRAFT 并停推 | 统计字段和运营配置字段未分权，状态更新只检查“不同”而未检查合法边 | I2 管理写 → runtime 模板选择/kill → A4/L1 | 新建通道 CTR 强制 0，编辑通道保留服务端现值；模板编辑仅限 DRAFT，状态只允许 `DRAFT→PUBLISHED→ARCHIVED`，归档终态不可重新发布 |
| PCFULL-144 | I2 未知结果重试会生成新防重号 | P1 | 已修复源码与跨仓合同，待 3002 新构建/断网注入复验 | I 域客户端每次调用都生成新 `Idempotency-Key`；若后端已提交但 BFF/网络超时，运营员点击重试会变成第二条业务命令 | 防重键生命周期错误地绑定到一次 fetch，而非“同一业务命令直到得到确定结果” | PC 写动作 → BFF → backend 幂等/A2/A4 | 按 method/path/body 建立不确定命令指纹；网络异常或 5xx 保留原 key，下一次完全相同的人工重试复用；成功或确定性 4xx 后清理，避免把未知结果误当新命令 |
| PCFULL-145 | I2 并发运营写入静默覆盖 | P1 | 已修复源码与跨仓合同，待统一 Maven/双运营员并发复验 | 通道节奏/kill、模板正文/状态、social 概率和真实事件状态均先读后盲写；不同防重号的两个运营员可互相覆盖，甚至重复发布/归档审计 | 幂等只处理同一命令重放，数据库更新没有携带“服务层刚读取的旧事实”，概率五行也没有同事务加锁 | 双运营员 → I2 通道/模板/概率/事件池 → runtime | 通道和模板改为旧字段快照条件更新，状态转换带 expected state；真实事件带 status+updated_at CAS；概率五行先 `FOR UPDATE`，PC 同时提交页面所见 expectedItems，不一致统一 409，所有冲突在审计和 runtime 生效前失败关闭 |
| PCFULL-146 | I2 九个非 social 通道缺真实服务端生产者 | P0 | 已修复源码；统一合同 14/14、App 3/3 与 type-check 通过，待统一 Maven/部署/真实账号逐通道复验 | 后端此前只有 `NovaSocialRuntimeService` 消费 I2 的 social cadence/template；welcome、market、upgrade、dailySummary、tradein、eventClaim、wrapped、taskLockMonthly、quest 没有统一服务端派发器或业务事件适配，PC 的 kill/冷却/模板对这些通道仍没有可证明的真实发送链 | 10 通道配置表先于跨域 producer contract 落地；App 消费层已补齐，但上游九类消息没有统一消费门 | I2 → I3/I6/B1/A2/A4 + E/F/G/H 业务事件 → App | 新增单一 `NovaBusinessRuntimeService` 与九通道真实 A4 事件适配表，统一执行 enabled/template/H1/tick/cooldown/slot/source-event receipt/notification bizNo/A4；receipt 唯一键保证回放不重复。只接受 allowlist 的 server-authoritative 事实；`tradein.completed`、`quest.completed` 等相邻事实不得冒充资格事实。上游尚未产生精确事实时明确 `NO_REAL_FACT:<contract>` 跳过，绝不恢复本地模拟；新增逐通道合同和同一事件回放仅投递一次测试 |
| PCFULL-147 | I3 CAP 并发与输入边界 | P1 | 已修复源码；PC 跨仓合同 3/3、TypeScript 通过，待统一 Maven/新 3002 双运营员复验 | CAP 请求只带新值，服务端先读后盲写；两个运营员可陈旧覆盖。low 档 `30 条 · TTL 24-48h` 被前端抽成 `302448`，指数输入 `1e2` 又会在通用数字窗被当作 100 放行 | 幂等键只防同命令重放，没有 expected value/CAS；展示标签用“删除全部非数字”解析；数字校验直接 `Number(value)` 接受指数记法 | I3 PC/A2 → `nx_notification_cap_rule` → App 保留窗口/合规通知可见性 | PC/A2 命令携可见 `expectedCap`；后端按 `tier+cap_label+locked/status` 原子条件更新，陈旧统一 `409 NOTIFICATION_CAP_STALE_VERSION`，成功后同事务执行 retention；low 档只取首个数字；通用操作确认拒绝指数格式，后端继续只接受明确整数 |
| PCFULL-148 | I3 Campaign 草稿编辑长度守卫 | P1 | 已修复源码并补服务测试，待统一 Maven 复验 | 新建 Campaign 会限制名称/标题 160、正文 4000，但编辑草稿没有相同长度检查；超长编辑可进入数据库层并产生截断或 500 | create 与 draft 两条输入校验分叉，编辑路径遗漏同一文本不变量 | I3 PC 草稿编辑 → MySQL Campaign → 排期/发送/App 展示 | `requireDraft` 与 create 共用相同上限语义，在估算受众和持久化前返回 `NOTIFICATION_CAMPAIGN_TEXT_TOO_LONG`；新增“超长不改原草稿”测试 |
| PCFULL-149 | I3 App 通知分页与失败空态 | P1 | 已修复源码；App 9/9 与 type-check 通过，待统一 H5/真实账号 100+ 通知复验 | App 固定只取 `/api/notifications?limit=100` 首屏且没有下一页入口；读取失败时仍显示“暂无通知”，旧的 critical/high 记录可能不可达 | store 忽略 `nextCursor`，页面把 loading/error/empty 合并为同一空态；续页 unread 总数又被错误按单页未读数校验 | I3 Campaign/保留策略 → 用户通知账本 → App 通知页/抽屉 | store 保存 cursor、显式加载更多并拒绝跨页重复 ID/游标不前进；续页正确接受全局 unread；通知页和抽屉区分加载/失败/真空态并提供重试，分页保留前页 |
| PCFULL-150 | I3 App 已读与 CTA 权威动作 | P1 | 已修复源码；App 9/9、跨仓合同 3/3 与 type-check 通过，后端定向用例已补齐但按统一构建约束待 Maven/H5 复验 | 通知页/抽屉吞掉 mark-read 错误后仍展开或跳转；CTA 忽略服务端回执 route，直接使用客户端缓存 `ctaHref`，错回执也未核对通知 ID/action；后端锁定未读事实后更新 0 行仍返回成功，CTA `INSERT IGNORE` 竞态失败后也未二次核对回执 | UI 把服务端动作当成非阻塞副作用，协议层没有响应归属校验，store 只返回布尔值；后端将“写入未发生”误当作可接受幂等结果，没有证明对应回执存在且归属一致 | App 通知点击 → I3 read/action receipt → A4/Nova CTR → 业务落地页 | mark-read 失败不再本地乐观已读或导航，并进入显式可恢复错误态；后端已读更新 0 行立即失败并回滚；CTA 使用稳定幂等键且只导航服务端 canonical route；协议强校验 receipt 的 notificationId/action，后端 `INSERT IGNORE` 未写入时二次读取并核对回执，缺失或冲突均失败关闭；未知结果保留同一 key 重试 |
| PCFULL-151 | I3 CAP A2 提案未知结果 | P1 | 已修复源码与跨仓合同，待新 3002 断网注入复验 | CAP 调整的 `run` 未返回/等待提案 Promise，确认窗同步关闭；提案也没有显式稳定 command key，后端已写入而响应丢失后重试会用新 key，可能重复建票 | 弹窗生命周期与异步命令生命周期脱节；A2 自动 key 仅覆盖单次 fetch，无法代表同一人工命令的未知结果重放 | I3 CAP → A2 提案/对象锁 → I3 批准回放 | CAP 提案按 tier、可见旧值、新值、理由生成指纹并持有稳定 command key；`run` await/return 提案，成功或确定性失败清 key，未知结果保留 key 和弹窗，同输入重试复用 |
| PCFULL-152 | I3 App CTA 被清理后的幂等重放 | P1 | 已修复源码与跨仓合同，后端定向用例已补齐，待统一 Maven/H5 复验 | CTA 首次已落回执但响应丢失后，通知若被 clear-read/retention 删除，同 key 重试先查通知事实而返回 404，无法拿回原 canonical route | 幂等回执查询错误地依赖可被保留策略删除的通知事实；长期回执没有作为未知结果恢复的第一入口 | App CTA → I3 action receipt → retention/clear-read → A4/Nova CTR/落地页 | 合法输入后先按 key 查回执并核对 user、notification、action；一致则直接返回回执中原 canonical route 且 `recorded=false`，错绑 409；仅无回执时才锁通知事实并执行新动作 |
| PCFULL-153 | I4 App 信任中心真实内容链 | P0 | 已修复源码；App API 3/3、全量 type-check 与跨仓合同通过，待统一 H5 部署后首次用户复验 | PC 已管理六个服务端信任版块，但 App `/trust` 整页是硬编码原型文案、数字、团队与交易所信息，既不读取当前发布版，也不产生版块曝光事件 | App 没有消费后端已存在的匿名 current/view API，用户端展示与运营发布事实完全分叉 | I4 PC 草稿/发布/回滚/下架 → 后端 current → App `/trust` → A4/BI 曝光 | 新增严格公开 Trust API；App 只渲染服务端当前发布版并按 zh/vi/en 选择字段，加载/失败/真空态分离，协议错误失败关闭且不回退本地旧事实；可见版块逐项记录 `content.trust_section_viewed`，链接仅允许受控内部路径和 HTTPS |
| PCFULL-154 | I4 双运营员陈旧覆盖 | P1 | 已修复源码与跨仓合同，后端定向用例已补齐，待统一 Maven/双运营员运行态复验 | 发布只校验草稿 revision；回滚、下架和草稿新建未绑定页面所见当前版，草稿删除不校验 revision；仓储读取又没有行锁，不同幂等键的并发运营员可陈旧覆盖 | 幂等仅防同命令重放，缺少当前 section `version+status` 快照 CAS 与数据库串行化 | I4 双运营员 → 草稿/发布/回滚/下架 → App 当前版本/A2/A4 | PC 草稿和三类 A2 命令携可见 section 快照，删除携 draft revision；后端事务内 `FOR UPDATE` 锁 section/version 后比较，缺失或漂移统一 409，冲突发生在内容、审计和 outbox 写入前 |
| PCFULL-155 | I4 未知结果重复变更 | P1 | 已修复源码与跨仓合同，待统一 Maven/断网重放复验 | I4 六类直接写虽然要求 `Idempotency-Key`，但服务层没有持久幂等回执；后端已提交而响应丢失后，同 key 重试仍可再次写审计/outbox 或撞入新状态失败，调用方无法恢复确定结果 | 请求头校验被误当成幂等实现，未使用已有 `AdminIdempotencyService` 领取、哈希核对和结果重放 | PC/BFF/A2 → I4 六类变更 → MySQL/审计/outbox | 发布、回滚、草稿增改删、下架全部纳入独立持久幂等 scope，同 key+同请求返回原结果，同 key+异请求冲突；事务失败记录失败态且不把部分写入当成功 |
| PCFULL-156 | I4 归档后无法恢复 | P1 | 已修复源码与跨仓合同，待统一部署后真实状态机复验 | 一个版块只有当前已发布版本时，下架后 PC 回滚选项排除当前版，后端也拒绝 target==current，导致该版块永久从 App 隐藏，除非额外新建并发布版本 | 状态机把“已发布版重复发布”与“已归档版恢复上线”混为同一非法转换 | I4 下架 → App 隐藏 → 历史版恢复/应急回滚 | PC 在 archived 状态把当前历史发布版纳入选择并显示“恢复上线”；后端仅对 archived+同版本开放重新发布，published+同版本仍拒绝，保留审计/outbox 与权限门 |
| PCFULL-157 | I4 A2 提案未知结果 | P1 | 已修复源码与跨仓合同，待新 3002 断网注入复验 | 发布、回滚、下架的 `run` 没有等待提案 Promise；确认窗可提前关闭，网络已提交但响应丢失后重试又生成新 command key | 弹窗生命周期和人工业务命令生命周期脱节，自动 key 只覆盖单次 fetch | I4 PC → A2 提案/批准回放 → 后端 I4 写入 | 按动作、版块、当前快照、目标、理由生成指纹并保留稳定 command key；`run` await/return 提案，未知结果保留弹窗和原 key，成功或确定失败才清理 |
| PCFULL-158 | I4 App 本地元数据覆盖 CMS | P1 | 已修复源码与跨仓合同，待统一 H5/App 包发布后传播复验 | App 新接 current API 后仍用本地 `SECTION_META/FIELD_LABELS` 覆盖服务端发布的 description、structure、field.label，PC 修改后用户端不生效 | 把界面翻译常量误当成受管 CMS 字段真值，破坏 server-canonical | I4 PC 草稿字段 → 发布 → public current → App | 删除本地业务元数据覆盖；页面 chrome 保持三语，CMS description/structure/label 原样使用服务端发布值，带 `.zh/.vi/.en` 的字段值按当前语言选择 |
| PCFULL-159 | I4 原生 App HTTPS CTA 无动作 | P1 | 已修复源码与跨仓合同，待 Android/iOS 真机复验 | H5 用 `window.open`，非 H5 编译分支为空；审计报告、市场、团队详情等 HTTPS 字段仍显示“查看”，原生端点击无效果 | 外链载体只实现浏览器分支，未复用项目已有 plus runtime 能力 | I4 current HTTPS 字段 → Android/iOS CTA | H5 保持安全新窗口；原生分支使用 `plus.runtime.openURL`；内部导航、H5 弹窗和原生 runtime 失败均显式提示，不假装成功 |
| PCFULL-160 | I4 操作归属可被 body.operator 污染 | P2 | 已修复源码与跨仓合同，后端定向用例已补齐，待统一 Maven 复验 | 主审计 actor 会被安全上下文纠正，但 I4 草稿/发布/回滚/下架的业务表 lastOperator 和治理 outbox 仍读取客户端 operator，可显示或下游归因为伪造账号 | I4 旧请求 DTO 的兼容字段仍被写模型信任，与 I5 的认证身份策略不一致 | I4 PC/A2 → 版本表/section/outbox → A4/BI | I4 持久化、required audit 参数和治理事件统一使用 `authenticatedOperator()`；body.operator 仅兼容接收但不参与归属，新增 spoofed operator 回归测试 |
| PCFULL-161 | I5 发布与重新确认语义 | P1 | 已修复源码与合同，待统一 3002 部署后确认窗复验 | PC 发布确认窗宣称发布后当前映射用户立即过期并触发服务器拦截，但后端发布只审批不可变版本，不改变 App 投放或确认状态 | 把“版本审批”和“法域映射切换”两个独立状态转换合并成一个用户后果 | I5 发布 → A2 → 版本历史；I5 映射切换 → App/G1/G7/D5/J2 | 发布确认窗和七章核对卡改为“仅审批不可变快照”；明确只有后续映射切换才触发 re-ack、通知和受限动作 |
| PCFULL-162 | I5 A2 提案未知结果 | P1 | 已修复源码与跨仓合同，待新 3002 断网注入复验 | I5 法域、矩阵、发布和闸范围的 `run` 不等待提案 Promise，确认窗可提前关闭；响应丢失重试生成新 command key | 弹窗生命周期和人工业务命令生命周期脱节，自动 key 只能覆盖一次 fetch | I5 PC → A2 maker-checker/对象锁 → I5 批准回放/A4 | 所有 I5 提案 await/return；按动作快照与理由持有稳定 command key，未知结果保留原 key 和输入，成功或确定性失败才清理 |
| PCFULL-163 | I5 矩阵/归档/闸范围陈旧覆盖 | P1 | 已修复源码与跨仓合同，后端定向用例已补齐，待统一 Maven/双运营员复验 | 两个运营员从同一旧页面调整映射、归档映射或变更受限动作时，后提交者可静默覆盖先提交事实；幂等键不识别陈旧快照 | A2 命令只携目标值，后端回放未比较页面所见 version/status/countries/scope | I5 双运营员 → A2 → matrix/gate → App re-ack/G1/G7/D5 | PC/A2 携完整可见快照；matrix/catalog 与 gate rows 在事务内 `FOR UPDATE` 串行化后比较，缺快照或漂移分别以明确 409 失败关闭，冲突发生在通知、配置、审计前 |
| PCFULL-164 | I5 A2 矩阵批准回放字段错位 | P0 | 已修复源码与跨仓合同，后端定向回放用例已补齐，待统一 Maven/A2 真实批准复验 | PC 提案写 `jurisdictionCode`，后端 replay 却读取 `jurisdiction`；真实审批执行得到空法域并失败 | I5 高敏命令 schema 的生产者与消费者字段名分叉 | I5 PC matrix proposal → A2 approve/replay → I5 matrix → App re-ack/A4 | replay 读取 canonical `jurisdictionCode`，仅为旧票据兼容回退 `jurisdiction`；新增 canonical 字段回放与快照冲突测试 |
| PCFULL-165 | I5 App 七章/语言/滚底/确认未知结果 | P1 | 已修复源码，App 6/6、type-check 与跨仓合同通过，待统一 H5/原生/真实账号复验 | App 协议只要求任意七章而非精确 01–07，并错误强制 `zh+vi` 合法版本也必须有英文；原生缺 `IntersectionObserver` 或 H5 sentinel 缺失时自动放行滚底；确认已提交但响应丢失时无法恢复权威结果 | 内容/语言协议、载体滚动凭证和写命令未知结果未形成失败关闭闭环 | I5 current/token/最短阅读 → App 七章/滚底/确认 → G1/G7/D5/J2/J4 | PC/后端/App 严格要求唯一 01–07 和 `zh+vi`/`zh+vi+en`；双语版不伪造英文且英文界面安全回退中文；原生只由 `onReachBottom` 证明滚底，H5 无 sentinel 失败关闭；确认响应未知时仅同法域同版本已确认可恢复 |
| PCFULL-166 | I6 发布文案未进入 App | P0 | 已修复源码；App 4/4、type-check 与跨仓合同通过，待统一后端/H5 部署后发布传播复验 | PC 可发布 zh/en/vi 词条，公开 `/api/content/i18n` 也存在，但 App `useT` 永远只读打包字典，运营发布对用户无任何效果 | PC 管理面和 App 渲染面之间缺少服务端权威消费链 | I6 PC 草稿/发布 → 后端公开 bundle → App 全局渲染 | App 启动、前台恢复和切换语言时读取严格校验的服务端 bundle；仅覆盖本地已知 key，24 小时缓存，网络失败保留已验证缓存，无缓存时安全回退打包字典 |
| PCFULL-167 | I6 App 伪支持八种语言 | P1 | 已修复源码；App type-check 与跨仓合同通过，待统一 H5/原生旧存储升级复验 | 语言页展示 11 种语言，但只有 zh/en/vi 三份字典；选择日/韩/俄等语言后徽标改变、正文却静默显示英文，旧存储还会持续保留已移除代码 | 把路线图语言当成已交付能力，持久化值又未按当前白名单校验 | I6 语言目录 → App locale store → 全局文案 | 只展示真实具备字典和服务端协议的 zh/en/vi；hydrate 时校验白名单，旧的 ja/ko 等值自动回退英文并允许重新选择 |
| PCFULL-168 | I6 双运营员陈旧覆盖 | P1 | 已修复源码与跨仓合同，待统一 Maven/双运营员运行态复验 | 草稿、发布和归档未在同一事务内锁定最新版本并比较页面所见版本，不同幂等键可用旧页面覆盖或错误转换新状态 | 幂等只防同命令重放，缺少版本 CAS 与数据库串行化 | I6 双运营员 → 草稿/发布/归档 → App 当前 bundle | PC 三类写携 `expectedVersion`；后端 `FOR UPDATE` 锁定最新或当前发布版本，缺失/漂移在内容、审计和事件写入前以明确 409 失败关闭 |
| PCFULL-169 | I6 发布/归档缺 A4 治理事实 | P1 | 已修复源码与 schema migration 279，待统一迁移/Maven/A4 运行态复验 | 词条发布和归档只写业务表与审计，A4、BI 和 App 缓存失效链无法获得可治理事实 | I6 状态机没有接入统一 outbox/schema registry | I6 publish/archive/rollback → A4 outbox → App/BI/审计 | 注册 `admin.i18n_published` 与 `admin.i18n_rolledback` revision 279；事务内发布 message/version/locale/operator 字段，归档与历史回滚共享受治理事件并用可选目标字段区分 |
| PCFULL-170 | I6 确认窗提前关闭 | P1 | 已修复源码；PC 9/9 合同与 TypeScript 通过，待统一 3002 部署后断网/慢网复验 | 保存、发布、完整性修复等确认回调未返回后端 Promise，写入和权威刷新尚未结束时弹窗即可关闭，失败结果只能在消失后的 toast 中看到 | 确认窗生命周期未绑定真实命令生命周期 | I6 PC 确认 → 后端写 → reload canonical overview | `runBackend` 返回完整 Promise 链，所有 I6 写回调 return/await 写入、权威刷新和结果提示后再结束确认 |
| PCFULL-171 | I6 文案允许原始 HTML | P1 | 已修复源码与定向回归源码，待统一 Maven/真实接口 XSS 载荷复验 | zh/en/vi 文案只拦 JSON 和 URL，`<img onerror>` 等 HTML 可保存并传播到多个前端载体 | 多载体渲染安全假设不一致，服务端未执行最小通用内容边界 | I6 PC 输入 → MySQL/version → App/PC/通知展示 | 服务端统一拒绝 HTML 标签；App 公开 bundle 客户端也严格拒绝 HTML、错误 locale、非 canonical 响应和超长值，双层失败关闭 |
| PCFULL-172 | I6 无词条历史回滚闭环 | P1 | 已修复源码与跨仓合同，待统一迁移/Maven/3002 真实状态机复验 | PRD 要求发布/回滚，但 PC 无版本历史入口，后端也没有词条版本查询和恢复接口；错误发布或归档后无法从可见入口恢复 | 版本表只被写入，没有形成运营可用的不可变历史状态机 | I6 当前/归档版本 → PC 历史 → 新发布版本 → App/A4 | 新增历史列表和“回滚到此版本”；后端只接受 archived 快照及当前版本 CAS，把目标内容复制成单调递增的新 published 版本，不改写旧快照，并记录审计与治理事件 |
| PCFULL-173 | J1 提现闸用户入口未生效 | P0 | 已修复源码、跨仓合同与 App 协议，待统一 8110/H5 部署后真实资金入口复验 | J1 `withdraw` 关停后，D2 管理审批会受阻，但真实用户 `POST /api/withdrawals` 仍可创建提现、冻结余额并写 D4 账本 | `AppWithdrawalService.submitOnce` 没有读取 J1 权威闸，管理审批门禁被误当成用户入口门禁 | J1 withdraw → App 提现 → D5/K3/K4/K5 → D2/D4/A2/A4 | 用户事务在 KYC、风控、余额和账本副作用前读取 `killswitch.withdraw`/兼容旧键；关停返回 `409 WITHDRAWAL_KILL_SWITCH_DISABLED` 且零副作用；提现 policy 投影 `withdrawalEnabled + gateSource=J1`，App 严格解析并禁用提交 |
| PCFULL-174 | J1 试用闸用户入口与状态传播断链 | P0 | 已修复源码、跨仓合同与 App 协议，待统一 8110/H5 部署后真实试用入口复验 | J1 `trial` 关停后，真实用户仍可 `POST /api/trial/start` 新建试用；`GET /api/trial/state` 也没有把 J1 闸传播给 App，促销入口仍可能显示 | H2 只检查 `phaseOpen` 和循环风险，没有消费 J1 权威闸；PC 管理处置检查不能替代 App start 边界 | J1 trial → H2 App start/state → E 设备/D4/B1/A2/A4 | `startOnce` 在任何 claim/风控/支付副作用前失败关闭；state 显式投影 `trialGateEnabled` 并令 `canStart=false`，既有取消/结算/延长不被错误封死；App 严格拒绝 `canStart=true && gate=false` 的矛盾响应 |
| PCFULL-175 | J1 应急参数与自动阈值陈旧覆盖 | P1 | 已修复源码、PC 合同与类型检查，待统一 8110 部署后双运营员并发探针复验 | 两个运营员从同一旧页面调整自动确认时限或 R2 阈值，不同幂等键均可成功，后提交静默覆盖先提交；同值也会生成假变更审计 | 请求只有新值，服务端 blind upsert；幂等键只能防同命令重放，不能证明页面基线未漂移 | J1 PC 多运营员 → emergency SLA/auto rule → 自动关停/补录/A2/A4 | PC 携可见 `expectedValue` 并禁止同值；服务端校验 expected、原子 `compareAndSetSetting`，缺基线/陈旧页/并发输家/同值分别失败关闭，冲突写 REJECTED 审计，成功后才写成功审计 |
| PCFULL-176 | J1 Genesis 恢复资金影响误分类 | P2 | 已修复源码、PC/后端合同通过，待统一 8110/3002 部署后确认窗复验 | Genesis 恢复在 PC 与后端矩阵被标成 `delayed`/“未来负债增加”，但 PRD 明确同时恢复节点排放和二级交易，存在即时资金与资产流影响 | 五闸风险语义表与 PRD v4 分叉，沿用了只看长期分红负债的旧分类 | J1 genesis restore → B1 → G4 排放/交易 → D4/A2/A4 | PC 与后端 `coverageImpactCategory` 统一改为 `immediate`，恢复继续强制 B1 红线，并在确认语义中按即时资金流出展示 |
| PCFULL-177 | J2 受限地区真实资金路由漏拦 | P0 | 已修复源码与跨仓合同，待统一 8110/H5 部署后真实 IP 终验 | 把某国加入 `limitedCountries` 后，真实 App `/api/stakes`、`/api/repurchase`、`/api/trial` 及任务/活动/积分/券奖励端点不在 J2 显式资金路由表内，受限 IP 仍可开仓、复投、开始试用或领取奖励 | 路由表沿用旧命名 `/api/staking` 和抽象 `/api/rewards/claim`，没有随真实 App Controller 路径演进；PC 列表显示受限不能证明每个服务端入口已裁决 | J2 limited → G1/G7/H2/H3/H4/H5 → D4/B1/A4 → App | 显式登记真实 `/api/stakes`、`/api/repurchase`、trial 资金动作、quest/event/points/earnings/voucher 奖励动作；保留 `/api/trial/cancel` 风险收缩通道；新增逐路由 GEO_LIMITED 与非资金动作不误拦合同 |
| PCFULL-178 | J2 App 地域失败提示泄露技术码 | P1 | 已修复源码、App 39/39 与 type-check 通过，待统一 H5/App 包部署后多语终验 | 登录、注册、提现、兑换、质押、Genesis、复投、试用、签到和活动入口收到 J2 拒绝时，部分页面显示 `GEO_*`/`GENESIS_*` 原始码，部分只说网络或服务不可用，用户不知道是地区限制及资金是否变化 | App 各页面分散读取 `Error.message`，没有统一识别全局封禁、只读受限、功能入口封禁和地区判定基础设施失败 | J2 edge/filter → App 登录与资金/奖励入口 → 用户决策/客服 | 新增统一 `geoPolicyUserMessage`，覆盖七类 J2 拒绝并提供中/英/越明确文案；各可见入口统一说明限制范围和“未改变余额”，非 J2 错误仍走原业务文案 |
| PCFULL-179 | J3 试用篡改拒绝未进入监控 | P0 | 已修复源码与 PC 合同，待统一 8110 部署后真实 App 身份拒绝事件终验 | 真实 `GET /api/trial/eligibility?clientStatus=...` 和 `POST /api/trial/charge` 已改由 H2 生命周期服务判断；客户端状态/扣款结果冲突虽然返回 409，但控制器直接构造失败响应，绕过 J3 事件发布，而看板覆盖注册表仍宣称 11/11 已接线 | H2 真实生命周期替换旧 canonical 实现时，只迁移了业务拒绝结果，没有迁移 `free_trial_state` 与 `charge_fail_rate` 的 server-only `risk.tamper_detected` 发布职责；服务层单测只测旧入口，未覆盖控制器真实路由 | App H2 eligibility/charge → server canonical 拒绝 → A4 outbox → J3 → K4/B5 | 试用状态冲突在 H2 权威比较后调用 server-only 记录方法；扣款结果冲突复用 canonical `TRIAL_CHARGE` 持久幂等范围，同 key 重试不重复计数且不进入 H2 结算；两类请求仍零业务副作用并返回原 409；新增控制器单测与跨仓 PC 合同，统一部署后须用 App 用户令牌触发并核对 J3/K4/B5/消费回执 |
| PCFULL-180 | J4 跨域剧本仅接通 J1/I3 | P0 | 已修复源码、J4 合同 21/21 与 Java 定向编译通过，待统一迁移/部署后真实破坏性夹具终验 | PRD J4 应编排 J1/J2/C2/K1/I3/I5，并由 J1/C2/K1 联动 D2；当前 actionOptions、PC 白名单和执行循环只允许 J1 提现/Genesis 与 I3 通知，其余域虽残留 config 占位方法却永远不执行，无法完成监管点名全链止血 | 编排器把“安全失败关闭的临时 V3 范围”长期当成交付范围，跨域参数、真实目标域 adapter 与 A2 maker-checker 没有形成同一契约 | J4 → A2 → J1/D2、J2、C2/D2、K1/C2/D2、I3、I5 → A4 | 升级 `J4_REAL_EXECUTION_V4`；动作引用携 ISO 国家码/用户 ID/账户簇 ID/法域+版本并由前后端双校验；J2 调用真实 emergency geo block，C2/K1/I5 通过唯一 gateway 进入各域权限、幂等、CAS、审计和真实副作用；实战统一先创建 `j4_playbook_execute` A2 提案，maker 不可自批，批准回放后逐步执行；中途失败保持已完成动作并跳过后续 |
| PCFULL-181 | J4 缺 A4 剧本治理事件 | P1 | 已修复源码、schema migration 281/282 与合同通过，待统一迁移/部署后 A4 运行态复验 | J4 新建/编辑/执行只写 A2 审计，没有发布 PRD 指定的 `admin.emergency_playbook_edited`、`admin.emergency_playbook_executed`，A4/BI 无法订阅剧本版本与终态执行事实 | J4 状态机未接统一 outbox，事件中台仅有展示字典但 registry 没有可校验 schema | J4 剧本编辑/执行 → outbox → A4/BI/审计 | 事务内发布两个 server-authoritative 事件；编辑事件带 playbook/operation/operator/reason/idempotency，执行事件带 execution/outcome/trigger/step count；新增幂等 migration 注册 revision 281/282，部分失败与完整成功均明确发终态 |
| PCFULL-182 | J4 进行中执行不可取消/恢复核对 | P1 | 已修复源码、J4 合同 21/21 与 Java 定向编译通过，待统一部署后并发慢步骤终验 | PC 对 pending/running 执行只禁用回滚，没有取消或人工恢复入口；同步跨域调用超时后，值班员只能等待同幂等键被动租约恢复，无法明确停止后续步骤或核对未知结果 | 执行记录有恢复租约和前向失败语义，但没有合作式取消标记、CAS 写边界和可见操作面 | J4 running/unknown → 取消/恢复核对 → 目标域幂等/CAS → 部分终态/A2/A4/回滚 | 新增原子 cancel-request：下一个步骤边界保留已完成动作、跳过未开始动作并落部分终态，进度写不会覆盖并发取消标记；新增超过 2 分钟租约的“恢复核对”，只读目标域持久事实，确认成功才记 done，未知结果记 failed，绝不盲重放；PC 提供取消、恢复核对与终态后 J1 所有权回滚三条分离入口 |
| PCFULL-183 | J4 直接执行接口可绕过 A2 双人复核 | P0 | 已修复源码、J4 合同 21/21 与 Java 8 文件定向编译通过，待统一部署后以直调 409 与 maker/checker 夹具双向复验 | PC 已改为先建 A2 提案，但后端 `executePlaybook` 仅在存在对象锁时拒绝非回放调用；攻击者或旧客户端在无待审批锁时可直接调用 `/executions`，且若前序为 J1/J2/I3，会在遇到 gateway 的回放门前产生真实副作用 | A2 门槛只放在 PC 和部分目标域 adapter，没有位于 J4 执行聚合根的首条不可绕过前置条件 | 旧客户端/直调 → J4 execution → J1/J2/I3；正确链路应为 maker → A2 checker → replay context → J4 → 各目标域 | 在 `executePlaybook` 进入命令校验、幂等缓存和任何目标域副作用前强制 `A2ReplayContext.isReplaying()`；非回放统一 409 `J4_A2_CONFIRMATION_REQUIRED` 并留拒绝审计；单测补无活动锁直调拒绝，合同测试锁定聚合根门禁 |
| PCFULL-184 | J4 进程中断恢复后可能缺 A4 执行终态事件 | P1 | 已修复源码、J4 合同与 Java 定向编译通过，待统一部署后 crash/recovery + outbox 唯一性终验 | 正常完成/partial 会发布 `admin.emergency_playbook_executed`，但 execution 已终态的幂等重放和 stale recovery 只补最终审计，没有核对或补发 A4 事件；若进程在落执行事实后、发 outbox 前中断，A2/J4 有终态而 A4/BI 永久缺事实 | “终态审计补偿”和“终态事件补偿”被拆成两条不对称路径，正常 happy path 掩盖了 crash window | J4 durable execution → 恢复/幂等重放 → A2 audit + A4 outbox | 新增 `ensureJ4ExecutionOutcomeEvent`：按 `EMERGENCY_PLAYBOOK + executionId + event name` 先查 outbox，缺失才按持久 execution 快照补发；终态重放和 stale recovery 在返回前都执行 ensure，completed/partial payload 与正常路径同 schema |
| PCFULL-185 | K1 三层证据与真实注册/发奖入口断链 | P0 | 已修复源码、PC 合同 30/30、TypeScript、MySQL 8 SQL 探针与当前 3002 浏览器走查通过；待统一 8110/3002 部署后以真实注册/支付夹具终验 | K1 后端聚类只消费设备指纹，IP 与支付工具为空；用户节点 sponsor、新人礼与累计入金均为空；真实注册不读取 K1 同 IP 24h 上限；H8 新人礼只在可选 holdRisky 分支阻断 flagged/frozen，导致 PC 虽显示三层和阈值，真实用户与资金入口仍可绕过 | 页面展示、聚类读模型、注册命令与奖励结算各自演进，缺少从真实证据生产者到 K1 裁决再到资金副作用的同一失败关闭合同 | C6/App 注册 OTP/IP、设备决策、C2 支付工具 → K1 → A2/C2/K4/J3/H8/D4/A4；并发注册与发奖重放 | K1 读模型改用已消费注册 OTP IP、真实设备指纹与绑定银行卡 token 三层事实，并投影 sponsor、已结算新人礼、钱包累计入金；注册事务锁 K1 参数行，IP 缺失/配置异常失败关闭，达到 24h 上限即 409；H8 对 detected/flagged/frozen 均无条件阻断结算；保留 A2、C2、K4、J3、H8、A4 权威调用和 CAS/幂等边界 |
| PCFULL-200 | K4 自动升级线与高风险分档互锁缺失 | P1 | 已修复 PC、客户端解析与后端当前分读取源码，PC 合同 12/12、TypeScript、diff check 通过；待统一 8110/3002 部署后执行新增浏览器故障注入终验 | PC 允许把 `autoEscalateScore` 配成低于 `bandHighMin` 后进入确认流程；若后端或历史数据返回这种矛盾模型，客户端仍展示并开放写入口，后端当前分读取也会把该模型用于 C1/D2 消费 | PC 只校验升级线 70–100，没有复用后端 `autoEscalateScore >= bandHighMin` 不变量；权威响应解析和下游当前分读取只做字段范围检查，没有做跨字段一致性检查 | K1/K2/C4/J3/行为事实 → K4 模型/评分 → D2/C1/B5/K5 | PC 编辑器显式校验并提示升级线不得低于高风险下限，保存按钮失败关闭；`normalizeK4Model` 拒绝矛盾权威响应；后端 `findCurrentScoreUser` 对低于 70 或低于高风险下限的升级线返回不可用，不把脏模型传播给下游 |
| PCFULL-201 | K4→D2 空队列验收误报 | P3 | 已修复验收脚本，旧运行态完整真实写入/恢复链已到达 D2/B5/刷新重登；待统一部署后随修后全量重跑 | D2 当前无提现单时，验收脚本仍强制查找某一行的 K4/K3 文本，把合法空态判成产品失败 | 自动化把“调用链存在”错误等同于“当前必须存在业务记录”，没有区分服务端空集合与加载失败 | K4 → D2 提现队列 | 验收改为先验证“按当前生效 K4 模型动态路由”的服务端口径；有单据时核对 K4/K3 行，空集合时核对“暂无提现记录”，两种分支都不得出现加载错误 |
| PCFULL-202 | B5 与 J1 缺省闸语义分叉 | P1 | 已修复后端源码与定向单测，diff check 通过；待统一 8110 部署后重跑 B5 页面/SSE | live J1 缺少 `killswitch.staking` 持久行时，J1 页面和各生效域按 canonical 规则解释为 enabled，但 B5 `/api/admin/risk/radar` 返回 503 `B5_KILL_SWITCH_SOURCE_UNAVAILABLE`，K4 跨域验收页面断链 | B5 新读模型绕过 `KillSwitchState`，把“缺行的权威默认”当成“来源不可用”；同一 J1 单源被两个消费者解释为相反结果 | J1 五闸 → B5 风险雷达；K4/J3/K1/K2 → B5；B5 SSE | B5 缺行复用 J1 `KillSwitchState` 的 enabled 默认并返回 `defaulted=true` 供运营识别；显式非法值仍 503 失败关闭，未直写数据库、未改变任何闸的业务状态 |
| PCFULL-205 | K5→G2 裁决可覆盖并发终态 | P1 | 已修复后端源码并新增 3 个定向单测；diff check 通过，待统一 8110 部署与 Maven 定向测试后执行真实 G2 KYC_REQUIRED 裁决终验 | K5 通过或驳回 G2 来源工单时，按兑换单号无条件把兑换单写为 `QUEUED` / `CANCELLED`；如果 G2 已被并发完成、取消或进入其它终态，晚到的 K5 裁决会把终态改写 | G2 适配器调用无前态条件的 `updateExchangeStatus`，没有复用仓储已经存在的 `updateExchangeStatusIfCurrent` CAS | G2 兑换 `KYC_REQUIRED` → K5 裁决 → G2 队列/终态；并发完成/取消 | 通过与驳回均改为只允许 `KYC_REQUIRED` 前态的 CAS 更新；CAS 失败返回 false，使 K5 同事务抛错回滚，不回写 C4、不伪造成功审计；单测锁定通过、驳回和 Mapper 前态条件 |
| PCFULL-206 | K5 独立角色验收脚本使用失效凭证和旧原因码 | P3 | 已修复验收脚本，真实 Chromium 全套 4/4 通过，临时风险/只读账号均按最新 version 清权；证据见 K5 报告 | 脚本创建临时风险/只读账号后使用请求体 `initialPassword` 登录，但 A1 权威接口会忽略该字段并返回服务端生成的 `temporaryPassword`；脚本驳回还发送已退役的 `DOCUMENT_INVALID`，清理写也漏 `expectedVersion` | 验收载具没有跟随 A1 服务端生成凭证、K5 拒绝原因闭集和 A1 CAS 契约演进 | A1/A6 临时账号 → K5 权限/裁决验收 → A1 清权；K5 驳回原因码 | 登录改用响应 `temporaryPassword` 并完成首登改密/TOTP；驳回改用 `KYC_MATERIAL_INVALID`；清理前读取最新账号 version，禁用后用新 version 移除角色；旧运行态完整专项验收 4/4 PASS |
| PCFULL-207 | D2/G2 畸形 K5 触发信号静默绕过复审 | P1 | 已修复后端源码并新增 2 个失败关闭单测；diff check 通过，待统一 8110 部署与 Maven 定向测试后执行调用方异常输入复验 | 大额提现/兑换触发入口收到空 userNo、空来源单号、空理由或非正金额时，若金额低于阈值会直接返回 `notRequired`；调用方或映射异常可能被误解释为“无需增强复审” | `requiresReview` 同时承担输入合法性和阈值判断，把非法输入与正常未达线折叠成同一 false | D2/G2 资金事实 → K5 阈值触发/合并/来源表 → C4/D2/G2 裁决 | 在读取阈值前独立校验 userNo、正金额、来源单号和理由；非法信号抛稳定错误并由上游事务失败关闭，只有合法且未达线的事实才能返回 `notRequired`；同时统一 trim 后再建单/合并/审计 |
| PCFULL-195 | K3 PC 响应协议失败开放 | P1 | 已修复源码、K3 合同 10/10 与 TypeScript 通过；待统一 3002 部署后 HTTP 200 畸形响应注入复验 | K3 overview 缺字段、非法动作/状态或错误分页时会被补成空串、`manual`、`draft`、0；dry-run 畸形成功响应也会显示成“已完成” | K3 仍使用适配旧 mock 的宽松归一化，未把服务端权威响应当作完整协议 | K3 PC → 风控 BFF → OpsRiskService；畸形读响应可制造可操作假规则，畸形写响应会掩盖未知结果 | overview 改为逐字段、枚举、整数范围、分页和路由总数严格校验，任何歧义进入 K3 失败卡且不暴露写入口；dry-run 畸形成功响应按未知结果处理并保留同一 Idempotency-Key 重试 |
| PCFULL-196 | K3→App 未知路由静默放行 | P1 | 已修复源码；App API 6/6、K3 源码合同 1/1、`vue-tsc` 通过，待统一 H5/App 部署后畸形响应复验 | App 对任意包含 `freeze/delay/manual/reject` 以外的 `riskRoute` 一律投影为 `pass`，例如未知 `teleport-pass` 会显示放行 | 客户端用包含匹配和默认 pass 兼容路由，没有和真实后端六种终态路由建立闭集合同 | K3/K4/K5 → AppWithdrawalService → App 提现跟踪；用户可看到与 D2 真实冻结/人工审核不一致的放行结论 | 只接受 `fast-pass/delay/manual/high-manual/escalated-manual/freeze` 六种服务端路由并精确映射；其它值在写入 canonical 跟踪记录前以协议错误失败关闭 |
| PCFULL-197 | K3 App remote 被本地 mock 预判干扰 | P1 | 已修复源码；App K3 源码合同 1/1 与 `vue-tsc` 通过，待统一 H5/App 真实用户提交复验 | remote 模式仍用本地 mock K3 的账户簇、首提和地址历史展示风险横幅，并可用 `eligibility.canSubmit=false` 阻止真实后端请求 | 页面把 mock 演示预览沿用到生产模式，破坏“服务端 K3 是提交时唯一裁决者”的边界 | App 金额/地址 → 本地 mock K1/K3 与真实 J1/J2/D5/K3/K4/K5 双源 → D2/D4 | remote 模式只用权威 policy、钱包绑定和基础金额/地址校验决定能否发起；不展示 mock K3 结论，也不让 mock eligibility 拦截；提交后只消费后端真实路由 |
| PCFULL-210 | K6 当前 App 与 Janus 服务端调用链断开 | P0 | 已修复源码、App 18/18 与跨仓合同通过，待统一 H5/App/8110 部署后真实用户终验 | 当前权威 App 仓库 `NX1.0` 没有调用 `/api/app/janus/reports`、pending、ack；PC 的策略、十二态与命令无法到达真实用户设备 | K6 先在旧 App 仓库落过实现，但当前交付路径切换后没有迁移，PC 页面通过掩盖了域外消费者缺失 | K6 PC 策略 → 8110 Janus report/pending/ack → 当前 App → A2/A4；刷新、重登与账号切换 | 当前 App 接入严格 Janus API、前后台 60 秒同步、账号隔离稳定上报/ACK；只认服务端十二态，命令应用成功后才 ACK，失败 ACK 不冒充目标态且可同事实重试 |
| PCFULL-211 | K6 批准目标在 App 消费边界缺少实际 URL | P0 | 已修复源码、后端定向测试与跨仓合同通过，待统一部署及活动目标夹具终验 | 待执行命令只返回 `key/version/catalog`，App 无法从任意批准目录条目取得真实 URL；旧实现按 `default/backup/promo` 硬编码页面，与不可变批准目标目录冲突 | 策略评估阶段验证了目录绑定，但命令消费阶段没有再次解析并投影 canonical URL，生产者和消费者协议不闭合 | K6 批准目标 → 策略/人工命令 → pending → App 导航/ACK；停用、DNS/来源漂移与旧命令 | pending 对精确三元组重新校验 ACTIVE、允许来源和网络 guard，成功返回 canonical HTTPS URL；App 要求四字段齐全并拒绝凭据、查询、片段、未知状态，非远程命令夹带目标也失败关闭 |
| PCFULL-212 | K6 验收脚本把空批准目录误判为产品失败 | P3 | 已修复并在当前 3002/8110 真实 Chromium 4/4 通过 | 脚本硬编码选择 `promo`；生产允许批准目录为空时下拉禁用，自动化超时，后续 RBAC、故障注入、生命周期与重登均被跳过 | 验收载具把可选业务夹具当成必备平台前置，没有把“合法空目录”与“远程目标链路”分轨验证 | K6 首次用户 UI → 策略发布 → App 上报/命令/ACK；批准目标目录 | 常驻主链改用不依赖远程目录的 `MANUAL_HOLD`，仍走真实 report→evaluation→command→ACK；远程目标精确绑定由跨仓合同及统一部署活动目标场景独立验收 |
| PCFULL-190 | K2 处置码与 H8/K4 消费合同分叉 | P1 | 已修复源码与历史数据迁移，PC 合同 8/8、MySQL 迁移通过；待统一 8110/3002 部署后重跑处置、刷新、重登和下游消费 | K2 动作把“已标记套利、已拦新人礼、已联动冻结”等中文展示文案写入 `disposition`，H8/K4 却只消费 `account_flagged/gift_blocked/cluster_frozen` 等 canonical code；页面操作成功后下游可能看不到处置 | 展示标签与跨域状态码共用一个持久字段，写端和消费端没有共享闭集 | K2 处置 → H8 新人礼、K4 评分、F4 排行榜、PC 刷新重登 | 服务端只持久化四个 canonical code；PC 在显示边界映射为中文且未知值失败关闭为“已处置”；幂等迁移归一化历史中文值，H8/K4 继续只消费 canonical code |
| PCFULL-191 | K2 检测依赖管理员打开页面 | P0 | 已修复源码并加入独立调度器与源码合同，真实 Mapper SQL 已执行；待统一部署后用“不打开 K2 页面”的定时探针终验 | E3/H2 投影和风险信号只在管理员请求 K2 overview 时刷新；无人值守期间 H8/K4/F4/B5 可能长期拿不到应有风险事实 | 把查询接口误当成检测触发器，缺少服务端持续投影所有权 | E3/H2/H8/F4 → K2 投影/信号 → K4/B5/A4 | 新增服务端 K2 调度器，默认每 60 秒在事务内刷新 E3、H2、新人礼、排行榜，退休失效投影/信号并按稳定业务键幂等发 outbox；overview 仍可触发同一幂等刷新但不再是唯一入口 |
| PCFULL-192 | K2 新人礼与排行榜没有真实检测生产者 | P0 | 已修复源码、迁移和 11 位 ID MySQL 事务夹具；待统一部署后以真实 H8 结算和跨 5 分钟排行榜快照终验 | K2 页面存在“新人礼套利”和“榜单刷量”视图及动作，但服务端没有从 H8 结算、F4 返佣事实生成对应行；只有人工/验收种子行，生产环境无法自行发现 | 页面、动作和下游消费先于权威事实生产链落地，读模型没有稳定来源、基线和退役规则 | H8 结算、F4 返佣/直推 → K2 → H8/K4/F4/B5/A4 | 新人礼按 K1 canonical 实体簇聚合已结算奖励；排行榜每 5 分钟记录累计返佣和直推快照，用排除已标记账户后的正增量中位数作基线并应用服务端倍率参数；两类投影都支持退役、重激活、幂等信号和 A4 outbox |
| PCFULL-193 | H2 试用循环按单用户而非 K1 实体聚合 | P0 | 已修复源码并以同簇双账号、4 次服务端事件的 MySQL 事务夹具通过；待统一部署后重跑真实 App 试用开始/取消链 | H2 检测按 `user_id` 分组，套利者在同一 K1 账户簇内轮换账号即可让每个账号都低于阈值；K2 也只向代表用户发信号 | 风控阈值错误地绑定登录账号，而 PRD 风险对象是可跨账号切换的 canonical 实体 | H2 `trial.started` → K1 实体簇 → K2 → K4/B5/A4 | 只消费已注册、服务端权威 H2 事件；先解析 K1 节点归属，再按 `cluster_id` 聚合，孤立用户才退化为用户实体；同簇所有 canonical 用户均投影信号，行 ID 由簇 ID 的 SHA-256 稳定生成 |
| PCFULL-194 | 八位 LPAD 截断高位用户 ID | P0 | K1/K2/K4 风险链已修复且 11 位 ID SQL 夹具通过；已加全仓零截断合同，当前仍列出风险链外 50 处，待主控统一清零并部署终验 | MySQL `LPAD(id,8,'0')` 在 ID 超过 8 位时不是“至少补到八位”，而是截断到八位；当前 `nx_user` 自增值已超过 600 亿，可能把不同用户投影成同一个 `Uxxxxxxxx`，导致 K1 节点、K2 信号、K4 分数和 D2/H8/F4 消费串号 | 把固定显示宽度误当成无损 canonical ID 编码，且没有高位 ID 合同 | 用户主键 → K1/K2/K4 userNo/subject → D2/H8/F4/B5/A4；全仓所有 canonical userNo 消费者 | K1/K2/K4 生成和解析统一改为 `LPAD(id,GREATEST(8,CHAR_LENGTH(CAST(id AS CHAR))),'0')`；H8/F4/D2 相关消费链同步修复；合同全仓扫描 `CONCAT('U',LPAD(...,8,...))`、`RIGHT(...,8)` 和 `SUBSTRING(...,-8)` 并输出文件行号，统一阶段必须把剩余命中清零 |

| PCFULL-220 | L2 | P1 | 已修复源码与合同，待统一部署后真实筛选终验 | `/funnel/drilldown`、留存矩阵、曲线接口收到 cohort/phase/locale/ref 后仍返回全量看板；不同筛选条件可得到同一份“成功”数据 | L2 仓储只暴露无参 `dashboard("L2")`，服务层只回显 query，没有把注册 cohort 条件传入同用户逐级计算 | A4 注册事件 → L2 cohort actor set → KYC/首购/复投/提现；L1/B3/I1/I2/E/H 真值对照 | 新增 `l2Dashboard(cohort, phase, locale, ref)`，筛选先锁定注册 actor 集合，再只连接同一用户、严格后发生的阶段事件；无匹配 cohort 明确 unavailable，不回落到全量事实；四个读接口统一校验参数并传递筛选 |
| PCFULL-221 | L2 | P1 | 已修复源码与合同，待统一部署后切片导出终验 | 页面“Phase/locale/渠道”按钮只改本地序号并提示“实时生效”，没有发请求；导出也始终冻结全量快照 | 视图状态、读 API 与报表快照三套契约没有共享切片对象，幂等 hash 也不包含筛选 | L2 PC 切片/下钻 → L2 读接口 → L5 报表快照/A2 审计 | 改为 cohort/Phase/locale/ref 真实输入与“应用切片”，阶段、留存窗、曲线、交叉指标均调用真实端点；报表请求、scope、rowCount、CSV、审计详情与幂等 hash 同时冻结当前切片，并把 funnel 与 cohort 留存一起写入创建时快照 |
| PCFULL-222 | L2→H2/A4 | P1 | 已修复源码与合同，待统一部署后篡改事件终验 | L2 的 trial 子漏斗允许 `trial.*` 非服务器权威事件进入统计，客户端或污染数据可抬高试用领取/启动/兑换人数 | SQL 为 trial 事件保留了 `is_server_authoritative` 例外，与 H2 权威状态机和 A4 schema accepted 边界冲突 | H2 trial 状态机 → A4 outbox → L2 trial 子漏斗/K4 风险信号 | L2 主查询统一要求 `analytics_event=1 + schema_registered=1 + is_server_authoritative=1`，trial 不再例外；技术合同锁定查询段禁止 trial 放行 |
| PCFULL-223 | L2→I/F/H | P1 | 已修复源码与合同，待统一部署后多 locale/渠道终验 | 交叉矩阵硬编码 `en/zh/es/pt`，缺少越南语 `vi`；渠道/Phase 分组 `.limit(4)` 静默截断，页面也固定只显示四列四行 | 演示样本常量被带入真实分析，没有从注册 cohort 事实动态发现维度，也没有全量/分页声明 | I1/I2/I6 locale、F 渠道 ref、H1 Phase → L2 交叉分析 | locale 列从当前注册 actor 动态去重排序，空数据默认 `en/vi/zh`；移除四组截断并返回 `columns`；PC 按服务端动态列与任意行数渲染，删除“固定四样本”豁免 |
| PCFULL-224 | L2 PC | P1 | 已修复源码、TypeScript 与 13/13 合同通过；旧 3002 畸形响应仍复现，待统一 3002 部署后浏览器终验 | HTTP 200 畸形漏斗含负人数、999% 转化、坏 cohort 时，旧运行态不展示坏数字但仍把该单行识别成“可导出”，导出按钮失败开放；生命周期 fallback 还会接受部分合法行 | PC 用泛型强转和默认值兼容旧 mock；详细分析与 fallback 都缺少整体协议、不变量和全有或全无校验 | L2/BFF 脏响应 → PC 决策/导出 → L5/A2 | 新增严格五级事件顺序、人数单调、百分比范围、cohort/曲线、动态交叉矩阵、quality 不变量校验；任何详细响应歧义整页 fail-closed 并关闭导出；fallback 必须六行唯一已知阶段、非负安全整数与权威来源全部通过，不再丢弃坏行后渲染部分结果 |
| PCFULL-225 | L2 | P1 | 已修复源码与 Java 定向编译，待统一部署后跨日/迟到事件终验 | 留存成熟度用 JVM 默认时区的 `LocalDate.now()`，与 MySQL/JDBC `UTC+08:00` 业务日可能在跨日窗口分叉；迟到 `app.dau` 的下一次查询更新语义未暴露 | 成熟度把“业务日”隐式绑定进程时区，质量元数据缺少时区与迟到事件规则 | A4 事件时间/迟到到达 → L2 Day1/7/14/30/60 分母与成熟窗口 → L1/B3 | 固定业务时间偏移 `UTC+08:00`，按注册精确时间加窗口判断成熟；质量响应声明 `businessTimeZone` 与 `included_on_next_query`，保持事件时间计算、同 actor 去重和下一查询吸收迟到事实 |
| PCFULL-226 | L2 验收载具 | P3 | 已修复，当前 3002 可执行分支 4/4 通过 | 旧串行脚本第一条硬断言必须存在完整漏斗，最后一条又硬断言同一环境必须是安全降级；生命周期解析测试名写“拒绝不完整行”却期待保留部分行 | 验收脚本没有区分“有完整注册 cohort”与“合法空 cohort”两种互斥运行态，测试期望与名称相反 | L2 首次用户、刷新重登、错误注入、匿名权限与修后回归证据 | 重写为按服务端能力分支且两支都校验不可伪造；覆盖可见入口、真实读端点、503 恢复、刷新、匿名 401、畸形 200；部分 fallback 期望改为整组拒绝。旧运行态除修后专属畸形导出断言外 4/4 通过，保留失败 trace 供统一部署后复验 |

| PCFULL-215 | L1 聚合 | P1 | 已修复源码、纯 Java 定向编译/行为探针与 PC 合同，待统一 8110 部署后真实事实终验 | Day7 在滚动 7 天内先裁掉 7 天前注册，成熟 cohort 几乎永远为空；其余转化 KPI 只做 actor 集合交集，事件倒序也可计入；重复事件、空 actor、坏时间/latency 会被静默丢弃或放大 | 时间窗在生命周期连接前全局截断，维度按每条后续事件而非分母锚点过滤，读模型缺少事件完整性与稳定去重边界 | A4 事件时间/cohort/phase/locale/ref → B3/L1/L2/L4；8 项 KPI 当前/上期/六点趋势 | 改为分母事件锚定、同 actor 严格后发生；Day7 用“成熟日落在观察窗”的注册 cohort；当前/上期/六点同一算法；按 event_id 去重，畸形权威事实整页失败关闭，稀疏趋势不再挤掉空点后错配标签 |
| PCFULL-216 | 注册/设备→A4→L1 | P0 | 已修复源码与迁移合同，待统一迁移/8110 部署后真实 App 注册与换机终验 | 当前注册成功发布未登记的 `user.registration.completed`，既可能触发 A4 schema gate 回滚，也不会进入 L1/L2；写入 sponsor 关系却不发 `referral.bound`；真实已支付换机订单不发 `checkout.completed` / `device.purchase_completed`，#3/#4/#5/F1/F2 调用链断开 | 业务表写入与 §2.4.6 canonical 事件在同事务内没有共用出口，A4 registry 也缺少多数 L1 锁定事件 | App 注册/直推/换机 → A4 → B3/L1/L2/L4 → F1/F2/I2 | 注册改发 `auth.register_completed`，有 sponsor 时同事务发 `referral.bound`；已支付换机同事务发 checkout/device purchase；新增 L1 八事件 schema 与 checkout 属性，任一 outbox 失败回滚业务事务，不伪造成功 |
| PCFULL-217 | F2→A4→L1 #7 | P1 | 已修复源码、schema 与定向测试合同，待统一部署后首笔佣金终验 | F2 写 `nx_commission_event(network)` 和 D4 台账后不发布 `commission.paid`，导致合法网络佣金永远不能进入 L1 #7；L1 也未约束佣金必须发生在 `referral.bound` 之后 | F2 结算事实与 A4/L1 消费协议未闭合；读侧用无序集合掩盖生产者缺失 | checkout.completed → F2 L1-L7 → commission.paid → L1 #7/L4/F5/D4 | 每笔 USDT network commission 与 D4 同事务发布 recipient `user_id` 的 `commission.paid`，附 source/layer/order；L1 只计 `kind=network`、`sourceUserId` 匹配被推荐人且严格发生在关系绑定后的首笔佣金，同一被推荐人去重 |
| PCFULL-218 | L1 PC | P1 | 已修复源码、TypeScript 与 12/12 合同；旧 3002 畸形响应仍复现，待统一 3002 部署后浏览器终验 | PC 接受任意长度/顺序/数值的 KPI 数组；下钻和趋势接口虽请求成功却丢弃响应，继续展示 overview；刷新失败后时间窗先切换、旧数字无陈旧标识 | 泛型强转代替权威响应协议；“发出请求”被误当成“消费调用结果”；视图状态先于服务端成功提交 | L1/BFF 畸形 200/503 → KPI 决策与导出；drilldown/trend → 运营归因 | 严格校验恰好 #1–#8、唯一顺序、数值/可用态、分子分母、定义集与趋势对齐；下钻响应真实覆盖明细和曲线；异常响应不覆盖；刷新失败保留上次已校验快照、明确陈旧且不切时间窗 |
| PCFULL-219 | L1 视图筛选/验收载具 | P1 | 已修复源码与验收脚本，待统一部署后真实 custom/cohort/Phase/locale/ref 终验 | “自定义”实际回退 7d；cohort 粒度与五个切片按钮只改本地样式/toast，硬编码 `2026-W22/P3/en/NX-*` 且不请求服务端；空态脚本扫描全页，被全局“兑付覆盖率 0.0%”误判 | 演示控件没有绑定 L1 query；自动化断言越过模块边界 | PC 时间窗/切片 → `/api/admin/bi/kpi`/drilldown/trend；L1 与全局 B1 页头 | 增加 from/to 自定义窗口并由服务端严格校验、不合法不回退；cohort/Phase/locale/ref 改为真实输入并贯穿 overview/downstream；空态断言收敛到 main/L1 作用域 |

| PCFULL-240 | L5 快照与大文件下载 | P1 | 已修复源码与合同 11/11，待统一迁移、MinIO 实物与流式下载终验 | 聚合与监管报告把完整 CSV 写进 MySQL `MEDIUMTEXT`，下载端再一次性组装 `byte[]`；运行态虽然可下载，但没有 MinIO 真实文件，较大快照同时受数据库字段上限和 JVM/HTTP 双份内存约束 | “创建时快照”只实现为数据库文本字段，通用对象存储和下载端点没有进入 L5 数据出口调用链 | L1/L2/L3/L4/I5/J4/D4 → L5 快照 → MinIO → 限时下载/A2 | 新增 `nx_bi_report_artifact` 元数据与 MinIO `bi-reports/*.csv` 权威文件；事务回滚时清理外部对象，MySQL 只留对象标记；下载改为 `StreamingResponseBody` 从对象流直传，旧数据库快照只读兼容且缺对象时失败关闭 |
| PCFULL-241 | L5 CSV 公式注入 | P1 | 已修复源码与合同 11/11，待统一部署后公式载荷真实下载终验 | L5 通用 KPI/漏斗/财务/运营 CSV 转义只处理双引号，服务端事实若以 `= + - @`（含前导空格）开头，运营下载后可能被 Excel/表格软件当公式执行；D4 与监管专用导出已有防护但口径不一致 | 三条 CSV 生产链各自实现转义，通用聚合链遗漏表格公式边界 | 各域服务端事实/运营输入 → L5 CSV → 运营电脑 | 三条生产链统一先移除换行参与单元格，再以 `stripLeading` 检查 `=+-@` 并加文本前缀；合同逐文件锁定公式与 CR/LF 防护 |
| PCFULL-242 | L5 下载授权与并发令牌 | P1 | 已修复源码与合同 11/11，待统一迁移、受限角色与双会话终验 | 监管报告生成角色虽可创建报告，却被下载端点类级权限表拦在令牌/文件之外；每个报告又只有一个 token hash，新签发会让另一个仍在 24 小时有效期内的会话立即失效，且 token 未绑定签发管理员 | 端点粗粒度 RBAC 与服务层报表类型权限分叉；把“当前 token”建模成报告单值而非多条限时授权 | risk/超管 → 监管报告 → token → 下载；刷新、重登、双会话、token 泄漏 | 下载端点补 `bi_l5_regulatory_generate` 后仍由服务层按报表类型二次校验；新增 `nx_bi_report_download_grant`，每次签发独立保存 token hash、管理员和过期时间，验证同时匹配报告、hash、actor、TTL，不再覆盖其他会话 |
| PCFULL-243 | L5/D4 → A4 治理事实 | P1 | 已修复源码、迁移与合同，待统一迁移后 A4 outbox 终验 | 聚合、敏感批准、D4 七类账单和监管报告只写 A2 审计，没有发布 PRD 指定的 `admin.report_exported`；A4/J4/BI 无法订阅服务端权威的数据出境事实 | 把 A2 操作日志当成了 A4 业务事件，生产者没有接入 schema registry/outbox | L1–L5/I5/D4 → A2 + A4 → J4/BI/监管追溯 | migration 289 注册事件与字段闭集；普通聚合创建、敏感快照批准、I5 监管报告和 D4 账单导出均在原事务内发布同一 canonical 事件，schema/outbox 失败即回滚，不虚报导出成功 |

| PCFULL-227 | D2→A4→L3 兑付终态 | P0 | 已修复源码、迁移、单测与调度消费合同，待统一迁移/8110 部署后真实链上回执终验 | D2 没有真实 `SENT→CONFIRMED` 写入口，A4 没有 `withdraw.confirmed` 生产者；L3 用提现订单当前状态和订单 `created_at` 归入报表周期，无法回答“本期实际确认了多少笔”，跨月确认会落错期 | 资金状态机、权威事件和报表发生期没有共享同一终态事实；页面能展示 confirmed，但生产调用链不存在 | D2 链上回执 → A2 审计/A4 outbox → D2 durable consumer → L3 兑付率/平均耗时 | 新增唯一服务端确认命令，只允许 SENT/历史 CHAIN_SUBMITTED 经状态 CAS 进入 CONFIRMED；链上哈希非空、不可变、单提现唯一且与 DB 128 长度一致；状态、A2 必达审计和精确 `withdraw.confirmed` schema/outbox 同事务。L3 submitted/confirmed/rejected/delayed/frozen 全按 A4 `event_ts` 发生期去重聚合，平均耗时改为最早 submitted event 到 confirmed event，完全移除 `created_at` 归期 |
| PCFULL-228 | L3 财务响应与周期 | P1 | 已修复源码、TypeScript 与 L3 合同 10/10 通过，待统一 8110/3002 部署后畸形 200、跨月和跨时区终验 | 月度上期用当前月天数倒推，7 月的上期被算成 5 月 31 日起；收入/兑付来源缺字段、负数或小数计数时，后端和 PC 会把异常补成 0，HTTP 200 畸形响应仍可显示并导出“成功”财务报表 | 把“缺失”与真实零混为一谈，周期比较窗口没有按自然日历平移，七个独立来源进入 UI 前没有整体协议和跨源算术校验 | L3 四类报表 → B1/B2/D3/D4/D6/A4；日/周/月/季/自定义、币种精度和服务器时区 | 后端自然日/周/月/季分别平移上期并返回服务器 `timeZone`；收入与兑付计数异常返回 503。PC 在任何格式化/补零前严格校验四条收入、A4 兑付源、B1 覆盖与净敞口、九项硬负债、7/30 连续日期及跨源合计，任一歧义整页失败关闭并禁用聚合/明细导出 |
| PCFULL-229 | L3→L5 脱敏资金明细 | P1 | 已修复源码、权限/行数/CSV 单测与后部署 E2E 验收脚本，待统一部署后审批下载终验 | L3 明示“用户级资金明细暂不可导出”；服务端对任何敏感财务导出直接拒绝，现有 L5 对待确认任务只显示“历史敏感任务已冻结”，没有可操作审批链 | 旧版只开放聚合快照，D4 账单事实、L3 专权、创建时快照、L5 审批和限时下载未组成闭环 | L3 当前周期/字段 → D4 账单 → L5 审批 → 24h 下载令牌/A2 审计；A8 RBAC | 增加 `bi_l3_export_detail` 专权、8–200 字原因、服务端日期范围与九字段白名单、最多 100,000 行、用户编码强制脱敏及 CSV 公式注入保护；无权角色在任何行数查询前 403。创建时固化 MASKED 快照并进入 PENDING_CONFIRM，L5 仅 `bi_l5_task_approve` 可审批，READY 后仍须签发 24h 令牌下载；前端同一未知结果意图复用 Idempotency-Key，明文/解密字段始终失败关闭 |

| PCFULL-230 | L4 PC 权威响应协议 | P1 | 已修复源码、TypeScript 与 6/6 合同，待统一 3002 部署后畸形 200 浏览器终验 | HTTP 200 缺字段、字符串金额、负数、>100% 比率、重复 Phase/历史桶或 quality 矛盾时，PC 会把它们补为 0、默认周/ALL 或部分可信数据；导出入口仍可能开放 | 真实分析响应仍沿用适配 mock 的宽松转换，没有整体结构和跨字段不变量 | L4/BFF 脏响应 → 四类报表展示与 L5 聚合/团队树导出 | PC 改为全有或全无协议：周期/日期/Phase、设备/任务/网络、P1–P6、历史、quality 和 liveFacts 任一歧义即失败关闭；不显示伪零，不允许坏响应继续开放导出 |
| PCFULL-231 | L4 查询窗口与业务日 | P1 | 已修复源码与跨仓合同，待统一 8110 部署后非法参数/跨日终验 | 未知 period/Phase、坏日期、倒置自定义范围会返回 200，并静默回退到 week/ALL、最近 30 天或交换首尾；JVM 默认时区还可使跨日报表漂移 | Controller/service 没有锁定参数闭集，domain 用 fallback/swap 代替拒绝，业务日隐式继承进程时区 | PC 周期/Phase/custom → L4 事件时间窗 → 聚合 CSV | 服务层统一拒绝未知值、非 custom 日期、坏/倒置/未来/超 399 天范围；domain 二次失败关闭；业务时区固定 `UTC+08:00`，迟到事实声明为下一查询纳入 |
| PCFULL-232 | F2/A4→L1 #7/L4 佣金触发归因 | P1 | 已修复源码、单测与跨仓合同，待统一部署后真实首笔网络佣金终验 | canonical F2 事件使用 camelCase `userId/sourceUserId`，L1/L4 只读 snake_case；即使读到 actor，两处也把佣金接收人 sponsor 与被推荐用户匹配，触发率会长期为 0 或失真 | mapper 丢失 canonical camelCase/source 字段，两个分析器错误复用 recipient actor 作为 commission trigger actor | F2 network commission → A4 `commission.paid` → L1 #7/L4/F5/D4 | L1/L4 mapper 同时读取 snake/camel actor、source actor 和 commission kind；两处仅用 network 佣金的 `sourceUserId` 与 `referral.bound` actor 做严格后发生匹配并按被推荐人首笔去重；L4 来源缺失保持不可计算，不制造 0% |
| PCFULL-233 | L4 重放、Phase 与权威边界 | P1 | 已修复源码、Java 定向编译和合同，待统一部署后事件夹具终验 | 重放同一 `event_id` 会重复放大金额/次数；P3 查询仍返回 P1–P6 六行；Phase 转化台阶跨空阶段桥接；历史按原始事件行计数；`store.viewed` 非服务器权威例外却仍声称 canonical | 分析器缺少稳定事件去重和切片输出约束，SQL 对单一客户端事实放宽权威门 | A4/E/F/H1 → L4 当前值、历史趋势和 Phase 效果 | 只消费 schema accepted 且 server-authoritative 事件；先按 event_id 去重，历史再按设备/任务/直推业务键去重；单 Phase 只返回一行，空 Phase 打断台阶，quality 暴露去重数、时区和迟到规则 |
| PCFULL-234 | L4→L5 团队树导出窗口 | P1 | 已修复源码与跨仓合同，待统一部署后边界时间导出核对 | 页面 day/week/month 是滚动 24h/7d/30d，团队树 SQL 却用自然日/自然月；相同选择下可见报表与下载行数不一致 | 读报表和敏感导出各自定义时间窗口，没有共享服务端口径 | L4 网络报表 → F 团队树 → L5 文件/令牌/A2 高风险审计 | 导出 SQL 改为 `NOW()-1 DAY/7 DAY/30 DAY`，与页面同口径；保留 1–10 层、8–200 字理由、部分隐藏、24h token、角色告警与同键幂等 |

| PCFULL-250 | L6 App 行为事实生产链缺失与页面目录漂移 | P0 | 已修复源码、App 6/6 与类型检查通过，待统一迁移/8110/H5/App 部署后真实用户终验 | 当前交付 App `NX1.0` 没有生产 `app.page_viewed` / `app.element_clicked`；L6 目录仍是 2026-07-23 旧 pages.json，14 个当前页面缺失、5 个已删除页面仍在 | PC/后端 L6 先于当前 App 仓切换落地，埋点 producer 与构建页目录未随当前交付路径迁移 | App 可见页面/点击 → `/api/app/analytics/events` → A4 非权威 behavior → L6 PC；pages.json+i18n → 目录 | AppChassis 接入全局 show/hide 同 eventId 停留回填与 tap 坐标；只传路由、坐标、受控 elementId，不传可见文本/查询参数；新增严格 API 协议；迁移把目录升到 `pages-i18n-20260727`、补 14 页并退役 5 页 |
| PCFULL-251 | L6 事件幂等、时间与查询参数失败开放 | P1 | 已修复源码与定向单测合同，待统一迁移/8110 部署后并发重复/乱序终验 | 客户端没有 eventId；服务端用字段哈希去重，缺失/超过 24h 的 clientTs 被替换成服务器当前时间，乱序不拒绝；未知 window/depth/sort 静默回退 7d/ALL/PV | “容错展示”被错误用于行为事实写入和分析查询，无法区分真实默认、脏输入和重复/迟到事实 | App eventId/time → L6 fact/outbox → 24h/7d/30d、层级/排序/热区 | 新增 32 位 `clientEventId` 唯一索引与 `FOR UPDATE` 幂等边界；page show/hide 同 id 只回填 dwell 不重复 PV/outbox；缺时、过期、乱序、点击 300ms 冲突及未知参数全部失败关闭；固定业务时区 `UTC+08:00` |
| PCFULL-252 | L6 PC 脏 200、失败刷新与空导出失败开放 | P1 | 已修复源码、PC 合同 4/4 与 TypeScript 通过，待统一 3002 部署后浏览器故障注入终验 | 缺字段、负数、UV>PV、跳出率越界会被补零或部分展示；筛选刷新失败继续保留旧数字，导出仍可能可用；空结果也可导出 | 宽松 mock 归一化与真实权威响应共用，错误状态只加提示但不撤销旧数据/动作 | 8110 HTTP 200/5xx → PC 热力/筛选/导出 → L5/A2/A4 | 全有或全无校验目录、计数、时间窗、趋势、坐标、质量与时区；异常清空当前数据和热区、展示“重新加载”，禁用导出；只有当前已校验、非空、无错误且有 `bi_l6_export` 才可导出 |
| PCFULL-253 | L6 点击曝光/CTR、去重与时区口径不可见 | P2 | 已修复源码与 PC 合同，待统一部署后多端/跨日数据终验 | 页面只展示点击总量，没有明确点击曝光分母、去重键、业务时区和迟到吸收规则；跨日或重复事件排查时无法判断口径 | 聚合实现细节没有成为前后端共同协议，默认 JVM 时区还可能与业务日漂移 | App 点击/PV → L6 统计/趋势 → PC 运营解释 | 服务端返回 `businessTimeZone=UTC+08:00`、clientEventId 去重、乱序拒绝与迟到下一查询规则；PC 新增“点击 / 页面曝光 PV”CTR 并在覆盖声明中显示质量口径 |
| PCFULL-254 | L6 导出缺 A4 治理事实 | P1 | 已修复源码与 migration 289 复用，待统一 8110 部署后 outbox/A2 双写终验 | L6 CSV 只写 A2 `admin.report_exported` 审计，没有发布已注册的 A4 `admin.report_exported`，且空集合也能生成表头文件 | L6 导出早于 L5 A4 统一事件闭环，未接入 schema/outbox | L6 当前筛选 → CSV → A2 + A4 → L5/J4/监管追溯 | 空结果服务端失败关闭；非空导出同事务写 A2 并发布 `BI_REPORT/admin.report_exported`，包含 reportId、筛选 scope、rowCount、无 PII、聚合脱敏、operator/reason/CSV；任一治理写失败不返回成功文件 |
| PCFULL-260 | M1 KPI 数字与落地队列口径错位 | P1 | 已修复源码，合同 7/7 与 TypeScript 通过，待统一 3002 部署后逐卡终验 | “进行中工单”数字排除 `pending_user`，但点击进入的 `scope=active` 会包含该状态；“待坐席回复”按最后消息发送人计算，却进入按 `unread>0` 过滤的 M3 队列 | 总览与目标模块分别推导相同业务口径，没有共享队列谓词 | M2/M3 权威快照 → M1 KPI → M2/M3 深链 | 工单 KPI 统一为 open/in_progress/pending_user 的“活跃工单”；待回复统一按未归档、进行中且 unread>0，与落地列表一一对应 |
| PCFULL-261 | M1 会话明细失败伪装为摘要成功 | P1 | 已修复源码，合同 7/7 与 TypeScript 通过，待统一 3002 部署后 500/畸形 200 注入终验 | 任一会话详情失败时前端静默回退列表摘要，仍把 `conversationsAvailable` 标为可用，M1 继续展示看似完整的会话 KPI | 工单明细已失败关闭，但会话仍沿用旧原型容错函数 `detailOrRow` | M3 列表 → 逐会话详情 → M1 会话 KPI/SLA/深链 | 会话明细改用 allSettled 全有或全无；任一失败即清空该事实集、`conversationsAvailable=false` 并显示“会话明细”同步告警，其他独立区块保持可用 |
| PCFULL-262 | M1 负载策略缺可见版本 CAS 与必达审计 | P1 | 已修复源码、迁移与定向合同，待统一 migration/8110/3002 后双运营员与 A2 终验 | 负载配置和手动均衡只有幂等键，没有 `expectedVersion`；两个运营员从同一旧页面提交可相互覆盖，审计使用非必达写入，审计失败仍可能返回业务成功 | 把“同命令重放”和“不同命令并发”混为一个问题，且 A2 被当作旁路日志 | M1 表单快照 → PATCH/POST → nx_config_item → A2 | 新增并物化 `content.support.load.version`；两条写链事务内 `SELECT ... FOR UPDATE` 比对 visible version，过期返回 409，成功递增版本；A2 改 `recordRequired`，失败回滚业务，审计记录前后版本与幂等键 |
| PCFULL-263 | M1 操作理由前后端边界不一致 | P2 | 已修复源码，合同 7/7 与 TypeScript 通过，待统一部署后 7/8/200/201 边界终验 | M1 负载、坐席分配和专属客服绑定前端允许 6 字；负载后端也无 200 上限，而坐席服务已要求 8–200，导致同页不同写动作表现不一致 | 前端沿用旧 6 字文案，负载服务没有复用横切原因边界 | M1 表单 → 负载/坐席接口 → A2 | 三类弹窗统一 8–200 字并设置 maxLength=200；负载服务强制 8–200，短/超长输入失败关闭，不产生配置或审计副作用 |
| PCFULL-270 | M2 未把稳定幂等键传入真实工单写接口 | P1 | 已修复源码，M2 合同 14/14 与 TypeScript 通过，待统一 8110/3002 部署后超时重试终验 | 容器虽为同一意图保留 `Idempotency-Key`，`writeTicketRows`、新建和升级会话调用却没有把该键传给 M2 API；首次请求结果未知后重试会生成新键，回复、状态或升级可能重复执行 | 幂等状态机只闭合到 M 容器，没有把 operation identity 穿透到域写客户端 | M2 页面意图 → M 容器未知结果重试 → 工单/会话/A2 | 新建、回复、状态、优先级、负责人、归档、内部备注和原子升级全部显式接收并复用容器稳定键；失败保留同一 value、基线、理由和 key，只有后端确认并成功回读权威快照后清除 |
| PCFULL-271 | M2 工单并发写缺 expectedStatus/version CAS | P1 | 已修复源码与迁移、补齐服务定向测试，PC 合同 14/14 与 TypeScript 通过；待统一 Maven、迁移/8110/3002 后双运营员终验 | 工单写只按 ticketNo 更新；两个运营员从同一旧详情发出不同幂等键时，后写会静默覆盖前写，状态、负责人、优先级、归档和回复均无并发冲突信号 | 把“同命令重放”误当成全部并发控制，数据库行没有可见版本 | M2 权威详情 → 任意工单写/M3 升级 → nx_support_ticket → A2 | 新增 `version` 迁移与详情字段；所有写请求强制携带可见 `expectedStatus + expectedVersion`，SQL 在单条 UPDATE 中比对并递增，缺失 422、陈旧或竞态 409；M2→M3 先 CAS 写工单跟踪，再同事务创建会话，任一步失败整体回滚 |
| PCFULL-272 | M2 HTTP 200 畸形/不完整分页仍可变成可写空队列 | P1 | 已修复源码，M2 合同 14/14 与 TypeScript 通过，待统一 3002 部署后畸形 200 注入终验 | 列表 HTTP 200 缺 records/total 或声称还有页却返回空页时，会被宽松适配成“0 条”；详情 HTTP 200 缺状态、版本、用户校验或消息字段时也会被补默认值，页面仍可能开放写操作 | 原型兼容转换被用于真实后端权威协议，只检查网络成功而未检查结构和不变量 | 8110 列表/分页/详情 → M2 展示与所有写动作 | 新增列表、分页完整性和详情全量协议校验；状态/分类/优先级/版本/userExists/消息任一歧义即抛错，详情全有或全无并令 `ticketsAvailable=false`，不显示伪空队列、不开放写入口 |
| PCFULL-273 | M2 内部备注与真实用户跨域边界未闭合 | P1 | 已修复源码与 PC 合同，待统一迁移/8110/3002 后真实用户、幽灵用户与备注可见性终验 | 页面没有独立内部备注入口；只要 ticket.userId 为正就开放 C1 支付方式、C/D/E 跨域链接和 M3 升级，后端升级也不确认该用户仍存在，幽灵 ID 可进入错误处置链 | 把正整数 ID 当作用户存在事实，并把内部协作文本与对客回复/系统轨迹混在同一载体 | M2 工单 → 内部备注/A2；M2 → C1/C/D/E/M3 | 详情由后端 `nx_user` EXISTS 返回 `userExists`，前端和升级服务双侧失败关闭幽灵用户；新增专权、幂等、CAS、A2 审计的 internal-note 端点，以独立 `internal` 消息类型渲染且明确不发送用户；抽屉同时显示分类 SLA 处置口径 |
| PCFULL-280 | M3 HTTP 200 畸形/残缺分页失败开放 | P1 | 已修复源码与合同，待统一 8110/3002 部署后畸形 200 注入终验 | 会话列表缺字段、非法枚举、重复会话号或声明还有数据却返回空中间页时，PC 会补默认值或把部分结果当完整队列；详情消息畸形仍可能进入可写工作台 | 原型兼容适配器被用于服务端权威协议，仅以 HTTP 成功判断事实可用 | 8110 会话列表/分页/详情 → M3/M1/Dock → 所有会话写动作 | 新增列表行、页元数据、总数/唯一性和详情消息全量校验；任何歧义或残页抛错，详情全有或全无并关闭 M3 写入口，不制造伪空或部分真相 |
| PCFULL-281 | M3 会话核心写缺可见版本 CAS | P1 | 已修复源码、迁移与合同，待统一 Maven/迁移/双运营员终验 | 回复、转交、接收、退回、等待、回落、状态、归档、批量归档与转工单只靠状态或无前态更新；不同幂等键从同一旧快照可重复追加消息或覆盖处置 | 把幂等重放当成并发控制，会话头没有可见 revision | M3 权威详情 → nx_conversation → M2/M1/SSE/A2 | `nx_conversation` 增加 version；所有核心请求携 `expectedStatus+expectedVersion`，批量携逐会话版本；SQL 单条 CAS 并递增，定时关闭同样递增，陈旧/竞态失败关闭 |
| PCFULL-282 | M3 队列/备勤接收与退回目标合同分叉 | P1 | 已修复源码与合同，待统一 8110/3002 部署后真实坐席终验 | PC 对队列/备勤转入显示可接收，但后端只允许指定 agent 接收；退回弹窗可选原坐席或备勤池，真实请求却不传 target，后端始终退原坐席 | UI 状态机比后端命令协议多两个分支，目标选择没有穿透到聚合根 | M1 坐席 → M3 transfer queue/standby → accept/return → M1 负载 | 当前可分配坐席可接收队列/备勤会话，指定坐席仍严格匹配本人；退回显式传 `from|standby`，后端按目标原子改 owner，系统消息与转交账本保持一致 |
| PCFULL-283 | M3 例行转交错误进入 A2 | P1 | 已修复源码与合同，待统一 8110 部署后 A2 负向终验 | 转交、接收、退回、等待、回落及自动回落均调用通用 A2 审计，且转交理由进入审计明细，违反 PRD “例行交接不进 A2”铁律 | 会话域复用了所有写动作通用审计助手，没有区分例行交接轻量账本与需审计处置 | M3 transfer family → nx_conversation_transfer/system message；不应 → A2 | 转交家族只写 `nx_conversation_transfer` 和会话系统消息，不调用 A2；回复、状态、归档、转工单等非例行写继续使用必达 A2，审计失败回滚业务 |
| PCFULL-284 | M3 持续接待 Dock 失败仍清空并报成功 | P1 | 已修复源码与合同，待统一 3002 部署后断网/超时终验 | Dock 回复未等待后端，立即清空草稿并提示“已回复”；网络失败或结果未知时用户丢失输入并误以为已送达 | Dock 绕过主工作台的 confirmed-write 闭环，把 Promise 发起等同成功 | M3 Dock → runMWrite 稳定幂等键 → reply API → 权威回读/SSE | Dock 改为等待 `setParam` 确认，发送中防重复；仅确认且回读成功后清空和提示，失败/未知保留原文并复用稳定命令身份重试 |
| PCFULL-290 | M4 HTTP 200 畸形知识响应失败开放 | P1 | 已修复源码，M4 合同 10/10 通过，待统一 3002 部署后畸形响应浏览器终验 | knowledge overview 返回 `{}`、缺字段、重复 FAQ ID 或不完整 SLA 时会被补成可写空视图 | 网络成功被误当成权威协议有效，原型 fallback 进入真实写链 | 8110 knowledge overview → M4 FAQ/SLA 写入口 → M1/M2 | 整体校验 FAQ、九类 SLA、目录数组、唯一 ID、枚举、版本与时间；任一歧义标记 knowledge unavailable 并关闭全部写入口 |
| PCFULL-291 | M4 FAQ 双运营员陈旧覆盖 | P1 | 已修复源码与合同，待统一 Maven/迁移/双运营员终验 | 编辑、发布、下架、删除只按 faqId 更新，不比较页面可见状态和版本 | 幂等只防同命令重放，无法裁决不同键的并发写 | M4 FAQ → nx_help_article → A2/A4/I6 | 所有 FAQ 写携 `expectedStatus+expectedVersion`；SQL 单条 CAS 并递增版本，缺失/漂移/竞态返回 409 且不写审计或事件 |
| PCFULL-292 | M4 SLA 无可见版本和 CAS | P1 | 已修复源码与 migration 290，待统一迁移/Maven/M1-M2 终验 | SLA 只有最后写入覆盖，PC 也看不到版本；两个运营员可静默覆盖队列、升级路径与时限 | `nx_support_sla_rule` 没有并发版本，SLA 被当作静态字典 | M4 SLA → M1 总览/M2 工单处置 | SLA 表增加 `version`；overview/PC 全链携带版本，更新 SQL 比较并递增，陈旧写 409，M1/M2 继续消费同一权威矩阵 |
| PCFULL-293 | M4 审计理由边界不一致 | P2 | 已修复源码与合同，待统一部署后 7/8/200/201 边界终验 | PC 要求 8 字而后端仅要求 6 字，且前后端均无 200 上限 | 同域不同入口没有共享不可变输入边界 | M4 FAQ/SLA → A2/A4 | FAQ 与 SLA 全部统一 8–200 字；短或超长理由在业务写、审计和 outbox 前失败关闭 |
| PCFULL-294 | M4 A2 非必达且 FAQ 缺 A4 治理事实 | P1 | 已修复源码与 migration 290，待统一迁移/Maven/A4 终验 | M4 使用 best-effort 审计，FAQ 生命周期没有 PRD 指定 `admin.support_faq_updated` outbox | 把操作日志当作旁路，把内容发布当成局部 CRUD | M4 FAQ → A2/A4 → M1/M2/I6 | A2 改 `recordRequired`；注册 canonical FAQ schema，create/update/publish/unpublish/delete 在原事务发布版本化事件，审计或 schema/outbox 失败即回滚业务 |
| PCFULL-295 | M4→M2 只有 SLA 文案、无权威期限/逾期裁决 | P1 | 已修复源码与合同，待统一 8110/3002 部署后跨时钟终验 | M2 仅把当前分类 SLA 显示成一行说明，没有服务端 target、首响/解决 deadline、规则版本和 overdue；不同浏览器可自行推导出不同结果 | M4 规则只进入展示层，没有进入工单详情权威派生协议 | M4 SLA → M2 工单详情/处置 → M1 KPI | 工单详情按当前 M4 分类规则版本在服务端计算首响/解决期限、实际首响/解决时间与逾期布尔值；缺规则返回 503；PC 严格校验并展示版本、两个目标时间和逾期状态，刷新重读同一服务端裁决 |
| PCFULL-300 | M5 HTTP 200 畸形响应失败开放 | P1 | 已修复源码，M5 合同 10/10 与 TypeScript 通过，待统一 3002 部署后畸形 200 浏览器终验 | 会话模板 overview 返回 HTTP 200 但缺类别、策略、受众、话术或模板字段时，PC 会补默认值/空数组并仍把 M5 写入口标为可用 | 把网络成功等同于权威业务协议有效，原型兼容转换进入真实写链 | 8110 `/session-templates/overview` → M5 展示/类别/策略/发布写入口 | 增加全有或全无协议校验：精确三类、策略范围、唯一受众、唯一 ID、状态机与只读 ai 任一不满足即清空 M5 事实、标记协议异常并禁用全部 M5 写入 |
| PCFULL-301 | M5 高风险变更缺 A4 治理事实 | P1 | 已修复源码与 schema migration 300，M5 合同 10/10 通过，待统一迁移/Maven/A4 运行态复验 | 类别、AutoPush、话术和模板变更只写配置/业务表与 A2 审计，A4/BI/M1/M3 无可订阅的服务端权威治理事实 | 把操作审计当作业务事件，M5 状态机未接 schema registry/outbox | M5 category/policy/script/template → A2 + A4 → M1/M3/BI | 注册五个 `admin.conversation_*` schema；成功写与 outbox 同事务，统一携目标、前后状态、认证运营员和理由，schema/outbox 失败回滚业务，不产生虚假成功 |
| PCFULL-302 | M5 发布未真实校验 I6 双语镜像 | P1 | 已修复源码与合同，待统一迁移/Maven/8110/3002 后 M5↔I6↔M3 真实发布终验 | 页面声称发布前校验 I6 双语镜像，但后端草稿可直接发布，I6 缺失、未发布、中文正文漂移或占位符不一致均不会阻断，M3 可消费未本地化内容 | 可见文案与服务端状态机分叉，M5 与 I6 没有共享稳定词条键和发布前置条件 | M5 draft → I6 published locale mirror → M5 published → M3 仅消费 published | 用 `conversation.script.<id>` / `conversation.template.<id>` 稳定键绑定 I6；发布前要求 zh/en/vi 已发布、中文正文一致且占位符集合一致，否则事务前失败关闭；M5 行内展示词条键和 I6 维护入口 |
| PCFULL-303 | 跨模块聚合编译 | P1 | 已修复，统一构建通过 | 汇总全部模块改动后，后端和 PC 曾因遗漏 helper/import 无法编译 | 并行模块改动分别自洽，但共享入口合并缺少聚合编译门禁 | 全模块候选包 | 补齐共享 helper/import；后端最终 2809 项测试与 package、PC `verify`/生产构建全部通过 |
| PCFULL-304 | 验收合同漂移 | P2 | 已修复，全量合同复验通过 | CAS、构造器注入和 UI 结构收口后，若干旧合同仍断言旧签名、旧 locator 或旧展示格式 | 验收脚本未随权威业务合同同步 | F1/F3/G3/H1/I6/K1/L3 等复验入口 | 逐项改为当前可见入口、数值等价、失败关闭和稳定表单作用域；PC 根测试 678/678、owner review 8/8 通过 |
| PCFULL-305 | F5 管理端入口/合同 | P1 | 已修复，真实浏览器复验通过 | 佣金生命周期页面的入口和服务端合同存在陈旧断言，首次走查不能稳定证明真实链路 | 页面收口后验收仍依赖旧 CTA/选择器 | F5 PC → 佣金事件/暂停恢复 | 对齐当前可见入口与权威接口；F5 真实部署 owner spec 2/2 通过 |
| PCFULL-306 | A2 审批哨兵 | P1 | 已修复，owner review 通过 | 旧哨兵仍允许过少的变更量，并把 `phase-current` 当作可接受审批目标 | 哨兵没有同步 A2 最新不可变约束 | A2 → D2/B1/跨域高风险操作 | 提升最小变更数、禁止 `phase-current`，要求 D2 立即业务闭环并验证 B1 覆盖率和幂等；8/8 通过 |
| PCFULL-307 | L1 migration producer 溢出 | P1 | 已修复，迁移两轮通过 | migration 写入的 producer 文案超过数据库列长度而中断 | 迁移值未遵守现有列宽合同 | L1 KPI 事件 → MySQL migration | producer 收口为稳定短码；26 个新增迁移首次执行和幂等重跑均 26/26 |
| PCFULL-308 | L6 MySQL DDL 不兼容 | P1 | 已修复，迁移两轮通过 | migration 使用 MySQL 8 不支持的 `ADD COLUMN IF NOT EXISTS` | 把其他数据库方言误用于 MySQL | L6 clientEventId/唯一索引 | 改为 `information_schema` 判断与动态 DDL，并对唯一索引做幂等保护；26/26 两轮通过 |
| PCFULL-309 | E1 设备 Mapper 启动解析 | P0 | 已修复，最终运行态通过 | 后端启动时 MyBatis 动态脚本因 Java 注解 SQL 中裸 `<` 解析失败 | XML 语义字符未转义/改写 | 后端启动 → E1 设备目录 → PC/App | 把比较式改写为不含裸 `<` 的等价表达；定向 121 项和最终 75 模块浏览器走查通过 |
| PCFULL-310 | L5 PRD owner-review 路径 | P2 | 已修复，8/8 通过 | owner review 指向已经迁移的旧 PRD 根目录，产生假缺失 | 脚本未从当前 `docs/PRD` 和产品 PRD 根推导路径 | L5 PRD 同步/签字审查 | 以 ops 当前目录和 `D:\nexion\PRD文档` 为权威根；dry-run 23 份均可解析，applyCheck 通过且未改写签字文档 |
| PCFULL-311 | M1/M2/M3 support schema 并发死锁 | P0 | 已修复，压力回归与全量复验通过 | 多个 M 域页面并发进入时，每个请求都执行 support 表 DDL/backfill，MySQL 出现 deadlock | `ensureSchema` 缺少进程内一次性初始化门闩 | M1/M2/M3 → support schema → MySQL | 用架构合规的 final gate 保证成功前串行、成功后只执行一次；8 并发回归仅初始化一次，最终日志 0 deadlock |
| PCFULL-312 | L3 流式下载 500 | P0 | 已修复，真实下载复验通过 | 下载接口返回 `ResponseEntity<?>`，Spring 把 `StreamingResponseBody` 当普通对象转换并返回 500 | 返回泛型丢失流式响应语义 | L3 报表 → L5 审批 token → CSV 下载 | 改为显式 `ResponseEntity<StreamingResponseBody>`，成功流与结构化错误流分别验证；控制器 5/5、L3 浏览器 4/4 |
| PCFULL-313 | F5 RBAC 路由缺失/误分类 | P0 | 已修复，权限与浏览器复验通过 | `/api/admin/commissions` 无规范授权规则，暂停佣金路由又被 user 前缀规则误拦截 | 路由前缀匹配早于更精确的网络佣金权限 | F5 PC → commissions / suspend | 增加精确 network RBAC 路由并调整匹配顺序，补 3 项后端回归与 PC 合同；F5 2/2 通过 |
| PCFULL-314 | F3 周期生效误导 | P1 | 已修复，真实浏览器复验通过 | 周期配置确认窗错误声称“立即生效”，与实际下一周期且不回溯相反 | 前端完成文案未消费后端生效语义 | F3 参数 → 下一结算周期 | 接通 completionCopy，明确“下一周期生效、不回溯”；F3 3/3 通过 |
| PCFULL-315 | G3 B1 覆盖率语义不清 | P2 | 已修复，真实浏览器复验通过 | G3 风险确认只写“覆盖率”，首次用户无法判断是 B1 备付金指标 | 跨域指标缺少域名和业务名 | G3 调参 → B1 红线 | 文案明确为“B1 备付金覆盖率”；G3 2/2 通过 |
| PCFULL-316 | L3 流式响应异步二次鉴权 | P1 | 已修复，最终日志复验通过 | 已认证的 `StreamingResponseBody` 首次请求成功后，容器 ASYNC continuation 被再次按普通请求鉴权，响应提交后抛 403/ERROR | 无状态安全链没有区分已授权 REQUEST 与容器管理的 ASYNC continuation | L3/L5 token 下载 → Spring Security → Tomcat | 原始 REQUEST 继续完整鉴权，仅放行不可由客户端伪造的 `DispatcherType.ASYNC` 续派；合同/架构/控制器 24/24，最终 L3 下载后日志 0 ERROR |
| PCFULL-317 | C5 session Mapper 重载冲突 | P1 | 已修复，最终启动与全量复验通过 | `countSessions()` 与 `countSessions(userId)` 生成同一 MyBatis statement id，启动记录 ERROR，用户筛选计数可能误用全局 SQL | MyBatis mapper 不支持以 Java 重载区分 statement id | C5 会话分页/计数 → MySQL | 用户范围查询重命名为 `countSessionsByUser` 并补 SQL 合同；最终启动日志 0 ERROR，后端 2809 项与 75 模块走查通过 |
| PCFULL-318 | C1 非规范手机号掩码与短号明文风险 | P1 | 已修复并完成统一构建、部署和真实接口复验 | 旧 Mapper 对长度小于 7 的手机号直接返回原值，历史非数字手机号还会生成不符合公开合同的伪掩码 | 数据格式异常被当成可安全展示，后端与 PC BFF 都缺少严格的脱敏输出边界 | C1 用户画像/CSV → PC BFF → 运营员 | 后端仅允许 `^[0-9]{7,15}$` 生成 `前三位****后四位`，其他值返回 `NULL`；PC BFF 递归清洗所有 `phoneMasked`，坏 JSON 502 失败关闭；最终直连 16 行为 9 个规范掩码、7 个 null、0 个非法输出，C1 与 75 模块终跑通过 |
| PCFULL-319 | App 匿名账户重绑请求风暴 | P0 | 已修复并完成全量测试、H5 构建和匿名运行态复验 | 匿名登录页账户重绑触发 V-Rank、VietQR 等认证读取，401 清理再次重绑全部 store，放大佣金配置刷新；实测 2.5 秒出现 604 次请求并可触发浏览器资源耗尽 | 无身份状态仍启动“当前用户”权威读取，401 teardown 与 store bind 形成反馈环 | PC E/F/I 配置 → App V-Rank/佣金/VietQR → 全局 401 handler | V-Rank、Deposits、Commission store 在无 access token 时不发认证业务请求，认证后才单次加载并复用 in-flight；App 233/233、类型检查、H5 构建通过，最终匿名探针仅五个公共配置各 1 次，受保护业务请求 0、request failure 0、pageerror 0 |
| PCFULL-320 | M3 SSE 客户端断开被改写为 JSON 500 与 ERROR | P1 | 已修复并完成延迟心跳运行态复验 | Playwright 关闭会话页面约 25 秒后，SSE 心跳向已断开的连接发送数据抛出 `IOException`；全局兜底处理器又尝试在已提交的 `text/event-stream` 响应中写入 `ApiResult`，每个断开产生 `HttpMessageNotWritableException` 和多条 ERROR | 同步 JSON 异常兜底被错误复用于已提交/ASYNC 流响应，没有把正常客户端断开与未提交的服务端 I/O 故障分开裁决 | M3 会话 SSE → 浏览器关闭/刷新 → 心跳调度 → 全局异常处理 | 新增 `IOException` 专项处理：响应已提交、ASYNC 派发或异步已启动时不再改写响应，仅按调试级记录；普通未提交 I/O 故障仍返回脱敏 500。TDD 修前编译红灯，修后专项 13/13、后端 2811 项通过；最终 6/6 浏览器完成后等待 40 秒覆盖延迟心跳，日志 0 ERROR、0 `HttpMessageNotWritableException`、0 committed-response 异常 |

## 统一终验状态覆盖声明

上表各行“待统一部署/待运行态复验”等措辞保留为缺陷发现当时的历史状态。2026-07-27 已完成统一迁移、统一构建、同一运行候选包部署、75 模块可见入口全量走查、重点 owner 套件和非 owner 对抗复审；对抗复审新增的 C1 脱敏与 App 匿名请求风暴，以及终局延迟观察发现的 SSE 断开处理问题，均已进入最终候选包并完整重跑。PCFULL-001 至 PCFULL-320 的 246 个唯一缺陷均已关闭，未关闭 P0/P1/P2/P3 为 0。

## 模块执行矩阵

| 域 | 模块 |
|---|---|
| A | A1、A2、A3、A4、A5、A6、A7、A8 |
| B | B1、B2、B3、B4、B5 |
| C | C1、C2、C3、C4、C5、C6 |
| D | D1、D2、D3、D4、D5、D6 |
| E | E1、E2、E3、E4、E5、E6 |
| F | F1、F2、F3、F4、F5 |
| G | G1、G2、G3、G4、G7 |
| H | H1、H2、H3、H4、H5、H7、H8 |
| I | I1、I2、I3、I4、I5、I6 |
| J | J1、J2、J3、J4 |
| K | K1、K2、K3、K4、K5、K6 |
| L | L1、L2、L3、L4、L5、L6 |
| M | M1、M2、M3、M4、M5 |

## 结论

通过。75 个模块均已完成修复后全流程重跑、独立 owner 初审/复审和非 owner 对抗复审；246 个唯一缺陷全部关闭，未关闭 P0/P1/P2/P3 均为 0。
