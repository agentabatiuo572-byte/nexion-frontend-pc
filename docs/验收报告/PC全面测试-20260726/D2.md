# D2 提现审核队列验收归档

- 模块/入口：D2 提现审核队列，`/finance/withdrawals`
- Owner/独立证据：`D:\workspace\bug-pic\20260725-domain-reacceptance\d-initial\D域初审报告-20260725.md`、`D:\workspace\bug-pic\20260725-domain-reacceptance\d-rereview\D域独立对抗复审报告-20260725.md`
- 当前结论：**2026-07-25 锁定组合通过；其后 D5/J1/J4/K3/K4/K5/L3 与高位用户 ID 修改均进入 D2 核心资金链，当前组合为“源码闭环、待统一部署终验”，不得沿用历史 PASS。**

## 当前缺陷关联

- `PCFULL-028`：D2 已补 D5 网络费率/min/max/实际费用快照，待运行态复盘历史单。
- `PCFULL-173`：J1 提现闸已前移到用户提交的任何资金副作用之前，待真实资金入口复验。
- `PCFULL-180`：J4 经 A2 maker-checker 编排 C2/K1/D2，待破坏性夹具终验。
- `PCFULL-194`：高位用户 ID 截断仍有全仓清零前置，D2 不得出现串号。
- `PCFULL-200/201/207`：K4 脏模型失败关闭、D2 合法空队列验收、畸形 K5 信号拒绝均已修，待统一部署。
- `PCFULL-227`：新增唯一 `SENT/CHAIN_SUBMITTED → CONFIRMED` CAS、链上哈希与 `withdraw.confirmed` A4 发生期；待迁移、8110 与真实链上回执终验。

## 源码合同与调用链

- 当前静态合同：`tests/d2-withdrawal-closure-contract.test.mjs`、`tests/d1-d6-vietnam-payment-contract.test.mjs`；浏览器载具：`tests/e2e/d-final-independent-20260725.spec.ts`。
- 权威链：App 提交 → J1/J2/D5/C2/C4/K3/K4/K5 → D2 状态机 → D4 钱包/账本、D3/B1 → A2/A4 → L3 兑付发生期；J4 可经 maker-checker 调用 C2/K1/D2。
- 详情必须保留创建时费率、费用组件、K3 路由、K4 模型/评分、K5/C4 裁决和完整状态历史，不用当前配置伪造历史。

## 权限、CAS 与失败关闭

- 匿名 401；读取、复核、延迟、发送/确认及高风险动作权限分离，无权写 403 并有 A2 拒绝审计。
- 所有状态迁移使用前态 CAS 和稳定幂等键；CONFIRMED 只允许指定前态，链上哈希必填、不可变、单提现唯一。终态重放无新增账本、审计或 outbox。
- K3/K4/K5/C4/D5/J1 任一来源缺失、矛盾或畸形时资金链失败关闭；K5 非法 userNo、金额、来源单号或理由不能降级为“不需复审”。
- 空集合是合法业务态但不能掩盖加载失败；500/超时/畸形 200 必须清旧事实、禁用动作。刷新/退出重登后责任人、持有期、路由和状态历史保持。

## 统一部署锁定复验

1. 应用最新迁移并清零 `PCFULL-194` 全仓高位 ID 风险，再用 11 位用户 ID 创建真实提现。
2. 复验 J1 关闸零副作用、D5 费用快照、K3 六路闭集、K4 脏模型、K5 合法/畸形信号。
3. 完成提交→审核/延迟→发送→链上确认；核对 A2、A4 `withdraw.confirmed`、D4、D3/B1、L3 跨月发生期。
4. 覆盖 maker-checker、CAS、同键重放/冲突、空队列、401/403/404/409/422/500、刷新重登；非 Owner 完整复审。
