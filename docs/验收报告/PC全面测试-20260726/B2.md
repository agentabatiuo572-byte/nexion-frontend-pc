# B2 资金池水位验收归档

- 模块：B2 资金池水位
- 可见入口：`/overview/liquidity`
- 当前结论：**历史 B 域双轮 99/99；本轮 B2 liquidity owner 表面走查 passed，当前合同 6/6 通过。由于 D3/L3 与多项负债生产者后续变更尚未统一部署，当前组合仍需重新核对九类负债与 7/30 日预测。**

## 1. 历史与本轮证据

- 更新日志记录 B2 已建立独立储备、八类原负债加待核实入金的现行九来源、7/30 日到期预测和配置接口；零到期时不把哨兵值解释为覆盖天数。
- 历史 owner/非 owner 99/99。
- `artifacts/pc-full-20260726/B2-liquidity-agent/.last-run.json` 为 `passed`。
- `backend-nine-categories-frontend-fail-closed.png` 记录九类别与前端失败关闭证据。

## 2. 当前源码合同

`tests/b2-liquidity-acceptance-contract.test.mjs` 6/6 通过：

- 独立严格客户端读取 D3 canonical reserve、liabilities、maturity、config、export。
- 九类 key、合计和累计预测不变量受校验。
- 页面读取失败清除陈旧财务事实并提供重新加载。
- 7/30 天窗口、事实来源和 D3 深链可发现。
- 零到期不制造覆盖天数。
- read/write/export 精确权限、reason、expectedVersion 和 Idempotency-Key 已锁定。

## 3. 调用链与不变量

```text
D3 储备 + 九类负债 + 到期事实
  -> B2 结构/预测
  -> B1 覆盖率分母
  -> L3 财务报表
```

- B2 不重算 D3，只展示同源事实。
- liability 明细和总计、maturity 累计、窗口日期必须算术一致。
- 配置于声明的 UTC 边界生效；旧版本、重复请求和未知结果不得覆盖。
- 读取失败或畸形 200 必须清旧数据，不能显示上次水位为当前。
- 导出权限不随读权限自动扩大。

## 4. 缺陷关联

- PCFULL-017 涉及 D6 与 B2 资金口径链，需统一复验。
- PCFULL-228 涉及 L3 对 B2 负债/日期/协议的严格交叉校验。
- D3 与各资金生产者的待部署修复同样可能改变 B2 真值。
- 本报告未新增或修改缺陷编号。

## 5. 统一部署后复验

1. 逐来源制造九类负债，核对列表、合计、B1 和 L3。
2. 核对 7/30 天提现、利息、Genesis 到期叠加；零到期显示不可计算而非伪天数。
3. 配置写覆盖权限、8/200 边界、CAS、同键重放、未知结果和生效日。
4. 注入缺类别、重复类别、负数、总计不符、非累计预测、503 和跨日时区。
5. 导出与页面当前切片一致；刷新/重登后服务器真值不漂移。
6. 非 owner 完成首次用户和墨菲复审。
