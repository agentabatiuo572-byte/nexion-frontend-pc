# D1 充值对账中心验收归档

- 模块/入口：D1 充值对账中心，`/finance/recon`
- Owner/独立证据：`D:\workspace\bug-pic\20260725-domain-reacceptance\d-initial\D域初审报告-20260725.md`、`D:\workspace\bug-pic\20260725-domain-reacceptance\d-rereview\D域独立对抗复审报告-20260725.md`
- 当前结论：**历史锁定组合初审 98.8、非 Owner 复审 99.2（D 域 310/310）通过；`PCFULL-001`–`004` 后续修复已在 2609 项全量包通过。当前最终组合仍须统一部署后从可见入口复跑。**

## 缺陷与证据边界

- `PCFULL-001`：现行 VietQR 状态域与迁移合同不一致；已修复并随全量包通过。
- `PCFULL-002`：服务与 Mapper 新签名不同步；已修复，D1 聚焦 16/16、全量 2609 通过。
- `PCFULL-003`：`MATCHED + OPEN` 待核实入金被 D3 漏算；已修复，D3/账本合同 7/7、全量通过。
- `PCFULL-004`：终态 intent 第二笔回单测试时钟自相矛盾；已修正事件时钟并通过。
- 本报告只复用已存在运行证据；没有在当前共享工作树执行 Maven、build、重启或新浏览器走查。

## 源码合同与调用链

- 当前静态合同：`tests/d1-finance-closure-contract.test.mjs`、`tests/d1-d6-vietnam-payment-contract.test.mjs`；浏览器载具：`tests/e2e/d1-live-reacceptance.spec.ts`、`tests/e2e/d-final-independent-20260725.spec.ts`。
- 权威链：App VietQR intent → D1 五视图/银行回单 → 匹配、按实收核销或退回 → 钱包/累计入金/D4 账本 → D3 待核实入金 → B1/L3；A2/A4 留痕，D6 锁价只读参与。
- 银行账号密文落库、页面只显示尾四位；到账分类使用银行 `receivedAt` 与越南业务日，不能用运营点击时间改写。

## 权限、CAS 与失败关闭

- 匿名 401，FINANCE/AUDITOR 的菜单、按钮、接口权限一致；无权写 403 并有拒绝审计。
- 配置写用版本 CAS；回单使用唯一 `paymentReference`/`evidenceRef` 和稳定幂等键。终态 intent 的新流水必须形成独立 LATE 回单，不得重开原 intent 或重复入账。
- 同键重放、终态新键重复、旧版本、非法金额/时间/账户、500 与畸形响应均须关闭写入口；钱包、累计入金、D4、A2/A4 任一步失败整笔回滚。
- 历史证据覆盖刷新、退出重登及恢复；当前组合仍需重新执行。

## 统一部署锁定复验

1. 用干净真实 intent 走五视图：精确、孤儿、差额、迟到、在途；完成匹配/核销/退回。
2. 验证 `RECEIPT_REVIEW`、`LATE_REVIEW`、终态第二笔回单、越南跨日与账户熔断。
3. 核对 D4 钱包/账本、D3/B1/L3 待核实入金、D6 锁价、A2/A4 完全一致。
4. 覆盖 401/403/409/422/500、CAS、同键同体/异体、结果未知、刷新重登；非 Owner 完整复审后签收。
