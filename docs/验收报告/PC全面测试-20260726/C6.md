# C6 注册与登录风控验收归档

- 模块/入口：C6 注册与登录风控，`/users/reg-risk`
- Owner 证据：`D:\workspace\bug-pic\c-domain-sequential-acceptance-20260720-041515\c6\C6-最终验收报告.md`
- 当前结论：**历史锁定组合通过；`PCFULL-185` 已改变真实注册与 K1 的边界，须统一部署后以真实注册夹具重验。**

## 历史缺陷与证据

- 历史 owner 修复 2 个 P1（GROWTH 缺只读权限、三类事件误登记为 `phase_risk`）和 1 个 P2（只读页面仍暴露写控件）。
- 修后首次用户 43/43，评分 99.2/99.7；独立技术核心链两轮 10/10，评分 99/99；后端全量 1940/1940、C 域 PC 合同 38/38、App 67/67。
- 当前 `PCFULL-185` 已修注册 OTP/IP、K1 24h 上限与 H8 风险阻断源码，但仍待统一 8110/3002 和真实注册/支付夹具终验。

## 源码合同与调用链

- 当前静态合同：`tests/c6-registration-risk-closure-contract.test.mjs`；浏览器载具：`tests/e2e/c6-live-reacceptance.spec.ts`。
- 权威链：C6 短锁/长锁/CAPTCHA → App 登录；注册 OTP IP + 设备决策 + C2 支付工具 → K1 三层聚类 → C2/K4/J3/H8/D4/A4；系统恢复审计进入 A2。
- `auth.login_locked` 等事件必须属于 A4 `risk` family；C6 与 K1 的参数边界清晰，PC 不能把本地阈值当注册裁决。

## 权限、CAS 与失败关闭

- GROWTH/客服只读可见但不能 PATCH；写控件与处理函数双重裁剪，无权直写 403。
- C6 配置用服务端 version CAS 和稳定幂等键；CAPTCHA 自动恢复同样写系统审计，陈旧版本/并发输家不得覆盖。
- 注册事务在创建身份前锁 K1 参数行；OTP IP 缺失/未知、配置非法或达到 24h 上限时失败关闭且不创建半用户。
- 500、超时、慢响应、畸形 200 必须清空旧策略并关闭写入口；刷新/退出重登后重新读取权威版本。

## 统一部署锁定复验

1. 从侧栏验证短锁、长锁、CAPTCHA、自动恢复、GROWTH 只读、A2/A4 risk 归类。
2. 以真实双账号和同 IP 并发注册验证 K1 24h 上限、缺 IP 失败关闭及用户零残留。
3. 串联设备、C2 支付工具、K1/H8 奖励阻断，核对钱包/D4/A4 零副作用。
4. 覆盖 CAS、同键重放/冲突、500/慢/畸形响应、刷新重登，并由非 Owner 重跑双轨复审。
