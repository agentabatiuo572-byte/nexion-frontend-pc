# D6 法币/USDT 汇率配置独立验收报告

## 结论

- D6 本轮最终 **PASS**。服务端权威报价、PC 配置写入、App VietQR 读价、D1/D3/D4/B1/B2/A2/A4 调用边界均已闭环。
- 缺陷发现后完成最小修复，并从可见入口重走 D6；配置最终恢复为 `base=26000 VND/USDT`、`spread=1.50%`、`lockWindow=30min`、`version=12`。
- 修复后初审 **98.2/100，通过**；独立对抗复审 **99.1/100，通过**。
- 当前无 D6 未关闭 P0/P1/P2/P3。

## 验收范围与环境

- PC 管理端：`http://127.0.0.1:3002`
- 后端：`http://127.0.0.1:8110`
- App H5：`http://localhost:5174`
- 可见入口：登录页 → 资金与财务 → D6 法币/USDT 汇率配置
- 覆盖范围：当前牌价、基准价/点差/锁价窗口修改、相同值拒绝、公式精度、CAS、幂等重放、畸形成功响应、读失败关闭、刷新/退出/重登、App 运行态、D1/D3/D4/B1/B2/A2/A4 上下游边界。

## 首轮真实用户走查

### 已通过

1. 从可见登录页进入 D6，刷新、退出并重新登录后仍能恢复页面和权威配置。
2. 当前报价严格满足 `round(base × (10000 + spreadBps) / 10000 / 10) × 10`：`26000 × 1.015 = 26390 VND/USDT`。
3. 可见页面写入 `26000 → 26010` 返回 200，报价变为 26400、版本递增；随后恢复到 26000。
4. 以同一幂等键重放恢复请求，两次均返回 200，版本和历史条数不重复增长。
5. 使用真实旧版本提交不同新值返回 409；页面给出中文版本冲突指引并停止展示旧事实。
6. 服务端相同值提交返回 422 `FX_QUOTE_NO_CHANGES`，版本不增长。
7. 注入公式不成立的 200 响应时，PC 清空旧牌价并显示“数据不完整或单位不可信”；恢复权威响应后可重新读取。
8. D6 的 D1、A2、A4 可见入口均能到达真实页面；A4 `admin.fx_quote_updated` schema 为 ACTIVE、revision 109。
9. 匿名访问 PC BFF 与后端 D6 接口均返回 401；读接口 500 时页面失败关闭，恢复后重新读取成功。
10. 数据库显示 D6 outbox 已 PUBLISHED，consumer group `d6-fx-quote-observer` 为 SUCCESS、`attempt_count=1`、`last_error=null`。
11. A2 同时保留成功 `FX_QUOTE_UPDATED` 和拒绝 `FX_QUOTE_VERSION_CONFLICT`、`FX_QUOTE_NO_CHANGES`，包含 before/after、原因、幂等键。
12. D6 调价只写配置、历史、A2、A4，不写 D4；App 创建 intent 只建立意向/在途事实，只有 D1 真实入账后才允许产生 D4 账本。

### 发现并修复的缺陷

| 级别 | 缺陷 | 根因 | 修复 |
|---|---|---|---|
| P2 | D6 相同值提交虽然被正确拒绝，但确认弹窗只显示通用错误，用户看不到“参数值未变化” | `OperationConfirmModal` 捕获异常后无条件覆盖为 generic 文案 | 新增 `operationConfirmErrorMessage()`：非空 `Error.message` 原样展示；空白/未知异常保留 generic fallback；弹窗继续保持开启并保留输入 |
| P1 | App H5 clean restart 后 `useStaking/useCommission().bindAccount is not a function`，阻断真实登录与充值页 | Pinia setup-store 初始化被中断后，注册表可能残留只有框架字段的 partial store | 新增 `bindAccountStore()`：检测缺失绑定契约时仅 dispose/recreate 一次；重建仍缺失则 fail-closed 抛明确错误 |

## TDD 与修复证据

### PC ActionConfirm

先新增失败测试，首次运行因实现模块不存在而红；再完成最小实现。

```powershell
node --experimental-strip-types --test tests/operation-confirm-error-message.test.mjs tests/k2-arbitrage-contract.test.mjs
```

结果：**9/9 通过**。覆盖具体错误优先、空白 Error fallback、未知异常 fallback，并保持 K2 既有异常消费合同。

### App partial Pinia store

先新增重现测试，首次运行因恢复函数不存在而红；再完成一次性重建和失败关闭。

```powershell
npm.cmd exec vitest run src/lib/account-store-bind.test.ts
```

结果：**3/3 通过**。覆盖正常绑定、partial store dispose/recreate、重建失败时 fail-closed。

## 自动化结果

### PC

```powershell
node --experimental-strip-types --test `
  tests/operation-confirm-error-message.test.mjs `
  tests/k2-arbitrage-contract.test.mjs `
  tests/d1-d6-vietnam-payment-contract.test.mjs `
  tests/rbac-platform-contract.test.mjs
.\node_modules\.bin\tsc.cmd --noEmit
```

- 契约测试：**36/36 通过**
- TypeScript：**0 error**

### App

```powershell
npm.cmd exec vitest run `
  src/lib/account-store-bind.test.ts `
  src/api/runtime-config.test.ts `
  src/api/payment-api.test.ts `
  src/store/deposits-remote.test.ts
```

- 运行时/支付/intent/账号绑定：**16/16 通过**
- 第二次 clean restart 后，以全新浏览器上下文进入 App：`pageerror=0`、`console error=0`，真实登录页完整渲染。
- App 全局 `vue-tsc --noEmit` 当前被并发 D5 改动阻断：`withdrawal-api.ts` 引用了未导出的 `WithdrawalRiskRoute`，且一处 `row` nullable 未收窄；这两个文件不属于 D6 修复，已交主 Agent 合并 D5 后统一复跑。

### 后端

- D6/App VietQR/outbox 聚焦套件：**64/64 通过**。
- 覆盖配置写入、相同值拒绝、CAS、公式、历史、A2、A4 schema/payload、App 新单锁价、consumer 幂等及 outbox 调度。

## 真实浏览器与数据库证据

- D6 CAS 冲突：`D:\workspace\bug-pic\20260727-d6-cas-custom2.png`
- D6 最终恢复：`D:\workspace\bug-pic\20260727-d6-final-cont.png`
- A4 schema：`D:\workspace\bug-pic\20260727-a4-cross-link.png`
- D6 读失败关闭：`D:\workspace\bug-pic\20260727-d6-reverify-snake-schema\06-d6-read-failure-fail-closed.png`
- App 修复前 clean restart 复现：`D:\workspace\bug-pic\20260727-d6-app-clean-restart-fresh.png`
- App 修复后全新上下文：`D:\workspace\bug-pic\20260727-d6-app-after-fix-fresh.png`

## 独立对抗复审

非 D6 owner 子智能体按墨菲定律复核 D4/D6/App 交界：

1. D6 更新不产生 D4 账本，避免把“报价变化”伪装成资金变化。
2. App intent 创建只写 intent/在途核对，不写钱包或账本。
3. 只有 D1 `MATCH_CREDIT/WRITE_OFF` 在钱包 CAS 成功后产生账本。
4. 未发现 D6 跨域 P0/P1。

## 评分

- 修复后初审：**98.2/100**。扣分项仅为共享工作树的 App 全局类型检查被 D5 并发改动阻断，不属于 D6 逻辑。
- 独立复审：**99.1/100**。公式、CAS、幂等、失败关闭、A2/A4、D4 隔离和 App clean restart 均通过。
- 任务完成恢复 10 点血量；血量按上限保持 **100/100**。
