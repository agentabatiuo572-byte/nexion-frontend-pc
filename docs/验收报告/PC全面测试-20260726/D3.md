# D3 资金池水位仪表盘验收归档

- 模块/入口：D3 资金池水位仪表盘，`/finance/pool`
- Owner/独立证据：`D:\workspace\bug-pic\20260725-domain-reacceptance\d-initial\D域初审报告-20260725.md`、`D:\workspace\bug-pic\20260725-domain-reacceptance\d-rereview\D域独立对抗复审报告-20260725.md`
- 当前结论：**历史锁定组合通过；`PCFULL-003` 已在后续全量包关闭。D4、G1/G2、L3 等后续负债与报表修改仍要求统一部署后重算九类负债和覆盖率。**

## 缺陷与历史证据

- `PCFULL-003`：旧合同排除 `MATCHED + OPEN` 回单，导致 D1 已匹配但未核销的资金不进入“待核实入金”；已改为所有有金额的 OPEN 回单进入 D3，聚焦 7/7、全量 2609 通过。
- 历史初审验证 D1 三笔处置使待核实入金从 6 USDT 归零；独立复审验证 103.85 USDT 核销后该科目归零。
- 历史 D3 注资同凭证重放返回 409，不重复增加储备、审计或事件；D 域非 Owner 总复审 310/310、99.2。
- 当前关联 `PCFULL-010/013/017/081/082/084/228` 会改变 D3/D4/质押/兑换/汇率/L3 的负债或展示边界，均须当前组合复验。

## 源码合同与调用链

- 当前静态合同：`tests/d3-treasury-closure-contract.test.mjs`、`tests/d1-d6-vietnam-payment-contract.test.mjs`；浏览器载具：`tests/e2e/d-final-independent-20260725.spec.ts`。
- 权威链：D1 OPEN 回单、D2 提现队列、G1 质押本息、G2 兑换、Genesis/佣金/锁仓/钱包等九类硬负债 → D3；真实储备注入 → B1 覆盖率/净敞口 → B2/L3；D4、D6、A2/A4 提供账本、折算和追溯。
- trial shadow、前端 seed、PENDING 本地推算不得混入储备或硬负债。

## 权限、幂等与失败关闭

- 匿名 401；查看与储备注入权限分离，AUDITOR 只读，无权注入 403 并有拒绝审计。
- 注资使用不可重复凭证、稳定幂等键与服务端事务；同凭证或同键重放不得重复储备、A2/A4 或 B1 变化。
- 九类来源、汇率、金额或时间窗任一缺失/负数/溢出/畸形时失败关闭，不以 0 或健康状态掩盖不可计算。
- 注入、D1 核销和下游 B1/L3 更新必须在刷新/退出重登后仍由服务器回读一致。

## 统一部署锁定复验

1. 用干净夹具逐项核对九类负债，特别是 D1 `MATCHED + OPEN` 待核实入金、D2 队列、G1 本息和 G2 资金。
2. 执行一次真实储备注入、同凭证/同键重放及并发，核对 D3→B1/B2/L3、A2/A4。
3. 注入来源 500、缺字段、负数、超大金额、D6 坏报价与跨日窗口，确认失败关闭且不显示伪健康。
4. 恢复配置和夹具，刷新、退出重登，再由非 Owner 按当前组合完成初审/复审。
