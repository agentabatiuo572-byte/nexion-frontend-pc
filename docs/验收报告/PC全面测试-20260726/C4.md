# C4 KYC 合规台账验收归档

- 模块/入口：C4 KYC 合规台账，`/users/kyc`
- Owner 证据：`D:\workspace\bug-pic\c-domain-sequential-acceptance-20260720-041515\c4\C4-最终验收报告.md`
- 当前结论：**历史锁定组合通过；G2/K4/K5 后续修改直接消费或回写 C4，当前组合必须统一部署后重验。**

## 历史缺陷与证据

- 历史 owner 修复 P3 实现型文案和 P1 `admin.kyc_status_changed` 的 A4 schema 类型错误。
- 修后首次用户初审/复审 99/99，独立技术/墨菲 99.2/99.4；后端 C4/K5/Outbox 145/145、PC 合同 5/5。
- 当前关联：`PCFULL-084`（G2/App 必须消费 C4 权威 KYC）、`PCFULL-200`（C4 事实进入 K4）、`PCFULL-205`（K5 不可覆盖 G2 并发终态）、`PCFULL-207`（畸形触发信号必须失败关闭）。均待统一部署真实链终验。

## 源码合同与调用链

- 当前静态合同：`tests/c4-kyc-closure-contract.test.mjs`；浏览器载具：`tests/e2e/c4-live-reacceptance.spec.ts`。
- 权威链：C4 KYC 台账 → D2/G2 资格判断；D2/G2 大额事实 → K5 工单/裁决 → C4 回写；C4 风险事实 → K4；监管导出 → L5；全部进入 A2/A4。
- 页面不得用当前 KYC 配置重写历史状态，证据编号、操作者、版本和裁决链必须可追溯。

## 权限、CAS 与失败关闭

- 读、状态变更、复审、网络白名单和监管导出权限分离；无权写 403。
- 状态变更携 `expectedState`/版本和稳定幂等键；旧前态 409 时保留表单，禁止覆盖并发终态。
- K5 只有从预期来源状态成功 CAS 后才可回写 C4；信号缺 userNo/source/amount/reason 时整条 D2/G2 事务失败关闭。
- A4 schema/outbox、A2 或下游回写失败不得伪报 KYC 成功。刷新、退出重登后状态、版本和导出任务从服务器重取。

## 统一部署锁定复验

1. 从侧栏完成 KYC 状态变更、陈旧前态冲突、K5 触发/合并/裁决和监管导出。
2. 并发推进 G2 终态后再裁决 K5，必须整体失败且不回写 C4/A2 假成功。
3. 注入空/负/畸形 D2/G2 信号、A4 schema/outbox 失败、权限与未知结果，确认零副作用。
4. 核对 App 兑换、D2、G2、K4、K5、L5、A2/A4，刷新/重登后由非 Owner 复审。
