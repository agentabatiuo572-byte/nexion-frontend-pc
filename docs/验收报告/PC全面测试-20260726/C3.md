# C3 余额与资产调整验收归档

- 模块/入口：C3 余额与资产调整，`/users/assets`
- Owner 证据：`D:\workspace\bug-pic\c-domain-sequential-acceptance-20260720-041515\c3\C3-最终验收报告.md`
- 当前结论：**历史资金闭环通过；D4 后续账本修复改变了下游证据合同，当前组合待统一部署后重新做账实终验。**

## 历史缺陷与证据

- 历史 owner 修复 2 个 P1：maker 可自审；已批准 DEBIT 的追加式冲正被当前 B1 覆盖率错误阻断。
- 修后首次用户双轮各 27 项通过，初审/复审 97/99；独立技术双轮均 6/6，评分 99.0/99.2；后端聚焦 161/161、PC 合同 4/4。
- 统一缺陷表没有为这两个旧问题补 `PCFULL-*`。当前关联 `PCFULL-010`：D4 用户下钻、结算状态和时间范围已修但仍待当前组合完整复验。

## 源码合同与调用链

- 当前静态合同：`tests/c3-adjustment-closure-contract.test.mjs`。
- 权威链：C1 精确选人 → C3 maker 发起 → checker 批准/驳回 → 钱包与 D4 追加式账本 → B1 覆盖率 → A2/A4；冲正引用原调整单且生成新单，不改写历史。
- NEX 折算使用创建时服务端价格/美元价值快照；冲正沿用原单 `amountUsd`，不使用当前价格伪造历史。

## 权限、CAS 与失败关闭

- maker/checker 必须不同主体且服务端大小写无关校验；无权/自审失败不得改变余额、调整单、D4 或事件。
- 创建、审批、驳回、冲正使用稳定幂等键和前态 CAS；同原单只允许一笔有效冲正，并发输家失败关闭。
- 新增 CREDIT 的放大流出方向服从 B1；恢复性冲正不得被当前覆盖率阻断。
- 钱包、D4、A2、A4 任一步失败整笔回滚；结果未知先查权威单据再用原键重试。刷新/重登后仅显示服务器持久事实。

## 统一部署锁定复验

1. 从可见入口完成小额即时调整、大额 maker-checker、同人自审拒绝及追加式冲正。
2. 在 B1 低覆盖率和 NEX 当前价不可用下复验 DEBIT 冲正仍按原单恢复。
3. 核对钱包、D4 Running Balance、A2/A4 一致；注入 D4 畸形/500、双击、陈旧前态与未知结果。
4. 精确冲正恢复夹具，刷新、退出重登并由非 Owner 完成当前组合复审。
