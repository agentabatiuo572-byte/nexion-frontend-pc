# B4 节奏状态验收归档

- 模块：B4 节奏状态
- 可见入口：`/overview/rhythm`
- 当前结论：**历史 B4 owner/非 owner 99/99 并完成 B4↔H1 双向闭环；当前源码合同 4/4 通过。`pc-full-20260726` 下未找到 B4 本轮独立 surface 目录，因此不虚构当前运行态 passed，必须统一部署后补跑。**

## 1. 历史证据

- 产品更新日志记录 B4 读取 H1 Phase、当前月、八个旋钮和分布，只做只读归因。
- CSV 文件名包装和 H1“去 B4 节奏看板”误跳 B5 已修复，B4↔H1 双向闭环。
- 历史 B4 owner 初审/非 owner 复审 99/99。
- 本轮未发现 `artifacts/pc-full-20260726/B4-*` 的 machine-readable owner 结果；此证据缺口留待统一终验补齐。

## 2. 当前源码合同

`tests/b4-rhythm-closure-contract.test.mjs` 4/4 通过：

- B4 有独立失败关闭客户端和精确 phase BFF。
- 页面展示 Phase/月粒度、8-dial、V1 生效状态、拐点和归因，且不提供修改 dial。
- 后端使用 `GrowthRhythmFacade`，read/export/jump 精确 RBAC 并必达审计。
- H1 回程路径固定为 `/overview/rhythm?from=B4...`，不再误跳 B5。

## 3. 调用链与不变量

```text
H1 权威节奏快照
  -> B4 Phase 分布/八旋钮/拐点
  -> B3/B1/B2 归因链接
  -> 导出与 A2 审计
```

- B4 只读，不复制 H1 dial 写入或恢复能力。
- Phase、月序号、旋钮、分布和 attribution links 必须来自同一版本快照。
- 读取失败清除陈旧归因，不把旧 Phase 当当前。
- 导出与跳转有独立权限，不能因 read 自动放权。
- 刷新/重登后仍从 H1 当前快照读取。
- B4 不拥有业务写，幂等/CAS 不适用；导出审计和跳转审计仍不得因重复点击重复记录。

## 4. 缺陷边界

- 当前缺陷总表没有新增 B4 专属编号。
- H1 的后续 PCFULL-105 等改动以及共享 3002/8110 变更仍需 B4 回归。
- 本报告未新增或修改缺陷编号。

## 5. 统一部署后复验

1. 从登录页和可见侧栏进入，补齐 `.last-run.json`、trace 和页面截图。
2. 核对 Phase/月粒度、八旋钮、分布、拐点、V1 状态和 H1 同一快照。
3. 逐一点击 B3/B1/B2/H1 归因链接，验证 B4↔H1 正确回程。
4. 用 read-only、无 export、无 jump 账号验证菜单/按钮/接口。
5. 注入 H1 缺字段、版本矛盾、503、导出失败和刷新重登，确认失败关闭。
6. 非 owner 执行初审与墨菲复审后才能标当前组合通过。
