# B1 双账本总览验收归档

- 模块：B1 双账本总览
- 可见入口：`/overview/dual-ledger`
- 当前结论：**历史 B 域锁定组合已完成 owner/非 owner 双轮 99/99；本轮 B1 表面走查 passed，当前合同 4/4 通过。由于后续 D/E/F/G/H/J/L 多项资金修复会改变 B1 分子、分母或红线门禁，当前组合须统一部署后重新进行账实终验。**

## 1. 历史与本轮证据

- 产品更新日志记录 B1–B5 曾在锁定 BUILD/JAR 上全部通过，B1 固化“真实储备 ÷ 应付负债”、7/30/90 日净敞口、阈值、D3 储备注入、健康告警和聚合导出。
- 历史 B1 owner 初审/非 owner 复审 99/99。
- `artifacts/pc-full-20260726/B1-agent/.last-run.json` 为 `passed`，并保存 `B1-live-page.png`。

## 2. 当前源码合同

`tests/b1-dual-ledger-acceptance-contract.test.mjs` 4/4 通过：

- 覆盖率与净敞口使用服务器事实，不制造前端趋势。
- 红/黄线、D3 储备注入和对账导出链存在。
- 储备注入明确是流入，不误标为放大流出。
- 阈值、注入和告警确认在结果未知重试时复用同一幂等键。

## 3. 权威调用链

```text
D1 真实到账 / D3 储备与九类负债
  + D2/D4/G/F/H 等资金事实
  -> B1 coverage = reserve / liabilities
  -> 全域放大流出 server gate
  -> B5/L3 只读引用
  -> A2/A4 追溯
```

- B1 是覆盖率裁决唯一口径；其他域不能复制计算。
- B1 只展示/预警，不自动替用户执行全局熔断。
- 红线前置必须在目标域服务器再次执行；PC 镜像不能替代。
- 阈值写须理由、权限、CAS、幂等和必达审计。
- 分母异常、来源缺失或除零不得显示伪造健康值。

## 4. 现行缺陷关联

- 已闭环历史：PCFULL-003。
- 待统一部署复验的重要关联：PCFULL-024、034、044、057、067、081、082、085、090、096、114、119、121、122、131、146、174、176、177、228。
- 这些修复会影响资金流出方向、负债或展示，不能因 B1 历史 99/99 自动通过。
- 本报告未新增/修改缺陷编号。

## 5. 统一部署后复验

1. 以干净资金夹具核对 D1→D3→B1 储备、九类负债、覆盖率和净敞口。
2. 覆盖红线下的放大/收紧方向：放大 422，止损方向可执行。
3. 双运营员修改阈值，验证 CAS、同键重放/冲突、未知结果和 A2。
4. D3 注入必须增加储备且不被标成流出；对账导出与页面一致。
5. 注入单来源 500、坏数字、零负债、巨大金额和跨日窗口，确认失败关闭。
6. 核对 B5/L3 只读引用一致，刷新/重登不漂移；非 owner 重跑初审/复审。
