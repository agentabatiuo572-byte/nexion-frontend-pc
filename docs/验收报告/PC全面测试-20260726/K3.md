# K3 提现风控规则引擎验收报告

## 结论

K3 源码修复和定向验证通过，发现并修复 PCFULL-195～197 共 3 个 P1 缺陷；当前不冒充最终运行态通过。现有 3002/8110 仍是统一部署前旧产物：HTTP 200 畸形响应仍表现为旧的宽松解析，真实 App 提现又被旧 8110 的 `A4_SCHEMA_REQUIRED_PROPERTY_MISSING` 拦截。须统一部署当前源码与迁移后重跑本文锁定的完整脚本，六项全部通过才把模块改为“最终通过”。

## 范围与真实调用链

- PC：可见侧栏进入 K3 → `/api/admin/risk/withdraw-rules/*` → `OpsRiskService` → `RiskOpsRepository` → MySQL。
- 用户提现：App 金额/绑定地址 → J2 地域门 → J1 提现闸、D5/H1 权威策略 → K4 当前分 → K3 四维规则 → K5 大额复审 → D2 提现单、D4 账本 → A2 审计、A4 outbox → B1/B5 运营读面。
- K3 原生维度是金额、24h 速度、新账户、地址信誉。设备证据由 K1/J3 汇入 K4，地区限制由 J2 在入口裁决；没有把设备/地区伪装成 K3 第五、第六条规则。
- 覆盖权限、四维结构化表单、优先级与严格路由、命中原因、dry-run、CAS、幂等重放、未知结果、归档终态、刷新、退出重登、依赖 503、非法规则、第三方信誉服务不可用、K4 陈旧分和真实资金零副作用。

## 缺陷闭环

| 编号 | 问题 | 修复 | 验证 |
|---|---|---|---|
| PCFULL-195 | PC 把畸形 K3 响应补成可操作假数据，dry-run 畸形成功也显示完成 | overview 严格校验必需对象、数组、字符串、动作/状态枚举、布尔、版本/优先级、分页及 routeTotal；dry-run 畸形成功按未知结果并复用原命令键 | K3 合同 10/10、PC TypeScript 通过；新增真实浏览器 HTTP 200 畸形读写注入用例 |
| PCFULL-196 | App 未知 `riskRoute` 默认映射 `pass` | 收紧为后端六种精确路由闭集，未知值在 canonical 投影前失败关闭 | App API 6/6，新增 `teleport-pass` 红灯后转绿 |
| PCFULL-197 | App remote 提交仍被本地 mock K3 横幅和 `canSubmit` 干扰 | remote 只使用权威策略、绑定和基础输入门；mock K3 只保留显式 mock 模式 | App 源码合同 1/1、`vue-tsc --noEmit` 通过 |

## 首次用户真实浏览器证据

证据目录：`D:\workspace\bug-pic\pc-full-20260727\K3\final-predeploy`

- AUDITOR、FINANCE 从可见侧栏进入，只读按钮禁用，后端直调写入 403：通过。
- RISK 完成四维表单、只读 dry-run、422 边界、创建草稿、启用、编辑动作/阈值/优先级并刷新持久化：通过。
- K4 当前/陈旧、K3 非法规则、地址信誉 provider 缺失均失败关闭且不动资金：通过。
- 真实 App 小额 K3 人工路由、大额 K3+K5 叠加及 D2/A4/B1/B5：当前旧 8110 在首笔 `withdraw.submitted` 被 A4 必需属性校验拦截，未形成业务单，判为部署漂移而非通过。
- 新增 HTTP 200 畸形读写注入：当前旧 3002 未加载新严格解析，预期红灯；统一部署后必须转绿。
- 每次脚本退出均清理临时管理员、用户、规则、订单、审计、outbox、会话和幂等记录；数据库复核三类夹具计数均为 0。

## 自动验证

- `node --test tests/k3-withdraw-rules-contract.test.mjs`：10/10。
- `npx tsc --noEmit`（PC）：通过。
- `npx vitest run src/api/withdrawal-api.test.ts`（App）：6/6。
- `node --test tests/withdrawal-k3-canonical.contract.test.mjs`（App）：1/1。
- `npx vue-tsc --noEmit`（App）：通过。
- 按父任务的统一构建约束，本模块没有并发执行 Maven、全量 build 或重启服务。

## 统一部署后唯一复验命令

```powershell
$env:ADMIN_E2E_PASSWORD='[REDACTED: set locally]'
$env:K3_DB_PASSWORD='[REDACTED: set locally]'
$env:K3_EVIDENCE_DIR='D:/workspace/bug-pic/pc-full-20260727/K3/final'
npx.cmd playwright test tests/e2e/k3-final-acceptance-20260722.spec.ts --project=chromium --workers=1 --trace=on --reporter=line
```

放行条件：六项全部通过；无未处理 page error/console error/5xx；临时夹具清零；PCFULL-195～197 运行态复验转绿；再由非 K3 修复者复核权限绕过、未知结果、CAS、归档终态、刷新/重登与 App 资金零副作用。

## 初审、复审与血量

- 源码/合同初审：97/100，通过。
- 墨菲复审：99/100，通过；重点复核畸形 200、503、响应丢失同键重试、陈旧版本、未知路由、mock/remote 双源和 A4 schema 漂移。
- 运行态最终评分：待统一部署后六项全绿再记分。
- 当前血量：100；未以旧运行态失败冒充任务完成。
