# C2 账户操作验收归档

- 模块/入口：C2 账户操作，`/users/actions`
- Owner 证据：`D:\workspace\bug-pic\c-domain-sequential-acceptance-20260720-041515\c2\C2-最终验收报告.md`
- 当前结论：**历史锁定组合通过；J4/K1 后续修改进入了 C2 调用链，当前组合待统一部署后完整复验。**

## 历史缺陷与证据

- 历史 owner 修复 2 个 P1：MySQL 左到右赋值覆盖 D2 冻结前状态；不同幂等键并发模拟登录生成两条 ACTIVE 会话。
- 修后首次用户初审/复审 97/99，独立技术/墨菲 99.0/99.4；真实并发为 200/409 且 ACTIVE 精确为 1，D2 状态可从 `FROZEN|PENDING` 恢复为 `PENDING`。
- 当前关联：`PCFULL-180`（J4 必须经 C2 权威 gateway 执行账户处置）与 `PCFULL-185`（C2 支付工具事实进入 K1 三层证据）；两项均为“源码已修、待统一部署运行态终验”。

## 源码合同与调用链

- 当前静态合同：`tests/c2-account-actions-contract.test.mjs`；浏览器载具：`tests/e2e/c2-live-reacceptance.spec.ts`。
- 权威链：C1 精确用户 → C2 冻结/恢复、名单、模拟登录 → C5 会话吊销、D2 待处理提现；C2 支付工具 → K1；J4 → A2 maker-checker → C2/K1/D2；结果进入 A2/A4。
- C2 只能通过服务端状态机改变账户；四页模拟登录保持只读，不得产生真实资金或用户副作用。

## 权限、CAS 与失败关闭

- 匿名 401，读写权限分离；绕过页面的无权写必须 403 并进入拒绝审计。
- 冻结/恢复、名单和模拟登录使用稳定幂等键及服务端锁/CAS；同用户并发只能有一条 ACTIVE 会话，终态重放不得复活。
- 权威 overview、D2/C5/K1 任一来源异常、畸形或结果未知时关闭危险动作，不沿用旧账户状态。
- 刷新、返回、退出重登后账户、D2 原状态、名单和模拟会话均从服务器回读。

## 统一部署锁定复验

1. 从 C1 可见深链进入 C2，执行冻结→刷新→重登→恢复，核对 D2 原状态与 C5 会话。
2. 双击和不同键并发模拟登录，确认 ACTIVE 唯一、终止/到期不可复活。
3. 通过 J4 maker-checker 执行受控 C2 动作，核对 A2/A4、部分成功边界和 D2 联动。
4. 以真实注册/支付夹具核对 C2 支付工具进入 K1；覆盖 401/403/409/500/畸形 200 后由非 Owner 复审。
