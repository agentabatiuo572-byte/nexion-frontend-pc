# C1 检索与画像验收归档

- 模块/入口：C1 检索与画像，`/users/search`
- Owner 证据：`D:\workspace\bug-pic\c-domain-sequential-acceptance-20260720-041515\c1\C1-最终验收报告.md`
- 当前结论：**历史锁定组合已通过双轨验收；当前合并组合须统一部署后重跑，本文不把 2026-07-20 的运行态冒充当前运行态。**

## 历史缺陷与证据

- 历史 owner 闭环了 4 个 P2：画像响应暴露内部会话字段、快速双击重复导出、非法分页被静默归一、同秒导出关联号冲突。
- 修后首次用户初审/复审为 98/99，独立技术/墨菲初审/复审为 99/100；后端 C1 聚焦 170/170、PC 合同 4/4、真实 Playwright 1/1。
- 上述旧缺陷未回填 `PCFULL-*` 编号，本报告不另造编号。当前关联缺陷为 `PCFULL-200`（K4 脏模型不得传播到 C1）和 `PCFULL-273`（M2 幽灵用户不得开放 C1 深链），均须跟随最新组合包复验。

## 源码合同与调用链

- 当前静态合同：`tests/c1-acceptance-contract.test.mjs`；浏览器载具：`tests/e2e/c1-live-reacceptance.spec.ts`。
- 权威链：精确检索 → 360 画像 → C2/C3/C4/C5；脱敏导出 → A2/A4；K1/K2/C4/J3 行为事实 → K4 当前风险分 → C1 画像；M2 仅在 `userExists=true` 时开放用户深链。
- 列表、详情和导出必须使用服务端事实；恶意 `returnTo` 收口同源，CSV 防公式注入，原始手机号和内部 token/session id 不得回显。

## 权限、幂等与失败关闭

- 匿名 401；角色裁剪须同时作用于菜单、按钮和接口。导出要求专权、稳定幂等键；同键同体只生成一份文件/审计/outbox，同键异体冲突。
- 非法页码、页容量和 offset 溢出须在访问仓储前 422；详情、列表或导出响应缺字段/畸形/500 时清除旧事实并关闭导出，不以空画像冒充成功。
- 刷新、退出重登后检索上下文和服务端画像可重新取得，不依赖 localStorage 业务事实。

## 统一部署锁定复验

1. 从登录页和可见侧栏进入 C1，完成检索、筛选、分页、360 画像和脱敏导出。
2. 注入非法分页、畸形 200、500、未知用户、越权与双击；核对失败关闭、幂等和 A2/A4。
3. 注入 K4 矛盾活动模型，C1 必须拒绝展示风险分；M2 幽灵用户不得出现 C1 深链。
4. 刷新、退出重登并由非 Owner 重跑首次用户与墨菲复审后，方可签署当前组合 PASS。
