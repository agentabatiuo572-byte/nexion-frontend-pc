# A8 权限字典验收归档

- 模块：A8 权限字典
- 可见入口：`/platform/permissions`
- 当前结论：**历史 A6/A7/A8 联动验收通过；本轮 A8 首次受登录会话恢复竞态失败，rerun passed，并保存严格协议、详情损坏和竞态证据；当前共享 RBAC 合同 21/21 通过。统一部署后需再次验证全量分页与跨角色失败关闭。**

## 1. 历史与本轮证据

- 更新日志记录 A8 严格分页、服务端全量权限字典、`UNMAPPED` 显式归类、溢出/响应损坏/失败清旧态、读写权限分离。
- 历史联动 Playwright 1/1、独立首次用户 100/100、墨菲复审 99/100。
- `A8-agent/.last-run.json` 初次 failed，根因是登录按钮在服务端会话恢复时被卸载，属于 PCFULL-006 类竞态。
- `A8-agent-rerun/.last-run.json` 为 `passed`。
- 深度证据：
  - `A8-agent-deep/evidence.json` 及可见入口/搜索/高风险筛选截图。
  - `A8-agent-corrupt-detail/corrupt.json` 与错误码截图。
  - `A8-agent-race/race.json` 与关闭抽屉竞态截图。

## 2. 当前源码合同

共享 RBAC 合同 21/21 通过，证明：

- 权限与菜单授权来自后端，显式空集合不回退。
- A6/A7/A8 成功协议畸形时不渲染伪空态。
- 只读用户所有 mutation 均无副作用，重试保留输入。
- 路由守卫和实际 authority 一致，陈旧别名不扩大权限。

## 3. 调用链与不变量

```text
A8 权限码字典
  -> A6 角色授权候选
  -> A1 账号角色
  -> effective authorities
  -> A7 菜单/PC 路由
  -> 后端接口最终鉴权
```

- A8 是权限码真值，不允许前端写死或把未知码静默丢弃。
- 分页 total、records、页界和分类统计必须一致；不完整 200 整页失败关闭。
- 禁用/失效权限不能进入 A6 新授权；既有引用由 A6/A7 关联保护处理。
- 无权、读取失败、快速筛选/详情竞态必须撤销旧详情和写入口。
- 刷新与重登后重新读取服务端字典。
- A8 当前为只读字典，幂等/CAS 不适用；任何角色授权写必须由 A6 承担。

## 4. 缺陷与边界

- PCFULL-006 已修复会话恢复竞态，A8 rerun 有通过证据。
- PCFULL-229 新增 L3 细粒度导出权限，是 A8 当前字典终验必须确认的新增跨域权限之一。
- 其余本次新增 M/L/K 等权限也必须核对字典、A6、菜单和接口四侧一致。
- 本报告未新增或修改缺陷编号。

## 5. 统一部署后复验

1. 用 superadmin、只读审计和无权临时账号从侧栏进入。
2. 遍历全部分页并核对 total、分类、`UNMAPPED`、详情与搜索。
3. 注入缺 records/total、重复 code、空中间页、坏详情、500/503 和慢响应竞态，确认清旧态。
4. 用 A6 授权新增细粒度权限，A1 重登后验证页面/按钮/接口三层。
5. A8 所有写方法对只读账号必须零副作用。
6. 非 owner 执行初审和同范围墨菲复审。
