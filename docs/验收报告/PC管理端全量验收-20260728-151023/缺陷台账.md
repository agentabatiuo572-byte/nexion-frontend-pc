# PC 管理端 A–M 全量验收缺陷台账

- Run ID：`pc-full-acceptance-20260728-151023`
- 状态口径：只有经过真实入口完整复验的缺陷才能关闭。

| 编号 | 域/模块 | 等级 | 复现 | 根因 | 影响链 | 实际修复 | 复验结果 |
|---|---|---|---|---|---|---|---|
| A-001 | A5 平台参数寄存器 / G1 Staking | P1 | M→A 非 Owner 在锁定候选 Build `q2cNmpfQ_JFRzTMXb2oiR`、JAR `199B656A…97F9D` 上从登录页和可见侧栏进入 A5，`GET /api/admin/platform/params-registry` 持续返回 500；同一认证会话直接重试 3 次仍为 500，后端日志稳定出现 `A5_OWNER_MAPPING_MISSING:G.staking.apy.usdt30d` | G1 真写已在 `nx_config_item` 生成 `G.staking.*` 服务端参数，但 A5 的 owner 单源只登记 G2/G3/G4，没有登记 G1 参数族；严格失败关闭因此让一条合法新参数阻断整个 A5 | A5 全量参数目录主流程不可用；G1 新增任意 Staking 参数都会切断 A5→G1 归属跳转及跨域参数治理 | 增加 `G.staking.* → G1 /finance-products/staking` 参数族映射；新增单测以当前 `G.staking.apy.usdt30d` 和未来任意 `G.staking.*` 锁定整个参数族，避免只修单键 | **已关闭**：最终候选 Build `zTGbuKtb03fA0de7etQ1C`、JAR `743C3F00…3D6A` 上 A5 从登录页和可见侧栏首轮、返回、刷新、退出重登均为 200；当前参数与未来参数族单测通过，页面与 API 均确认 owner=`G1`、route=`/finance-products/staking`；A1–A8 表面 `8/8`、A5 故障恢复 `1/1`，非预期 5xx 为 0 |
| A-AUTO-003 | A1–A8 表面锁定登录载具 | P3 | 最终候选 A6 表面轨退出后首次重登，按钮点击没有产生 `POST /api/admin/auth/login`，载具等待 shell 20 秒后失败；trace 中只有匿名 session GET，未发生产品登录请求或业务副作用；立即使用全新上下文重跑通过 | 生产构建首次加载时登录表单已可见但客户端提交处理器尚未完成 hydration；旧 helper 只等待 DOM 可见，未等待可观测登录请求，也不会在零请求时重新执行用户点击 | 造成一次 A6 表面轨假阴性；产品 A6 页面、登录接口及权限状态没有失败或漂移 | 登录 helper 在可见提交后等待精确 `POST /api/admin/auth/login`；若 5 秒内完全没有请求且 shell 未恢复，则重新填写并再次真实点击，最多两次；不使用固定睡眠、不忽略真实 4xx/5xx | **已关闭**：新 helper 在最终候选全新 Chromium 中 A6 首轮、刷新、退出重登 `1/1`；随后 A6–A8 完整权限/maker-checker 主载具另以全新账号、会话和夹具 `1/1`，无产品写入漂移 |
| A-AUTO-001 | A6/A7/A8 验收基础设施 | P1 | superadmin 创建临时角色后，同一账号审批删除提案，后端返回 `403 A2_MAKER_CHECKER_REQUIRED`；后续侧栏脚本在已展开时反复折叠，动画期间阻断链接点击 | 脚本未跟进独立 maker/checker、CAS/服务端临时密码契约，且导航 helper 无条件切换折叠状态 | 阻断 A 域权限夹具闭环及 D/I 权限轨 | 已改为独立 checker，补齐 CAS、服务端临时密码、旧夹具恢复；侧栏限定在 `aside`，仅入口不可见时展开并等待 | 新 Run 1/1 通过（43.8 秒）；遗留角色、菜单、工单及活跃测试账号清零 |
| A-AUTO-002 | A4 验收基础设施 | P3 | 临时可逆写脚本把 Day0 从 `90 秒` 调为 `91 秒` 后，用无空格的 `91秒` 断言服务端真值，误判失败并在回滚前中断 | 脚本未按服务端规范化后的“数字 + 空格 + 单位”格式比较 | 验收载具产生一次短暂配置漂移；产品写入与规范化本身正常 | 中断后立即从可见 A4 页面恢复为 `90 秒`；脚本目标值和回读断言统一为服务端规范格式，并保留最终回滚检查 | 最终 A4 前向 `90 秒 → 91 秒`、回滚 `91 秒 → 90 秒`、刷新、退出重登全部通过；页面错误和非预期 5xx 均为 0 |
| B-001 | B3 转化漏斗 / B4 节奏状态 | P1 | superadmin 从登录页和可见侧栏进入 B3/B4；故障注入返回 HTTP 200、`code=0`、`data={malformed:true}`，页面没有进入模块失败关闭，而是触发 Next 全局“This page couldn’t load” | B3/B4 客户端仅把 TypeScript 类型当运行时协议，成功响应未校验五级漏斗、八个 H1 dial、筛选、来源与跨域链接的完整性 | 网关或后端协议漂移时，B3/B4 整页崩溃；运营无法区分真实空态与数据契约损坏，B3→A4/L2/H1、B4→H1/B1/B2 的跨域判断中断 | 新增 B3/B4 深层运行时协议；B3 要求完整五级同用户漏斗和来源，B4 要求精确八个 H1 dial 与四条归因链接；畸形成功响应清空旧快照并进入模块失败关闭 | **已关闭**：锁定候选 `goWbiGaLpmFEiZfiVwSmm` 上运行时合同 `4/4`、B 域合同 `22/22`、可见入口 Owner `4/4` 通过；B3/B4 畸形 200 均模块内失败关闭，移除注入后恢复，刷新和重登无回归 |
| B-002 | B2 预测配置 / B3 保存视图 | P1 | 审查未知结果分支发现：B2 每次提交都用时间戳生成新 key；B3 每次保存视图同样生成新 key，且后端控制器不读取 `Idempotency-Key`。代理超时后按原输入重试无法安全判定重放，同键异载荷也没有后端持久拒绝 | PC 没有保存“载荷指纹 + 当前 command key”；B3 代理未标注带 key 写请求的未知结果；B3 服务未接入 `AdminIdempotencyService` | B2 全局资金预测与 B3 服务端视图在网络中断时可能重复执行；B3 同 key 不同载荷无法拒绝，A2 审计和运营状态可能重复或歧义 | B2/B3 PC 均保留未知结果 key，仅原载荷重试复用，载荷变化生成新 key并提示先刷新核对；B3 代理增加 20 秒边界和 unknown header；B3 后端接入 `B3_FUNNEL_VIEW` 幂等域，以管理员和规范载荷 SHA-256 作为请求哈希 | **已关闭**：统一 Maven/PC 质量门、unknown `2/2`、B3 真实幂等 `1/1`、B2 双运营员 CAS/幂等/恢复 `1/1` 通过；B2/B3 本 Run 业务与幂等残留为 0 |
| B-AUTO-001 | B3 未知结果验收基础设施 | P3 | 首轮载具在视图名称为空时先断言“保存为视图”按钮启用；修正顺序后又只把真实空态的 `available` 改为 `true`，没有同时提供运行时协议要求的完整五级 stage，按钮继续正确禁用 | 故障注入没有满足页面真实前置条件和 B3 深层响应协议，且交互步骤顺序错误 | 阻断 B3 unknown 分支的自动化证据，不影响产品；产品按钮和运行时协议均正确失败关闭 | 先填写视图名称再断言按钮；异常分支注入独立、完整、明确标记为 `acceptance-fault-injection` 的五级 stage，仅用于触发 POST 结果未知，不替代主流程权威数据 | **已关闭**：锁定候选上 B2/B3 unknown 全轨 `2/2` 通过；同载荷重试复用 key，修改载荷生成新 key；主流程仍以真实服务端空态裁决 |
| B-AUTO-002 | B2 双运营员验收基础设施 | P3 | K 域刚使用完共享 `d_checker` 后，B2 首次登录复用了同一 30 秒 TOTP step，后端正确返回 `ADMIN_MFA_CODE_REPLAYED`，CAS 请求尚未发出 | 载具只避开临界点，没有把跨 Agent 已消费的 TOTP counter 建模为共享棘轮状态 | 短暂阻断 B2 双运营员轨；产品 MFA 正确失败关闭，且 D3 本 Run 幂等记录为 0、配置未写 | 新建浏览器 challenge 后强制等待下一 TOTP 时间步，再由独立 checker 登录；运行前核对零写入后完整重跑 | **已关闭**：B2 CAS/幂等/恢复 `1/1` 通过；配置恢复，本 Run 幂等清零，Build/PID/JAR 无漂移 |
| C-001 | C2 账户操作总览 | P1 | superadmin 从登录页和可见侧栏进入 C2；将 `/api/admin/users/account-actions/overview` 故障注入为 HTTP 200、`code=0`、`data={malformed:true}` 后，客户端会把缺失数组和统计当作空数组/零值继续渲染；首轮修复候选虽显示错误，但模拟登录、信任名单和禁入名单三个全局写入口仍可操作 | `fetchUserAccountActionOverview` 仅用 TypeScript 类型接收响应，C2 渲染层又以 `?? []` 和数值默认值吞掉运行时协议缺失；原错误提示没有在模块渲染边界统一阻断旧数据及写控件 | 网关或后端协议漂移时，冻结账户、活跃会话、名单和模拟登录会被误判为真实空态/零值，运营可能基于不可信快照继续执行 C2 高风险处置；C2→D2/K1/A2/A4 链路失真 | 已增加 `requireC2Overview` 运行时协议，严格校验六类数组、九项统计、来源/红线及关键记录身份；同时在 C2 模块渲染边界对 `!overview` 提前返回，仅保留失败说明与重新加载，统一隐藏全部数据和写入口 | **已关闭**：锁定候选 Build `9hoJ5Kpe5kMgLKBZr_EIk` 从可见侧栏完成 C1–C6 故障/恢复轨 `1/1`；C2 畸形 200 时旧数据与全部危险写入口均隐藏，解除注入后真实恢复；合同 C2+C5 `21/21` |
| C-002 | C5 安全与会话失败关闭 | P1 | superadmin 从可见侧栏进入 C5；畸形 200、503 或超时初始显示 `C5 数据加载失败`，约 260ms 后错误消失并把全局会话、2FA、锁定和凭证参数显示为全 0/空数组 | 无关键词的用户搜索清理 effect 无条件 `setError(null)`；它与总览读取并发，误把 overview 协议/网络错误当作搜索错误清除 | 运营会把 C5 权威状态不可用误判为真实零会话/零锁定/空参数；虽然用户级写按钮因无目标而禁用，但 C5→C2/C6/K5 的安全判断已经失真 | 空搜索清理只清除 `C5 用户搜索失败`，不得清除 `C5 数据加载失败` 或 `C5 操作失败`；保持既有 `setOverview(null)` 与写入口禁用 | **已关闭**：锁定候选 Build `9hoJ5Kpe5kMgLKBZr_EIk` 独立 C5 轨 `1/1`；畸形 200、503、timeout 均持续失败关闭并禁用写入口，解除注入后真实 200 恢复，pageerror/非预期 console 为 0；合同 C2+C5 `21/21` |
| C-003 | C3 余额与资产调整 | P1 | maker 从可见 C3 入口真实扣减 0.01 USDT，两个独立运营员并发冲正得到唯一成功并恢复余额；随后以成功方相同 `Idempotency-Key` 和完全相同载荷回放，后端错误返回 `409 C3_ALREADY_REVERSED` | `reverseAssetAdjustment` 在进入 `idempotentC3` 查询已提交结果前先执行 `assetAdjustmentHasReversal` 可变状态守卫；首次成功命令产生的冲正记录反过来阻断自己的安全回放 | 代理超时或响应丢失后无法用原 key 安全确认结果，运营可能把已成功冲正误判失败；若改用新 key 又只能依赖状态守卫拒绝，破坏 C3 结果未知闭环 | 将“已冲正”守卫移入 `idempotentC3` supplier，使同 key+同载荷优先命中已提交结果；同 key 异载荷仍由幂等层 409，新 key 重复冲正仍由对象状态 409；补同键回放与异键拒绝单测，无 API/DTO/Schema/migration 变化 | **已关闭**：新 JAR `935E94C8…20665` 从可见 C3 入口完整重跑 `1/1`；真实扣减、双运营员冲正 `[200,409]`、同键同载荷回放 200、同键异载荷 409、新键重复冲正 409、D4/A2/A4/outbox/delivery 均通过；钱包恢复 USDT `100.000000`，本轮调整、账本、幂等、审计、outbox、delivery 精确清零 |
| C-AUTO-002 | C1 故障注入验收载具 | P3 | C1 统计 `/api/admin/users/overview` 被注入畸形 200 时，载具把仍由独立真实 `/api/admin/users/profiles` 支撑的“导出脱敏视图”误判为必须禁用的危险动作 | 载具按页面整体而非接口数据所有权划分失败关闭边界，混淆了统计快照和用户列表两条独立权威链 | 造成 C1 对抗用例假失败；产品仍使用真实用户列表生成脱敏导出，不存在 mock、旧快照或不可信数据写入 | 将 C1 从“总览失败即禁用全部按钮”的通用断言中剥离，继续单独校验统计失败关闭，并保留真实用户列表及脱敏导出链路 | **已关闭**：验收断言已按接口所有权收窄，无产品代码变更；后续全量复跑继续分别监控 `/overview` 与 `/profiles` |
| C-AUTO-003 | C 域权限轨退出重登载具 | P3 | menu-no-write 完成退出后立即重登时，页面已恢复同账号 shell，载具仍继续等待登录表单字段/按钮并超时 | 登录助手只在循环入口判断 shell；账号状态恢复可能发生在用户名、密码或提交按钮三个等待窗口内 | 造成权限轨假失败并阻断后续 C-001/C-002 故障用例；未触发业务写，账号权限和产品登录结果均未漂移 | 在用户名、密码、登录按钮各等待边界重新检查已认证 shell；若服务端已恢复目标账号会话则收敛到会话形状校验，否则继续真实表单登录 | **已关闭**：锁定候选 menu-no-write 与 no-menu 两条权限轨各 `1/1`，刷新、退出重登、五层权限均通过 |
| C-AUTO-004 | C5 超时故障注入载具 | P3 | C5 请求被 `route.abort("timedout")` 注入超时时，载具仍使用只接受 HTTP response 的通用开页助手，等待 20 秒后假失败 | 网络中止只产生 `requestfailed` 事件，不会产生 HTTP response；载具把网络失败与 4xx/5xx 响应错误混为同一种等待模型 | 阻断 C5 超时失败关闭及真实恢复断言；产品已经进入错误态，未发生业务写 | 新增超时专用可见侧栏开页助手，按精确 URL/GET 等待 `requestfailed`，随后继续校验失败关闭、解除注入和真实 200 恢复 | **已关闭**：锁定候选 C1–C6 故障/恢复轨 `1/1`，C5 timeout 按 `requestfailed` 到达且真实响应恢复 |
| C-AUTO-005 | C5 隔离故障轨选择与重入 | P3 | 以宽泛 `--grep C5` 单跑故障轨时误命中标题含“C5/C2/A2/A4”的 C2 可逆写用例；同一 C5 页面内第二轮再点击相同侧栏 href 又未重新挂载请求 | 命令选择器没有锁定唯一测试标题，且故障循环错误假设点击当前路由会重新执行数据加载 | 意外多执行一次已恢复的 C2 ACTIVE→FROZEN→ACTIVE，并造成 C5 第二轮故障假失败；用户最终 ACTIVE、冻结来源为空，新增幂等已精确清零 | 后续以当前文件唯一“畸形”标题选择 C5；每轮注入前先回到 `/`，再经可见侧栏重新进入 C5，确保组件和请求均重新挂载 | **已关闭**：唯一“畸形”标题独立重跑 `1/1`；malformed 200、503、timeout 三轮全部通过；误触发 C2 最终为 ACTIVE，冻结来源/引用为空，幂等残留 0 |
| C-AUTO-006 | C5 故障轨控制台判定 | P3 | malformed 200、503、timeout 三轮功能断言和真实恢复均已执行，但载具把登录前匿名 session 的预期 401，以及主动 `route.abort("timedout")` 产生的 `net::ERR_TIMED_OUT` 当作非预期控制台错误 | 控制台监控启动早于登录完成，且白名单只覆盖 500/503 与 `ERR_FAILED`，没有按故障注入模型区分预期网络中止 | 造成已通过的 C5 失败关闭链在末尾假失败；没有隐藏任何登录后非预期 401/5xx | 将监控移到成功登录之后；仅为本用例主动注入的 `ERR_TIMED_OUT` 扩展过滤，登录后的 401 和其他控制台错误仍判失败 | **已关闭**：独立 C5 故障轨 `1/1`，功能与恢复通过，pageerror 和非预期 console 均为 0 |
| D-AUTO-001 | D1–D6 验收基础设施 | P3 | D 域锁定脚本 3 项中 2 项因旧文案定位和 CAS 提示 strict mode 双命中失败 | 脚本仍定位旧文案“过期后到账”，且未将 CAS 提示限定到唯一作用域 | 阻断 D 域自动化证据，不影响产品失败关闭行为 | 已更新稳定可见语义定位、收紧 CAS 断言作用域，并扩展 D2/D4/D5 权威读取与恢复覆盖 | D1–D6 页面 6/6、扩展 Playwright 4/4、PC 合同 65/65、App 28/28+1/1 通过 |
| D-AUTO-002 | D1–D6 权限验收基础设施 | P3 | D readonly 完成 MFA 并进入仅 D 域侧栏后，脚本在自动 reload 竞态中重复登录；后续 maker/checker 对响应形状和陈旧载荷也存在错误假设 | 登录 helper 没有以最终侧栏/session 状态裁决成功，且载具未按真实响应与状态机生成合法陈旧请求 | 阻断 D readonly/no-write/no-menu/maker-checker 自动化证据，不是产品权限失败 | 已按 session/侧栏稳定状态结束登录；按权威响应解析并构造合法陈旧动作，保留 409 失败关闭断言 | 最终权限套件 4/4、D 主链 4/4；D2 状态先恢复后精确删除，业务/幂等/审计/outbox/delivery/risk/alert 七类残留均为 0 |
| D-001 | D2 提现审核队列 / A4 事件治理 | P1 | 最终候选使用无 K4 评分的合法 `REVIEW_PENDING` 隔离提现；maker 从登录页和可见 D2 侧栏点击“延迟”，后端返回 422，幂等记录为 `BizException: A4_SCHEMA_PROPERTY_NOT_REGISTERED`，提现状态未推进 | D2 在风险分不可用时合法发送 `risk_score_status=UNAVAILABLE`，但 `withdraw.delayed` A4 schema 未注册该字段且仍把 `risk_score` 设为必填 | 无 K4 评分时 D2 合法延迟处置被事务回滚，D2→A2/A4/outbox/consumer 及 K/L 下游中断；失败关闭，未产生错误资金或状态副作用 | **已修复**：仅将 `withdraw.delayed` 升至 schema rev 15，注册可选 `risk_score_status:string` 并将该事件的 `risk_score` 改为可选；同步既有库增量 migration、干净库 D2 baseline、生产者/合同棘轮测试，其他六类提现事件仍强制 `risk_score` | **关闭**：统一重建候选后以全新 maker/checker 和夹具从可见 D2 入口重跑 `2/2`；无评分延迟 200，同键同载荷重放 200、同键异载荷 409、独立双运营员并发 `[200,409]`；A2 `2`、outbox `2`（rev 15、`risk_score_status=UNAVAILABLE`、无 `risk_score`）、consumer delivery `2` 全部成功；业务与幂等精确清理为 0 |
| G-AUTO-001 | G1/G2/G3/G4/G7 验收基础设施 | P3 | G3 页面走查首次等待 `/api/admin/market/history`，误把历史请求当作模块总览完成信号 | 响应等待器没有按总览端点的精确路径和请求语义匹配 | 造成一次 G3 自动化误判，不影响产品权威数据 | 已将等待条件收紧到 G3 总览真实响应，保留刷新与重登数据一致性断言 | 超管读取轨 2/2、G2 独立轨 1/1、跨仓合同 20/20、App API 18/18 通过 |
| G-001 | G1/G2/G4/G7 权威总览失败关闭 | P1 | 新浏览器上下文从可见侧栏进入各模块，将真实总览响应替换为 HTTP 200、`code=0`、`data={}`；G3 正确显示加载失败，G1/G2/G4/G7 却把缺失字段默认成 0、空数组或占位文案并渲染业务页 | 四个客户端的运行时边界只检查 HTTP/业务码，规范化器用可选字段和兜底值吞掉不完整成功载荷，没有验证 `serverCanonical` 及模块必需结构 | 网关截断、版本漂移或后端异常返回畸形 200 时，运营会把伪造的零值/空态误认为权威数据，且页面可能保留高风险写入口；G1→D4/B1、G2→D4/G3、G4→H1/G7、G7→G4/D4 的跨域判断均可能失真 | 增加四模块逐字段权威响应 validator，规范化前校验 domain、stats、records、sources、B1 redline、关键枚举/数值范围与 `serverCanonical=true`；读取失败清空旧快照并只保留恢复入口 | **已关闭**：统一 Build `nXIUnp090z-thvAfE4RAj` 上 G1/G2/G3/G4/G7 畸形 200、500、timeout 与真实恢复 6/6；运行时合同 4/4、真实后端 payload 5/5、TypeScript 通过 |
| G-002 | G1/G2/G3/G4/G7 写命令结果未知 | P1 | 从真实登录页和可见侧栏打开参数编辑，拦截写请求并返回 HTTP 200、`code=0`、`data={}` 后以保留表单重试；G1/G2/G3/G4 第二次请求均生成新 `Idempotency-Key`，G7 甚至把畸形写响应当成功并关闭弹窗 | 幂等键在请求 envelope 成功后、领域响应完成运行时校验前即清除；G1/G4完全没有 pending intent，G7忽略写响应并另行刷新，五模块没有共享“完整验收成功后才完成命令”的棘轮边界 | 首次命令可能已生效但响应被截断时，运营重试会使用新键造成参数双写、重复批处理或重复审计/事件；G7还会误报成功，破坏结果未知失败关闭 | 新增共享 `stable-mutation` 执行器：按完整载荷指纹保留原键，仅在领域 validator/ack 接受后清除；显式区分确定性拒绝与结果未知，4xx/2xx 业务拒绝释放键，网络、5xx、成功包解析/规范化失败保留键；G1/G2/G3/G4/G7 全部写命令统一接入 | **已关闭**：统一 Build `yscCV52TBV-tx4G22DXJz` 上五模块 unknown 同键重试及 deterministic 释键 10/10、畸形 200/500/timeout 与真实恢复 6/6；共享/闭包合同 30/30、TypeScript 通过 |
| G-003 | G7 成功写响应与 PC 权威总览合同不兼容 | P1 | 对照当前 PC `updateG7RepurchaseParam` 与后端 `/api/admin/repurchase/config/{key}`：PC 在同一稳定命令边界内按完整 G7 总览规范化写回；后端成功事务只返回 `updated/paramKey/before/after/receiptId/serverCanonical` 的 ack | G7 从“忽略写 ack 后另行读取”收紧为完整响应验收时，仅修改了 PC；后端命令成功 DTO 未同步成与 G1/G2/G3/G4 一致的 canonical overview | 真实写已落库、审计与 outbox 后 PC 必然判定 `G7_RESPONSE_INVALID`，弹窗不关闭且同键重试只回放同一不兼容 ack，运营无法确认结果并可能反复重试 | 后端 `OpsRepurchaseAdminService` 在同一事务内读取并返回完整 `repurchaseOverview`，合并 `updated/receiptId`；增加后端合同测试与 PC 跨仓闭包棘轮 | **已关闭**：统一 Build `yscCV52TBV-tx4G22DXJz` 上从可见侧栏真实修改 G7 锁仓天数，canonical 响应、receiptId、同键同载荷 200 回放、同键异载荷 409、App/public 投影及原值恢复全部通过 |
| G-004 | G4 参数命令重复写 A2 审计 | P2 | G4 从可见侧栏修改并恢复 royalty 后核对数据库：每个成功命令各产生两条 `G4_GENESIS_PARAM_CHANGED`，动作、资源、操作者、原因和幂等键相同；outbox 每次仅一条 | `G4AdminCommandService.updateParam` 与其调用的 `OpsNexMarketService.updateGenesisParam` 在同一命令链各写一次审计，外层为 required、内层为普通审计 | A2 同一真实动作显示为两次，审计计数、检索和合规复核失真；不会造成资金、配置、outbox 或幂等副作用重复 | 将 G4 canonical 审计所有权收敛到实际持有 old/new 状态的 `OpsNexMarketService` 并升级为 `recordRequired`；移除命令包装层重复审计，保留一次 outbox 与原幂等执行器；补服务单测和跨仓合同棘轮 | **已关闭**：最终 JAR `2B96413C…D1EF2` 从可见侧栏真实修改并恢复 G4 royalty；每个成功命令数据库恰好一条 required 审计和一条 outbox，最终值 `2.5`，本轮可变残留 0 |
| G-005 | G7 审计与事件操作者写成数值 principal | P2 | G7 maker 从可见侧栏真实修改并恢复锁仓天数后核对数据库：A2 记录 `actor_id=99376` 正确，但 `actor_username=99376`，A4/outbox 的 `detail.operator` 同样是 `99376`；同一会话其他 G 模块均记录真实 maker 用户名 | `OpsRepurchaseAdminService.operator()` 直接采用 `Authentication.getName()`；当前安全上下文 principal 是管理员数值 ID，真实 username 位于 authentication details，未走统一 `AdminActorResolver` | A2/A4 无法按运营用户名检索 G7 变更，页面显示和跨事件追责链退化为不具可读性的数值 ID；配置、资金和幂等结果不受影响 | G7 操作者解析改用统一 `AdminActorResolver.resolve(null)`，在已认证会话优先取 details.username，同时继续单独保存数值 actorId；后端测试同时锁定 audit actorUsername 与 outbox operator，跨仓合同禁止回退到 `auth.getName()` | **已关闭**：最终 JAR `2B96413C…D1EF2` 真实 G7 写与恢复中，A2 `actor_id` 保持数值 ID，`actor_username` 和 A4/outbox operator 均为真实 maker 用户名；最终 lockDays `90`，本轮可变残留 0 |
| I-001 | I1 A/B 文案 | P1 | 从可见侧栏进入 I1 后，故障注入返回 HTTP 200 且 `data.copies="malformed"`，页面抛出 `.map is not a function` 并进入整页错误边界 | I 域客户端只做 TypeScript 强转，没有验证运行时响应结构，渲染层直接对不可信字段调用 `.map` | 上游或网关返回畸形 200 时 I1 白屏；同类风险可能波及 I2–I6 | 已增加 I1–I6 overview 运行时协议校验；畸形 200 显示模块化中文失败关闭，隐藏旧数据和写控件，仅保留重新加载出口 | 最终候选 I1–I6 故障注入/真实恢复 6/6，权限轨 3/3，合同 110/110 通过 |
| I-002 | I3 通知 Campaign | P1 | readonly 账号从可见侧栏进入 I3 后仍可见“新建 Campaign”、调度/立即下发、CAP 调整等 14 个写控件；对应后端写接口返回 403 | I3 组件没有读取当前 session/authority，所有写按钮无条件渲染 | 菜单、路由、按钮、接口权限不一致，误导只读运营并扩大误操作面 | 已按 `content_i3_write`、`content_i3_cap_adjust`、`content_i3_critical_send` 三类精确权限控制按钮、行操作和抽屉 | 最终候选 readonly/no-write/no-menu 3/3；I1–I6 读取 200、写入 403、按钮隐藏、刷新/重登/缓存边界全部通过 |
| I-AUTO-001 | I3/I4/I5 验收基础设施 | P3 | I-001 修复后的候选已按模块显示“数据加载失败，请刷新重试”，旧 503 用例仍断言“暂无真实接口数据”，主流程正确但故障轨产生假失败 | I-001 收紧运行时协议和失败关闭文案后，三个历史用例未同步棘轮化当前错误契约 | 阻断 I3/I4/I5 非 Owner 故障证据，产品失败关闭行为本身正确 | 将三处断言收紧到各模块当前中文错误文案，继续要求权威表格隐藏、解除注入后真实恢复 | 新 Build `yscCV52TBV-tx4G22DXJz` 上 I3 `2/2`、I4 `2/2`、I5 `2/2` 通过；另以新用例完成 I1–I6 500/timeout `12/12` |
| I-003 | I6 i18n 草稿并发 | P1 | superadmin 与独立 MFA checker 从可见 I6 入口打开同一 `v1` 隔离草稿，携不同幂等键和相同 `expectedVersion=v1` 并发提交不同三语文案，真实结果为 `[200,200]`，后提交者静默覆盖先提交者 | `saveLocalizedDraft` 只比较版本标签；Repository 编辑现有草稿时原地更新同一个 `v1`，版本标签不递增，行锁只能串行化写入却不能让第二个请求的 CAS 失效 | 两名内容运营员并发编辑会丢失其中一方文案，且双方都收到成功；I6 A2 审计虽记录两次操作，但最终内容无法证明采用了哪份可见快照 | 后端草稿持久化改为每次成功编辑都分配新的单调版本，旧草稿以 `id + status=DRAFT + is_deleted=0` 条件执行数据库原子 CAS 软退役；受影响行必须为 1 才能插入新版本，否则抛明确版本冲突并由 Service 映射 409，禁止旧快照无条件 `updateById`；新增 Repository 成功推进/竞争失败、Service 409 映射棘轮与双运营员真实 E2E | 最终候选 backend `2B96413C…`/PID `14284`：双运营员真实并发 `1/1`，状态严格为 `[200,409]`；完整生命周期 `1/1`，同载荷重放 200、同键异载荷/陈旧 CAS 409，v2/v3 发布、App 匿名消费、归档、回滚生成 v4 与再归档全部通过；message/version/idempotency/A2 待审/对象锁均为 0，不可变审计 16 条与 outbox 5 条按主控要求保留，关闭 |
| M-AUTO-001 | M4 FAQ/SLA 验收基础设施 | P3 | M4 主流程成功后，finally 回滚 SLA/删除并发 FAQ 未携带最新 `expectedVersion`/`expectedStatus`，后端正确返回 `409 SUPPORT_*_CONCURRENT_MODIFICATION` | 清理逻辑没有在回滚前逐项重读权威版本，也没有区分本用例变化和外部并发变化 | 阻断 M4 自动化收尾；产品 CAS 失败关闭正确 | 回滚前重读权威 SLA/FAQ；仅在当前值与本用例写入值、版本演进同时匹配时携最新 CAS 清理；补中断前缀残留的精确清理 | 统一候选 M4 2/2 通过；九类 SLA 恢复原值，FAQ 前缀残留为 0 |
| M-AUTO-002 | M 权限/M3/M5 验收基础设施 | P3 | 夹具创建请求自带 `initialPassword`，脚本随后用该值首次登录却返回 `401 ADMIN_CREDENTIAL_INVALID` | A1 安全契约会忽略客户端密码并生成一次性 `temporaryPassword`；旧脚本误把请求字段当权威凭据 | 阻断 M 权限、M3/M5 独立账号轨，并产生 3 个未激活账号残留 | 权限夹具及 M3/M5 脚本统一只使用 A1 创建响应的 `temporaryPassword`，完成强制改密与 MFA；受限文件记录账号与清理目标 | **已关闭**：M 权限夹具创建 1/1、readonly/no-write/no-menu 3/3 通过；最终 cleanup r4 已确认 `99377/99378/99379` 及本轮全部隔离账号均为 `disabled + unassigned + MFA off + sessions 0`，活动角色关系、幂等记录和 Redis DB14 均为 0 |
| M-001 | M4 知识库/SLA | P1 | 源库与隔离库的 `nx_support_sla_rule` 仅有 withdrawal 1 类；真实 overview 200 后严格合同要求 9 类，页面显示“知识库后端当前不可用”并移除入口；首次加载窗口还会短暂暴露“新增文章” | 初始化/迁移未物化 M4 九类 SLA，且前端 availability 初始状态没有在 loading 完成前失败关闭 | M4 主流程不可稳定使用；权威数据不完整时存在短暂失败开放和误写窗口 | 新增九类 SLA 单一基线、幂等 migration、干净库 seed 与启动只补缺；不覆盖已有运营配置；PC 只有严格协议校验写入显式 `1` 后才开放 M4 写控件；同步后端单元/迁移合同、PC 合同和 M4 E2E | 统一候选从登录页和可见侧栏完整复验 2/2：首次加载无写入口、九类 SLA 全量、FAQ 未知结果同键重试/CAS/并发/503 失败关闭、刷新重登及清理全部通过，关闭 |
| M-002 | M2 工单台 / M3 即时会话台 | P1 | 页面首次加载时 `ticketsAvailable` / `conversationsAvailable` 尚未写入，但代码以“不是 `0`”判定可用；超级管理员会短暂看到并可触发“新建工单”“主动发起会话”等写控件 | availability 将 `undefined/loading` 错当成功态，未要求后端权威响应完成并通过协议校验 | M2/M3 在加载竞态、畸形 200 或上游未知结果窗口中失败开放，可能基于空快照提交真实写操作 | M2/M3 统一改为仅显式 `=== "1"` 才开放写入；合同增加严格相等正向断言和禁止 `!== "0"` 的负向棘轮 | 统一候选延迟持有全部 M2/M3 权威请求时，路由可达但写入口为 0；释放且真实协议通过后才显示写入口，1/1 通过；M2 真实闭环另 1/1 通过，关闭 |
| M-003 | M3 即时会话台 | P1 | 从可见侧栏真实发起会话并转交到独立 SUPPORT 坐席后，权威快照为 `TRANSFERRED/version=5`；超级管理员点击“等待处理”时请求却携带 `expectedStatus=OPEN/version=5`，后端正确返回 409；同一映射还影响接收、退回和超时回落 | 前端用 `status=open + transfer` 表达转入待处理，但 transfer decision 写链直接把视图态 `before.status` 当后端 CAS 状态，丢失权威 `TRANSFERRED` 语义 | M3 所有转入待处理决策无法提交，跨坐席调用链中断；若放宽后端校验则会破坏 CAS 安全 | transfer decision 的期望状态类型加入 `transferred`，accept/return/wait/fallback 四条写链显式发送 `TRANSFERRED`；增加四类调用不得退回 `before.status` 的棘轮合同 | 新候选 Build `4QKkqEM_9AH1_6bkQWmha` 从登录页/可见侧栏完整重跑 M3 `1/1`：wait 200、非目标接收 403、目标坐席接收/回复/刷新/转工单及审计链通过；TypeScript 通过、M3 合同 16/16，关闭 |
| PC-BUILD-001 | PC 构建基线 | P3 | 统一生产构建成功，但 Tailwind 内容扫描把旧验收报告里用于描述历史缺陷的损坏圆角类名再次当作候选类，输出 1 条 CSS 优化警告 | 文档中的历史故障样例仍保留 Tailwind 可识别的任意值类名语法 | 候选包可运行，但构建日志不干净，可能掩盖后续真实 CSS 告警 | 将旧报告中的损坏类名改写为业务描述，不改变历史缺陷结论或产品代码 | 再次生产构建完成 20/20 页面，CSS 优化警告归零，关闭 |
| E-001 | E1 商品目录与上架门 | P1 | superadmin 从可见侧栏进入 E1；故障注入让 `/api/admin/e1/skus` 返回 HTTP 200、`code=0` 但 `data={}`，页面把协议错误显示为“0 个 SKU”正常空态，仍暴露新增 SKU、阶段、上架门等写控件 | E1 客户端只做 TypeScript 强转并把缺失 `records` 当空数组；刷新失败时未统一清空旧权威快照，写权限也未与有效 canonical 状态绑定 | 网关/后端协议漂移时运营可能把全量目录丢失误判为空业务，并基于无权威快照发起真实写操作；E1→App/E2/E4/E5 链路均受影响 | 增加 SKU page 与阶段/上架门严格运行时协议；任何歧义清空旧权威数据，并将全部 E1 写入口绑定到有效 canonical 快照，仅保留重新加载 | 新候选 Build `vgYxznUzp2OLw1oR0m_y2` 从登录页/可见侧栏定向通过：畸形 200 失败关闭、三个写入口均为 0，解除注入后真实请求恢复；运行时合同 5/5、TypeScript 通过，关闭 |
| E-002 | E4 订单状态机 | P1 | superadmin 从可见侧栏进入 E4；`/api/admin/devices/orders` 返回 HTTP 200、`code=0`、`data={}` 后，页面将缺失分页与 records 当成合法空订单继续展示 | E4 客户端用可选字段和默认值吞掉协议缺失，没有运行时校验 page/records/订单身份 | 订单全量丢失会被误判成真实空态，E4 与 D1/D4/E5 的履约、支付和设备证据链失真 | 已增加 E4 严格分页与订单身份协议，合法空页保留，缺字段/非法分页/重复或无身份记录均抛错并由现有 E4 错误态清空旧数据 | 最终候选 Build `YKme4v6o2TF7j7gGGORU2` 从登录页/可见侧栏通过：畸形 200 失败关闭、真实请求恢复；E1–E6 最终短重跑 12/12，关闭 |
| E-003 | E5 设备运维 | P1 | superadmin 从可见侧栏进入 E5；`/api/admin/devices/overview` 返回 HTTP 200、`code=0`、`data={}` 后，页面把缺失统计全部归零且继续展示真实设备列表和写控件 | E5 overview/device page/datacenter 客户端均依赖 TypeScript 强转与默认值，聚合请求中的单一路径协议错误未让整页失败关闭 | 设备库存和数据中心统计可能来自不一致快照，强制激活、解绑、暂停派单等运维决策失去权威前提 | 已对 E5 设备分页、overview 全字段和 datacenter 数组增加严格运行时协议；任一来源畸形即由 Promise.all 进入既有失败关闭，清空设备、统计和数据中心 | 最终候选 Build `YKme4v6o2TF7j7gGGORU2` 从登录页/可见侧栏通过：畸形 200 显示“设备库存读取异常”、真实请求恢复；E1–E6 最终短重跑 12/12，关闭 |
| E-004 | E6 算力与设备配置 | P1 | superadmin 从可见侧栏进入 E6；`/api/admin/devices/compute-config` 返回 HTTP 200、`code=0`、`data={}` 后，页面未进入“配置读取失败”，仍按空配置渲染 | E6 客户端直接把未知 JSON 强转为聚合视图，没有校验 domain、数组、六档 GPU、下载配置和 sources | App/H5 算力入口、系数、收益估算、显卡映射与客户端下载配置可能基于不完整快照继续操作 | 已增加 E6 聚合视图深层运行时协议，要求 domain、非空关键数组、G1–G6 唯一六档、下载字段和来源完整；畸形响应清空配置并仅保留重新加载 | 最终候选 Build `YKme4v6o2TF7j7gGGORU2` 从登录页/可见侧栏通过：畸形 200 失败关闭、真实请求恢复；独立 maker/checker 临时写入与精确恢复 1/1，关闭 |
| E-005 | E3 生命周期与 Trade-in | P1 | 仅有 `device_e1_read`–`device_e6_read` 的 no-write 账号从可见侧栏进入 E3，仍显示 12 个“调整”写按钮；后端 E3 写探针正确返回 403 | E3 子视图没有接入 `device_e3_write` 权限，参数单字段与多字段按钮均无条件渲染 | 菜单、路由、按钮、接口权限不一致；只读运营会被诱导进入必然失败的高敏配置提案流程 | 已在 E 域上下文接入 `device_e3_write`，E3 单字段/多字段调整入口仅在精确写权限存在时渲染；后端 403 边界保持不变，并增加静态棘轮合同 | 最终候选 Build `4QKkqEM_9AH1_6bkQWmha` 使用 readonly/no-write/no-menu 三账号从可见侧栏完整五层复验 3/3；E1–E6 Owner 短重跑 12/12、E3 独立输出重跑 3/3，关闭 |
| E-006 | E3 A2 对象锁调用链 | P1 | E3 页面发起单字段或批量 A2 提案时，锁目标保存前端别名（如 `E.device.capacity.subsidyDays`）；后端直写先执行 `normalizeE3Key`，再按 canonical 键 `capacitySubsidyDays` 查询活动锁。两者不相等，A2 pending 期间同对象直写可绕过对象锁 | `high-ops-registry` 的 E3 `buildTarget/buildTargets` 直接复用了回放 command 的页面键，没有把“回放可接受别名”和“锁唯一标识”分开；后端已经以 canonical 键作为配置行、幂等资源和锁查询真源 | 两名运营员可能在同一 E3 生命周期参数上形成 pending A2 与直接写并发；后续批准会基于陈旧 before 快照覆盖中间变更，影响 E3、App Trade-in/设备展示以及 A2/A4 一致性 | 保持 command 页面键与后端回放兼容不变，仅把 E3 单字段、批量 `target.id` 统一映射为 `OpsDeviceService.normalizeE3Key` 使用的 canonical 键；对象锁仍由服务端唯一约束和直写 409 检查负责，不放宽锁、不把并发检查移到前端；新增单字段/批量/别名棘轮合同 | **已关闭**：最终 Build `9hoJ5Kpe5kMgLKBZr_EIk` 从可见入口通过同键同载荷同票、同键异载荷 409、第二运营员 409、pending 直写 409、自审 403、独立 checker 批准、页面生效、恢复批准与精确恢复；A2/A4 200，配置 `capacitySubsidyDays=30`、pending/活动锁/临时幂等残留均为 0 |
| E-007 | E3/A2 结果未知失败关闭 | P1 | superadmin 从可见侧栏进入 E3 提交参数提案；对 `POST /api/admin/platform/audit/operations` 注入明确 `UPSTREAM_OUTCOME_UNKNOWN` 后，确认弹窗和输入虽保留，但持久错误被归类为普通“操作失败”，未告知同一命令号重试或核对 A2 审计 | A2 客户端只按完全匹配响应头识别未知结果，未兼容明确未知机器码和 `200/code=0/data=null`；共享 proposer 与 K2 调用方又依赖 `instanceof`，跨模块边界可能降级；确认弹窗只透传已被泛化的错误文本 | 操作员可能把可能已生效的提案当作明确失败并生成新幂等键重提，造成重复高敏动作；所有复用 A2 proposer 的域均受影响 | 在 A2 客户端统一识别网络中断、响应不可读、未知结果头/机器码及畸形成功，统一抛出携带原 command key 的可操作错误；增加跨模块安全谓词；共享 proposer、K2 和确认弹窗均持久提示“结果暂不确定、保留弹窗与输入、使用同一命令号重试并核对 A2 审计”，普通失败语义不变 | **已关闭**：最终 Build `9hoJ5Kpe5kMgLKBZr_EIk` 对同一表单连续注入 503+unknown 与 `200/code=0/data=null`，两次均保留弹窗、值、理由和同一 key/body，持久提示同键重试并核对 A2 审计，真实 E3 配置未写；共享 E/K2 合同通过 |
| E-008 | E6 算力与设备配置 / A2 批量提案 | P1 | 仅具 `device_e6_read`、`device_e6_write`、`platform_a2_proposal_create` 的隔离 maker 从真实登录进入 E6，按 PC 双语文案实际 command 提交 `e6_compute_config_batch + targets`，后端返回 `403 A2_BUSINESS_CONTEXT_UNMAPPED`；同账号身份和最小权限均已核实 | 后端 `AuditReplayBusinessPermissionGuard.delegatedEDescriptor` 只映射 E6 单字段开关，没有映射 PC `param-multi` 固定产生的 E6 batch；delegated 上下文因此在落票前失败关闭 | 合法 E6 maker 无法把双语下载文案、显卡档位等多字段变更送入独立 checker，PC→A2→E6→App 公共配置投影调用链中断；未产生 pending 工单或配置漂移 | 后端从 command `values` 服务器侧校验 `ComputeConfigRegistry` 明确白名单并排序生成 canonical 展示对象和逐对象 targets，要求 single target 为空且请求 targets 全等；空批量、未知/跨域 key、重复/跨域 target 均失败关闭；PC 展示对象同步按同一键序排序，权限仍要求 `device_e6_write` | **已关闭**：最终 Build `G_xqr1e1jOiGdjdxUT076`、JAR `A952148D…B037` 使用最小 maker `99484` 完成同键同载荷 200、异载荷 409、独立双 checker `[200,409]`、A2 前向/恢复、PC 与 App 公共投影同步及精确恢复；E6 pending/对象锁/marker/临时幂等均为 0，账号与角色已安全清理 |
| E-AUTO-001 | E1 只读与失败关闭验收合同 | P3 | 非 Owner 复审运行 E 域 51 项合同，产品已把 E1 写入口从单纯 `canWriteE1` 加强为 `canUseE1Writes`（写权限、非加载、无读取错误且 generation-gates canonical 快照存在），但旧静态合同仍要求 header 直接使用 `canWriteE1`，产生 50/51 的载具假失败 | Owner 修复 E-001 时加强了产品门禁，未同步该条旧源码正则；合同锁定的是实现文本而非“权限且权威快照有效”的现行安全语义 | 阻断 E 域统一合同门，不影响产品运行；若为消除假失败而退回旧门禁，反而会重新引入畸形 200 下的失败开放风险 | 保留并明确锁定 `canUseE1Writes = canWriteE1 && !e1Loading && !e1Error && e1Gates !== null`，同步断言上下文与 header 均消费该加强门禁，未放宽产品权限或 canonical 前置条件 | **已关闭**：最终 Build `9hoJ5Kpe5kMgLKBZr_EIk` 从登录页和可见侧栏完成 E1 权威读取、畸形 200 失败关闭、三类写入口隐藏、真实恢复、刷新重登；E/K2 扩大合同 70/70 |
| H-001 | H1 Phase 调度器（同类风险覆盖 H2/H3/H4/H5/H7） | P1 | 使用仅有 `growth_h1_read` 的 finance 独立账号从可见侧栏进入 H1；读取接口 200、写接口 403，但“改总时长”“设定位置”等写按钮仍可见且可点击 | H1–H7 组件未读取当前 session authority，写入口无条件渲染；只有 H8 已单独实现按钮权限守卫 | 菜单、路由、按钮、接口权限不一致；只读运营会被诱导进入必然失败的编辑流程，扩大误操作与社会工程攻击面 | 已修复：H 域上下文接入当前 session authority；H1–H7 按模块精确控制普通写入、H1 PIN/撤销、H2 会话取消/扣款、H4 奖池等入口，非按钮交互区同步守卫，并增加 UI 权限合同测试 | 已关闭：新候选 Build `4QKkqEM_9AH1_6bkQWmha` 使用独立 finance 只读账号从登录/MFA/可见侧栏进入，读取 200、写入 403、页面可操作写入口 0；Playwright 1/1、H 权限合同测试 35/35、TypeScript 均通过 |
| H-002 | H5 后端验收基础设施 | P3 | 全量 Maven 在东京跨午夜窗口稳定出现 2 个 H5 日期断言失败；生产节奏仍按越南业务日正确运行 | 生产代码以 `Asia/Ho_Chi_Minh` 计算业务日期，历史单测却用执行机系统时区的 `LocalDate.now()` 生成预期值；东京已跨日而越南尚未跨日时两者偏移一天 | 造成统一后端质量门假失败并阻断候选重建，不影响 H5 生产业务日期、数据或状态 | 单测预期日期改为与生产相同的 `Asia/Ho_Chi_Minh` 业务时区，保留跨午夜边界语义，不改产品代码 | **已关闭**：修正后全量 Maven 通过；确认为测试载具缺陷，生产 H5 行为无变更 |
| F-001 | F1–F5 服务端权威总览 | P1 | superadmin 从登录页和可见侧栏进入 F1–F5；分别故障注入各总览接口返回 HTTP 200、`code=0`、`data={}`，五个页面均未显示数据加载失败，而是把缺字段默认成空值/零值继续渲染 | `lib/admin/f1-client.ts` 的五个 normalize 函数仅依赖 TypeScript 类型，并用 `?? []`、`toNumber`、默认文案吞掉运行时协议缺失 | 网关/后端协议漂移时，运营会把丢失的等级、费率、结算、奖池或佣金数据误判为真实空态/零值；可能基于不可信快照发起 F1–F5 高敏提案，并污染 B1/D4/A2/A4 及 App 消费判断 | 已增加 F1–F5 严格运行时协议；畸形成功响应清空旧快照、显示模块错误、隐藏写入口，仅允许真实重新加载 | 最终候选 Build `lwVpU_zYt9FH9Zgon5PVf` 从登录页和可见侧栏完整复验：畸形 200 `5/5`、500/超时/404/422 `3/3`、真实恢复通过，关闭 |
| F-002 | F1–F5 按钮权限 | P1 | 仅有 `network_f1_read`–`network_f5_read` 的 readonly/no-write 独立账号经登录、MFA 和可见侧栏进入 F1；后端写探针正确返回 403，但页面仍显示 17 个可点击的奖励、门槛、晋升、保护和展示配置写入口；F2–F5 组件同样未读取会话权限 | F 域上下文未接入当前 session authorities，五个子视图的普通写入和高敏写入控件均无条件渲染 | 菜单、路由、按钮、接口权限不一致；只读运营会被诱导进入必然失败的高敏提案/资金处置流程，扩大误操作与社会工程攻击面 | F 域接入真实 session authority，并按 22 个 F 权限点逐模块、逐动作守卫；后端 403 保持不变，增加 UI 权限棘轮合同 | 最终候选 readonly/no-write/no-menu `3/3`；菜单、路由、按钮、接口、数据五层权限及刷新重登全部通过，关闭 |
| F-003 | F1–F4 A2 配置审批链 | P1 | 合法 `f_maker`（growth，持 `network_f1_write` 与 `platform_a2_proposal_create`）经登录、MFA、可见侧栏进入 F1，点击“修改奖品名”并确认；真实 `POST /api/admin/platform/audit/operations` 返回 `403 A2_BUSINESS_PERMISSION_UNMAPPED`，未生成待审票 | `AuditReplayBusinessPermissionGuard.requiredAuthority` 的 F 域映射只有佣金、晋升、奖励和奖池结算，漏掉 `f_config`、`f_ui_config`、`f_unilevel_rule` 三类配置回放操作 | F1–F4 页面配置按钮虽可见，但合法 maker 无法创建 A2 提案，独立 maker/checker、对象锁、幂等/CAS 与真实生效链全部中断；跨域账号也被安全拒绝，未形成越权写 | 后端按配置 key、操作族和 sourceDomain 单源映射补齐三类 F 操作；合法 F maker 放行、D maker 跨域拒绝，未知 key 继续失败关闭 | 最终后端 JAR `37BA6F…88CB` 上 maker/checker `1/1`：前向/恢复/幂等工单完成，自批 403，同键异载荷/对象锁/CAS 409，D maker 403；配置精确恢复、pending/锁/幂等残留均 0，关闭 |
| F-004 | F1/F2/F3 命令幂等 | P1 | 独立 `f_maker` 使用同一 `Idempotency-Key` 依次向 F3 结算端点提交两个不同的不存在 owner；修复前两个请求均为 HTTP 200、`code=0`、`BLOCKED/BINARY_OWNER_NOT_ACTIVE`，第二请求未返回 409。源码复核同时确认 F1 阈值/奖励 CRUD 与 F1–F4 多态配置只校验 key 非空，不持久绑定载荷 | F3 controller/service 完全未接 `AdminIdempotencyService`；F1/F2 共用的 `OpsTeamService.requireCommand` 只做请求格式校验，未 claim、回放或比较请求哈希 | 代理超时/结果未知后无法用原 key 安全确认；同 key 异载荷可操作另一个 owner、奖励或配置，F1 奖励新增还可能重复写业务表，破坏 A2/A4、B1/D4/H1 及佣金结算链的一次性语义 | F3 强制接收 `Idempotency-Key`，以 SHA-256 绑定 owner/date/reason 并由持久幂等服务包裹事务；F1 阈值、奖励增删改及 F 多态配置分别按稳定 scope 和规范载荷接入同一服务；PC F3 客户端支持显式稳定 key，补全前后端合同与安全实证用例 | **已关闭**：最终 Build `nXIUnp090z-thvAfE4RAj` / JAR `D99C951B…BD2CB` 上，F1 `F.prize.name` 与 F2 `F.influence.clampMin` 同键回放 200、异载荷 409且终值无漂移；F3 同键回放 200、异载荷 409、新 key 仍受唯一 mutex；F4 A2 缺失 409、F5 404 后异载荷 409。测试 mutex、业务、账本、outbox、幂等残留均为 0 |
| F-005 | F1 展示/治理配置接口权限 | P1 | 对照页面、A2 guard 与共享配置端点复核：页面把 `F.prize.name`、`F.vrank.titles`、`F.vrank.leadership.*`、`F.fulfillment.*` 守卫为 `network_f1_write`；但 controller 不允许纯 F1 writer 进入，service 又未给这些 key 映射 F1 权限，持任一 F2/F3/F4/F5 配置权限即可越权直调 | 共享 `PATCH /api/admin/teams/commissions/config/{key}` 的入口权限漏掉 `network_f1_write`，同时 key 级 `CONFIG_KEY_PERMISSION/resolvePermissionCode` 漏掉四类 F1 UI key | 纯 F1 运营员合法动作被 403；其他 F 子域运营员可绕过页面和 A2 直接改 F1 展示/治理配置，菜单、按钮、接口、数据权限互相矛盾 | controller 仅增加 `network_f1_write` 作为共享入口资格；最终授权仍由 service 对四类 F1 key 精确映射 `network_f1_write`，未知 F1 key继续失败关闭；补纯 F1 正向、F2–F5 跨域负向、F1→其他域负向与未知 key 合同 | **已关闭**：最终 JAR 对应 `OpsTeamServiceTest 71/71`、`OpsTeamControllerTest 9/9`，覆盖纯 F1 正向、F2–F5→F1 403、F1→其他域 403、未知 key 拒绝；PC F 合同 `36/36`，真实 readonly/no-write/no-menu 与 D→F 跨域攻击均通过，未产生越权工单或配置漂移 |
| J-AUTO-001 | J4 中断恢复验收基础设施 | P2 | 在本轮隔离数据库运行 J4 stale replay 用例时，夹具 SQL 仍固定连接 `nexion`，会把故障注入写到日常库且无法改变候选服务实际读取的专库执行记录 | 验收脚本只参数化 MySQL 可执行文件和密码，数据库名硬编码，未把 Run 级数据隔离纳入载具契约 | J4 中断恢复证据失真，并可能污染非本轮数据库；不影响产品运行代码 | 先增加数据库选择合同并得到 21/22 红灯，再引入 `NEXION_MYSQL_DATABASE`，所有中断注入 SQL 仅连接显式指定的验收专库，默认值仅保留旧脚本兼容 | J4 合同 22/22 通过；运行命令已锁定 `nexion_acceptance_20260728_151023`，后续真实 stale replay/回滚只在专库执行 |
| J-AUTO-002 | J3 验收证据归档 | P3 | J3 独立走查将截图、CSV、trace 和浏览器日志固定写入 2026-07-22 旧目录，无法按本轮 Run 分区到受限目录 | 脚本未暴露证据根目录参数，运行隔离只覆盖业务数据而未覆盖证据数据 | 证据可能串 Run、误纳入 Git 或在清理时误删，产品行为不受影响 | 先增加证据路由合同并得到 17/18 红灯，再用 `J3_EVIDENCE_DIR` 路由本轮证据，保留旧路径仅作向后兼容 | J3 合同 18/18 通过；本轮所有 J3 证据写入 `.restricted/pc-full-acceptance-20260728-151023/J/J3-full` |
| J-001 | J4 应急 SOP | P1 | superadmin 从可见侧栏进入 J4 后点击“新增剧本”，真实 `POST /api/admin/emergency/sop/playbooks` 返回 HTTP 500；隔离库已有活动自定义编号 1–2、软删除编号 3–9 | 服务只按未删除剧本计算下一个 `SOP-CUSTOM-*` 编号，重复分配已被软删除行占用且仍受唯一键约束的 `SOP-CUSTOM-3` | J4 新增剧本主流程在发生过软删除的环境中完全中断，进而阻断演练、maker/checker 应急执行与 J1/J2/I3 跨域恢复链 | 先增加“活动行 + 软删除行共同参与编号上界”合同并得到 22/23 红灯；仓储新增跨全部行的最大自定义序号查询，服务统一按该上界递增，测试覆盖活动编号 1–2、软删除 3–9 后必须分配 10 | 最终 JAR `37BA6F…88CB` 从可见入口成功创建 `SOP-CUSTOM-10` 并完成真实演练，证明软删除编号不再复用；J4 合同 23/23，关闭 |
| J-AUTO-003 | J4 maker/checker MFA 验收载具 | P3 | 专用 maker 首次进入前，脚本把 `locator.isVisible({timeout})` 误当等待；后续又在 `ADMIN_MFA_CODE_REPLAYED` 后复用已消费的 challenge，三次均停在 MFA 页且未产生 J 写请求 | 登录 helper 没有等待 MFA 控件真实出现，也没有把 TOTP counter 与一次性 challenge 一并建模为棘轮状态 | 阻断 J4 独立 maker/checker 终验；产品正确拒绝重放，J1–J4 业务基线未变化 | 改为先等待 MFA 可见；按账号记录最后 TOTP step；任何 replay/invalid 都清理当前 challenge/cookie、重新执行密码登录签发新 challenge，并进入下一时间步后再验证；禁止同一 challenge 内盲重试 | 新浏览器上下文中 maker、d_checker、maker 三次 MFA 均通过，完整到达提案、批准、逐步追溯和回滚请求；TypeScript、Playwright 收集通过，关闭 |
| J-AUTO-004 | J4 maker 跨域 A2 权限夹具 | P3 | `SOP-CUSTOM-10` 演练就绪后，“提交应急复核”按钮因 `缺少 A2 提案权限` 保持禁用 | maker 夹具只授予 J1–J4 域权限，遗漏 J4 提交真实 A2 工单所需的 `platform_a2_proposal_create` | 产品正确失败关闭且 Genesis 未变化，但 maker/checker 链无法进入独立复核 | 权限单源补齐 `platform_a2_proposal_create`；既有 maker 角色由 superadmin 提交变更、A 域 d_checker 独立批准，新增 grants-only 模式避免重置账号/MFA | 角色详情与 j_maker 新会话双重核对 1/1，旧账号凭据未重置；随后真实 A2 提案、d_checker 执行及 J4 逐步追溯均成功，关闭 |
| J-002 | J4→J1/B1 回滚失败关闭 | P1 | `SOP-CUSTOM-10` 经 maker 提案、d_checker 执行后，B1 覆盖率为 0%；maker 点击回滚，J1 正确拒绝恢复 Genesis，但 J4 接口最终返回 HTTP 500 `INTERNAL_SERVER_ERROR`，日志为 `UnexpectedRollbackException` | J4 回滚块使用默认 `PROPAGATION_REQUIRED` 嵌套在幂等事务中；J1/B1 抛出业务拒绝后把外层事务标记 rollback-only，J4 捕获并准备返回 422，但外层提交阶段改抛 `UnexpectedRollbackException` | 运营只看到未知 500，无法区分 B1 红线拒绝；J4 rollback 元数据未闭环，Genesis 保持关闭并需应急恢复 | 先补事务边界红灯，确认期望 `REQUIRES_NEW`、实际为 `REQUIRED`；将 J4 rollback 的 claim/CAS、J1 恢复和元数据完成块改为独立新事务，使业务拒绝仅回滚子事务，外层幂等可稳定保存/返回 422 | **已关闭**：最终 JAR `DCA3841B…208F` 上，同一 execution `SOP-CUSTOM-10-9245E8BD` 低覆盖真实返回 `422 COVERAGE_BELOW_REDLINE`，Genesis 保持关闭且 ownership 未污染；隔离 reserve 达标后同 execution 回滚 200 并进入 `ROLLED_BACK`，Genesis 恢复。临时 reserve 已精确删除，资金基线恢复为 `2 行/净额 0`、marker 0 |
| J-003 | J4 回滚后的 J1/A4 恢复事件 | P1 | 修复 J-002 后，同一 execution 在 B1 达标时回滚 200，Genesis 从关闭恢复开启且 J4 审计成功；按 A4/outbox 核对，执行关闭有 `J1_KILLSWITCH_CHANGED`，回滚恢复时 outbox 为 0 | `restoreFromJ4Execution` 直接调用只负责状态恢复的 `restoreFromLinkedDomain`，绕过了 `changeFromLinkedDomain` 中统一的 `broadcastGateChange` 与 `J1_LINKED_DOMAIN_GATE_CHANGED` 审计 | Genesis 权威状态已恢复，但 A4/下游消费者收不到恢复事件，可能继续按“已关闭”状态运行，J4→J1→A4 调用链不完整 | 先补红灯并确认同一 J4 execution 的关闭+恢复期望发布 2 次、实际仅 1 次；提取统一 `recordLinkedGateChange` 边界，J4 恢复成功后在同一新事务内发布 `J1_KILLSWITCH_CHANGED` 并记录 `J1_LINKED_DOMAIN_GATE_CHANGED`，任一失败则回滚恢复 | **已关闭**：最终 execution `SOP-CUSTOM-10-9245E8BD` 的 J1 审计 `47102/47488` 分别记录 `true→false`、`false→true`，A4/outbox `8611/8625` 均为 `J1_KILLSWITCH_CHANGED`；J4 rollback 审计与 Genesis 权威状态一致。SOP、execution、A2 ticket/lock、验收幂等残留均为 0，不可变 audit/outbox 保留 |
| K-AUTO-001 | K2 验收基础设施 | P3 | 执行 `tests/e2e/k2-independent-reverify.spec.mjs` 时，入口实际导入 2026-07-16 的 `.codex-logs` 归档脚本；脚本固定定位“登录”按钮，当前产品按钮为“继续”，5/5 在登录入口超时 | 稳定入口文件错误绑定到不可维护的历史快照，而不是仓库当前 K2 独立验收脚本 | 阻断 K2 自动化复验并产生无效失败 trace；产品登录和 K2 页面本身未失败 | 稳定 reverify 入口改为导入当前 `k2-independent-acceptance.spec.mjs`，沿用现行登录/MFA、可见侧栏、失败关闭和参数边界断言 | 锁定候选 `goWbiGaLpmFEiZfiVwSmm` 用必需凭据复验 3/3 通过，关闭 |
| K-AUTO-002 | K2 跨域真实验收基础设施 | P2 | 审查 `k2-live-acceptance-20260722.spec.ts` 发现所有夹具、清理及权威数据断言固定连接日常库 `nexion`，与本轮后端实际使用的专用库不一致 | 脚本只参数化 MySQL 可执行文件与密码，遗漏 Run 级数据库选择契约 | 若直接运行，会污染日常库且从错误数据库取证，K2→K1/H2/H8/F4 跨域闭环结论失真；产品运行代码不受影响 | 先增加数据库隔离合同并得到 8/9 红灯，再引入 `K2_DB_NAME`，所有 SQL 统一连接显式选择的验收专库，保留 `nexion` 默认值仅作旧脚本兼容 | 专库 `nexion_acceptance_20260728_151023` 上从入口执行 K2 6/6 通过，夹具残留为 0，关闭 |
| K-AUTO-003 | K3 提现跨域验收基础设施 | P2 | 专用验收库不存在固定用户 `990731` 的历史 KYC 行；旧脚本仅执行 `UPDATE nx_kyc_profile`，实际更新 0 行，提现在地址配对前置门以 409 拒绝，无法进入预期的 K3/K4 失败关闭分支 | 验收夹具错误依赖旧数据库遗留行，没有自行创建完整的 KYC 与只读提现地址权威记录 | 阻断 K3→K4→D2→K5/A4/B1/B5 跨域验收；产品正确地在缺失 KYC/地址配对时失败关闭，资金与订单未变化 | 先增加隔离夹具合同并得到红灯，再把 update-only 改为完整 `INSERT ... ON DUPLICATE KEY UPDATE`，包含唯一 KYC 编号、APPROVED 状态、TRC20 配对地址、版本和软删除恢复 | **已关闭**：最终候选 K3 `6/6`，干净夹具完成两笔真实提现与全跨域断言；KYC、地址、用户及业务残留均为 0 |
| K-001 | K2/F5 佣金生产链 | P1 | 在专用验收库创建 K2 正反事实佣金夹具时，`INSERT nx_commission_event` 稳定返回 MySQL `ERROR 1267 Illegal mix of collations (utf8mb4_unicode_ci, utf8mb4_0900_ai_ci)`；即使暂停表为空也无法写入 | F5 migration 把 `nx_commission_operation`、`nx_commission_user_suspension` 固定建为 `utf8mb4_unicode_ci`，而权威 `nx_commission_event` 随 MySQL 8 库基线使用 `utf8mb4_0900_ai_ci`；BEFORE INSERT 触发器直接比较两种排序规则 | 所有佣金生产者均会在 F5 用户-佣金类型暂停守卫处失败，阻断 K2 正反事实、F5 返佣及上游订单/结算发佣链 | 原始建表、干净库 schema 统一为 `utf8mb4_0900_ai_ci`；新增兼容既有库 migration，转换两张 F5 表后重建 fail-closed 触发器；增加后端迁移合同和 PC 跨仓棘轮合同 | migration 在验收库连续执行两次保持幂等，三表与触发器统一为 0900；新 JAR 上 K2 正反事实与 F5 联动包含在 6/6 通过结果中，关闭 |
| K-AUTO-004 | K2 临时账号验收基础设施 | P2 | K2 创建 checker/精确权限账号时把客户端提交的 `initialPassword` 当作真实凭据；A1 实际忽略该值并返回服务端一次性 `temporaryPassword`。旧清理只停用、解绑且不携 CAS，不清 MFA/会话，异常分支还吞清理失败 | 载具未跟进 A1 服务端凭据、版本和安全清理契约 | K2 maker/checker 无法登录；中断时可能残留有 MFA/会话的临时账号或临时角色 | 登录统一使用创建响应的 `temporaryPassword`；清理逐步读取最新版本，依次清 MFA、解绑、停用、撤销会话并断言四项归零；异常分支不再吞清理错误，临时角色在 finally 由独立 checker 删除 | 新候选真实账号链 K2 6/6 通过；临时账号均禁用、解绑、MFA/会话清零且临时角色删除，关闭 |
| K-AUTO-005 | K5 独立角色验收基础设施 | P2 | K5 脚本无法接收已有专用账号的 TOTP secret，只能创建临时 risk/support；其 afterAll 只停用/解绑，不清 MFA 和会话 | 载具未支持 Run 级权限夹具复用，也未执行 A1 完整安全清理 | 不必要扩大平台写入面；失败或中断时可能留下认证残留 | 新增 `K5_RISK_TOTP_SECRET`、`K5_READONLY_TOTP_SECRET` 以复用 K 域受限夹具；保留的临时账号 fallback 改为最新 CAS 下清 MFA、解绑、停用、撤销会话并断言残留为 0 | 使用 K maker/readonly 从可见入口复验 K5 4/4 通过，权限夹具后续由 A checker 删除，关闭 |
| K-AUTO-006 | K5→C4 跨域验收基础设施 | P2 | 仅具 K1–K6 权限的合法 K maker 已完成 K5 驳回，脚本随后用同一会话直读 C4 `/api/admin/users/kyc/users/52`，后端正确返回 403，导致在恢复 C4 前中断 | 脚本把 K5 写权限错误扩张为 C4 读权限；跨域验真没有使用独立授权会话 | K5 裁决实际成功但载具误判，并把隔离用户短暂留在 REJECTED，阻断恢复与后续异常轨 | K5 业务写继续由最小权限 maker 执行；C4 副作用改由独立新 root 浏览器上下文只读核验，不给 maker 增权；保留 finally/后续完整恢复 | 新候选 K5 4/4 通过；用户 52 恢复 APPROVED，三张临时票及 source/alert/history、订阅、两轮幂等记录均为 0，关闭 |
| K-AUTO-007 | K3→J1/B1 高风险验收基础设施 | P2 | K3 guard 清除逾期自动确认后直接请求恢复提现闸，真实 J1 按 B1 当前覆盖率 `0% < 100%` 返回 422 `COVERAGE_BELOW_REDLINE` | 载具只准备了 J1 pending CAS，没有为“真实恢复闸后执行 K3”准备可恢复的 B1 隔离储备夹具 | 产品正确失败关闭，但高风险 K3 主轨无法开始；若绕过 B1 则会破坏真实安全契约 | guard 先断言红线下 422 与闸状态不变，再按权威负债计算并新增唯一储备台账夹具，使覆盖率安全高于红线后通过真实 J1 API 开闸；finally 真实关闸、删除唯一储备行、恢复精确快照并双查 B1/J1 | **已关闭**：最终 guard `1/1`、K3 子轨 `6/6`；J1 before/after SHA-256 完全一致，B1 储备恢复，reserve 与 J1 幂等残留均为 0 |
| K-AUTO-009 | K3→D4 提现验收基础设施 | P2 | K3 完整轨第 5 项首次提交真实 D2 提现时返回 500 `D4_LEDGER_INSUFFICIENT_BALANCE`；夹具只有 `nx_user_wallet.usdt_available=10000/nex_available=100`，D4 权威账本没有对应初始入账 | 验收载具把钱包快照误当 D4 可用余额，未遵守提现按 `nx_wallet_ledger` 最新 `balance_after` 扣减的真实资金不变量；只补 USDT 后调用推进至 NEX 手续费抵扣并再次正确失败关闭 | 阻断 D2→K3/K4/K5/A4/B1/B5 真实链；若放宽产品会掩盖账本与钱包不一致并允许透支 | 为隔离用户增加唯一 USDT 10000 与 NEX 100 的 `K3_ACCEPTANCE_OPENING/IN/POSTED` 初始账本，`balance_after` 与钱包一致；finally 按用户、唯一 `biz_no`、资产和方向精确删除；增加棘轮合同覆盖双资产与精确清理 | **已关闭**：合同红灯后修复为 13/13；最终候选后续两轮真实提现均越过 D4 入账并完成第 5 项的两笔提现、风险命中、审计/outbox和上下游业务断言；每轮结束用户、钱包、账本、提现和幂等残留均为 0 |
| K-AUTO-010 | K3 MFA 棘轮验收基础设施 | P2 | K3 最终轨前 5 项通过后，第 6 项使用同一 resilience 账号第三次登录；30 秒内复用了已消费 TOTP counter，helper 抛出 `K3_TOTP_WINDOW_EXHAUSTED`，尚未进入业务动作 | 载具只在临界点附近避让，没有把后端 TOTP anti-replay counter 建模为共享单调棘轮 | 阻断 unknown/CAS/归档/503/重登终局轨；产品 MFA 正确失败关闭，未产生业务副作用 | 登录 helper 用 `expect.poll` 等待真实下一个 30 秒 counter，再生成新 TOTP；不使用固定睡眠，并增加合同锁定 counter 递增约束 | **已关闭**：最终候选 guard `1/1`、K3 子轨 `6/6`；第 6 项 unknown、CAS、归档、503、退出重登全部通过，残留 0 |
| K-AUTO-011 | K1/A2 结果未知棘轮合同 | P3 | J→K 非 Owner 复审运行 K1–K6 合同，K1 合同仍要求旧文案“结果未知 / 同一请求”，而共享 A2 边界已统一为“结果暂不确定 / 同一命令号”，导致 87 项中 1 项假失败 | K1 的静态棘轮复制了历史展示文案，没有复用本轮已经由 `a2-outcome-uncertain-contract` 锁定的统一业务语义 | 阻断 K1 合同质量门；真实产品仍保留弹窗、输入、稳定命令号和 A2 审计出口，不影响运行功能 | K1 合同同步为统一 A2 业务文案，继续强制“暂不确定、同一命令号重试、核对 A2 审计”的安全不变量 | **已关闭**：K1–K6 合同重跑 `87/87`，共享 A2 结果未知合同 `3/3`，TypeScript 通过；未改产品代码或运行候选 |
| K-AUTO-012 | K4 双运营员并发验收载具 | P3 | 最终候选两名真实运营员竞争同一 K4 版本连续得到唯一 200 与另一方 409，override 和 winner history 均正确落库；载具仍把 history 判为不匹配并在正常恢复前中断 | 本轮显式 suffix `RJZF729` 含大写，A1 按账号 canonical 规则保存为小写；权限 setup 却把客户端请求的原始大写 username 写入共享 fixture。K4 载具拿 fixture 原值与后端审计的 canonical operator 做大小写敏感比较，造成假阴性；清理动作又原本位于业务断言之后 | 产生产品假阴性并短暂遗留 active K4 override；产品 CAS、操作者归属、history 与审计实际一致 | 权限 setup 在账号创建前统一小写化 username；K4 不再信任 fixture 原文作为审计身份，而从各自认证会话读取服务端 canonical username；winner history 使用无固定睡眠的权威轮询；按最新 `rowVersion` 的 recompute 恢复加入 `finally`，任何断言失败也必须恢复模型分 | **已关闭**：失败 Run `1785260692053` 已恢复 v29→v30，第二、三轮失败均由 `finally` 自动恢复且 active override=0；新 Run `K4-J-2OP-1785260939774` 得到 root/checker=`409/200`，winner 为 canonical `acc_kmaker_r151023_rjzf729`，history 命中且 v34→v36 恢复模型分，1/1 通过 |
| K-002 | K3 沙盒模拟结果未知反馈 | P1 | 对 `POST /api/admin/risk/withdraw-rules/dry-runs` 注入 HTTP 200 但 `data={}`；该写式命令的真实执行状态不可判定，但锁定候选没有呈现“结果暂不确定”且会清空重试键 | 畸形成功响应的协议错误在 dry-run 规范化边界没有稳定保持 `K1OutcomeUncertainError(stableCommandKey)` 语义；UI 又只以 `instanceof` 判断，错误跨边界后会按普通失败清除 `commandAttempt`，短暂 toast 也不足以形成可操作闭环 | 操作员可能生成新幂等键重复执行或误判模拟成功；破坏写式命令“未知结果必须同键重试”的资金/风控安全不变量 | `dryRunK3` 改为显式 await 后在 `normalizeK3DryRunForWrite(value, stableCommandKey)` 边界包装协议错误；UI 增加兼容错误名与 `commandKey` 的 unknown 判定，未知时不清原键；同时增加持久 `role=alert`，成功同键重试后才清除 | **已关闭**：最终 Build `zCif7saxOJ0oXACiaV4BV` 从登录页和可见侧栏完整 K3 `6/6`；畸形 200 后对话、持久告警和原键保留，同键真实重试 `COMPLETED` 后告警清除；K3 合同 `14/14`、TypeScript 通过，幂等/审计残留 0 |
| K-003 | C1 用户总览 PC 代理缺路由 | P1 | K3 第 5 项从可见侧栏进入 C1 核对当前 K4 分时，浏览器稳定收到 `GET /api/admin/users/overview` 404 `USERS_ROUTE_NOT_FOUND`；后端 `OpsUserController` 明确提供同路径且 C1 客户端固定请求 `/overview` | `app/api/admin/users/[...path]/route.ts` 的白名单路由遗漏单段 `overview`，请求在到达后端前被 PC 代理拒绝 | C1 每次打开都会丢失权威总览/KPI并产生控制台 404；同时污染 K3→C1/K4 跨域验收，不能用忽略 404 掩盖 | 在代理 `backendPath` 增加 `parts.length===1 && parts[0]==='overview'` 到 `/api/admin/users/overview` 的显式映射；新增 C1 代理棘轮合同，不开放任意透传 | **已关闭**：最终 Build `zCif7saxOJ0oXACiaV4BV` 上 K3 `6/6`；C1 当前 K4 分可见，`/api/admin/users/overview` 200、控制台无 404；C1 合同 `7/7`、K3 合同 `14/14`、TypeScript 通过 |
| K-004 | K1/K2 权威响应协议 | P1 | J→K 非 Owner 使用新浏览器上下文分别把 K1、K2 overview 注入为 HTTP 200、`code=0`、`data={malformed:true}`；两页均把畸形载荷静默归一化成“0 条”空态，没有出现失败关闭提示，写入口仍按空快照渲染 | K1/K2 客户端使用宽松 `rec/rows/str/num` 归一化，缺少 `serverCanonical`、域标识、必需统计/参数/分页记录、枚举/数值和来源的运行时协议；K2 后端还返回空统计且没有来源声明。主控定向 Maven 红测又证明 K1 service 若先写 canonical 元数据、再 `putAll` repository 数据，repository 可覆盖协议所有权；mock repository 缺 `sources` 也会造成测试假绿/假红漂移 | 上游代理串线、字段丢失、协议漂移或畸形 200 会被误当真实零值，操作员可能基于伪空态处置；污染 K1→C2/K4/J3/H8/A4 与 K2→K1/H2/H8/F4/A4 调用链 | 先以两条真实 Playwright 红灯和 K1/K2 合同红灯锁定；后端 overview 增加 `serverCanonical=true`、`domain=K1/K2` 和九项服务端来源，K2 四项统计改由当前权威投影实时派生；K1 改为先复制 repository 业务数据、再由 service 最终覆盖 canonical/domain/sources，明确元数据唯一所有者；PC 为 K1/K2 建立完整严格运行时协议，任何字段、关键枚举/数值/来源不符均抛错，沿既有 `contentError` 清空旧快照、隐藏全部写入口且只留模块级恢复 | **已关闭**：统一 Build `wFMibyXH-QAZp3csVOjUx`、JAR `2DD86F9B...713E79` 上，从登录页和可见侧栏重跑 K1/K2 畸形 200、HTTP 500、真实恢复、刷新与退出重登均通过；K1–K6 复审异常轨 `7/7`，K/L 后端定向 `161/161`、后端全量 `2838/2838`、PC 定向合同 `84/84`、TypeScript、`npm run verify` 和生产构建全部通过 |
| K-005 | K3 失败关闭页头写入口 | P2 | 对 K3 overview 注入 HTTP 200、`code=0`、`data={malformed:true}` 后，正文正确显示“K3 数据加载失败”，但页头“沙盒模拟（不写生产）”仍保持启用；该动作会发起带幂等键的真实 POST 并产生审计/模拟批次 | K3 正文在 `contentError` 前置返回，但页头 `K3HeaderActions` 独立于正文渲染，`canDryRun` 只检查 `risk_k3_write`，没有绑定当前模块权威快照的 loading/error 状态 | 操作员可能在规则响应畸形或上游串线时仍基于不可见/不可信规则发起模拟，并得到与当前页面无法核对的结果；失败关闭不完整 | 先以真实 Playwright 红灯和 K3 合同红灯锁定；`canDryRun` 收紧为精确权限且 `!contentLoading && !contentError`，失败态禁用动作并明确提示“权威数据不可用” | **已关闭**：统一 Build `wFMibyXH-QAZp3csVOjUx` 上，从登录页和可见侧栏复验畸形 200、HTTP 500 时模拟按钮禁用且仅保留模块级重试；解除故障后真实数据恢复，K1–K6 复审异常轨 `7/7`，K3 合同、TypeScript、`npm run verify` 和生产构建通过 |
| K-006 | K2/E3 权威投影等级协议 | P1 | J→K 非 Owner 在锁定 Build `zTGbuKtb03fA0de7etQ1C`、JAR `743C3F00…D3D6A` 上以新会话从可见侧栏进入 K2，真实 overview 返回 E3 换新套利投影 `level=4`；PC 按 K2 等级 `0..3` 严格协议正确失败关闭为 `K2 数据加载失败 · UNKNOWN_ERROR`，K2 完整写轨首项红灯 | 后端 `RiskOpsMapper.upsertE3TradeinArbitrageRows` 把合法 `nx_tradein_application` 高频换新证据投影为硬编码 `level_value=4`，但 K2 产品语义及 PC 权威协议仅定义 0–3，其中 3 已是“全中/确认”最高级；历史软删除 `K2-E3-U990628` 也证明确为生产映射缺陷 | 任一满足 E3 高频换新+礼金/返佣条件的真实用户都会令 K2 整页失败关闭，阻断 K2→K1/H2/H8/F4/A4 的查看、处置、幂等/CAS 与审计闭环；PC 未放宽协议，避免把未定义风险级别静默展示 | 保留 PC 严格协议；将 E3→K2 投影等级归一化为最高合法值 3，并在 Mapper SQL 合同中同时锁定 `NULL,3,'flag',0` 且禁止回归 `NULL,4,'flag',0` | **已关闭**：统一 Build `zfPfSm8yHDpNEEN8zcqdN`、JAR `F1DF1420…A260` 上，用新账号/新会话/新夹具从登录与可见侧栏完整 K2 `6/6`；新增运行时棘轮直接断言 E3 正事实行 `level=3`、overview 200、PC 正常可用，负佣金及未完成置换不投影；K1–K6 入口/异常 `7/7`、权限 `3/3`、合同 `92/92`，业务、账号角色、Redis、幂等与 pending lock 可变残留均为 0 |
| K-AUTO-008 | K4 评分模型验收基础设施 | P2 | K4 第一轮用包含文本定位“高风险下限”，同时命中帮助文案中含同词的“自动升级线”；修正后又因 `page.once("request")` 被无关请求抢占，真实草稿已保存但载具误判未发请求 | 载具未使用输入框的完整可访问名称，且用一次性无谓词事件监听捕获关键写请求；中断路径也没有 finally 清理已写草稿 | 产生假失败并短暂留下 v78 草稿，阻断 K4 发布/回滚/解释性/下游闭环 | 输入统一改为精确可访问名；关键写请求改用带 method+URL 谓词的 `waitForRequest`；重算等待改为 `expect.poll`；中断草稿与其唯一幂等记录按 id+version+reason 精确清理 | 锁定候选 K4 r4 1/1 通过；draft=0、active override=0、本轮 8 条幂等记录=0，最终 v79 权威配置与起始 v77 完全一致，关闭 |
| L-001 | L4 设备/任务/网络报表 | P1 | superadmin 从登录页和可见侧栏进入 L4，点击“自定义”后页面立即请求缺少起止日期的 `period=custom`；后端正确拒绝，前端清空权威快照并显示“L4 数据加载失败”，日期输入框随即消失，用户无法填写自定义周期 | 自定义按钮直接把服务端查询状态切到 `custom`，而日期仍只存在子组件草稿；父组件监听 query 变化立即取数，未区分“正在编辑筛选草稿”和“已提交完整筛选” | L4 自定义历史报表主流程完全不可完成；运营无法查询任意自定义周期，且失败后只能退回预设周期 | 未同时具备合法开始/结束日期时仅保留当前权威数据并展示日期输入，不发服务端请求；提交时校验开始日期不晚于结束日期，再一次性应用完整查询；增加 2 项棘轮合同 | **已关闭**：最终 Build `FL6den7TMkSYQjxGFfpkR` 从登录页和可见侧栏完整复验 L4 `4/4`；自定义空态、顺序校验、服务端查询、503 恢复、聚合/网络导出、刷新重登全部通过 |
| L-002 | L6 用户行为热力图 | P1 | superadmin 从登录页和可见侧栏进入 L6，真实 `/api/admin/bi/behavior` 与 page-catalog 均返回 200，但响应中三条活动行 `pageCount=0`；PC 严格协议正确失败关闭为 `L6_RESPONSE_INVALID`，热力矩阵、筛选和导出均不可用 | `BehaviorAnalyticsMapper.activity` 的 SELECT 列顺序为 `dwellMs,pageCount,bounceRate`，Java record 构造顺序为 `dwellMs,bounceRate,pageCount`；MyBatis 构造器位置映射把实际跳出率截断写进 pageCount，并把 pageCount=1 写进 bounceRate | 任何跳出率低于 1 的页面都会得到非法 pageCount=0，导致 L6 整页失败关闭；即使放宽前端也会展示错误的跳出率和聚合页数 | Mapper SELECT 改为与 record 一致的 `dwellMs,bounceRate,pageCount`，新增 SQL 构造顺序棘轮合同；PC 继续严格拒绝 `pageCount<1` 的畸形 200 | **已关闭**：后端定向合同 1/1、PC L 合同 52/52；最终 JAR `DCA3841B…208F` 从登录页和可见侧栏完成真实活动、L1 聚合、单页热区、CSV、刷新重登 1/1 |
| L-003 | L5 导出与监管报告 | P1 | 非 Owner 以全新浏览器从登录页和可见侧栏进入 L5，将 `/api/admin/bi/export/overview` 注入为 HTTP 200、`code=0`、`data={}`；页面把畸形权威响应显示为“累计导出任务 0”，并保留聚合快照、监管报告和账单导出写入口 | `normalizeL5` 对缺失对象、数组和数值统一兜底为空/0，没有运行时验证 L5 身份、权威标记、统计、任务分页、状态枚举和来源；页头创建按钮也未绑定 `biError/biData` | 网关截断或接口漂移时，运营会把不可信数据误判成真实空态并继续创建、审批或下载，L1–L4/D4/I5→L5→MinIO/A2/A4 数据出口链失去失败关闭 | 新增 L5 深层运行时协议，后端同步发布 `domain=L5`、`serverCanonical=true`、完整状态枚举和来源；畸形成功响应清空旧快照，页头创建按钮绑定权威数据/错误态，主体写入口全部隐藏，仅保留重新读取 | **已关闭**：锁定 Build `q2cNmpfQ_JFRzTMXb2oiR`、JAR `199B656A…97F9D` 上，L5 畸形 200 明确失败关闭且不暴露写入口；真实四聚合、D4 七账单、四监管、下载、刷新重登和匿名 401 为 `2/2`，500 注入后真实恢复；L5/D4 A2+A4/MinIO 一致， mutable 残留 0 |
| L-004 | L4 设备/任务/网络报表 | P2 | 非 Owner 从可见侧栏进入 L4，将 `/api/admin/bi/operations/overview` 注入为 HTTP 200、`code=0`、`data={}`；深层 validator 禁用了导出，但页面显示“后端已响应、当前没有可展示的数据” | `readL4Operations` 只在组件渲染层把畸形数据折叠为 `null`，没有把协议错误传回统一加载边界，导致 `biError` 仍为空 | 不会暴露写入口，但运营无法区分真实空态与后端协议损坏，错误恢复和告警语义失真，L4→E/F/H1/A4/L5 判断受影响 | 在统一 LView 接收边界复用完整 `readL4Operations`；不通过即抛 `L4_RESPONSE_INVALID`，清空旧快照、显示后端异常并保持导出禁用 | **已关闭**：同一锁定候选从可见侧栏完成四报表、周期/Phase、自定义空态、保存视图、畸形 200、500/503 恢复、聚合和团队树导出 `4/4`；畸形响应显示 `L4 数据加载失败`，所有导出禁用，解除注入后真实恢复 |
| L-005 | L4 团队树敏感导出 / A4 | P2 | 新候选从登录页和可见侧栏真实导出、下载并同键重放两份团队树脱敏 CSV；报告、MinIO artifact、download grant、A2 导出/高风险提醒/下载审计均成功，但执行前后 `nx_event_outbox` 最大 ID 均为 `8862`，新增为 0 | `OpsBiService.exportNetworkTreeOnce` 只调用 A2 `audit(...)` 与高风险提醒，没有像 L5、D4、L6 一样调用 `reportRepository.publishReportExported(...)` | L4 团队树文件已经对外生成，但 A4/J4/L5 等消费者收不到 `admin.report_exported`，导出事件目录、告警联动和跨域追踪不完整 | 增加失败合同和后端单测；在同一事务的成功提交链发布 schema rev 289 要求的 `reportId/exportType/scope/rowCount/containsPii/maskingPolicy/operator/reason/format` canonical 事件，发布失败则整笔回滚 | **已关闭**：两个不同命令分别为 `l4-network-tree-1785257245011-fhvkauhi → L4TREE-524EB926 → outbox 8871`、`99104-l4-duplicate-1785257245127 → L4TREE-9AFCFCCD → outbox 8872`；每个命令均仅 `1 idem + 1 report + 1 artifact + 1 A4`。第二命令同键同载荷重放仍返回原 report 且不新增 MinIO/A4，同键改 depth 返回 `409 IDEMPOTENCY_KEY_PAYLOAD_MISMATCH`；事件为注册、服务端权威的 canonical `admin.report_exported`，清理后 mutable 残留 0 |
| L-AUTO-001 | L1–L6 五层权限验收基础设施 | P3 | 新建 readonly/no-write 夹具后，L1–L6 页面均从可见侧栏真实加载；载具先把 L5 总览误写成不存在的 `/api/admin/bi/exports`，修正后又把 L6 导出前后缀写反为 `/behavior/export`，两次均得到 404 | 验收载具未复用 `l-client` 的真实路由单源：L5 总览是 `/api/admin/bi/export/overview`，L6 导出是 `/api/admin/bi/export/behavior` | 阻断 L5/L6 权限自动化收尾，不影响产品页面、权限、权威数据或导出实现 | 两条权限探针均改为与页面客户端、后端 Controller 同源的真实路径；保留 L5/L6 写绕过 403、按钮禁用、刷新重登与 no-menu 全轨 | **已关闭**：同一隔离权限夹具从登录页和可见侧栏完整重跑 3/3；L1–L6 读取 200、写入 403、按钮禁用、no-menu 读写 403，刷新重登不漂移 |
| L-AUTO-002 | L6 Owner 验收启动参数 | P3 | 最终候选 L5 2/2 通过后，同批 L6 在浏览器启动前抛出 `L6_E2E_CREDENTIAL_ENV_REQUIRED` | 启动命令只设置了 `NEXION_E2E_ADMIN_PASSWORD`，遗漏载具显式要求的 `NEXION_E2E_ADMIN_USER`；未产生 L6 页面或业务请求 | 阻断一次 L6 自动化启动，不影响候选服务、产品数据或前一轨 L5 结论 | 补齐 `NEXION_E2E_ADMIN_USER=superadmin`，保留独立 restricted 输出目录后单独重跑 L6 | **已关闭**：最终候选 L6 从登录页和可见侧栏完成真实筛选、下钻、CSV 导出、刷新和重登，1/1 通过 |
| L-AUTO-003 | L1–L6 Owner 最终锁定验收载具 | P3 | 最小权限 L Owner 从登录页和可见侧栏完成 L1–L6、逐模块刷新及退出重登后，载具仍把全局壳层按 RBAC 正确返回的 401/403 资源诊断计为产品控制台错误，导致最终断言假失败；L 域 `/api/admin/*` 5xx 与页面异常均为 0 | 控制台监听覆盖登录切换与全局壳层，过滤器只排除 DevTools/favicon，没有区分明确的 401/403 权限边界与真实 404/5xx/运行时异常 | 产生最终锁定假阴性，不影响产品权限、L1–L6 数据或业务调用链 | 仅忽略浏览器明确标注 HTTP 401/403 的通用资源诊断；继续对 pageerror、404/5xx 控制台错误和所有 `/api/admin/*` 5xx 零容忍，并保留权限轨对 401/403 的精确状态断言 | **已关闭**：同一最终候选从新浏览器上下文重跑 L1–L6 可见侧栏、逐模块刷新与退出重登 1/1；pageerror、`/api/admin/*` 5xx 均为 0 |
| L-AUTO-004 | L3 maker/checker 验收载具 | P3 | L Owner 真实发起敏感财务明细时，首轮载具把展示文案“七账本脱敏资金明细”直接作为 `fields` 提交，后端按白名单正确返回 400 `FINANCE_DETAIL_SCOPE_INVALID`，未创建报告 | 载具没有复用 L3 页面与后端共同锁定的九个允许导出字段，把字段集合说明误当成字段名 | 阻断 maker/checker、幂等/CAS 与下载轨；产品按白名单失败关闭，未产生敏感报告或越权副作用 | `fields` 改为页面和后端同源的九项白名单：交易时间、脱敏用户编码、业务编号、账单类型、资产、方向、金额、余额、状态；继续断言 CSV 不含明文身份字段 | **已关闭**：新报告、新幂等键下完整 maker/checker 1/1；创建/重放 200，同键异载荷 409，自批 403，独立 Checker 竞争 200/409，脱敏 CSV 下载 200 |
| L-AUTO-005 | L3 双 Checker 登录载具 | P3 | 已创建待审敏感明细后，载具先完成 MFA Checker 登录、再启动临时 MFA 绕过的 superadmin 新会话；第二个会话一次停留在登录页直至 30 秒超时，未发起批准请求 | 并发准备阶段把两个独立认证链串在同一临界窗口，未先稳定建立无需 TOTP 等待的 root Checker 会话；独立手工探针确认同一候选 superadmin 密码登录可立即进入控制台 | 阻断一次 CAS 竞争发起；报告保持 `PENDING_CONFIRM`，无审批、下载或明文泄露副作用 | 调整会话准备顺序：先建立 root Checker，再完成独立 MFA Checker；批准仍由两个隔离浏览器上下文同时发起，并继续要求唯一 200、另一方 409 | **已关闭**：两个隔离浏览器上下文分别以 superadmin 与 d_checker 建立独立会话，真实并发批准得到唯一 200 与另一方 409，CAS 闭环 |
| L-AUTO-006 | L6 Owner 导出验收载具 | P2 | L6 脚本先把筛选切到 `H5 + zh-CN + 24h` 空结果，再只把统计粒度切回“全部”；由于设备、Locale、时间窗仍为空，条件分支允许“无行则跳过下钻和导出”后仍整项通过 | 主流程把可接受的空态分支错误复用为导出验收分支，未强制恢复到有权威事件的筛选窗口，也未要求产生 A2 审计与 A4/outbox | 会形成“L6 导出已验收”的假阳性，遗漏真实 CSV、审计和事件链；产品代码未失败 | 空态核对后显式恢复 `ALL + ALL + 7d`，强制至少一条活动行、单页点击坐标热力、导出按钮可用及真实 `l6-behavior.csv` 下载；不再允许条件跳过 | **已关闭**：最终 Build 新会话 1/1，真实下载 `l6-behavior.csv`；A2 审计 `48425` 与 A4/outbox `8634` 共享 `L6-23CD2A8C`，状态一致 |
| L-AUTO-007 | L6 Owner 最终导出定位器 | P3 | 强制导出首轮在选择“近 7 天”时报 Playwright strict mode：同时命中筛选 chip“近 7 天”和“导出近 7 天全端”按钮，尚未发起导出 | 新增步骤使用包含文本定位，没有锁定完整可访问名称 | 阻断一次载具启动后的筛选动作；未产生产品写入、报告、MinIO 对象或幂等记录 | 时间窗定位改为 `getByRole(button, { name: \"近 7 天\", exact: true })`，继续从同一可见入口用新会话完整重跑 | **已关闭**：精确可访问名定位后同一最终候选从可见侧栏完成空态→有数据筛选→下钻→导出全轨 1/1 |
| L-AUTO-008 | L3 Checker 认证终态兼容 | P3 | 非 Owner 复审已创建待审敏感明细后，旧 helper 强制要求登录提交后一定出现 MFA 输入；一次独立 Checker 会话已直接进入认证后 shell，载具仍等待“一次性验证码”直至超时 | helper 只接受“MFA 挑战”一个中间态，没有把“服务端已完成认证并返回可见控制台”作为等价成功终态 | 中断一次双 Checker CAS 载具；maker 创建、同键重放、异载荷 409 和自批 403 已完成，待审报告未泄露且没有误审批 | 登录 helper 改为无固定睡眠地轮询两个合法终态：MFA challenge 或可见 `aside`；出现 challenge 才提交 TOTP，已认证 shell 直接继续。产品认证与权限代码未改 | **已关闭**：用三个隔离浏览器上下文分别完成 L Owner MFA、独立 Checker MFA/已认证 shell 和 root shell；双 Checker 并发批准 `[200,409]`、脱敏下载 200、原始身份字段不可见；测试业务、MinIO、幂等和 A4 已精确清零 |
| FINAL-AUTO-001 | A–M 统一 75 模块终验登录定位器 | P3 | 统一终验首次从匿名登录页执行时，载具只接受按钮精确文案“登录”，当前产品主按钮为“继续”，因此在业务请求前即停止 | 终验定位器把展示文案当稳定契约，没有使用登录动作的可访问语义兼容范围 | 阻断统一终验启动；没有发起写请求或产生业务副作用 | 登录提交统一使用可见按钮语义 `/登录\|继续/`，同时仍以精确 `POST /api/admin/auth/login` 响应作为成功边界 | **已关闭**：最终 r9 从匿名登录页真实提交成功，随后首轮、刷新和退出重登各完成 75/75 |
| FINAL-AUTO-002 | A–M 统一 75 模块终验退出入口 | P3 | 首轮与刷新完成后，载具按用户名寻找页头账号按钮，但当前壳层展示运营员昵称/角色，未绑定登录名，导致无法打开退出菜单 | 把账号按钮的可变展示文本当稳定定位器，未复用页头菜单的可访问结构 | 阻断退出重登轨；此前 150 次模块读取均已完成且无业务写副作用 | 退出入口收敛为页头唯一 `button[aria-haspopup=menu]`，退出动作等待精确 `POST /api/admin/auth/logout` 成功响应 | **已关闭**：r9 真实退出成功、匿名 session 401 符合预期，重新登录后 75/75 再次通过 |
| FINAL-AUTO-003 | A–M 统一 75 模块页面健康与请求收敛 | P3 | 早期轮次把侧栏同名文本误认为模块标题，并在最后一个响应到达瞬间即断言零在途请求；登录 hydration 尚未完成时还可能出现“点击但无 POST”的载具假阴性 | 页面健康断言作用域过宽，请求模型缺少静默窗口，登录 helper 只等待 DOM 可见而没有等待匿名 session/hydration 的可观测边界 | 可能造成模块假通过或终验假失败，降低对页面错配、尾部 4xx/5xx 和登录竞态的识别能力 | 页面身份改为 `main` 内精确 `h1 + moduleId`；每模块等待 750ms 请求静默窗；登录先等待匿名 session，再等待精确 login POST；错误监控覆盖 pageerror、console、admin 4xx/5xx 与 requestfailed | **已关闭**：r9 75×3 路径全部通过；pageerror、console error、admin 4xx/5xx、admin request failure 均为 0 |
| FINAL-AUTO-004 | A–M 统一 75 模块导航中止分类 | P3 | 快速切换模块及退出重登产生 47 个非 SSE `net::ERR_ABORTED`；若与真实网络失败混计会造成假红，若无条件忽略又可能掩盖产品问题 | 浏览器导航会主动取消上一页尚未完成的 GET，原始采集没有把“被下一次同路径成功响应替代”的导航中止与真实请求失败分开 | 终验错误口径可能误报或漏报；产品接口本身未出现 4xx/5xx/请求失败 | 将中止单列为 `expectedNavigationAborts`，保持 `adminRequestFailures` 零容忍；最终由非 Owner 全量解析 r9 trace 复核替代响应、logout 成功边界和 SSE 长连接断开 | **已关闭**：r9 的 46 个 GET 均在 61.6–94.4ms 内被同方法、同 path+query 的 200 替代，logout POST 已先返回 200；另 18 个 `/stream` 为换页主动断开。`adminRequestFailures=0`、匿名 session 401 仅 1，运行摘要与 trace SHA-256 已锁定。后续棘轮建议再把 allowlist 收紧到“同 stage、同 path、已出现替代 200” |
