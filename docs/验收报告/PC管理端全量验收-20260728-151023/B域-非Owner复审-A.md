# B 域非 Owner 复审报告（A Owner）

> Run ID：`pc-full-acceptance-20260728-151023`  
> 复审者：A 域 Owner（非 B Owner）  
> PC 最终候选：Build ID `zCif7saxOJ0oXACiaV4BV`，PID `22160`  
> 后端候选：PID `4476`，JAR SHA-256 `DCA3841B0099723B0C1BE49A703681FEF276F4C691BCBC8B914C11F18589208F`  
> 数据隔离：Run 专用 MySQL、Redis 和 MinIO 前缀  
> 复审结论：B1–B5 `5/5` 通过；非 Owner 复审 `99.2/100`，通过

## 1. 复审范围与裁决原则

本次没有复用 B Owner 的浏览器上下文、登录态或模块结论。A Owner 使用新浏览器上下文和独立夹具，从登录页和可见侧栏重跑 B1–B5，并按《页面业务逻辑通顺性验收方法》补充权限篡改、异常响应、结果未知、失败关闭、幂等、CAS、刷新重登和跨域不一致攻击。

主流程只访问真实 PC 与后端服务，不使用 mock、DOM 注入或 localStorage 作为权威数据。畸形响应和结果未知仅作为单独标记的故障注入轨，不替代真实主流程。

## 2. 逐模块复审结果

| 模块 | 可见入口与首用流程 | 权威数据与跨域核对 | 对抗性复审 | 当前结论 |
|---|---|---|---|---|
| B1 资金总览 | 登录、侧栏进入、首轮、刷新、返回、退出重登均成功；页面错误和 `/api/admin/*` 5xx 为 0 | 储备、负债和覆盖率来自服务端；与 B2/B5 同源；资金红线失败关闭 | 匿名 401、无菜单直达 403、伪造权限仍 403、非法参数/路由拒绝 | 通过 |
| B2 流动性/预测 | 登录、侧栏进入、首轮、刷新、返回、退出重登均成功 | 与 B1/B5 的储备、负债和覆盖率一致 | 只读/无写按钮与接口双重拒绝；超时关闭；结果未知保留稳定命令键；双运营员真实 CAS、幂等与恢复通过 | 通过 |
| B3 转化漏斗 | 登录、侧栏进入、空态、刷新、返回、退出重登均成功 | 当前 Run 库只存在提现事件时正确显示不可用/空态，不伪造漏斗 | 畸形 200 失败关闭；真实同键同载荷重放成功、同键异载荷 409；结果未知稳定重试 | 通过 |
| B4 增长节奏 | 登录、侧栏进入、首轮、刷新、返回、退出重登均成功 | B4 八个节奏拨盘与 H1 八项完全一致 | 畸形 200 失败关闭；独立详细走查通过 | 通过 |
| B5 风险雷达 | 登录、侧栏进入、首轮、刷新、返回、退出重登均成功 | B5 五个应急门与 J1 五个权威门完全一致；风险摘要读取真实服务端 | 500 失败关闭；无菜单、伪造权限和写接口均拒绝 | 通过 |

## 3. 验收文档十步证据

### 3.1 页面与动作盘点

- B1–B5 均由可见侧栏进入。
- 每个模块独立新上下文执行稳定入口、刷新、返回、退出和重新登录。
- 五个模块的模块面锁定用例分别为 `1/1`，没有把域级结果替代模块结果。

### 3.2 字段到数据库的数据溯源

- B1/B2/B5 的储备、负债、覆盖率使用同一服务端权威口径；复审快照为储备 `0`、负债 `887989.56`、覆盖率 `0`。
- B3 对照 Run 库事件状态；只有提现事件时不得伪造注册/KYC/首投漏斗。
- B4 与 H1 对照八项节奏拨盘。
- B5 与 J1 对照五项应急门，并读取 K 域真实风险信号。

### 3.3 真实 Chromium 首用走查

- B 域全范围浏览器轨 `4/4`。
- B3/B4 独立详细轨 `4/4`。
- B1–B5 独立模块面轨 `5/5`。
- 全程 `workers=1`，使用可见定位器和响应/状态等待，不使用固定睡眠。

### 3.4 主流程、空态、刷新、返回、退出重登

- B1、B2、B4、B5 正常数据态均保持一致。
- B3 在数据不完整时显示明确不可用/空态，没有把缺数渲染为成功。
- 五个模块刷新、浏览器返回和退出重登后仍从服务端恢复，未从本地缓存伪造成功。
- 最终 Build 上又执行一次完整 B1–B5 锁定轨 `4/4`，写轨恢复后仍无跨域漂移。

### 3.5 五层权限

- `b_readonly`：B1–B5 菜单和读取可用，写按钮不可用，写接口 403。
- `b_no_write`：B1–B5 菜单和读取可用，写按钮不可用，写接口 403。
- `b_no_menu`：侧栏模块数为 0；直接路由、读取和写接口全部拒绝，刷新重登后权限不恢复。
- 额外伪造 `nexion-admin-auth-v2`、`nexion-admin-platform-v1` 的前端权限攻击仍被服务端拒绝：路由回到 `/`，B1 读取 403，B2 写入 403；伪造缓存被清除。

### 3.6 异常响应与失败关闭

- 匿名请求 401，非法路由/参数按合同拒绝。
- B3/B4 畸形 200 不得渲染成功。
- B2 超时和 B5 500 均进入可见失败态，没有显示旧数据为成功。
- B2/B3 结果未知轨保留原操作标识，同一载荷重试不生成新的命令键。

### 3.7 幂等、同键异载荷、CAS 与并发

- B3 真实后端：首次 200；同键同载荷 200 且返回体一致；同键异载荷 409，消息为 `IDEMPOTENCY_KEY_PAYLOAD_MISMATCH`。
- B2 结果未知：同一载荷复用同一幂等键；载荷变化生成新键。
- B3 结果未知：同一载荷复用同一幂等键；载荷变化生成新键。
- B2 双运营员同版本 CAS：初始版本 `6`，两个独立运营员并发提交同一 `expectedVersion`，结果严格为 `[200,409]`；失败者返回 `D3_FORECAST_CONFIG_VERSION_CONFLICT`。
- B2 胜者同键同载荷回放返回与首次完全一致；同键异载荷返回 409 `IDEMPOTENCY_KEY_PAYLOAD_MISMATCH`。
- B2 正向变更后版本为 `7`，精确恢复原配置后版本为 `8`；配置内容与运行前一致。版本只增不退是正确 CAS 历史，不属于残留。

### 3.8 数据库、状态、A2/A4 与上下游

- B3 首次落库生成唯一保存视图、幂等记录和 `ADMIN.FUNNEL_VIEW_SAVED` 审计；重放没有生成第二条业务记录。
- B4/H1 的拨盘数量均为 `8`；B5/J1 的应急门数量均为 `5`。
- B1/B2/B5 的资金口径同源，页面间未见漂移。
- B2 两次成功写入生成 `D3_FORECAST_CONFIG_CHANGED` 不可变审计 `2` 条及 `admin.treasury_forecast_config_changed` outbox `2` 条；均保留。
- B2 最终配置内容与初始值一致；恢复后跨域快照仍为储备 `0`、负债 `887989.56`、覆盖率 `0`、B4/H1 `8/8`、B5/J1 `5/5`。

### 3.9 精确清理

- B3 保存视图按唯一名称清除 `1` 行；幂等记录按 scope 与 key 精确清除 `1` 行；最终业务/幂等残留均为 `0`。
- B3 不删除不可变审计。
- B2 原配置已恢复；maker、checker、restore 三条幂等记录精确删除，残留 `0`；不可变审计和 outbox 保留。
- 权限账号 `99432–99434` 均为 `disabled/unassigned/tfa=false/sessions=0`，禁用后登录均返回 403。
- 隔离角色 `4064 / ACC_B_RO_R151023_3BBFED` 经独立 checker 批准删除；活动角色、角色菜单、角色权限、账号角色关系及待审工单均为 `0`。
- 权限清理产生的 `12` 条幂等记录已按本轮唯一前缀精确删除，残留 `0`；不可变审计保留。
- B3 保存视图、B2/B3 幂等、B 权限清理幂等、活动角色关系和待审工单最终复核全部为 `0`。

### 3.10 复审评分

**99.2/100，通过。**

- 扣分 `0.8`：权限清理载具首轮因脚本变量初始化顺序错误中止；隔离账号已先完成安全清理，角色删除工单保持可恢复，修正载具后同一工单由独立 checker 批准并完成，最终残留为 0。该问题没有进入产品代码或业务主流程。
- 通过依据：B1–B5 每模块独立通过；权限、异常、结果未知、真实幂等、双运营员 CAS、数据库/A2/A4/outbox、最终候选重跑和精确清理全部闭环；未发现未关闭产品缺陷。
- 当前血量：`100/100`。

## 4. 自动化结果（当前）

| 轨道 | 结果 |
|---|---:|
| B 域 Owner 范围独立重跑 | `4/4` |
| B1–B5 独立模块面 | `5/5` |
| B 权限夹具 | `3/3` |
| B2/B3 结果未知 | `2/2` |
| B3/B4 独立详细走查 | `4/4` |
| B1–B5 合同与运行时合同 | `32/32` |
| B3 真实后端幂等 | `1/1` |
| 权限缓存伪造攻击 | `1/1` |
| B2 双运营员 CAS | `1/1` |
| 最终锁定候选全范围重跑 | `4/4` |

合计：浏览器及实盘定向用例 `25/25`，合同 `32/32`。最终候选重跑期间页面错误为 0，`/api/admin/*` 5xx 为 0。

## 5. 缺陷与载具判断

- 当前未发现新的 B 域产品缺陷，未向共享缺陷台账新增条目。
- 权限篡改脚本初次对重定向完成时点的判断过早，修正为等待最终 URL 后通过；这是复审载具等待条件，不是产品缺陷，也没有产生业务副作用。
- 最终权限清理载具首轮存在 JavaScript 变量初始化顺序错误；当时账号已处于安全的停用/解绑状态，角色删除工单保持待复核且没有执行越权删除。修正后复用原工单完成独立批准，清理幂等和待审工单均归零。该项为载具问题，不记产品缺陷。
- B2 写轨严格等待 K 域释放资金/全局控制锁后执行，没有并发抢锁或绕过。
- 最终核对 `.next/BUILD_ID`、PC PID、后端 PID 和 JAR SHA-256 均未漂移。

## 6. 受限证据

受限根目录：

`D:\workspace\bug-pic\.restricted\pc-full-acceptance-20260728-151023\B\nonowner-A`

- `final-zCif7saxOJ0oXACiaV4BV/final-lock-owner/`：最终 Build 上 B1–B5 完整锁定重跑及跨域权威快照。
- `final-zCif7saxOJ0oXACiaV4BV/surface-B1` 至 `surface-B5`：五个模块独立浏览器上下文结果。
- `final-zCif7saxOJ0oXACiaV4BV/permission-results/`：三类权限夹具结果。
- `final-zCif7saxOJ0oXACiaV4BV/permission-tamper/`：前端权限缓存伪造攻击。
- `final-zCif7saxOJ0oXACiaV4BV/unknown-results/`：B2/B3 结果未知稳定命令轨。
- `final-zCif7saxOJ0oXACiaV4BV/b2-live/b2-live-cas.json`：B2 真实 CAS、幂等、恢复证据。
- `final-FL6den7TMkSYQjxGFfpkR/b3-live/b3-live-idempotency.json`：后端 SHA 未漂移期间的 B3 真实幂等、重放和同键异载荷证据。
- `permission-cleanup.json`：权限账号、会话、MFA、角色和待审工单最终清理结果。

| 关键证据 | SHA-256 |
|---|---|
| `final-zCif7saxOJ0oXACiaV4BV/final-lock-owner/cross-domain-authoritative-check.json` | `DBD916315100D5A3F7293E1FA04C964E6C716A6560F6A589F0C8092C959F6231` |
| `final-zCif7saxOJ0oXACiaV4BV/b2-live/b2-live-cas.json` | `3E32F7D9B9B6818C25E8ADF2738BEDCB6B66AC7DFB57639E5E468783D2A077D8` |
| `final-zCif7saxOJ0oXACiaV4BV/permission-tamper/runtime-evidence.json` | `FF5831BA8B6FF61F85017E1CBBD89B029727C3161835ACD1D0ABD82FBE8204DD` |
| `permission-cleanup.json` | `1F410B145F61212AE2FD4E6DBE696A1793E0ED570B9A5CAD51F32652C2A3E77F` |
| `final-FL6den7TMkSYQjxGFfpkR/b3-live/b3-live-idempotency.json` | `E13E22979A4BE0142753C5710330798730DB1A3472728A13B84FC17192717FA0` |

Trace、认证状态、TOTP 和临时凭据不进入 Git。
