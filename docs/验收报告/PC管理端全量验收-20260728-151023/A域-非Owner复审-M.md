# A 域非 Owner 复审报告（M→A）

## 1. 最终裁决

- Run ID：`pc-full-acceptance-20260728-151023`
- 复审角色：M 域 Owner 轮换复审 A 域
- 范围：A1–A8，共 8 个模块
- PC：Build `zTGbuKtb03fA0de7etQ1C`，PID `7196`
- 后端：JAR SHA-256 `743C3F0028C7DF7B1A283C4363F4BAB8F9E6A49DA9312EC3FB6AF9E742BD3D6A`，PID `18796`
- 隔离环境：MySQL `nexion_acceptance_20260728_151023`、Redis DB 14、MinIO `nexion-acceptance-20260728-151023`
- 结论：**A1–A8 8/8 通过，未关闭 P0/P1/P2/P3 为 `0/0/0/0`，本轮可变测试残留为 0**
- Owner 初审：`97.8 / 100`
- M→A 非 Owner 复审：**`99.2 / 100，通过`**

本轮没有复用 A Owner 的认证状态、业务夹具或旧候选结论。M→A 首轮在旧候选真实发现 `A-001`：G1 新参数使 A5 全页持续 500。完成失败单测、最小参数族映射、全量 Maven/PC 质量门和统一候选重建后，使用新账号、新会话、新浏览器上下文从登录页和可见侧栏重新执行完整范围。

## 2. 模块逐项结论

| 模块 | 首次用户与主流程 | 权限、异常与一致性 | 独立结论 |
|---|---|---|---|
| A1 运营账号与 RBAC | 服务端签发临时密码；临时 checker、linked、auditor 三账号真实完成首次登录、MFA、首次改密；状态、角色、MFA、会话按最新版本清理，停用后登录 403 | 匿名拒绝；高风险账号操作均有 A1 审计；账号没有物理删除入口；最终均 `disabled/unassigned/tfa-secret-cleared/sessions=0` | **通过** |
| A2 审计与操作确认 | `superadmin` 作为 maker，独立 `rbac_check_ms4ww12n` 作为 checker；授权工单与删除工单均从可见 A2 页面执行 | maker/checker 身份不同；提案、批准、拒绝审计可回查；只读页面不显示写控件；结果未知保留同一命令号合同通过 | **通过** |
| A3 系统配置 | 从可见侧栏把 `ops.maintenanceBanner` 真实执行 `off → on → off`；刷新、返回、退出重登后仍为 `off` | 写入携带服务端观察值和理由；审计 `56566/56567` 前后值对称；页面错误、控制台错误、5xx 为 0 | **通过** |
| A4 埋点事件体系 | 打开 6 个 family 的事件清单；Day0 真实执行 `90 秒 → 91 秒 → 90 秒`；刷新、返回、退出重登后恢复 | 审计 `56568/56569` 前后值、理由、幂等键完整；畸形 overview、非法 schema/参数由运行时合同失败关闭；A4/outbox 权威来源保持服务端单源 | **通过** |
| A5 平台参数寄存器 | 真实目录、搜索、域筛选、A3/J1/J2 owner 跳转通过；新增 G1 参数 `G.staking.apy.usdt30d` 首轮、返回、刷新、退出重登均 200 | `owner=G1`、路由 `/finance-products/staking` 在页面和 API 一致；403、503、重复 canonical key 的畸形 200 均失败关闭并可恢复 | **通过** |
| A6 角色管理 | 新建空角色 `RTMS4WW12N`；未知权限/菜单 422 且零副作用；权限与菜单联合授权经 A2 批准后原子生效；删除再次经 A2 | linked 账号只具 `platform_a6_read`；A7/A8 接口 403；授权前后回读、刷新重登一致；最终角色、关系、pending 工单为 0 | **通过** |
| A7 菜单管理 | 创建父/子/并发菜单；同键重放同 ID；同键异载荷拒绝；并发同键得到 `[200,409]` 后稳定重放 | 活跃子菜单和角色绑定分别阻止父菜单停用/删除；CAS、短理由、关系约束均失败关闭，并形成 3 条拒绝审计；最终菜单为 0 | **通过** |
| A8 权限字典 | 权限总数 319，未归类 31 与服务端一致；搜索、越界空页、刷新重登正常 | AUDITOR 只读，POST/PUT/PATCH/DELETE 均 405；畸形成功和 503 清空旧快照，仅保留恢复入口；总数前后不变 | **通过** |

## 3. 十步验收结果

1. **页面与动作盘点**：范围严格取自 `lib/nav/console-nav.ts` 的 A1–A8；账号、审计、配置、事件、参数目录、角色、菜单、权限字典逐模块裁决。
2. **字段与权威来源**：账号/会话来自 `nx_admin`、`nx_admin_account_state` 和 Redis；角色/菜单/权限来自 `nx_admin_role`、`nx_admin_menu`、关系表和权限字典；A3/A4/A5 来自服务端配置与事件读模型；A2 来自 operation ticket/history 与 `nx_audit_log`。
3. **真实 Chromium**：A1–A8 均从 `/` 登录页和可见侧栏进入；表面轨 `8/8`。A3/A4、A5、A6–A8 分别使用新的浏览器上下文，主流程无 mock、无 DOM 篡改、无 localStorage 权威数据。
4. **主流程与连续性**：八个模块均覆盖首轮、刷新、返回、退出重登；A3/A4 可逆写完成后从服务端重新读取；A5 G1 owner 跳转返回后仍保留正确目录。
5. **五层权限**：匿名 A6/A7/A8 为 401；superadmin owner 可写；AUDITOR 可读不可写；linked 有 A6 菜单和读权限、无 A7/A8 菜单与接口权限；未部署自定义菜单即使在服务端授权也被注册路由白名单过滤。菜单、路由、按钮、接口、数据五层未发现旁路。
6. **异常与失败关闭**：覆盖 401、403、405、409、422、503、畸形 200、结果未知、关系冲突、旧快照与理由不足；A5 真实 500 已按 `A-001` 根修，最终候选非预期 `/api/admin/*` 5xx 为 0。
7. **幂等、CAS、并发**：A7 同键同载荷稳定重放、同键异载荷拒绝；双请求同键为 200/409 且仅一条菜单；账号清理每一步重新读取最新版本；A6 授权和删除均由独立 checker 执行。
8. **数据库、状态、A2/A4 及上下游**：A3/A4 审计为对称前向/回滚；A6 工单 `WO-260729021030694-400` 和 `WO-260729021103138-300` 均由独立 checker 批准；A5→G1、A5→A3/J1/J2 owner 路由与服务端真值一致。
9. **精确清理**：角色 `4171`、菜单 `2469/2470/2471` 均已逻辑删除，活跃角色/菜单/角色关系/pending 工单为 0；20 条本轮幂等记录按精确 ID 与 scope 删除；Redis DB14 的三个 MFA counter 按完整键删除；A3=`off`、A4 Day0=`90 秒`；不可变审计保留。
10. **评分与复审**：Owner 初审 97.8；M→A 复审 99.2。复审按墨菲定律真实发现并关闭一个 P1 产品缺陷和一个 P3 载具缺陷，随后从入口重跑全部锁定范围。

## 4. 缺陷闭环

### A-001（P1，已关闭）

- 真实复现：旧候选从可见侧栏进入 A5，`GET /api/admin/platform/params-registry` 连续返回 500；日志为 `A5_OWNER_MAPPING_MISSING:G.staking.apy.usdt30d`。
- 根因：G1 已能真实写入 `G.staking.*` 参数，但 A5 owner 单源只登记 G2/G3/G4。
- 根修：服务端增加整个 `G.staking.* → G1 /finance-products/staking` 参数族映射；单测同时锁定当前键和未来任意参数键，避免单键补丁。
- 复验：最终候选页面/API 三次均 200；首轮、owner 跳转、返回、刷新、退出重登均通过；A5 通用故障恢复 `1/1`。

### A-AUTO-003（P3，已关闭）

- 真实复现：一次表面轨在生产构建首次加载期间，登录表单 DOM 已可见但提交处理器尚未 hydration；点击没有产生登录 POST，零产品副作用。
- 根修：载具等待精确登录 POST；若完全没有请求且 shell 未恢复，重新填写并再次真实点击，最多两次，不使用固定睡眠。
- 复验：新 helper 的 A6 首轮、刷新、退出重登 `1/1`；独立 A6–A8 完整权限主载具另为 `1/1`。

## 5. 自动化与质量门

| 质量门 | 结果 |
|---|---:|
| A1–A8 表面轨 | `8/8` |
| A5 G1 根修专项 | `1/1` |
| A5 目录/筛选/403/503/畸形 200/恢复 | `1/1` |
| A3/A4 真实可逆写 | `2/2` |
| A6–A8 权限、maker/checker、幂等、CAS、并发与清理 | `1/1` |
| A2 可见过滤与只读护栏 | `2/2` |
| A1–A8 定向 PC 合同 | `57/57` |
| 页面未处理错误 | `0` |
| 最终候选非预期 `/api/admin/*` 5xx | `0` |
| 可变业务/权限/幂等/Redis 残留 | `0` |

全量 Maven、PC `npm run verify` 和生产构建由主控在该最终候选统一执行，本复审没有并发启动 Maven、构建或服务。

## 6. 证据与校验值

受限证据根目录：

`D:\workspace\bug-pic\.restricted\pc-full-acceptance-20260728-151023\A-review-M\candidate-zTGbuKtb03fA0de7etQ1C`

| 证据 | SHA-256 |
|---|---|
| `a5-g1-rerun/runtime-evidence.json` | `A59BEFF06D197C314EA2594A12C9D08701705DBB911AC5143DB6EC9BDA222A85` |
| `a5-g1-rerun/trace.zip` | `36EDDEBE9CBF21AA28BBEEAEFCB72E420F7CB41F01770CA0FA12A60080397EC7` |
| `a3-a4-live/runtime-evidence.json` | `4C5CFD3271A6D49AB5DA48CF27F6E423252EE8CBF9A882B11F320771630F151E` |
| `a3-a4-live/a3-final-trace.zip` | `106D31C0C03C90CF10C1F5E0606AC835CF50F15BDB0E98F7DD6EDE5B7B23A598` |
| `a3-a4-live/a4-final-trace.zip` | `4E1CE84925423E2D2B7B6A25BC200E646A7FB3FAB6245743D1AAD2999E0497A5` |
| `a6-a8/runtime-evidence.json` | `86699DC6483E99CF80DF2CBE5C41CF082B518EA38020ACEB5EE32132C298B6F1` |
| `a6-a8-results/.../trace.zip` | `2653A61CF68AB3CB6FE7CB3E2800165FDD2A6780320FE347A59568F8536E8273` |
| `a5-results/.../trace.zip` | `F2EAA7AD0C8D635D64C9E92AB863BAA6544E603D7C690AE4E119A74C44CA5431` |

失败视频、认证状态、临时密码和 MFA 密钥仅在受限目录中短期留存，结案清理时删除，不进入 Git。

## 7. 最终评分

- 页面定位与业务对象：`10.0/10`
- 数据来源与一致性：`15.0/15`
- 操作闭环：`15.0/15`
- 真实用户走查与路径可达性：`9.8/10`
- 状态机：`10.0/10`
- 权限与安全：`10.0/10`
- 表单及控件合理性：`8.0/8`
- 文案、状态反馈与多语言：`6.8/7`
- 异常处理：`4.8/5`
- 审计、幂等和回滚：`9.8/10`

**非 Owner 复审得分：99.2 / 100，通过。**

当前血量：`100 / 100`。
