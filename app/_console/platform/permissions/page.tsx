"use client";
import A8Permissions from "@/app/components/domain-views/a-tabs/a8-permissions";

/** A8 权限字典（client component，由 app/platform/permissions/page.tsx 服务路由 ConsoleLayout 包裹）。 */
export default function A8PermissionsPage() {
  return <A8Permissions />;
}
